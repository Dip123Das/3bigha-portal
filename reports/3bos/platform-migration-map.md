# PROJECT NEEV — Phase 5

## 3BOS Platform Integration Audit

Generated: 2026-07-11T09:35:12.648Z

**Audit mode:** Read-only source inspection

- Database connection: No
- Production mutation: No
- Runtime application change: No
- Route change: No
- API change: No

## Executive Summary

- Files scanned: **1273**
- Total references detected: **9257**
- Legacy or mixed migration findings: **8737**
- Existing 3BOS foundation references: **520**
- Unreadable files: **0**

## Findings by 3BOS Area

| Area | Findings |
|---|---:|
| Human Identity | 4534 |
| Module-Oriented Architecture | 1553 |
| Workspace | 1366 |
| Business Growth Plan | 1181 |
| Capability and Permission | 333 |
| Onboarding | 217 |
| Visible AI Terminology | 73 |

## Findings by Application Area

| Application Area | Findings |
|---|---:|
| Application Route | 2253 |
| Business Logic | 1723 |
| Dashboard | 1697 |
| API | 1215 |
| Administration | 1190 |
| 3BOS Foundation | 520 |
| Shared UI | 459 |
| Onboarding | 120 |
| Operational Script | 76 |
| Middleware | 4 |

## Risk Distribution

| Risk | Findings |
|---|---:|
| HIGH | 1219 |
| MEDIUM | 3257 |
| LOW | 4781 |

## Highest-Concentration Files

| File | Findings |
|---|---:|
| `lib/3bos/workspace/registry.ts` | 211 |
| `lib/3bos/identity/registry.ts` | 182 |
| `app/dashboard/vendor/page.tsx` | 167 |
| `app/dashboard/inbox-v2/page.tsx` | 153 |
| `app/search/page.tsx` | 149 |
| `app/admin/dashboard/page.tsx` | 141 |
| `app/onboarding/business/BusinessOnboardingPageClient.tsx` | 119 |
| `app/dashboard/subscription/SubscriptionPageClient.tsx` | 114 |
| `app/auth/register-role/RegisterRolePageClient.tsx` | 101 |
| `app/dashboard/buyer/page.tsx` | 89 |
| `app/dashboard/buyer/quote-compare/[rfqId]/page.tsx` | 88 |
| `app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx` | 86 |
| `app/property/add/AddPropertyPageClient.tsx` | 79 |
| `app/price-today/PriceTodayClient.tsx` | 73 |
| `app/admin/dashboard/investment/plans/page.tsx` | 68 |
| `app/dashboard/thread/[conversationId]/page.tsx` | 65 |
| `app/page.tsx` | 65 |
| `lib/access/resolveAccess.ts` | 64 |
| `app/admin/dashboard/investment/plans/[id]/page.tsx` | 60 |
| `app/seo/[module]/[state]/[district]/[city]/page.tsx` | 60 |
| `app/layout.tsx` | 59 |
| `components/layout/TopHeaderClient.tsx` | 58 |
| `app/_components/AuthButtons.tsx` | 55 |
| `app/auth/post-login/PostLoginPageClient.tsx` | 55 |
| `app/api/rfq/vendor-matches/route.ts` | 53 |
| `app/dashboard/buyer/rfqs/page.tsx` | 53 |
| `app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx` | 50 |
| `app/services/add/page.tsx` | 49 |
| `app/admin/dashboard/investment/page.tsx` | 48 |
| `app/property/builder/projects/[projectId]/units/page.tsx` | 48 |
| `app/globals.css` | 46 |
| `lib/3bos/capability/registry.ts` | 46 |
| `app/admin/dashboard/investment/plans/new/page.tsx` | 45 |
| `app/api/ai/vendor-alert/route.ts` | 44 |
| `app/seo/[module]/[state]/[district]/page.tsx` | 43 |
| `app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx` | 42 |
| `app/services/turnkey/add/page.tsx` | 42 |
| `app/admin/dashboard/master-data/page.tsx` | 41 |
| `app/property/builder/projects/BuilderProjectsPageClient.tsx` | 41 |
| `lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts` | 41 |
| `app/api/rfq/create/route.ts` | 39 |
| `app/components/investment/InvestmentDealRoomClient.tsx` | 39 |
| `lib/search/procurement-recommendation-engine.ts` | 39 |
| `app/property/builder/projects/add/page.tsx` | 38 |
| `app/seo/[module]/[state]/page.tsx` | 38 |
| `app/vendor/price-updates/new/page.tsx` | 38 |
| `app/need/[slug]/page.tsx` | 37 |
| `app/rfq/general/browse/[module]/page.tsx` | 37 |
| `app/api/ai/procurement-command-palette/route.ts` | 36 |
| `app/dashboard/vendor/master-data/page.tsx` | 36 |

## Production-Safe Migration Model

Human Identity → Workspace → Capabilities → Business Growth Plan → Available Actions → Human-First UI

Existing production behavior remains below this model as a compatibility layer.

## Migration Map

### HIGH — app/api/admin/approve-user/route.ts:4

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorApproved } from "@/lib/marketplace/vendor-conversion-analytics";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(formData.get("role"));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:20

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/approve-user/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data: admin } = await supabase
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:25

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:29

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
if (admin?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:30

**Area:** API

**Category:** Capability and Permission

**Matched:** `forbidden`

**Current system**

```text
return NextResponse.json({ error: "Forbidden" }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/approve-user/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:44

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role === "vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
role === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:46

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "hub_vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "blogger"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:52

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/approve-user/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/users", req.url));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/geography-audit/route.ts:28

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/geography/manage/route.ts:33

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await query.limit(1).maybeSingle();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/geography/manage/route.ts:57

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/geography/route.ts:43

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:131

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
error: "Unauthorized.",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:139

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:143

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(prof?.role || "");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:149

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
error: profErr.message || "Unable to verify admin role.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:155

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role !== "master_admin" && role !== "investment_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:159

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
error: "Access denied.",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:195

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: "Plan id is required." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:208

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: error.message || "Failed to load plan." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:215

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: "Plan not found." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:244

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: "Plan id is required." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:332

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: error.message || "Failed to update plan." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:339

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: "Plan not found or not updated." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/[id]/route.ts:346

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
message: "Plan updated successfully.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/route.ts:120

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
error: "Unauthorized.",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/investment/plans/route.ts:128

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/route.ts:132

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(prof?.role || "");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/route.ts:138

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
error: profErr.message || "Unable to verify admin role.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/route.ts:144

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role !== "master_admin" && role !== "investment_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/investment/plans/route.ts:148

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
error: "Access denied.",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/investment/plans/route.ts:212

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
{ ok: false, error: error.message || "Failed to load plans." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/route.ts:270

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: "Plan type is required." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/route.ts:313

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ ok: false, error: error.message || "Failed to create plan." },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/investment/plans/route.ts:321

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
message: "Plan created successfully.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/operations/status/route.ts:19

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
if (!authHeader) return { error: "Unauthorized" };
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/operations/status/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/operations/status/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (profile?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/operations/status/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return { error: "Master admin required" };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/operations/status/route.ts:82

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
count("business_profiles"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/ai-draft/route.ts:24

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return { user: null, error: "Unauthorized. Please login again." };
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/price-updates/ai-draft/route.ts:40

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/ai-draft/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (profile?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/ai-draft/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return { user: null, error: "Master admin required." };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/ai-draft/route.ts:90

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- all rows are only draft estimates for master admin verification
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/route.ts:24

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return { user: null, error: "Unauthorized. Please login again." };
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/price-updates/route.ts:40

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (profile?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return { user: null, error: "Master admin required." };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/price-updates/route.ts:71

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/price-updates/route.ts:93

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ error: auth.error ?? "Unauthorized. Please login again." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/price-updates/route.ts:158

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{ error: e?.message || "Admin action failed." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/reject-user/route.ts:18

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/reject-user/route.ts:21

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data: admin } = await supabase
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/reject-user/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/reject-user/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
if (admin?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/reject-user/route.ts:28

**Area:** API

**Category:** Capability and Permission

**Matched:** `forbidden`

**Current system**

```text
return NextResponse.json({ error: "Forbidden" }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/reject-user/route.ts:38

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/users", req.url));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/schema-audit/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
"business_profiles",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/schema-audit/route.ts:26

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:17

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function getPlanAmount(plan: string) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:18

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "hub_vendor") return 2999;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:19

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "premium_vendor") return 1299;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:20

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor") return 599;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:29

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(form.get("subscription_plan") || "free");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/dashboard", req.url));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/update-subscription/route.ts:36

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/dashboard", req.url));
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/admin/update-subscription/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/update-subscription/route.ts:44

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subscription_plan: plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:50

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (status === "active" && plan !== "free") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:53

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subscription_plan: plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:54

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
amount: getPlanAmount(plan),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/update-subscription/route.ts:64

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/dashboard", req.url));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/update-subscription/route.ts:64

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return NextResponse.redirect(new URL("/admin/dashboard", req.url));
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/admin/vendor-control/route.ts:18

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
if (!authHeader) return { supabase, user: null, error: "Unauthorized" };
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/admin/vendor-control/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (profile?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return { supabase, user: null, error: "Master admin required" };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:52

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/admin/vendor-control/route.ts:110

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:118

**Area:** API

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
"Approved as BuildCon / WhatsApp Founding Vendor by 3Bigha admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:129

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Founding Vendor access approved",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:131

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"🎉 Your 3Bigha Founding Vendor free access has been approved. You can now complete your profile and add listings. Public visibility remains subject to 3Bigha listing review.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:151

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
ai_visibility_reason: "Restricted by Admin Control AI",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:164

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:175

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: action === "boost" ? "Admin boost activated" : "Boost reset",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:178

**Area:** API

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
? "🚀 Admin has activated visibility boost for your vendor profile."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/admin/vendor-control/route.ts:179

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
: "⚠️ Your admin visibility boost has been reset.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/autonomous-execution/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
return `Urgent procurement escalation detected for "${threadTitle}". Immediate supplier response required.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/autonomous-execution/route.ts:42

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
return `Final confirmation pending for "${threadTitle}". Please confirm pricing, timeline and execution readiness.`;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:6

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
type DealSide = "buyer" | "vendor" | "unknown";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (side === "vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (side === "buyer") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:52

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (side === "buyer") return "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (side === "vendor") return "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:60

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m?.role || ""} ${m?.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:146

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`${String(m?.role || "user")}: ${String(m?.body || "").slice(0, 500)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:156

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Based on this buyer-vendor chat, suggest exactly 3 short smart replies.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:160

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Detect buyer hesitation and reduce deal loss.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:161

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
- Help vendor move the buyer toward confirmation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:162

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Help buyer ask for final price, delivery, bill, document, or commitment.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:172

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
- If side is vendor, replies should help convert the buyer.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:173

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- If side is buyer, replies should help get better clarity, price, and confirmation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/chat-reply-suggestions/route.ts:190

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-conversion/route.ts:50

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ✅ GET → fetch vendor stats
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-message/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:6

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:39

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m?.role || "user"}: ${m?.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:95

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function getConversionLock(plan: unknown, confidence: number, ready: boolean) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:96

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p: string = String(plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:100

**Area:** API

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
"gold_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:101

**Area:** API

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
"platinum_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:113

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
label: "Premium Flow",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
label: ready ? "Buyer Waiting" : "High-Intent Buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:124

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "This buyer looks ready. Premium vendors get faster alerts and stronger deal protection."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:124

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "This buyer looks ready. Premium vendors get faster alerts and stronger deal protection."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:125

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "This buyer is showing strong interest. Upgrade to unlock faster AI alerts and priority follow-up.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:125

**Area:** API

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
: "This buyer is showing strong interest. Upgrade to unlock faster AI alerts and priority follow-up.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/deal-ready/route.ts:201

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`${String(m?.role || "user")}: ${String(m?.body || "").slice(0, 500)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:208

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Decide whether this buyer-vendor conversation is ready for final deal confirmation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-ready/route.ts:246

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:7

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m?.role || "user"}: ${m?.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:100

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m?.role || "user"}: ${m?.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:308

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`${String(m?.role || "user")}: ${String(m?.body || "").slice(0, 500)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:315

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Score this buyer-vendor deal conversation from 0 to 100.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:346

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Detect buyer hesitation, urgency, lead loss risk and deal temperature.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-score/route.ts:362

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:7

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m.role || "user"}: ${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:133

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m.role || "user"}: ${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:211

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Ask missing quantity, address and bill details before the buyer cools off.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:364

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`${String(m?.role || "user")}: ${String(m?.body || "").slice(0, 500)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:371

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Detect the current stage of this buyer-vendor deal chat.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:406

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Detect deal momentum, stale lead risk, buyer cooling-off and follow-up timing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/deal-stage/route.ts:422

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:93

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
senderSide === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:94

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:95

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: senderSide === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:96

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/execute-task/route.ts:97

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/inventory-intelligence/route.ts:152

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/inventory-intelligence/route.ts:159

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/inventory-intelligence/route.ts:166

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/inventory-intelligence/route.ts:173

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/inventory-intelligence/route.ts:180

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/inventory-intelligence/route.ts:226

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "system",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/inventory-intelligence/route.ts:231

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/inventory-intelligence/route.ts:234

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Generate inventory intelligence for a vendor. Detect low stock, fast-moving products, dead stock, reorder recommendations, billing insight, dispatch insight and fleet utilization insight. Keep it practical for local building material sell…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/lead-score/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Classify this buyer message into:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/lead-score/route.ts:58

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/marketplace-discovery/route.ts:56

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha AI is reading vendor, category and local marketplace signals.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/marketplace-discovery/route.ts:91

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha AI is reading vendor, category and local marketplace signals.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/memory-events/route.ts:15

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || null,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/price-prediction/route.ts:70

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/price-prediction/route.ts:106

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "string",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/price-prediction/route.ts:134

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/price-suggestion/route.ts:32

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/ai/price-suggestion/route.ts:65

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-anomaly/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Missing Supabase admin env variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-anomaly/route.ts:54

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Vendor inactivity anomaly detected.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-anomaly/route.ts:83

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(120);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-anomaly/route.ts:105

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.context_type || "conversation",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-anomaly/route.ts:111

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${row.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-assistant/route.ts:74

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: url.searchParams.get("module") || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-auto-action/route.ts:33

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(body?.module || body?.contextType || "rfq").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-auto-action/route.ts:84

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? side === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-auto-action/route.ts:134

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-auto-action/route.ts:194

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{ role: "system", content: "You return strict JSON only." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-auto-action/route.ts:195

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{ role: "user", content: prompt },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-assist/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`, `supplier`

**Current system**

```text
"Trigger proactive supplier and buyer follow-up review.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-assist/route.ts:50

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Review critical workflows and reroute risky supplier chains.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-decisions/route.ts:8

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Steel RFQ supplier inactivity",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-decisions/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier silence exceeded safe procurement continuity threshold.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-decisions/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Trigger escalation workflow and alternate supplier recovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-decisions/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier overload risk indicates procurement delay probability.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-decisions/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Activate backup supplier routing intelligence.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:46

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (row?.vendor_user_id) return "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (row?.buyer_user_id) return "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
target === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:69

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: target === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:74

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
target === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:75

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Escalate inactive vendor conversation"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:76

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: target === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:77

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Recover buyer engagement"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:81

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
target === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:83

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: target === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:124

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
target: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:138

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title: "Recover buyer engagement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:142

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
target: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:146

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Preview buyer recovery action. Real execution will activate when a conversationId is available.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-autonomous-tasks/route.ts:168

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-closure-agent/route.ts:22

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
blocker: "Supplier price uncertainty",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-closure-agent/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
aiDirective: "Introduce alternate supplier comparison.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-closure-agent/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
blocker: "Low buyer engagement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:11

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-anomaly",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:19

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-health",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:27

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-execution",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:35

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-actions",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:43

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-analytics",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:66

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:87

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:97

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-copilot",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:105

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:113

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-briefing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:121

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-war-room",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:129

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-situation-room",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:137

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-crisis-center",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:145

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:154

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-followup-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:161

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-inbox-actions",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:168

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"pricing",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-command-palette/route.ts:169

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:175

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-negotiation-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:181

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:182

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:186

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "AI Supplier Reliability Engine",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:188

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Open procurement supplier intelligence",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:189

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
href: "/dashboard/procurement-supplier-reliability",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:189

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-supplier-reliability",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:190

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
category: "Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-command-palette/route.ts:197

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-memory-intelligence",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:205

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-closure-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:213

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-autonomous-tasks",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:221

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-task-execution-log",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:229

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-real-execution",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:300

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-anomaly",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:311

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-health",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:335

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:354

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-command-palette/route.ts:364

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-mission-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-control-tower/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (!url || !key) throw new Error("Missing Supabase admin env variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-control-tower/route.ts:35

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 80), 200);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-control-tower/route.ts:41

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-control-tower/route.ts:68

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.context_type || "conversation",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-control-tower/route.ts:73

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${row.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-control-tower/route.ts:82

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const rfq = enriched.filter((r) => String(r.module).includes("rfq")).length;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-control-tower/route.ts:83

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const direct = enriched.filter((r) => !String(r.module).includes("rfq")).length;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-control-tower/route.ts:126

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Pipeline conversion needs stronger follow-up and supplier engagement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-briefing/route.ts:107

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Monitor weakening supplier continuity behavior."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-command/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
if (s.includes("vendor") || s.includes("supplier")) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-command/route.ts:18

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
return "supplier";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-command/route.ts:64

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
} else if (intent === "supplier") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-command/route.ts:71

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? `${strong} supplier/conversation signals look reliable. Prioritize those before weak-response threads.`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot-command/route.ts:72

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Supplier reliability looks weak or insufficient. Increase follow-up and compare more vendors.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot/route.ts:46

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-copilot/route.ts:80

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
The buyer says:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot/route.ts:89

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"reply": "short helpful buyer-facing answer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot/route.ts:92

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"intent": "buyer intent summary",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-copilot/route.ts:94

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "materials/services/rentals/property/marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-copilot/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-center/route.ts:52

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Increase supplier engagement frequency."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-escalation/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
directive: "Increase RFQ monitoring and supplier follow-ups.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-escalation/route.ts:22

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
directive: "Escalate vendor coordination and monitor shortages.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-escalation/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Steel supplier exhaustion risk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-escalation/route.ts:39

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier response collapse",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-crisis-escalation/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Emergency supplier rerouting recommended."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-daily-briefing/route.ts:72

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Increase procurement momentum and supplier engagement."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-emergency-rerouting/route.ts:55

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Monitor supplier continuity.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-emergency-rerouting/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Prepare backup supplier negotiation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-emergency-rerouting/route.ts:66

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"AI emergency rerouting engine is actively monitoring supplier failures and backup procurement recovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-execution-actions/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
automation: "Recommend new supplier outreach campaigns.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-execution-engine/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
steps.push("Create fresh procurement momentum using supplier outreach.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-execution-readiness/route.ts:33

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-execution-readiness/route.ts:39

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-execution-readiness/route.ts:45

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-executive-synthesis/route.ts:137

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Increase supplier diversification focus and continuity preparedness for unstable procurement segments.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:16

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:20

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Send escalation follow-up to vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:29

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property_inquiry",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Recommend alternate supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"AI supplier recovery campaign",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:41

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-followup-agent/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Trigger buyer re-engagement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-forecast/route.ts:19

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-forecast/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Missing Supabase admin environment variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-forecast/route.ts:77

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(200);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-forecast/route.ts:98

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.context_type || "conversation",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-forecast/route.ts:116

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${row.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-inbox-actions/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Escalate vendor follow-up immediately",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-inbox-actions/route.ts:18

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"No supplier response for 72 hours.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-inbox-actions/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Send buyer re-engagement message",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-inbox-actions/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Recommend alternate supplier options",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-live-events/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (!url || !key) throw new Error("Missing Supabase admin env variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-live-events/route.ts:53

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-live-events/route.ts:58

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,event_type,module,entity_title,category,score,created_at,entity_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-live-events/route.ts:61

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-live-events/route.ts:91

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.context_type || "conversation",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-live-events/route.ts:97

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${row.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-live-events/route.ts:115

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
row.module === "rfq"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-live-events/route.ts:116

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `/dashboard/buyer/rfqs/${row.entity_id}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-live-events/route.ts:116

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/buyer/rfqs/${row.entity_id}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-live-events/route.ts:118

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/thread/${row.entity_id}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-live-events/route.ts:119

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-live-events/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Review RFQ and vendor matching"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-live-events/route.ts:128

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Track buyer discovery signal"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-live-events/route.ts:156

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: sourceRow?.href || "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:74

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Save supplier weakening pattern and reduce automatic preference."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:76

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Save buyer seriousness signal for future procurement prioritization."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:90

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Procurement memory is learning positive closure and supplier patterns."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:101

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
const [outcome, memory, supplier] = await Promise.all([
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:104

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
safeJson(`${origin}/api/ai/procurement-supplier-reliability`),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:110

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplierReliability: Array.isArray(supplier?.suppliers)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:111

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? supplier.suppliers
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-evolution/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplierCount: Array.isArray(supplier?.suppliers) ? supplier.suppliers.length : 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-intelligence/route.ts:14

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
type: "supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-intelligence/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier consistently delivered within SLA over 12 procurement cycles.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-intelligence/route.ts:19

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Maintain preferred supplier priority.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-intelligence/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`, `supplier`

**Current system**

```text
"Buyer engagement increases after alternate supplier recommendations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory-intelligence/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"AI procurement memory intelligence actively preserving workflow continuity and supplier intelligence.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Decision-ready buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:68

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Exploratory buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:85

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:86

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor learning should focus on faster replies, complete final terms and reliability signals."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:87

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: side === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:88

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Buyer memory should focus on repeat requirements, preferred categories and quote comparison behavior."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:89

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
: "Platform memory should connect RFQs, vendors, conversations, pricing and closure outcomes.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-memory/route.ts:105

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Supplier appears reliable based on response and risk signals."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:107

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Supplier reliability is moderate; watch response speed and final terms."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:108

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Supplier reliability risk exists; collect more proof before repeat procurement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Save this as a strong procurement memory for future supplier ranking."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:164

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
- Focus on Indian local procurement, RFQs, vendors, pricing, delivery, payment and closure.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-memory/route.ts:172

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{ role: "system", content: "You return strict JSON only." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-memory/route.ts:173

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{ role: "user", content: prompt },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-negotiation-agent/route.ts:14

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
leverage: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-negotiation-agent/route.ts:18

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Push final supplier discount negotiation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-negotiation-agent/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Electrical Vendor Contract",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-negotiation-agent/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
leverage: "supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-negotiation-agent/route.ts:38

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Introduce alternate supplier competition.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-notification-engine/route.ts:84

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
priority === "critical" ? "dashboard_and_whatsapp" : "dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`${origin}/api/ai/procurement-supplier-reliability`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
const supplier =
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:61

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier?.reliability || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:64

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier?.deliveryConsistency || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier?.operationalRisk === "high"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:69

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: supplier?.operationalRisk === "medium"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:88

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier?.deliveryConsistency < 50
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-outcome-learning/route.ts:101

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `admin`, `vendor`, `buyer`

**Current system**

```text
type RecommendationSide = "buyer" | "vendor" | "admin" | "platform";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const side = String(body?.side || "buyer").toLowerCase() as RecommendationSide;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:46

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Strong supplier availability. Compare multiple vendors before decision."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Limited supplier availability. Negotiate clearly and seek one more quote."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
: "Low supplier availability. Improve RFQ details or expand vendor discovery.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Reply to pending vendor conversations and confirm price, timeline and payment terms."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:62

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Open vendor alerts and respond to pending RFQs quickly."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:79

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Good conversion potential if buyer compares and negotiates soon."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:82

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "More vendor response data is needed for stronger conversion prediction.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:84

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:86

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
: side === "admin" || side === "platform"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:87

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Monitor RFQ volume, vendor response quality, price signals and conversion gaps."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:91

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Best supplier prediction",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:117

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-recommendations/route.ts:140

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
body?.module ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-recommendations/route.ts:172

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-recommendations/route.ts:173

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
category: body?.category || body?.module || "supplier recommendation",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:173

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
category: body?.category || body?.module || "supplier recommendation",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-recommendations/route.ts:232

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "system",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:236

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recommendations/route.ts:259

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-recommendations/route.ts:260

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
category: "supplier recommendation",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recovery-agent/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Activate emergency supplier rerouting.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recovery-agent/route.ts:69

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Trigger autonomous supplier follow-up.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recovery-agent/route.ts:105

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(25);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-recovery-command-center/route.ts:8

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Steel supplier stabilization",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recovery-command-center/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
action: "Activate alternate supplier routing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-recovery-command-center/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor negotiation stabilization",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-shortage-forecast/route.ts:51

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Immediate supplier diversification recommended."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-situation-feed/route.ts:19

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor response SLA exceeded for procurement workflow.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-situation-feed/route.ts:24

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Supplier response delay increasing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-situation-feed/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Procurement AI detected slowing vendor engagement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-situation-feed/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Supplier engagement improving",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`${origin}/api/ai/procurement-supplier-reliability`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`${origin}/api/ai/procurement-supplier-collapse`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:111

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
type: "supplier-diversification",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:113

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Reduce supplier dependency concentration",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:115

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Increase alternate supplier onboarding for weak reliability segments.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:115

**Area:** API

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
"Increase alternate supplier onboarding for weak reliability segments.",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/ai/procurement-strategic-orchestration/route.ts:140

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Prepare supplier continuity safeguards",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-collapse/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
id: `supplier-collapse-${index}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-collapse/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: name,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-collapse/route.ts:38

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Initiate emergency supplier rerouting immediately."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-collapse/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "Supplier stability currently acceptable.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-collapse/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"AI supplier-collapse predictor is actively monitoring supplier instability and procurement continuity risks.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
// app/api/ai/procurement-supplier-reliability/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "North Bengal Cement Supply",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:19

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Preferred supplier for high-priority procurement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Eastern Steel Logistics",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Rapid Electrical Vendors",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:39

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Recommend alternate supplier activation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"AI supplier reliability engine actively monitoring procurement continuity risks.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-supplier-reliability/route.ts:56

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier reliability intelligence failed.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-task-execution-log/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (!url || !key) throw new Error("Supabase admin env missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-task-execution-log/route.ts:40

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-telemetry/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Missing Supabase admin env variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-telemetry/route.ts:48

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-telemetry/route.ts:56

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-telemetry/route.ts:61

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,event_type,module,score,created_at"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-telemetry/route.ts:64

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-telemetry/route.ts:105

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
m.module === "rfq"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-telemetry/route.ts:110

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
m.module === "chat"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-timeline/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Missing Supabase admin env variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-timeline/route.ts:28

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "chat",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-timeline/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
category: m.role || m.sender_role || "message",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/procurement-timeline/route.ts:74

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Number(searchParams.get("limit") || 120);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-timeline/route.ts:79

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,event_type,module,entity_id,entity_title,category,type,score,created_at,metadata"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/procurement-timeline/route.ts:82

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(Number.isFinite(limit) ? limit : 120);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/procurement-unified-cognition/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
safeJson(`${origin}/api/ai/procurement-supplier-collapse`),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:75

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
riskReasons.push("Vendor trust score is weak.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:80

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
riskReasons.push("AI vendor score is low.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:85

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
riskReasons.push("Vendor has high risk indicators.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:88

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
riskReasons.push("Vendor has medium risk indicators.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:123

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:150

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Analyze whether a vendor quote is safe, risky, incomplete, overpriced, suspiciously cheap, or operationally weak.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:157

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "string",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/quote-risk-analysis/route.ts:184

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-generator/route.ts:94

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Analyze the buyer requirement and generate a structured procurement RFQ.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-generator/route.ts:103

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"intent": "buyer intent summary",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-generator/route.ts:123

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Requirement:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-generator/route.ts:139

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-intelligence/route.ts:36

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = txt(body?.module) || "marketplace";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/rfq-intelligence/route.ts:74

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Urgency is clear enough for vendor response."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-intelligence/route.ts:79

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Budget is provided. Compare with vendor quotes and market price before closing."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-intelligence/route.ts:92

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/rfq-intelligence/route.ts:107

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `RFQ appears to be about ${category || module}. Current quality score is ${Math.round(score)}/100.`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/rfq-intelligence/route.ts:108

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "RFQ data is limited. Add more information for better vendor matching.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/rfq-intelligence/route.ts:131

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "string",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/rfq-intelligence/route.ts:158

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/search-intent/route.ts:47

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
let module: SearchModule = "all";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (/(land|plot|flat|house|property|katha|bigha|sq\.?ft|apartment|builder|জমি|প্লট|বাড়ি|ফ্ল্যাট|জায়গা|जमीन|प्लॉट|घर|फ्लैट)/i.test(q)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/search-intent/route.ts:50

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:52

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
} else if (/(mason|plumber|electrician|contractor|labour|labor|service|turnkey|interior|painter|মিস্ত্রি|প্লাম্বার|ইলেকট্রিশিয়ান|কন্ট্রাক্টর|শ্রমিক|प्लंबर|इलेक्ट्रीशियन|मिस्त्री|ठेकेदार|मजदूर)/i.test(q)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/search-intent/route.ts:54

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:56

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:58

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "blog";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:79

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:102

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = allowedModules.has(parsed?.module) ? parsed.module : "all";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:109

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:143

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Allowed module values:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:155

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
- If query is about land, plot, flat, house, builder project, জমি, প্লট, বাড়ি, जमीन, प्लॉट: module property.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/search-intent/route.ts:155

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
- If query is about land, plot, flat, house, builder project, জমি, প্লট, বাড়ি, जमीन, प्लॉट: module property.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:156

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
- If query is about cement, steel, sand, bricks, tiles, paint, plumbing/electrical goods, সিমেন্ট, রড, বালি, ইট, सीमेंट, सरिया, रेत: module materials.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:157

**Area:** API

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
- If query is about mason, plumber, electrician, labour, contractor, turnkey, repair, মিস্ত্রি, প্লাম্বার, ইলেকট্রিশিয়ান, प्लंबर, मिस्त्री: module services.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/search-intent/route.ts:157

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
- If query is about mason, plumber, electrician, labour, contractor, turnkey, repair, মিস্ত্রি, প্লাম্বার, ইলেকট্রিশিয়ান, प्लंबर, मिस्त्री: module services.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:158

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
- If query is about JCB, equipment, machine, generator, scaffolding on rent, ভাড়া মেশিন, किराये की मशीन, जेसीबी: module rentals.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:159

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
- If unsure, use module all.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:165

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "all",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/search-intent/route.ts:186

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:122

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
let marketSituation = "Market data is limited. Compare at least 3 vendor quotes before final decision.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:133

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: text(body?.module, "marketplace"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-decision/route.ts:138

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Ask the vendor to confirm final price, delivery charge, invoice, delivery timeline, and payment terms before closing."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:142

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Shortlist this vendor, negotiate final terms, and proceed only after written confirmation."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:143

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Collect more vendor quotes before making a decision.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:146

**Area:** API

**Category:** Visible AI Terminology

**Matched:** `ai score`

**Current system**

```text
? "Decision is based on price, trust, AI score, delivery confidence, response speed, boost signal, and risk indicators."
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:147

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Not enough vendor data was provided for a strong recommendation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:174

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Choose the best decision for the user using vendor data, price data, urgency, budget, city, category and buyer intent.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:181

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module": "string",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-decision/route.ts:197

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
- Prefer reliable vendor over cheapest vendor if risk is high.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:200

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
- Every ranked vendor should include smart_decision_score if possible.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-decision/route.ts:208

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
type SmartFillTone = "professional" | "luxury" | "urgent" | "friendly";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:71

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return "Return a builder/project description focusing on project features, location, investment potential, and verification reminders.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:74

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
return "Return a professional, SEO-friendly listing description of 180-300 words.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:81

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: moduleName,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-fill/route.ts:91

**Area:** API

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
? "• Down payment and payment timeline should be clearly mentioned.\n• EMI period and installment amount must be verified.\n• Registration, deed custody, guarantor, and default terms should be clearly agreed in writing.\n• Please verify al…
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/ai/smart-fill/route.ts:92

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "This listing is designed to present the key details clearly, professionally, and in a buyer-friendly manner. Please add exact location, price, specifications, amenities, and legal/availability details before publishing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:94

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Clear and buyer-focused presentation",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:96

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
"Suitable for professional listing use",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:119

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = safeString(body?.module) as SmartFillModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-fill/route.ts:121

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
const tone = (safeString(body?.tone) || "professional") as SmartFillTone;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:144

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ ok: false, error: "module and action are required." },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-fill/route.ts:154

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
You are the AI Smart-Fill Assistant for 3bigha.com, an Indian real estate, materials, services, rentals, turnkey, builder project, and blog/news platform.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:156

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module: ${moduleName}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-fill/route.ts:194

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module guidance:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/smart-fill/route.ts:196

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- builder_project: focus on project features, unit availability, amenities, location, buyer confidence, and verification reminders.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:209

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "system",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:213

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{ role: "user", content: prompt },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/smart-fill/route.ts:224

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: moduleName,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/ai/support-issue-draft/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:68

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:121

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(body?.role || "user").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:152

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
"ai_issue_category": "login | listing | payment | rfq | chat | vendor | buyer | technical | fraud | abuse | legal | general",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:164

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
- Mention the user role and issue category only if useful.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:172

**Area:** API

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
- Use "payment" for payment, refund, invoice, subscription, boost, or billing issues.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/support-issue-draft/route.ts:179

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- escalation_level 2 = urgent admin escalation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:184

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
User role: ${role}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-issue-draft/route.ts:200

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:78

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const side = m?.is_admin_message ? "Admin" : "User";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:103

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (mode === "admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:105

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
You are a STRICT message polishing assistant for 3bigha.com admin support replies.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:107

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Your ONLY job is to rewrite the ADMIN'S EXISTING DRAFT professionally WITHOUT changing its meaning.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:113

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- Do NOT say the issue is fixed unless the admin draft says it is fixed.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:114

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- Do NOT ask for missing details unless the admin draft asks for details.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:115

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- Do NOT change the admin's intention.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:117

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- Keep the rewritten message close to the admin draft.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:121

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin original draft:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:132

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
- Do NOT pretend to be admin.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:137

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
- Make it clearer and more professional.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-reply-suggestion/route.ts:162

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-ticket-summary/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
issue_summary: complaint || "Support issue needs admin review.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-ticket-summary/route.ts:70

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const side = m?.is_admin_message ? "Admin" : "User";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-ticket-summary/route.ts:83

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
"likely_category": "login | listing | payment | rfq | chat | vendor | buyer | technical | fraud | general",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-ticket-summary/route.ts:100

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
User role: ${ticket?.user_role || "user"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/support-ticket-summary/route.ts:118

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/translate/route.ts:100

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "system",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/translate/route.ts:102

**Area:** API

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
"You are a professional marketplace UI translator for a real estate and construction platform. Translate each item naturally. Preserve numbers, prices, brand names, place names, URLs, emojis, and symbols. Return ONLY a valid JSON array of …
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/translate/route.ts:105

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:16

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function isPremiumPlan(plan: unknown, status: unknown, boostPriority: unknown) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:17

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p = String(plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:26

**Area:** API

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
p === "gold_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:27

**Area:** API

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
p === "platinum_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
audience: "buyer" | "vendor" | "both";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:42

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
priority: "free" | "premium";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:74

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium vendors can receive stronger priority alerts when buyers show closing intent.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:101

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m.role || "user"}: ${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:156

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${m.role || "user"}: ${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:211

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium vendors can receive priority alerts when buyers move toward confirmation.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:230

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
priority: "premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:232

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title: "High Intent Buyer Detected",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:236

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer should verify price, quantity, address, delivery time and bill before payment.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:238

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
"Vendor should respond immediately. This buyer may be ready to close.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:240

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Premium vendors should receive instant priority alert for this buyer signal.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:240

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium vendors should receive instant priority alert for this buyer signal.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:279

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Reply quickly with clear final terms before the buyer loses interest.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:298

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
reason: "Not eligible or Supabase admin unavailable",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:303

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:314

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
const premium = isPremiumPlan(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:320

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (!premium) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:323

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: false,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:324

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor is not premium or boosted",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:324

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
reason: "Vendor is not premium or boosted",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:357

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: true,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:364

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: true,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:376

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (!monetization?.premium || !monetization?.notificationId) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:379

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
reason: "Not premium or notification missing",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:388

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch(`${origin}/api/vendor/whatsapp-alerts/send`, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:421

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
row.audience === "buyer" || row.audience === "vendor" || row.audience === "both"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:426

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
row.priority === "premium" || row.priority === "free"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:476

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const side = String(body?.side || "buyer").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:496

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
reason: "No vendorUserId or alert not premium eligible",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/ai/vendor-alert/route.ts:515

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m) => `${String(m.role || "user")}: ${String(m.body || "").slice(0, 500)}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:521

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Detect whether this buyer-vendor conversation needs an alert.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:528

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"title": "High Intent Buyer Detected",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:537

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendorNextAction": "Short best action for vendor."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:541

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
- vendor: alert vendor when buyer is serious or waiting.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:542

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
- buyer: alert buyer when vendor is responsive or final details are missing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:549

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
- Detect buyer hesitation, vendor urgency, lead loss risk and follow-up need.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:552

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
- vendorNextAction must help the vendor prevent lead loss or close safely.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:568

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-alert/route.ts:615

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-coach/route.ts:29

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Respond faster to buyer messages",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-coach/route.ts:57

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-document-verify/route.ts:2

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { validateGstin, documentTextContainsNeedle } from "@/lib/vendor-verification/gstin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-document-verify/route.ts:113

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
You are verifying vendor onboarding documents for 3bigha.com.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-document-verify/route.ts:113

**Area:** API

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
You are verifying vendor onboarding documents for 3bigha.com.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/ai/vendor-document-verify/route.ts:157

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/ai/vendor-document-verify/route.ts:262

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
error: e?.message || "Vendor document verification failed.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/api/buyer/rfq/[rfqId]/accept/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:142

**Area:** API

**Category:** Visible AI Terminology

**Matched:** `ai ranking`

**Current system**

```text
message: "Your profile performed well. AI ranking boosted.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:231

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"A buyer accepted your quote. This improves your future AI reputation and routing strength.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:247

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
conversationWarning = "Buyer user id is invalid for RFQ conversation.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:249

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
conversationWarning = "Vendor user id is invalid for RFQ conversation.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:305

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:311

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:326

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
"Quote accepted. Buyer and vendor can now coordinate here for confirmation, delivery schedule, and updates.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:364

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// 🔥 TRACK VENDOR SELECTION
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:390

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}?accepted=1`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/buyer/rfq/[rfqId]/accept/route.ts:390

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}?accepted=1`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/construction-cost/create-rfq/route.ts:25

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (value === "economy" || value === "standard" || value === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-cost/estimate/route.ts:31

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (value === "economy" || value === "standard" || value === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:50

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:94

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:101

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = generateConstructionMilestonePlan({
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:112

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
body.grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:126

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const rows = plan.milestones.map((milestone) => ({
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/milestones/route.ts:184

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/recovery/route.ts:64

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Unable to generate construction recovery plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/recovery/route.ts:107

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Unable to generate construction recovery plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/route.ts:40

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/route.ts:90

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/route.ts:108

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
body.grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/route.ts:179

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/[id]/supervisor/route.ts:63

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Unable to generate construction supervisor plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/[id]/supervisor/route.ts:104

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Unable to generate construction supervisor plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/construction-projects/route.ts:36

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/route.ts:80

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/construction-projects/route.ts:105

**Area:** API

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
body.grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/[conversationId]/close/route.ts:20

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(SUPABASE_URL, SUPABASE_SERVICE, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/close/route.ts:24

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { error } = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/[messageId]/route.ts:137

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/messages/[messageId]/route.ts:309

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:262

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: null as "buyer" | "vendor" | null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:272

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: null as "buyer" | "vendor" | null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:276

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role =
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:278

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:280

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:283

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:290

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: null as "buyer" | "vendor" | null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:297

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:322

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:384

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:399

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
message: "Buyer is ready. Respond immediately to close this deal.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:415

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
message: "Buyer is discussing payment. Push for deal closure now.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:438

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const actualRole = validated.role;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:481

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: actualRole,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:497

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
actualRole === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:514

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
actualRole === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:515

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "New buyer message"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:516

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "New vendor message",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:520

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
url: `/dashboard/thread/${conversationId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/conversations/[conversationId]/messages/route.ts:532

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "chat",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:63

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:92

**Area:** API

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = String(conv.buyer_user_id ?? "") === String(user.id);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:93

**Area:** API

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const isVendor = String(conv.vendor_user_id ?? "") === String(user.id);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:95

**Area:** API

**Category:** Capability and Permission

**Matched:** `isvendor`, `isbuyer`

**Current system**

```text
if (!isBuyer && !isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:102

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
const role = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:102

**Area:** API

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const role = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:161

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:173

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/seen/route.ts:183

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at,last_seen_message_id")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/[conversationId]/upload/route.ts:126

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/conversations/ensure/route.ts:17

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(bp?.subscription_plan || "free");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:31

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "hub_vendor") return 20;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:32

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "premium_vendor") return 10;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:33

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor") return 5;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:105

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: "Missing item for vendor routing." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/ensure/route.ts:119

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:130

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: "No verified vendor found for this item in this location." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/ensure/route.ts:144

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/ensure/route.ts:161

**Area:** API

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
// 🧠 SMART SCORING + SUBSCRIPTION PRIORITY
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/conversations/ensure/route.ts:189

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: "No valid vendor found after scoring." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/ensure/route.ts:211

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: "Invalid vendor user id." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/conversations/ensure/route.ts:238

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ error: "Buyer and vendor cannot be the same user." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/boost-expiry/route.ts:22

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/boost-expiry/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/marketplace-opportunities/route.ts:32

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 25,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/cron/procurement-execution/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Missing Supabase admin environment variables.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/procurement-execution/route.ts:126

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${conversationId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/cron/procurement-execution/route.ts:145

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ ok: false, error: "Unauthorized." }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/cron/procurement-execution/route.ts:158

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(40);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/cron/procurement-execution/route.ts:216

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
url: `/dashboard/thread/${row.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/cron/rfq-followups/route.ts:62

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
reason: "Maximum follow-up limit reached.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/cron/rfq-followups/route.ts:92

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor has not responded after first reminder.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:140

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Please respond today to avoid missing this buyer enquiry."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:147

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
${message || title || "A buyer enquiry is waiting for your response."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:157

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Write a short WhatsApp RFQ follow-up message for a vendor on 3bigha.com.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:165

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Mention that a buyer enquiry/RFQ is waiting.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:166

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
- Ask vendor to open and respond with price, availability, delivery/work timeline.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:174

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
${message || title || "Buyer enquiry waiting."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:185

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:279

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized cron request." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/cron/rfq-followups/route.ts:297

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/cron/rfq-followups/route.ts:358

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const rfqLink = `${siteUrl}/dashboard/vendor/rfqs/${row.rfq_id}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/rfq-followups/route.ts:358

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const rfqLink = `${siteUrl}/dashboard/vendor/rfqs/${row.rfq_id}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/cron/vendor-intelligence-refresh/route.ts:2

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { refreshVendorMarketplaceIntelligence } from "@/lib/marketplace/vendor-intelligence-refresh";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/cron/vendor-intelligence-refresh/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
job: "vendor-intelligence-refresh",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/enquiries/create-and-chat/route.ts:21

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function subjectTypeFromModule(module: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:22

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const m = String(module ?? "").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:30

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function contextTypeFromModule(module: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:31

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const m = String(module ?? "").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:62

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function listingIdFieldFromModule(module: string, refId: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:63

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const m = String(module ?? "").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:143

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sender_role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/enquiries/create-and-chat/route.ts:181

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(body?.module ?? "").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:188

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rental" || module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:196

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!module || !refId || !vendorUserId || !message) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:205

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return NextResponse.json({ error: "Invalid vendor user id." }, { status: 400 });
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/enquiries/create-and-chat/route.ts:210

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ error: "Buyer and vendor cannot be the same user." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/enquiries/create-and-chat/route.ts:229

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const subjectType = subjectTypeFromModule(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:231

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// use admin client for writes that may involve multiple-user rows
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/enquiries/create-and-chat/route.ts:246

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const contextType = contextTypeFromModule(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:255

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
(module === "rental" || module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:259

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/enquiries/create-and-chat/route.ts:267

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
...listingIdFieldFromModule(module, refId),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/geography/options/route.ts:15

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
function pagedResponse(data: any[] | null, limit: number, offset: number) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:17

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const options = rows.slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:21

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
hasMore: rows.length > limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:35

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Math.max(Number(searchParams.get("limit") || 50), 1), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:45

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:58

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:69

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:84

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:95

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:110

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:124

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:133

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
return pagedResponse([], limit, offset);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:140

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:149

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
return pagedResponse([], limit, offset);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:158

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:174

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:188

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:196

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
if (!localBodyCodes.length) return pagedResponse([], limit, offset);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:205

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:220

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:231

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:247

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:260

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:269

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
return pagedResponse([], limit, offset);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:278

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:299

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:309

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:327

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:333

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const searchLimit = Math.min(limit, 25);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:398

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/geography/options/route.ts:419

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/inbox-ai-action/route.ts:29

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai-action/route.ts:48

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module: ${String(module ?? "")}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai/route.ts:15

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai/route.ts:23

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = String(data.module ?? "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai/route.ts:34

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
summary = `Unread ${data.module || "thread"} needs review`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai/route.ts:71

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/inbox-ai/route.ts:99

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module: ${String(module ?? "")}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/investment/applications/route.ts:154

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const redirect_path = `/dashboard/investor/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:32

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:39

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:43

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:44

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/accept-nda/route.ts:193

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
{ error: "Access denied." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:32

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:39

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:43

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:44

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:199

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
{ error: "Access denied." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:318

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
{ error: "Access denied." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/documents/[documentId]/route.ts:340

**Area:** API

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
{ error: "You do not have permission to delete this document." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:29

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:33

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:46

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:81

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (String(room.builder_user_id || "") === userId) return "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:82

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (isAdminLikeRole(viewerRole)) return "admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:177

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
{ error: "Access denied." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/documents/route.ts:266

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
{ error: "Access denied." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:14

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:18

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:34

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:40

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:44

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:71

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (String(room.builder_user_id || "") === String(userId || "")) return "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:72

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (isAdminLikeRole(viewerRole)) return "admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:159

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return NextResponse.json({ error: "Access denied." }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:163

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limitParam = Number(searchParams.get("limit") || "100");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:164

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Number.isFinite(limitParam)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:173

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:231

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return NextResponse.json({ error: "Access denied." }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/messages/route.ts:288

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if ("builder_last_read_at" in room && senderRole === "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:37

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:119

**Area:** API

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
{ error: "You do not have permission to update NDA settings." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:152

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Builder enabled NDA protection for this deal room."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/nda/route.ts:153

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: "Builder disabled NDA protection for this deal room."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:14

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:28

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:32

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:40

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:44

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:185

**Area:** API

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
{ error: "You do not have permission to access this deal room." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:196

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const builder = userMap[String(room?.builder_user_id || "")] || null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:200

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:204

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
? String(viewerRole || "admin")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:208

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
resolvedViewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:209

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? `/dashboard/builder/deal-rooms/${encodeURIComponent(String(room?.id || ""))}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:209

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/builder/deal-rooms/${encodeURIComponent(String(room?.id || ""))}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:210

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: `/dashboard/investor/deal-rooms/${encodeURIComponent(String(room?.id || ""))}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:232

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
resolvedViewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:249

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_name: builder?.name ?? builder?.full_name ?? null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/route.ts:250

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_email: builder?.email ?? null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:11

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:37

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/seen/route.ts:102

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return NextResponse.json({ error: "Access denied." }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:20

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:21

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:25

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:37

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:51

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:180

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return NextResponse.json({ error: "Access denied." }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/deal-rooms/[id]/stage/route.ts:185

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{ error: "Only the builder can update deal stage." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:7

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:8

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:10

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
r === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
r === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:24

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:28

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:40

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:152

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const roleParam = String(searchParams.get("role") || "")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:161

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (roleParam === "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:166

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
} else if (roleParam === "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:171

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
["builder", "promoter", "vendor"].includes(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:204

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const builder = userMap[String(room?.builder_user_id || "")] || null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:233

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_name: builder?.name ?? builder?.full_name ?? null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/deal-rooms/route.ts:234

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_email: builder?.email ?? null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:21

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:22

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!error && data?.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return String(data.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:46

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isBuilderLikeRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
return r === "builder" || r === "vendor" || r === "promoter";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:145

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{ ok: false, error: "Opportunity builder is missing." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:164

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
error: "Builder accounts cannot express interest as investors.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:178

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:195

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirectTo: `/dashboard/investor/deal-rooms/${existingRoom.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:211

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/[id]/express-interest/route.ts:290

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirectTo: `/dashboard/investor/deal-rooms/${createdRoom.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/investment/opportunities/[id]/route.ts:52

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
let q = supabase.from("investment_opportunities").select("id").eq("slug", slug).limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/[id]/route.ts:75

**Area:** API

**Category:** Capability and Permission

**Matched:** `canedit`

**Current system**

```text
const canEdit = isOwner && ["draft", "pending_review"].includes(row.status);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/opportunities/[id]/route.ts:120

**Area:** API

**Category:** Capability and Permission

**Matched:** `canedit`

**Current system**

```text
canEdit,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/opportunities/[id]/route.ts:274

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/opportunities/[id]/submit/route.ts:94

**Area:** API

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
{ error: "You do not have permission to submit this opportunity." },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/opportunities/[id]/submit/route.ts:111

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:44

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:66

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ ok: false, error: "Unauthorized" },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/investment/opportunities/route.ts:148

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:261

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(50, Math.max(1, Number(searchParams.get("limit") || 12)));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:302

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const from = (page - 1) * limit;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:303

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const to = from + limit - 1;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:319

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/investment/opportunities/route.ts:321

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
totalPages: Math.ceil((count || 0) / limit),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/marketplace/vendor-conversion/route.ts:5

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/lib/marketplace/vendor-conversion-analytics";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/marketplace/vendor-conversion/route.ts:23

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ ok: false, error: "Invalid vendor conversion event type." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/marketplace/vendor-conversion/route.ts:31

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: body?.module || null,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/marketplace/vendor-conversion/route.ts:54

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("vendor conversion route error:", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/marketplace/vendor-conversion/route.ts:56

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ ok: false, error: error?.message || "Vendor conversion tracking failed." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/mobile/push/register/route.ts:18

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
error: "Unauthorized",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/mobile/push/register/route.ts:77

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/mobile/push/register/route.ts:78

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
typeof body?.role === "string"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/mobile/push/register/route.ts:79

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
? body.role
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/mobile/push/register/route.ts:107

**Area:** API

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
console.error("Push registration failed", err);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/mos/generate-opportunities/route.ts:27

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: Number(body?.limit || 10),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/all/route.ts:3

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
const MODULES = [
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/nearby/all/route.ts:12

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
type NearbyModule = (typeof MODULES)[number];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/nearby/all/route.ts:65

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = searchParams.get("limit") || "10";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/all/route.ts:72

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
params.set("limit", limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/all/route.ts:76

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
MODULES.map(async (moduleName) => {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/nearby/materials/route.ts:30

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/materials/route.ts:79

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/places/route.ts:24

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/places/route.ts:66

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/property/route.ts:30

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/property/route.ts:73

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/rentals/route.ts:32

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/rentals/route.ts:76

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/services/route.ts:33

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/services/route.ts:78

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/vendors/route.ts:16

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { enrichVendorMarketplaceIntelligence } from "@/lib/marketplace/live-vendor-intelligence";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/nearby/vendors/route.ts:41

**Area:** API

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: boolean | null;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/nearby/vendors/route.ts:49

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 50), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/vendors/route.ts:68

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/nearby/vendors/route.ts:70

**Area:** API

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
"user_id,business_name,business_type,nature_of_business,contact_person,phone_primary,phone_whatsapp,city,district,state,pincode,locality,service_radius_km,delivery_radius_km,statewide_service,nationwide_service,verified_lat,verified_lng,lo…
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/nearby/vendors/route.ts:78

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/nearby/vendors/route.ts:98

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
getCoordinates: (vendor) => resolveVendorCoordinates(vendor).coordinates,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/nearby/vendors/route.ts:100

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.filter((vendor) => matchesBusinessCategory(vendor, category))
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/nearby/vendors/route.ts:104

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const nearbyVendors = sortNearbyVendors(filteredVendors).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/notifications/read-all/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/notifications/read/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/notifications/route.ts:13

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/notifications/route.ts:52

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/notifications/route.ts:65

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
id: "smart-open-dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/notifications/route.ts:67

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
message: "Open your dashboard to review pending requirements, leads, conversations and marketplace opportunities.",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/notifications/route.ts:68

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/notifications/route.ts:69

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
cta: "Open Dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/onboarding/verify-location/route.ts:171

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/onboarding/verify-location/route.ts:193

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
console.error("business_profiles update error:", updateErr);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/payment/create-order/route.ts:41

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/payment/create-order/route.ts:54

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(body?.plan || "");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:56

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (!PLAN_PRICES[plan]) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:57

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
return NextResponse.json({ error: "Invalid plan" }, { status: 400 });
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:60

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan.startsWith("boost_")) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:61

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const boost = BOOST_META[plan];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:67

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:68

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
amount_paise: PLAN_PRICES[plan],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:105

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
amount: PLAN_PRICES[plan],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:110

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:114

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan.startsWith("boost_")) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/create-order/route.ts:122

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
.eq("plan", plan)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:38

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:53

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (String(plan).startsWith("boost_")) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:54

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const days = BOOST_DAYS[plan] || 3;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/payment/verify/route.ts:61

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
boost_priority: PLAN_BOOST_PRIORITY[plan] || 0,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:63

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
ai_revenue_score: (PLAN_BOOST_PRIORITY[plan] || 0) * 3, // 🧠 BOOST REVENUE SIGNAL
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:85

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/payment/verify/route.ts:87

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subscription_plan: plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:90

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
boost_priority: PLAN_BOOST_PRIORITY[plan] || 0,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:91

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
ai_revenue_score: (PLAN_BOOST_PRIORITY[plan] || 0) * 5, // 🧠 REVENUE SIGNAL
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/payment/verify/route.ts:94

**Area:** API

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
return NextResponse.json({ ok: true, type: "subscription" });
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/price-explanation/route.ts:138

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/property/submit-for-review/route.ts:36

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
{ error: { message: "Unauthorized." } },
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/public/vendor-opportunities/route.ts:15

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await supabase.from(table).select("id,name").limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/public/vendor-opportunities/route.ts:25

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = searchParams.get("module");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:27

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!["property", "materials", "services", "rentals"].includes(String(module))) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:34

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,promotion_score,promotion_type,shortage_score,geo_state_id,geo_district_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:35

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:38

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(3),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/public/vendor-opportunities/route.ts:41

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,recommended_vendor_count,priority,geo_state_id,geo_district_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:42

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/public/vendor-opportunities/route.ts:45

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(3),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/public/vendor-opportunities/route.ts:81

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:196

**Area:** API

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = String(conv.buyer_user_id) === String(user.id);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:197

**Area:** API

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const isVendor = String(conv.vendor_user_id) === String(user.id);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:199

**Area:** API

**Category:** Capability and Permission

**Matched:** `isvendor`, `isbuyer`

**Current system**

```text
if (!isBuyer && !isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:203

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
const senderRole = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:203

**Area:** API

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const senderRole = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:285

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:290

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:442

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
senderRole === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:457

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
senderRole === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:458

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "New vendor response"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:459

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "New buyer message",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:462

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
senderRole === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:468

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
senderRole === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:469

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${rfqId}/chat`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:469

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${rfqId}/chat`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq-conversations/[rfqId]/messages/route.ts:470

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: `/vendor/inbox-v2/${rfqId}/chat`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:98

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// ✅ map rfqs.module -> rfq_items.item_type (plural -> singular)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:99

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleToItemType(module: RfqModule): RfqItemType {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:100

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "material";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:101

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "service";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:102

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return "rental";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:145

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// ✅ B) Admin client
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:152

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// ✅ module (required)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:153

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = normalizeModule(body?.module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:154

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:156

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"Module is required and must be one of: materials, services, rentals, properties."
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:204

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
message: "⚠️ Your RFQ content looks suspicious and may be reviewed by admin.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:236

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module, // ✅ must match rfqs_v2_module_check
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:266

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const matchUrl = new URL("/api/rfq/vendor-matches", req.url);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:268

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
matchUrl.searchParams.set("module", module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:299

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
starterMessage: `🚀 New Buyer Requirement
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:309

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
⚡ Buyer is actively comparing vendors. Fast response increases chances of deal closure.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:313

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ignore per-vendor failure
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:329

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const item_type = moduleToItemType(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:370

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:373

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
category: cleanItems?.[0]?.title || title || module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:391

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// 2.5) ✅ NEW: Create vendor targets (so Vendor Inbox table shows newest RFQs)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:396

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `business_profiles`

**Current system**

```text
// NOTE: assumes vendors are stored in business_profiles (as your vendor APIs do)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:407

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:412

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/create/route.ts:414

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ✅ Fallback: if location match finds no vendor, use boosted/general vendor pool
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:417

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:421

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/create/route.ts:462

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const rfqLink = `https://www.3bigha.com/dashboard/vendor/rfqs/${rfqId}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:462

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const rfqLink = `https://www.3bigha.com/dashboard/vendor/rfqs/${rfqId}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq/create/route.ts:497

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
url: `/dashboard/vendor/rfqs/${rfqId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:497

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
url: `/dashboard/vendor/rfqs/${rfqId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq/create/route.ts:500

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:537

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ✅ AUTO CHAT CONNECT: create unified chat with top matched vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:575

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/create/route.ts:590

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/thread/${autoConversationId}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq/create/route.ts:593

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
autoChatVendorName = "matched vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:633

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
url: `/dashboard/buyer/rfqs/${rfqId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/create/route.ts:633

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
url: `/dashboard/buyer/rfqs/${rfqId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq/create/route.ts:646

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { enrichVendorMarketplaceIntelligence } from "@/lib/marketplace/live-vendor-intelligence";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:19

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Number(searchParams.get("limit") || 25), 100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:37

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:39

**Area:** API

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
"user_id,business_name,business_type,nature_of_business,contact_person,phone_primary,phone_whatsapp,city,district,state,pincode,locality,service_radius_km,delivery_radius_km,statewide_service,nationwide_service,verified_lat,verified_lng,lo…
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:47

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/nearby-vendor-recommendations/route.ts:68

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:3

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorTrustReputation } from "@/lib/vendors/vendor-trust-reputation";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Score this vendor for a buyer RFQ on 3bigha.com.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:35

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer RFQ:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:36

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module=${input.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:63

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
input: [{ role: "user", content: prompt }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:94

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = clean(row.subscription_plan).toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:104

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "hub_vendor" || plan === "platinum") return 20;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:105

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "premium_vendor" || plan === "gold") return 10;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:106

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor" || plan === "silver") return 5;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:112

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:144

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = clean(input.module).toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:146

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module.includes("materials")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:151

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module.includes("rentals")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:156

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module.includes("services")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:161

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module.includes("propert")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:173

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
reason: reasons.length ? reasons.join(" • ") : "basic buyer browsing signal",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:177

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
async function getLatestPriceSignal(admin: any, row: any, input: VendorMatchInput) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:180

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!userId || clean(input.module).toLowerCase() !== "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:184

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
let query = admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:191

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:446

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reasons.push("boosted vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:517

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "AI ranked vendor based on location, relevance, trust, and boost",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:651

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
for (const plan of levelPlan) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:652

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
considered_levels.push(plan.level);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:655

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
.filter((row) => row.geo_expansion_level === plan.level)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:673

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.level === "national" &&
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:682

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
expansion_level = plan.level;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:684

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (picked.length >= plan.target) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:689

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
expansion_target: plan.target,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:712

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = clean(url.searchParams.get("module") || "materials");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:724

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:736

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(SUPABASE_URL, SUPABASE_SERVICE, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:740

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data, error } = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:741

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:750

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:764

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
? await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:787

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:800

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const priceRanked = await getLatestPriceSignal(admin, row, input);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:815

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Local vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:824

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// 🧠 AI CONTROL: reduce boost impact if pricing is poor
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:829

**Area:** API

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
boostMultiplier = 0.4; // very poor pricing → heavy penalty
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:895

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (planBoost > 0) boostReasonParts.push(`plan boost +${planBoost}`);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:904

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `${ranked.reason} • AI match: ${aiRanked.reason}${boostReason} • Buyer intent: ${buyerIntent.reason}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:905

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: `${ranked.reason}${boostReason} • Buyer intent: ${buyerIntent.reason}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:969

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
source: "business_profiles",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:1007

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
// 🔥 LIMIT TOP RECOMMENDED (ONLY ONE)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/rfq/vendor-matches/route.ts:1079

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "New vendor matches found",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:1083

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
url: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:1083

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
url: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/rfq/vendor-matches/route.ts:1088

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor match push failed", e);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/rfq/vendor-matches/route.ts:1094

**Area:** API

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### HIGH — app/api/rfq/vendor-matches/route.ts:1108

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
source: "business_profiles",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,is_vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:63

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:64

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
profile?.role ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:66

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(profile?.is_vendor ? "vendor" : "user")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:69

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/messages/[ticketId]/route.ts:69

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/messages/[ticketId]/route.ts:84

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin && ticket.user_id !== user.id) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/messages/[ticketId]/route.ts:106

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
isAdmin,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/messages/[ticketId]/route.ts:107

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:155

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,is_vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:159

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:160

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
profile?.role ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:162

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(profile?.is_vendor ? "vendor" : "user")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:165

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:165

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/reply/route.ts:180

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin && ticket.user_id !== user.id) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/reply/route.ts:192

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
sender_role: role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:195

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
is_admin_message: isAdmin,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/reply/route.ts:214

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/reply/route.ts:312

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/reply/route.ts:365

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("id,role,requested_role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:367

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
"role.eq.master_admin,role.eq.admin,requested_role.eq.master_admin,requested_role.eq.admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:374

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
.filter((admin: any) => admin?.id)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:375

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
.map((admin: any) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:377

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
user_id: admin.id,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:386

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
? `Ticket ${ticket.ticket_no || ""} needs urgent admin review.`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:388

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
action_url: `/admin/dashboard/support/${ticketId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/reply/route.ts:388

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
action_url: `/admin/dashboard/support/${ticketId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/support/tickets/route.ts:12

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
throw new Error("Supabase admin environment variables are missing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:101

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:102

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:149

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,approval_status,is_vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:153

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:154

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
profile?.role ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:156

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(profile?.is_vendor ? "vendor" : "user")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:159

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:159

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const isAdmin = role === "master_admin" || role === "admin";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/tickets/route.ts:165

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/support/tickets/route.ts:167

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/tickets/route.ts:182

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:183

**Area:** API

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
isAdmin,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/support/tickets/route.ts:261

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,approval_status,is_vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:266

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
profile?.role ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:268

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(profile?.is_vendor ? "vendor" : "user")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:305

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("id,role,requested_role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:307

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `admin`

**Current system**

```text
"role.eq.master_admin,role.eq.admin,requested_role.eq.master_admin,requested_role.eq.admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:324

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
.filter((admin: any) => admin?.id)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:325

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
.map((admin: any) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:327

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
user_id: admin.id,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:338

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
action_url: `/admin/dashboard/support/${data.id}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/support/tickets/route.ts:338

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
action_url: `/admin/dashboard/support/${data.id}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/system/autonomous-vendor-recruitment-refresh/route.ts:2

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { refreshAutonomousVendorRecruitmentIntelligence } from "@/lib/marketplace/autonomous-vendor-recruitment-intelligence";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/system/vendor-intelligence-refresh/route.ts:2

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { refreshVendorMarketplaceIntelligence } from "@/lib/marketplace/vendor-intelligence-refresh";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/billing/[billNo]/pdf/route.ts:169

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorBusinessName = clean((bill as any).vendor_business_name || "Vendor Business");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/billing/[billNo]/verify/route.ts:58

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "This bill/challan exists in the 3Bigha vendor billing system.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/dispatch/send-whatsapp-update/route.ts:114

**Area:** API

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return NextResponse.json({ ok: false, error: "Buyer phone number not available." }, { status: 400 });
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/inbox/row/route.ts:15

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Must be logged in vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:19

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (score >= 70) return "🔥 High Visibility Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:20

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (score >= 55) return "⚡ Rising Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:21

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Active Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:27

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:32

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/leaderboard/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.map((vendor: any) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:43

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorId = clean(vendor.user_id);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:45

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
boostScore: Number(vendor.boost_priority || 0) * 100,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
clean(vendor.subscription_status).toLowerCase() === "active" ? 10 : 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reputationScore: vendor.reputation_score,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
authorityScore: vendor.authority_score,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:50

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
conversionRate: vendor.conversion_rate,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:51

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
responseRate: vendor.response_rate,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:52

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
activityScore: vendor.activity_score,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:53

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
demandScore: vendor.demand_score,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:54

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
liquidityScore: vendor.liquidity_score,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
name: clean(vendor.business_name) || "Local Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:60

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
city: clean(vendor.city),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:61

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
locality: clean(vendor.locality),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:62

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
plan: clean(vendor.subscription_plan) || "free",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:62

**Area:** API

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: clean(vendor.subscription_plan) || "free",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/leaderboard/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reputationScore: Number(vendor.reputation_score || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:68

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
authorityScore: Number(vendor.authority_score || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:69

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
conversionRate: Number(vendor.conversion_rate || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:70

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
responseRate: Number(vendor.response_rate || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:71

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
activityScore: Number(vendor.activity_score || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:72

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
demandScore: Number(vendor.demand_score || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:73

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
liquidityScore: Number(vendor.liquidity_score || 0),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/leaderboard/route.ts:74

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
updatedAt: vendor.marketplace_intelligence_updated_at,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/notifications/route.ts:40

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(50, Math.max(1, Number(searchParams.get("limit") || 10)));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/notifications/route.ts:48

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/notifications/route.ts:66

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: error?.message || "Failed to load vendor notifications" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/performance/route.ts:3

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorPerformanceAnalytics } from "@/lib/marketplace/vendor-performance-analytics";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:27

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/vendor/price-updates/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,is_vendor,approval_status")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:52

**Area:** API

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
"master_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:60

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `profile.role`

**Current system**

```text
!allowedRoles.includes(profile.role) ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:63

**Area:** API

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return NextResponse.json({ error: "Access denied" }, { status: 403 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/vendor/price-updates/route.ts:67

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:89

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
{ error: "Daily limit reached (5 submissions)" },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/price-updates/route.ts:94

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `profile.role`

**Current system**

```text
let sourceType = profile.role;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:96

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `profile.role`, `vendor`

**Current system**

```text
if (profile.role === "vendor") sourceType = "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/price-updates/route.ts:97

**Area:** API

**Category:** Human Identity

**Matched:** `role`, `profile.role`, `builder`

**Current system**

```text
if (profile.role === "builder") sourceType = "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:26

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3Bigha Vendor Alert",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:30

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Open your vendor dashboard to improve visibility:",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:30

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
"Open your vendor dashboard to improve visibility:",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/vendor/rank-history/route.ts:31

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"https://www.3bigha.com/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:31

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"https://www.3bigha.com/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/vendor/rank-history/route.ts:74

**Area:** API

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
? "Improve response speed, update business profile, add better quotes, and consider boost/subscription visibility."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/rank-history/route.ts:74

**Area:** API

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
? "Improve response speed, update business profile, add better quotes, and consider boost/subscription visibility."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### HIGH — app/api/vendor/rank-history/route.ts:97

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor rank improved compared with previous snapshot."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:99

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor rank dropped compared with previous snapshot."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:104

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor rank and score are stable.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:114

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/vendor/rank-history/route.ts:140

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/rank-history/route.ts:163

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alertMessage = `⚠️ Your vendor score reduced from ${previousScore} to ${score}. Improve profile activity and RFQ response quality.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rank-history/route.ts:208

**Area:** API

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
dashboard: true,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — app/api/vendor/rfq/mark-viewed-bulk/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/api/vendor/rfq/mark-viewed-bulk/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfq/mark-viewed-bulk/route.ts:18

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/vendor/rfq/mark-viewed/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/api/vendor/rfq/mark-viewed/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/api/vendor/rfqs/[id]/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:9

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
const VENDOR_PROFILE_TABLE = "business_profiles";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:36

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
// Service role
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:37

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(SUPABASE_URL, SUPABASE_SERVICE, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:41

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Ensure vendor exists
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data: vp, error: vpErr } = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:49

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (!vp?.user_id) return NextResponse.json({ error: "Not a vendor account." }, { status: 403 });
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:54

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const oldRes = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:71

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const newRes = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/[id]/route.ts:80

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Map NEW -> OLD shape expected by your vendor UI
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:1

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/api/vendor/rfqs/[id]/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:17

**Area:** API

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
const VENDOR_PROFILE_TABLE = "business_profiles";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:42

**Area:** API

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
// 3) Service role client
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:43

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(SUPABASE_URL, SUPABASE_SERVICE, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:47

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// 4) Load vendor profile (kept for meta display)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:48

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data: vp, error: vpErr } = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:55

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (!vp?.user_id) return NextResponse.json({ error: "Not a vendor account." }, { status: 403 });
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:57

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendor = vp as VendorProfile;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:58

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vCity = String(vendor.city ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:59

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vLocality = String(vendor.locality ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:60

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vDistrict = String(vendor.district ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:61

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vPincode = String(vendor.pincode ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:69

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limitRaw = Number(url.searchParams.get("limit") || "100");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/rfqs/route.ts:70

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Number.isFinite(limitRaw) ? Math.min(Math.max(limitRaw, 1), 200) : 100;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/rfqs/route.ts:75

**Area:** API

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
let q = admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/rfqs/route.ts:98

**Area:** API

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### HIGH — app/api/vendor/whatsapp-alerts/send/route.ts:99

**Area:** API

**Category:** Capability and Permission

**Matched:** `unauthorized`

**Current system**

```text
return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### HIGH — app/api/vendor/whatsapp-alerts/send/route.ts:147

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
whatsapp_error: "Vendor WhatsApp auto-send is not enabled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — app/api/vendor/whatsapp-alerts/send/route.ts:154

**Area:** API

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor WhatsApp auto-send is not enabled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — middleware.ts:57

**Area:** Middleware

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
pathname.startsWith("/dashboard") ||
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### HIGH — middleware.ts:58

**Area:** Middleware

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
pathname.startsWith("/admin") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — middleware.ts:59

**Area:** Middleware

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
pathname.startsWith("/vendor") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### HIGH — middleware.ts:60

**Area:** Middleware

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
pathname.startsWith("/buyer") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:13

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:34

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(profile?.role ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:55

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(profile?.role ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:77

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isBusinessRole(role?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:78

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = String(role ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:104

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
"role,is_vendor,onboarding_version,onboarding_completed,portal_use_reason,role_display_label"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:109

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setTimeout(() => reject(new Error("Role lookup timed out")), 4000)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:124

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
currentPath.startsWith("/auth/register-role") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:124

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
currentPath.startsWith("/auth/register-role") ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/_components/AuthButtons.tsx:131

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(profile?.role ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:133

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
// Master admin is a system-level role.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:134

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role === "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:138

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (isBusinessRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role) qs.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:147

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
router.replace(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/AuthButtons.tsx:147

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
router.replace(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/_components/AuthButtons.tsx:294

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`, `buyer`

**Current system**

```text
const displayRole = isMasterAdminEmail(email) ? "Master Admin" : (roleLabel || "Buyer");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/GlobalAiCopilot.tsx:165

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<section className="globalAiPanel" role="dialog" aria-label="3Bigha AI Tools">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/_components/GlobalAiCopilot.tsx:165

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
<section className="globalAiPanel" role="dialog" aria-label="3Bigha AI Tools">
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/blog/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/blog/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:6

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/blog/page.tsx:61

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const [isAdmin, setIsAdmin] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:88

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=${encodeURIComponent("/admin/blog")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:98

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const ok = await ensureFreshSession("/admin/blog");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:111

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
// 2) Fallback: check profiles.role directly (master_admin or blog_admin)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:114

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const prof = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:117

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const ok = await ensureFreshSession("/admin/blog");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:120

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const profRetry = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:122

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
roleOk = !!profRetry.data && ["master_admin", "blog_admin"].includes((profRetry.data as any).role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:124

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
roleOk = !!prof.data && ["master_admin", "blog_admin"].includes((prof.data as any).role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:149

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/blog/page.tsx:156

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/blog/page.tsx:169

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const ok = await ensureFreshSession("/admin/blog");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:186

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && isAdmin) load();
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:187

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && !isAdmin) setLoading(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:189

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
}, [checking, isAdmin]);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:201

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const ok = await ensureFreshSession("/admin/blog");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:217

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Blog Review" subtitle="Checking access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:218

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Please wait" description="Verifying your admin access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:223

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:226

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Blog Review" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:227

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a blog admin." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:227

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a blog admin." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/blog/page.tsx:229

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:229

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/blog/page.tsx:230

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:230

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/blog/page.tsx:240

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Blog Review" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:248

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Blog Review" subtitle="Publish / unpublish posts" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:274

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:274

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/blog/page.tsx:275

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/blog/page.tsx:275

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/banker-verification/BankerVerificationActions.tsx:3

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/banker-verification/LenderOfferActions.tsx:3

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/banker-verification/page.tsx:30

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/banker-verification/page.tsx:52

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/banker-verification/page.tsx:76

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
employee ID and verification status before allowing banker dashboard access.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/finance-leads/[id]/FinanceLeadActionPanel.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/finance-leads/[id]/FinanceLeadActionPanel.tsx:216

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Notes
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/finance-leads/[id]/page.tsx:2

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/finance-leads/[id]/page.tsx:66

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/finance-leads"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/finance-leads/[id]/page.tsx:66

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/finance-leads"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/finance-leads/FinanceLeadFilters.tsx:165

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/finance-leads/FinanceLeadFilters.tsx:165

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/finance-leads/page.tsx:27

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/geography/page.tsx:2

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
import GeographyOperationsPanel from "@/components/admin/geography/GeographyOperationsPanel";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/geography/page.tsx:3

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
import GeographyEditorGrid from "@/components/admin/GeographyEditorGrid";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/geography/page.tsx:29

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/geography/page.tsx:41

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
supabase.from("geo_places").select("id,district_id,subdivision_id,block_id,name,slug,place_type,pincode").order("name", { ascending: true }).limit(500),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/geography/page.tsx:92

**Area:** Administration

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
coverage("business_profiles"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/geography/page.tsx:111

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
search, RFQ routing, vendor matching and SEO geography expansion.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/geography/page.tsx:325

**Area:** Administration

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
{row.table.replace("_listings", "").replace("business_profiles", "vendors").replace("_", " ")}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/opportunities/[id]/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/opportunities/[id]/page.tsx:250

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/opportunities/[id]/page.tsx:250

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/opportunities/[id]/page.tsx:269

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/opportunities/[id]/page.tsx:269

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/opportunities/page.tsx:399

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/investment/opportunities/${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/opportunities/page.tsx:399

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/investment/opportunities/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:3

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:8

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isInvestmentAdminRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:9

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin" || role === "investment_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:103

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
redirect("/login?next=/admin/dashboard/investment");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:103

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/login?next=/admin/dashboard/investment");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:110

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:114

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:116

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!isInvestmentAdminRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:121

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Access denied for Investment Admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:121

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
Access denied for Investment Admin
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/investment/page.tsx:124

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
You do not have permission to open this module.
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/investment/page.tsx:124

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
You do not have permission to open this module.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/investment/page.tsx:129

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:129

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:132

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:132

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:145

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Role: <b>{role ?? "none"}</b>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:163

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Investment Control
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:167

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Investment & Participation Plan Master
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:171

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Manage the executive framework for builder-project participation
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:173

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
models. This module will control the approved plans that builders
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:173

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
models. This module will control the approved plans that builders
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/investment/page.tsx:184

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: {role}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:193

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
hint="Admin participation-plan master setup"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:193

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
hint="Admin participation-plan master setup"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:219

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
to create attractive, admin-controlled participation plans so that
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:219

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
to create attractive, admin-controlled participation plans so that
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:229

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
eyebrow="Plan Master"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:230

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
title="Participation Plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:231

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
description="Create and manage approved builder-project participation plans. This will cover cash investment plans, joint venture land contribution plans, and hybrid models with executive public highlights, risk notes, return summaries, te…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:231

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
description="Create and manage approved builder-project participation plans. This will cover cash investment plans, joint venture land contribution plans, and hybrid models with executive public highlights, risk notes, return summaries, te…
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:232

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:232

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:232

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:233

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
cta="Open Plan Master"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:238

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"admin-controlled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:245

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
description="Review submitted investment opportunities, moderate status, and continue using the existing approval workflow already present in your investment admin side."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:246

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:246

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:264

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
return, profit share, revenue share, or discount-entry style plans.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:292

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Next module sequence
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/investment/page.tsx:296

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"1. Admin Participation Plan Master",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:296

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"1. Admin Participation Plan Master",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/page.tsx:297

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"2. Builder Project Investment Enablement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:313

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:313

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/page.tsx:316

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/page.tsx:316

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:230

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError("Plan id missing from URL.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:240

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(planId)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:240

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(planId)}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:252

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error(json?.error || "Failed to load plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:257

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error("Plan not found.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:262

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError(e?.message || "Failed to load plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:270

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError("Plan id missing from URL.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:280

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(planId)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:280

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(planId)}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:293

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error(json?.error || "Failed to update plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:296

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setSuccess("Plan updated successfully.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:299

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError(e?.message || "Failed to update plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:312

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError("Plan id missing from URL.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:322

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(id)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:322

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
`/api/admin/investment/plans/${encodeURIComponent(id)}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:332

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error(json?.error || "Failed to load plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:336

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error("Plan not found.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:341

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setError(e?.message || "Failed to load plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:361

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Loading plan...
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:364

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Please wait while the plan details are being loaded.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:376

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Edit Participation Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:380

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Update Builder Project Participation Plan
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:380

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Update Builder Project Participation Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:384

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Refine the approved executive structure of this participation plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:386

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
prepared for plan editing across cash investment, joint venture land
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:393

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:393

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:393

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:396

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
← Back to Plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:414

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{saving ? "Updating..." : "Update Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:426

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Controlled master-plan refinement
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:432

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
designed to keep the plan library standardized, attractive, and
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:433

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
ready for builder-project integration.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:439

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Live Plan Snapshot
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:468

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{form.title || "Untitled Participation Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:471

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{form.slug || "plan-slug-preview"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:477

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan ID
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:538

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Update the main plan identity, category, and public-facing positioning."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:542

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<FieldLabel required>Plan Title</FieldLabel>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:561

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="example-plan-slug"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:582

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<FieldLabel required>Plan Type</FieldLabel>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:622

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Write a crisp executive summary of the plan..."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:631

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtitle="Control how this plan appears later on builder-project and public-facing discovery screens."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:631

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Control how this plan appears later on builder-project and public-facing discovery screens."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:670

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<FieldLabel>Builder Customisation Note</FieldLabel>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:677

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Example: Builder may customise public summary within policy limits"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:708

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
subtitle="Use these values for money-based participation plans or hybrid structures that include capital contribution."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:750

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Example: Net realised profit distributed as per plan structure"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:785

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<FieldLabel>Builder Obligation Note</FieldLabel>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:792

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Example: Builder to undertake approvals, development, marketing, and monetisation"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:814

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Use this when the plan combines capital contribution with land or strategic participation."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:839

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Write the master terms and conditions for this participation plan..."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:871

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:871

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:871

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:874

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
← Back to Plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:892

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{saving ? "Updating..." : "Update Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:899

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This page is now connected to the real Plan Master API for loading and
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:901

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
move to builder-project plan attachment without touching your already
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/[id]/page.tsx:901

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
move to builder-project plan attachment without touching your already
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:196

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/investment/plans", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:196

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
const res = await fetch("/api/admin/investment/plans", {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:209

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error(json?.error || "Failed to create plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:215

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error("Plan created, but no id was returned.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:218

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setSuccess("Plan created successfully.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:221

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/admin/dashboard/investment/plans/${encodeURIComponent(createdId)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:221

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/admin/dashboard/investment/plans/${encodeURIComponent(createdId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:221

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
`/admin/dashboard/investment/plans/${encodeURIComponent(createdId)}`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:235

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
New Participation Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:239

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Create Builder Project Participation Plan
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:239

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Create Builder Project Participation Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:243

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Define a fully styled executive plan template that builders can later
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:253

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:253

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:253

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:256

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
← Back to Plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:274

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{loading ? "Creating..." : "Create Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:286

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin-controlled participation framework
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:290

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Builders should not invent free-form schemes. This master plan lets
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:292

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
hybrid structures before they are exposed on public builder project
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:319

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{form.title || "Untitled Participation Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:322

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{form.slug || "plan-slug-preview"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:371

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Define the main identity, category, and public positioning of this participation plan."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:375

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<FieldLabel required>Plan Title</FieldLabel>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:394

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="example-plan-slug"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:415

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<FieldLabel required>Plan Type</FieldLabel>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:455

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Write a crisp executive summary of the plan..."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:464

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtitle="These fields shape how the plan will later appear on builder projects and public-facing investment discovery UI."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:464

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="These fields shape how the plan will later appear on builder projects and public-facing investment discovery UI."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:503

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<FieldLabel>Builder Customisation Note</FieldLabel>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:510

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Example: Builder may customise public summary within policy limits"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:541

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Use these fields for money-based investor participation. Keep them filled when relevant or when the plan is hybrid."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:583

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Example: Net realised profit distributed as per plan structure"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:618

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<FieldLabel>Builder Obligation Note</FieldLabel>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:625

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Example: Builder to undertake approvals, development, marketing, and monetisation"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:647

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle="Use this when the plan combines financial participation and land / strategic contribution."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:672

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
placeholder="Write the master terms and conditions for this participation plan..."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:704

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:704

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:704

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:707

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
← Back to Plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:725

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Create Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:732

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This page is now connected to the real Plan Master create API. After
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/new/page.tsx:733

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
successful creation, it redirects to the edit page of the new plan so
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:42

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title: String(row.title || "Untitled Plan"),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:146

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
const [plans, setPlans] = useState<PlanRow[]>([]);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:169

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/api/admin/investment/plans${qs ? `?${qs}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:169

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
`/api/admin/investment/plans${qs ? `?${qs}` : ""}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:181

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
throw new Error(json?.error || "Failed to load plans.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:187

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
setError(e?.message || "Failed to load plans.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:201

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`, `plans`

**Current system**

```text
return plans.filter((plan) => {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:205

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.title.toLowerCase().includes(q) ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:206

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.planType.toLowerCase().includes(q) ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:207

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.shortDescription.toLowerCase().includes(q) ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:208

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.highlightText.toLowerCase().includes(q) ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:209

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.publicLabel.toLowerCase().includes(q) ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:210

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.id.toLowerCase().includes(q)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:213

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
}, [plans, search]);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:217

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
total: plans.length,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:218

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
active: plans.filter((x) => x.status === "active").length,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:219

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
inactive: plans.filter((x) => x.status === "inactive").length,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:220

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
draft: plans.filter((x) => x.status === "draft").length,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:222

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
}, [plans]);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:229

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan Master
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:233

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Project Participation Plans
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:233

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Builder Project Participation Plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:237

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Create and manage the platform-approved plans that builders can
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:238

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
later attach to their projects. These plans will support cash
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:247

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:247

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:263

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard/investment/plans/new"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:263

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard/investment/plans/new"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:263

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href="/admin/dashboard/investment/plans/new"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:266

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
+ Create New Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:273

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
label="Total Plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:275

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
hint="All configured participation plans"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:280

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
hint="Currently available for builder usage"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:305

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
This plan master will later power the builder project investment
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:305

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This plan master will later power the builder project investment
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:306

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
layer. Builders will choose approved plans here first, then attach
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:317

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Search plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:381

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<h2 className="text-lg font-semibold text-gray-900">Plan Library</h2>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:383

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Executive view of all approved or draft participation plans.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:391

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Loading plans...
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:394

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Please wait while the plan master is being loaded.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:400

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
No plans found
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:403

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Try changing your filters or create a new plan.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:407

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
filteredPlans.map((plan) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:409

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
key={plan.id}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:416

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.category
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:419

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{categoryLabel(plan.category)}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:424

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.status
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:427

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.status}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:432

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.title}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:436

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.shortDescription}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:445

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.highlightText}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:454

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.roiSummary}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:462

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan Type
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:465

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.planType}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:472

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.publicLabel}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:480

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div className={`mt-2 text-sm font-semibold ${riskClasses(plan.riskLevel)}`}>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:481

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.riskLevel}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:488

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{fmtDate(plan.updatedAt)}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:492

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan ID
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:495

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.id}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:501

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/investment/plans/${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:501

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/investment/plans/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:501

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
href={`/admin/dashboard/investment/plans/${encodeURIComponent(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:502

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan.id
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:506

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Edit Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:514

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.status === "active" ? "Deactivate" : "Activate"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/investment/plans/page.tsx:528

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This page is now connected to the real Plan Master API for listing.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:14

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:30

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:41

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:55

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:70

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:87

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:119

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,module,category,opportunity_score,priority,kind,recommendation,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id,created_at"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:122

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:132

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:140

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,growth_score,growth_level,geo_state_id,geo_district_id,geo_block_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:142

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:150

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,growth_score,shortage_score,expansion_score,recommendation,reason,geo_state_id,geo_district_id,geo_block_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:152

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:160

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,opportunity_score,shortage_score,recommended_vendor_count,priority,status,reason,geo_state_id,geo_district_id,geo_block_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:162

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:170

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,expansion_score,optimization_score,shortage_score,action_type,action_priority,recommendation,reason,geo_state_id,geo_district_id,geo_block_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:172

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(18);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:182

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(24);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:195

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:328

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `${topExpansion.module} is the leading expansion candidate with score ${topExpansion.expansion_score}. Recommendation: ${topExpansion.recommendation}.`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:344

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Monitor marketplace demand, supply saturation, vendor shortage,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:398

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<div className="mt-2 text-lg font-black text-indigo-950">{topGrowth?.module || "—"}</div>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:419

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Shortage
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:445

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Combines expansion score, vendor shortage and autonomous recruitment intelligence to decide where 3Bigha should expand, strengthen recruitment or pause.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:474

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {row.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:516

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Measures whether buyers can find active vendors, listings and RFQ capacity in each module and geography.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:567

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {row.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:629

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Recruitment Targets
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:637

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · Need {row.recommended_vendor_count} vendors
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:666

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<th className="px-3 py-2">Module</th>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:683

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:761

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {label(row.category)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/marketplace-intelligence/page.tsx:812

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {label(row.category)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/materials/attributes/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/materials/attributes/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:54

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:55

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:57

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaterialsAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:58

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === "materials_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:64

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:66

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof, error: profErr } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:69

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:70

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isMaterialsAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:71

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:135

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:171

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:174

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:174

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:379

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Materials → Attributes" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:380

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:390

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Define variation fields (Brand, Grade, Diameter…) and their allowed values (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:395

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:395

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:398

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:398

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:401

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:401

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:664

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping">Open Mapping Page →</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/attributes/page.tsx:664

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping">Open Mapping Page →</ActionButton>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/materials/mapping/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/materials/mapping/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:70

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:71

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:73

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaterialsAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:74

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === "materials_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:88

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:92

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:97

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:98

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isMaterialsAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:99

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:205

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:342

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:345

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:345

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:359

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setMsg("No attributes returned from material_attributes. Check: (1) table has rows, (2) RLS SELECT allows admin, (3) correct project/schema.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:690

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Materials → Mapping" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:691

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:701

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Step B: Subcategory → Product Group • Attributes map to Product Group (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:706

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:706

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:709

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:709

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:712

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:712

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/mapping/page.tsx:1067

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
Then map attribute <b>Cement Type</b> (single_select) → values: OPC 43, OPC 53, PPC, PSC, SRC, PCC, Premium
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/materials/taxonomy/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/materials/taxonomy/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:32

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:33

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:35

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaterialsAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:36

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === "materials_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:44

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:45

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:46

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isMaterialsAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:47

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:78

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:129

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from("material_taxons").select("id").eq("parent_id", id).eq("is_active", true).limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:143

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from("material_listings").select("id").eq(col, id).limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:190

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:233

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:236

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:236

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:344

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
const canCreate = useMemo(() => {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:393

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
if (!canCreate) return;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:603

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Materials Taxonomy" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:604

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:612

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<SectionHeader title="Materials → Taxonomy Manager" subtitle={`Type → Category → Subcategory → Product Group (role: ${role ?? "—"})`} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:616

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:616

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:619

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:619

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:622

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:622

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/materials/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:768

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
<div className="mtx-readonly">{busy ? "Saving…" : canCreate ? "Ready" : "Incomplete"}</div>
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:772

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
<button className="mtx-primaryBtn" type="button" onClick={onCreate} disabled={!canCreate || busy}>
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/materials/taxonomy/page.tsx:787

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
{!canCreate ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:46

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
revalidatePath("/admin/dashboard/master-data/measurement");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:46

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
revalidatePath("/admin/dashboard/master-data/measurement");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:121

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
revalidatePath("/admin/dashboard/master-data/measurement");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:121

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
revalidatePath("/admin/dashboard/master-data/measurement");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:213

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<option value="Builder usage">Builder usage</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:280

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/dashboard" className="rounded-2xl border px-4 py-3 text-sm font-bold text-slate-700">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:280

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/admin/dashboard" className="rounded-2xl border px-4 py-3 text-sm font-bold text-slate-700">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:281

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/measurement/page.tsx:281

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:13

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:14

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:20

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!session) return { ok: false, role: null, email: null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:23

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof } = await supabase.from("profiles").select("role").eq("id", session.user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:24

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof as any)?.role ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:26

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok: isMaster(role), role, email };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:59

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="group"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:108

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:117

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:117

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:122

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:142

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Master Data Entry Centre" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:143

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:156

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:156

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:157

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:157

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:162

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<span>role: {role ?? "—"}</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:169

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/property/taxonomy"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:169

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/property/taxonomy"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:170

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/property/attributes"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:170

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/property/attributes"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:171

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
valuesHref="/admin/dashboard/master-data/property/values"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:171

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
valuesHref="/admin/dashboard/master-data/property/values"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:172

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
mappingHref="/admin/dashboard/master-data/property/mapping"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:172

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
mappingHref="/admin/dashboard/master-data/property/mapping"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:178

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/materials/taxonomy"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:178

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/materials/taxonomy"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:179

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/materials/attributes"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:179

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/materials/attributes"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:185

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/services/taxonomy"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:185

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/services/taxonomy"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:186

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/services/attributes"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:186

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/services/attributes"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:192

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/rentals/taxonomy"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:192

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
taxonomyHref="/admin/dashboard/master-data/rentals/taxonomy"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:193

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/rentals/attributes"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/page.tsx:193

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
attributesHref="/admin/dashboard/master-data/rentals/attributes"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:25

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:26

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:34

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof } = await supabase.from("profiles").select("role").eq("id", session.user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:35

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof as any)?.role ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:36

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok: isMaster(role) };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:92

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="dialog"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:178

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:178

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:344

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Property · Attributes" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:345

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:366

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:366

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:370

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:370

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:374

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:374

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:378

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:378

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:472

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton variant="secondary" href="/admin/dashboard/master-data/property/values">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:472

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton variant="secondary" href="/admin/dashboard/master-data/property/values">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:476

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton variant="secondary" href="/admin/dashboard/master-data/property/mapping">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/attributes/page.tsx:476

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton variant="secondary" href="/admin/dashboard/master-data/property/mapping">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/property/mapping/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/property/mapping/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:43

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:44

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:56

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof as any)?.role ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:61

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok: isMaster(role) };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:122

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:122

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:372

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Property · Mapping" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:373

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:388

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:388

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:392

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:392

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:396

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:396

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:400

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/mapping/page.tsx:400

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/property/taxonomy/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/property/taxonomy/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:29

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:30

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:46

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof as any)?.role ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:47

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok: isMaster(role) };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:110

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:110

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:345

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Property · Taxonomy" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:346

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:359

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:359

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:363

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:363

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:367

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:367

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:371

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/taxonomy/page.tsx:371

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:43

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:53

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:57

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof as any)?.role ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:58

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok: isMaster(role) };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:114

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:114

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:228

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Property · Values" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:229

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:242

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:242

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:245

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:245

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:248

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/property/values/page.tsx:248

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/property/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/attributes/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/attributes/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:73

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:74

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:76

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:77

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:93

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null, email: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:95

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof, error: profErr } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:98

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:99

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:100

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role, email: user.email ?? null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:272

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:335

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:340

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:340

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:347

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const probe = await supabase.from(VALUE_TABLE).select("id,product_group_id").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:804

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Rentals → Attributes Manager" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:805

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:831

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Create Attributes + Values, and map Attributes to Product Groups (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:836

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:836

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:840

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:840

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:843

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:843

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:846

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:846

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:849

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:849

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/attributes/page.tsx:864

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<span className="mtx-chip subtle">role: {role ?? "—"}</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/mapping/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/mapping/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:54

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:55

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:57

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:58

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:74

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null, email: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:76

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof, error: profErr } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:79

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:80

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:81

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role, email: user.email ?? null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:172

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:219

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:224

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:224

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:446

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Rentals → Mapping" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:447

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:457

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Map Attributes to Product Groups (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:462

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:462

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:466

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:466

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:469

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:469

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:472

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:472

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:475

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:475

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/mapping/page.tsx:490

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<span className="mtx-chip subtle">role: {role ?? "—"}</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/taxonomy/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/taxonomy/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:39

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:40

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:43

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:59

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null, email: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:61

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof, error: profErr } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:64

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:65

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:66

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role, email: user.email ?? null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:121

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:146

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const [newSource, setNewSource] = useState<string>("admin");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:174

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:179

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:179

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:346

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setNewSource("admin");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:404

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Rentals → Taxonomy" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:405

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:415

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Manage Types → Categories → Subcategories → Product Groups (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:420

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:420

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:424

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:424

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:427

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:427

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:430

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:430

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:433

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:433

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:448

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<span className="mtx-chip subtle">role: {role ?? "—"}</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/taxonomy/page.tsx:552

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<input value={newSource} onChange={(e) => setNewSource(e.target.value)} placeholder="admin / seed / import" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/values/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/rentals/values/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:63

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:64

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:66

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:67

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:83

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null, email: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:85

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: prof, error: profErr } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:88

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:89

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:90

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role, email: user.email ?? null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:228

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:272

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:277

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:277

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:283

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const probe = await supabase.from(VALUE_TABLE).select("id,product_group_id").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:538

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Rentals → Values Manager" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:539

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:565

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Manage Attribute Values (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:570

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:570

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:574

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:574

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:577

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:577

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:580

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:580

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:583

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:583

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/rentals/values" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/rentals/values/page.tsx:598

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<span className="mtx-chip subtle">role: {role ?? "—"}</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/services/attributes/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/services/attributes/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:40

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:41

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:43

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:44

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:50

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:54

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:59

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:61

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:92

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:121

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:137

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
const canCreate = useMemo(() => {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:155

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:158

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:158

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:200

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
if (!canCreate) return;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:281

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Services → Attributes" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:282

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:295

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Create global/product-specific attributes (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:300

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:300

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:303

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:303

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:306

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:306

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/attributes/page.tsx:366

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
<button className="sax-primaryBtn" type="button" onClick={onCreate} disabled={!canCreate || busy}>
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/dashboard/master-data/services/mapping/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:1

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/admin/dashboard/master-data/services/mapping/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:75

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:76

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:78

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:79

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:93

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:97

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:102

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:103

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:104

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:204

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:330

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:333

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:333

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:348

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"No attributes returned from service_attributes (global only). Check: table has rows + RLS SELECT for admin + scope values."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:670

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Services → Mapping" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:671

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:681

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Step B: Subcategory → Product Group • Attributes map to Product Group (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:686

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:686

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:689

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/taxonomy" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:689

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/taxonomy" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:692

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:692

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/mapping/page.tsx:1039

**Area:** Administration

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Example: “Legal Service” → product group “Legal” and variations like “Registration / Documentation / Consultation” are attribute values.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:38

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:39

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:41

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isModuleAdmin(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === ADMIN_ROLE;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:48

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!user) return { ok: false, role: null as string | null };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:52

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:58

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = ((prof as any)?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:59

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const ok = isMaster(role) || isModuleAdmin(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return { ok, role };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:95

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:135

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:167

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setRole(a.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:170

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:170

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:237

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
const canCreate = useMemo(() => {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:270

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
if (!canCreate) return;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:350

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Services Taxonomy" subtitle="Admin access required" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:351

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:361

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
subtitle={`Category → Subcategory → Service (role: ${role ?? "—"})`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:365

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:365

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:370

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/mapping" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:370

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/mapping" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:374

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/attributes" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:374

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data/services/attributes" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:511

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
<div className="mtx-readonly">{busy ? "Saving…" : canCreate ? "Ready" : "Incomplete"}</div>
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:515

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
<button className="mtx-primaryBtn" type="button" onClick={onCreate} disabled={!canCreate || busy}>
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/master-data/services/taxonomy/page.tsx:530

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `cancreate`

**Current system**

```text
{!canCreate ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/operations/page.tsx:31

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/operations/status", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/operations/page.tsx:39

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
setErr(json?.error || "Access denied.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/page.tsx:3

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:16

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAnyAdminRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:18

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
role === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:19

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "property_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:20

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "materials_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:21

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "services_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:22

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "rentals_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:23

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "blog_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:24

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "investment_admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:28

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isMaster(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:29

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
redirect("/login?next=/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:42

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/login?next=/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:49

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:53

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (prof?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:56

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!isAnyAdminRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Dashboard" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:60

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
<SectionHeader title="Admin Dashboard" subtitle="" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:66

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/login?next=/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:66

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/login?next=/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:71

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
<EmptyState message="Access denied. (Admin role required)" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:71

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<EmptyState message="Access denied. (Admin role required)" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/page.tsx:76

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Role: <b>{role ?? "none"}</b>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:85

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: subscriptionRequests } = isMaster(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:87

**Area:** Administration

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:91

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:94

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: activeSubscriptions } = isMaster(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:96

**Area:** Administration

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:101

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: paymentRecords } = isMaster(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:106

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:109

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const { data: aiMonetizationAlerts } = isMaster(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:114

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:124

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(row.subscription_plan || "").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:126

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor") basic++;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:127

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
else if (plan === "silver_vendor") silver++;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:128

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`, `gold_vendor`

**Current system**

```text
else if (plan === "gold_vendor" || plan === "premium_vendor") gold++;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:129

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`, `platinum_vendor`

**Current system**

```text
else if (plan === "platinum_vendor" || plan === "hub_vendor") platinum++;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:155

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · User Approvals",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:156

**Area:** Administration

**Category:** Human Identity

**Matched:** `roles`

**Current system**

```text
desc: "Review new user registrations, requested roles, and approve or reject access.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:157

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/users",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:158

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:162

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Property",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:164

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/property",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:165

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role) || role === "property_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:169

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Materials",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:171

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/materials",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:172

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role) || role === "materials_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:176

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Services",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:178

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/services",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:179

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role) || role === "services_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:183

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Rentals",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:185

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/rentals",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:186

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role) || role === "rentals_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:190

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Blog",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:192

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/blog",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:193

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role) || role === "blog_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:197

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Price Verification",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:198

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
desc: "Verify or reject vendor-submitted Price Today updates.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:199

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/price-updates",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:199

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/price-updates",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:200

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:204

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Support Desk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:205

**Area:** Administration

**Category:** Human Identity

**Matched:** `roles`

**Current system**

```text
desc: "View user complaints, written support tickets, roles, priorities and issue status.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:206

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/support",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:206

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/support",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:207

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:211

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
title: "Admin · Vendor Control",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:212

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
desc: "Control vendor boost, ranking power, and marketplace visibility.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:213

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
href: "/admin/dashboard/vendor-control",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:213

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/vendor-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:214

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:218

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
title: "Admin · Vendor Control AI",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:219

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
desc: "Control vendor boost, visibility priority, and manual admin alerts.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:220

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
href: "/admin/dashboard/vendor-control",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:220

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/vendor-control",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:221

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:225

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · SEO Control Center",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:227

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/seo",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:227

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/seo",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:228

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:232

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Geography Control Center",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:234

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/geography",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:234

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/geography",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:235

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:247

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Marketplace Intelligence",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:248

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
desc: "Monitor demand, supply, vendor shortage zones, oversupplied markets and opportunity signals across modules and geography.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:248

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
desc: "Monitor demand, supply, vendor shortage zones, oversupplied markets and opportunity signals across modules and geography.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/page.tsx:249

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/marketplace-intelligence",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:249

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/marketplace-intelligence",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:250

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:256

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor shortage",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:261

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Production Operations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:263

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/operations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:263

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/operations",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:264

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:269

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
title: "Admin · Vendor Recruitment",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:270

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
desc: "AI-generated vendor acquisition targets by geography, shortage, priority and market expansion need.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:271

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
href: "/admin/dashboard/vendor-recruitment",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:271

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/vendor-recruitment",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:272

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:282

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title: "Admin · Measurement Master Data",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:284

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href: "/admin/dashboard/master-data/measurement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:284

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/admin/dashboard/master-data/measurement",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:285

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
show: isMaster(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:293

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Dashboard" subtitle="Choose a module to manage." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:293

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
<SectionHeader title="Admin Dashboard" subtitle="Choose a module to manage." />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:293

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<SectionHeader title="Admin Dashboard" subtitle="Choose a module to manage." />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/page.tsx:310

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<Badge>role: {role}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:317

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<EmptyState message="No modules available for your role." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:317

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
<EmptyState message="No modules available for your role." />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/page.tsx:348

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{isMaster(role) && (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:406

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/finance-leads" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:406

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/finance-leads" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:425

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/banker-verification" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:425

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/banker-verification" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:435

**Area:** Administration

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Open banker-facing workspace for assigned leads, follow-ups and lender offer submission.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:444

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/banker" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:488

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscriptions`, `upgrade`, `premium`

**Current system**

```text
Monitor premium subscriptions, WhatsApp alert monetization, and upgrade pressure.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:528

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscriptions`

**Current system**

```text
<div style={{ fontSize: 12, color: "#64748b" }}>Active paid subscriptions</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:565

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<Badge>Basic: low AI boost</Badge>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:627

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
Manual Subscription Activation
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:631

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Safe mode is active. After confirming manual payment, approve the requested vendor plan here.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:631

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Safe mode is active. After confirming manual payment, approve the requested vendor plan here.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:645

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div>Requested Plan: <b>{String(r.subscription_plan || "free").replaceAll("_", " ").toUpperCase()}</b></div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:655

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<form action="/api/admin/update-subscription" method="POST" style={{ display: "grid", gap: 8, width: "100%" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:655

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
<form action="/api/admin/update-subscription" method="POST" style={{ display: "grid", gap: 8, width: "100%" }}>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:660

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Activate Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:673

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<option value="basic_vendor">Basic AI Boost</option>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:674

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<option value="silver_vendor">Silver AI Boost</option>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:675

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
<option value="gold_vendor">Gold AI Boost</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:675

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<option value="gold_vendor">Gold AI Boost</option>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:676

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
<option value="platinum_vendor">Platinum AI Boost</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:676

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<option value="platinum_vendor">Platinum AI Boost</option>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:677

**Area:** Administration

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
<option value="premium_vendor">Gold AI Boost (Legacy)</option>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:718

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Notes
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:754

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
<EmptyState message="No pending subscription activation requests." />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/page.tsx:762

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{isMaster(role) && (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:775

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:775

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/master-data" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/page.tsx:785

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Tip: If you get “Access denied”, confirm your role in <b>public.profiles.role</b>.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/page.tsx:785

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
Tip: If you get “Access denied”, confirm your role in <b>public.profiles.role</b>.
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:54

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setMsg("No login session found. Please login again as master admin.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:58

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/price-updates", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:86

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"API timeout. Admin price verification could not load within 8 seconds."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:103

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Approve paid boost for this vendor price?"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:124

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setMsg("Please login again as master admin.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:129

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/price-updates", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:174

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (!window.confirm("Generate AI draft prices for admin verification?")) return;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:191

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setMsg("Please login again as master admin.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:195

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/price-updates/ai-draft", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:228

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:228

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:231

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:231

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:239

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin · Price Verification
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:242

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Verify genuine vendor prices. Reject removes fake or doubtful
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:282

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Loading pending price updates... checking admin API and session.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:312

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Source type: {row.source_type || "vendor"} · Trend:{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:318

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/price-updates/page.tsx:318

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/seo/page.tsx:205

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Google Search Console Action Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:94

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (s === "in_review") return "Under admin review";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:182

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=/admin/dashboard/support/${ticketId}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:182

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace(`/login?next=/admin/dashboard/support/${ticketId}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:200

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!json.isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:201

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError("Admin access required.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:269

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
mode: "admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:279

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError(json?.error || "AI could not generate admin reply.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:285

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError("AI admin reply generation failed.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:295

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError("Please write an admin reply before sending.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:307

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=/admin/dashboard/support/${ticketId}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:307

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace(`/login?next=/admin/dashboard/support/${ticketId}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:327

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError(json?.error || "Failed to send admin reply.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:334

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError("Failed to send admin reply.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:349

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Admin Support Ticket Thread"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:354

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard/support" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:354

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard/support" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:519

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Generate a compact support intelligence summary for admin review.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:609

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
No conversation replies yet. Send the first admin response below.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:640

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{m.is_admin_message ? "Admin Support" : "User"} ·{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:667

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Send Admin Written Reply
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:721

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{aiReplyLoading ? "Generating..." : "✨ Generate Admin Reply with AI"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/[ticketId]/page.tsx:727

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
placeholder="Write admin reply..."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:4

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/page.tsx:82

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (s === "in_review") return "Under admin review";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:196

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/login?next=/admin/dashboard/support");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:196

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/admin/dashboard/support");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/page.tsx:215

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!json.isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/dashboard/support/page.tsx:216

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setError("Admin access required.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:250

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
.channel("admin-support-desk-realtime")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:292

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Admin Support Desk"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:293

**Area:** Administration

**Category:** Human Identity

**Matched:** `roles`

**Current system**

```text
subtitle="View written support tickets, user roles, ticket numbers and issue details."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:297

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:297

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/admin/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/page.tsx:298

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:298

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/support/page.tsx:508

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Reply
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:622

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/support/${ticket.id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/support/page.tsx:622

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/support/${ticket.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:42

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
const res = await fetch("/api/admin/vendor-control", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:67

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
const res = await fetch("/api/admin/vendor-control", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:97

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
<h1 style={{ fontSize: 26, fontWeight: 950 }}>🧠 Admin Vendor Control AI</h1>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:100

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Control vendor boost, visibility priority, and marketplace ranking pressure.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:104

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<a href="/admin/dashboard" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:104

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/admin/dashboard" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:105

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:105

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Back to Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:145

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{row.business_name || "Unnamed Vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:153

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<span>Plan: {row.subscription_plan || "free"}</span>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:155

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<span style={{ color: "#8e168f" }}>🎉 Founding Vendor Approved</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:198

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🎉 Approve Founding Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-control/page.tsx:214

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
🚀 Admin Boost
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:14

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await supabase.from(table).select("id,name").limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:44

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(200),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:47

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("event_type,module,category,opportunity_id,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id,created_at")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:49

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:54

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:99

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
event.module || "unknown",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:115

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: event.module || "unknown",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:162

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:173

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:204

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module === row.module &&
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:214

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:268

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Recruitment Command Center
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:272

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
State, district, subdivision, block and locality-wise vendor demand.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:280

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Conversion Analytics · Last 30 Days
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:283

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Opportunity to Vendor Activation Funnel
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:286

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Measures whether public vendor opportunities are generating real vendor registrations, approvals and first listings.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:322

**Area:** Administration

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<div className="text-xs font-black uppercase text-violet-700">Registration Completion</div>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:330

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div className="text-xs font-black uppercase text-emerald-700">Vendor Activation</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:341

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Autonomous Vendor Recruitment Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:376

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {row.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:401

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
No autonomous recruitment decisions yet. Run /api/system/autonomous-vendor-recruitment-refresh after marketplace intelligence refresh.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:416

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Combines shortage pressure, opportunity score and real conversion behavior to decide which vendor opportunities should receive more visibility.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:430

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} · {item.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:461

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} · {item.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:476

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
No visibility upgrade candidate yet.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:492

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} · {item.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:528

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<div key={`${item.module}-${item.category}-${index}`} className="rounded-2xl border border-slate-200 bg-slate-50 p-4">
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:532

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} · {item.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:555

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
No attributed conversion data yet. It will appear after visitors view, click and register from vendor opportunities.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:555

**Area:** Administration

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
No attributed conversion data yet. It will appear after visitors view, click and register from vendor opportunities.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:572

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<div key={`${item.module}-${item.category}-${index}`} className="rounded-2xl border border-slate-200 bg-slate-50 p-4">
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:577

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} · {item.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:639

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<th className="px-3 py-2">Module</th>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/dashboard/vendor-recruitment/page.tsx:657

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{row.module} · {row.category || "all"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/materials/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/materials/page.tsx:217

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/operations/page.tsx:31

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/operations/status", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/operations/page.tsx:39

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
setErr(json?.error || "Access denied.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/page.tsx:11

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/page.tsx:11

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/[id]/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/property/[id]/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:6

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/[id]/page.tsx:67

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const [isAdmin, setIsAdmin] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:100

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=${encodeURIComponent(`/admin/property/${id}`)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:144

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && isAdmin) load();
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:145

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && !isAdmin) setLoading(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:147

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
}, [checking, isAdmin, validId]);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:162

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const note = window.prompt("Admin note (optional):", row.admin_note ?? "") ?? "";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:177

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const note = window.prompt("Admin note (optional):", row.admin_note ?? "") ?? "";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:215

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:218

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:219

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Property
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:229

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="Checking access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:230

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Please wait" description="Verifying your admin access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:235

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:238

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:239

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a property admin." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:239

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a property admin." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/[id]/page.tsx:247

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:256

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:259

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:260

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Property
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:270

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:273

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:274

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Property
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:286

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Property Preview" subtitle="Review & update status" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:305

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/[id]/page.tsx:347

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<b>Admin note:</b> {row.admin_note || "—"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/property/inventory/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/inventory/page.tsx:342

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// ✅ accept both param names (builder page sends projectId)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:595

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// ✅ load project listings (BUILDER ONLY)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:808

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// 2) Upsert pricing (price_total)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/property/inventory/page.tsx:809

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// If price is cleared, we delete pricing rows for this unit to avoid stale price.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/property/inventory/page.tsx:1110

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Property • Builder Inventory"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:1111

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
subtitle="Edit units + pricing + listing link. Reads from v_builder_inventory_items_ui, writes to builder_inventory_units / builder_inventory_pricing / property_listing_sources."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/property/inventory/page.tsx:1185

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{/* Builder listings only */}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:1211

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
No builder listings found for this project yet. (Requires property_listings.builder_project_id = project id and
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:1388

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<li>Is RLS blocking admin read?</li>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/inventory/page.tsx:1707

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Property • Builder Inventory"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/listings/page.tsx:349

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: `Builder listings load failed: ${friendlyDbError(invRes.error)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:374

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"No builder listings are linked to inventory for this project yet. Direct / non-builder listings are shown below separately."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:395

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: `Builder listings query failed: ${friendlyDbError(lRes.error)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:410

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
`Loaded ${lData.length} builder listing(s) linked via inventory. ${listingIds.length - lData.length} linked listing(s) could not be loaded. Direct / non-builder listings are shown below separately.`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:633

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
function renderListingCard(l: ListingRow, opts?: { builder?: boolean }) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:637

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const isBuilder = opts?.builder === true || l.is_builder_listing === true;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:648

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const previewHref = `/admin/property/preview?id=${encodeURIComponent(id)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:663

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>{isBuilder ? "builder" : "direct"}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:713

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/property/inventory?projectId=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:767

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`, `builder`

**Current system**

```text
Direct / non-builder listing. Admin can publish this directly without builder inventory linkage.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:900

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ fontSize: 12, opacity: 0.75, marginBottom: 6 }}>Builder project</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:983

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Grid>{pendingListings.map((l) => renderListingCard(l, { builder: l.is_builder_listing === true }))}</Grid>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:992

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Grid>{publishedListings.map((l) => renderListingCard(l, { builder: l.is_builder_listing === true }))}</Grid>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:1001

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Grid>{draftListings.map((l) => renderListingCard(l, { builder: l.is_builder_listing === true }))}</Grid>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/listings/page.tsx:1010

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Grid>{rejectedListings.map((l) => renderListingCard(l, { builder: l.is_builder_listing === true }))}</Grid>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/property/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/page.tsx:17

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Property Admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/page.tsx:39

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property/listings" className="inline-block">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property/inventory" className="inline-block">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/page.tsx:81

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property/preview" className="inline-block">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/property/preview/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:6

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/preview/page.tsx:216

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const invHref = `/admin/property/inventory?project_id=${encodeURIComponent(projectId || "")}&listing_id=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:223

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Admin Property Preview"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:226

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/property">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:227

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton variant="secondary">← Back to Admin Property</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:249

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Tip: open this page like <b>/admin/property/preview?id=UUID</b>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/preview/page.tsx:394

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Admin Property Preview"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/property/projects/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/projects/page.tsx:114

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
// If not logged in, send to login; else send to dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/projects/page.tsx:117

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=${encodeURIComponent("/admin/property/projects")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:119

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:119

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/property/projects/page.tsx:352

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<SectionHeader title="Builder Projects" subtitle="Loading…" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:360

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<SectionHeader title="Builder Projects" subtitle="Access denied" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:360

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<SectionHeader title="Builder Projects" subtitle="Access denied" />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/property/projects/page.tsx:361

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<EmptyState message="master_admin OR property_admin access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:371

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Builder Projects · Master Data"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:376

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href="/admin/property" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:377

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Property Admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:390

**Area:** Administration

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
<Badge>access: master_admin / property_admin</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/property/projects/page.tsx:406

**Area:** Administration

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<MessageBox title="No builder projects yet" description="Click “Add Project” to create the first project." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/rentals/[id]/page.tsx:72

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const [isAdmin, setIsAdmin] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:86

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=${encodeURIComponent(`/admin/rentals/${id}`)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:146

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && isAdmin) load();
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:147

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && !isAdmin) setLoading(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:149

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
}, [checking, isAdmin]);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:168

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="Checking access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:169

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Please wait" description="Verifying your admin access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:174

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:177

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:178

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a rentals admin." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:178

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a rentals admin." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/[id]/page.tsx:186

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:195

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:198

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/rentals" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:199

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Rentals
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:209

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:212

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/rentals" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:213

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Rentals
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:224

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Preview" subtitle="Review & update status" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/[id]/page.tsx:227

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/rentals" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:5

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/rentals/page.tsx:61

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const [isAdmin, setIsAdmin] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:77

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.replace(`/login?next=${encodeURIComponent("/admin/rentals")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:106

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/rentals/page.tsx:113

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/rentals/page.tsx:120

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/rentals/page.tsx:133

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && isAdmin) load();
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:134

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!checking && !isAdmin) setLoading(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:136

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
}, [checking, isAdmin]);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:156

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Review" subtitle="Checking access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:157

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Please wait" description="Verifying your admin access..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:162

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:165

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Review" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:166

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a rentals admin." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:166

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<MessageBox title="Access denied" description="You are not a rentals admin." />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/rentals/page.tsx:174

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Review" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:182

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin Rentals Review" subtitle="Approve / Reject rental listings" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:230

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{/* if you later add an admin preview route, keep this link.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:232

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
so we link to a future admin preview path. */}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:233

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href={`/admin/rentals/${p.id}`} style={{ fontWeight: 700 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:280

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href={`/admin/rentals/${p.id}`} style={{ fontWeight: 700 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:281

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Preview →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/rentals/page.tsx:318

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href={`/admin/rentals/${p.id}`} style={{ fontWeight: 700 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/services/[id]/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:6

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/services/[id]/page.tsx:67

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:68

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin" || role === "services_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:81

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:100

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:110

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = (prof?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:194

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Service Details" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:200

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!isAdminRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:203

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Service Details" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:205

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
Access denied. (Admin role required)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:205

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
Access denied. (Admin role required)
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/services/[id]/page.tsx:207

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/services" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:208

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Services
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:217

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Service Details" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:219

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/services" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:220

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Services
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:229

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Service Details" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:231

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/services" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:232

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Services
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:243

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Service Details" subtitle="Admin preview (any status)" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:246

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/services" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:247

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Back to Admin Services
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/[id]/page.tsx:300

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Pricing</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/services/page.tsx:1

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// app/admin/services/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:6

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/services/page.tsx:59

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isAdminRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:60

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
return role === "master_admin" || role === "services_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:70

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<string | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:88

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
// role lookup (same pattern as other admin modules)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:88

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
// role lookup (same pattern as other admin modules)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/services/page.tsx:91

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:101

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const r = (prof?.role ?? null) as string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:131

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(200);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/services/page.tsx:161

**Area:** Administration

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
// approval should set published_at (same as other modules)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/admin/services/page.tsx:187

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Services" subtitle="Loading..." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:194

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!isAdminRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:198

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<SectionHeader title="Admin · Services" subtitle="" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:200

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
Access denied. (Admin role required)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:200

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
Access denied. (Admin role required)
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/services/page.tsx:214

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
title="Admin · Services"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:222

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:222

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/admin/dashboard" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/services/page.tsx:223

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:223

**Area:** Administration

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
← Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/services/page.tsx:262

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<ActionButton href={`/admin/services/${r.id}`} variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/services/page.tsx:271

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
{price ? <div>Pricing: {price}</div> : null}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/users/page.tsx:2

**Area:** Administration

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/admin/users/page.tsx:16

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
redirect("/login?next=/admin/users");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:21

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:25

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (prof?.role !== "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:26

**Area:** Administration

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
return <div style={{ padding: 14 }}>Access denied</div>;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/admin/users/page.tsx:37

**Area:** Administration

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:61

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div><b>Requested Role:</b> {u.requested_role || "—"}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:69

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
action="/api/admin/update-subscription"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:69

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
action="/api/admin/update-subscription"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/users/page.tsx:86

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="basic_vendor">Basic Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:87

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="premium_vendor">Premium Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:87

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
<option value="premium_vendor">Premium Vendor</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/users/page.tsx:88

**Area:** Administration

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="hub_vendor">Hub Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:111

**Area:** Administration

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
<button type="submit">Save Subscription</button>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/admin/users/page.tsx:118

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<form action="/api/admin/approve-user" method="post">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:120

**Area:** Administration

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<input type="hidden" name="role" value={u.requested_role || ""} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/admin/users/page.tsx:124

**Area:** Administration

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<form action="/api/admin/reject-user" method="post">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:30

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,is_profile_complete")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
router.replace("/auth/register-role");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:37

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
router.replace("/auth/register-role");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/awaiting-approval/page.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `profile.role`

**Current system**

```text
if (!profile.role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:42

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
router.replace("/auth/register-role");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:42

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
router.replace("/auth/register-role");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/awaiting-approval/page.tsx:67

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace("/onboarding/business");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/awaiting-approval/page.tsx:71

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.replace("/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/awaiting-approval/page.tsx:71

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:33

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function isBusinessRole(role: string | null | undefined) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:35

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
(role || "").trim().toLowerCase()
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:94

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
"role",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:135

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:167

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`/auth/register-role${next ? `?next=${encodeURIComponent(next)}` : ""}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:167

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
`/auth/register-role${next ? `?next=${encodeURIComponent(next)}` : ""}`
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:179

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `profile.role`

**Current system**

```text
const role = normalizeRole(profile.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:182

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role === "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:183

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
hardRedirect(next || "/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:183

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
hardRedirect(next || "/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:187

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
// MASTER ADMIN MUST NEVER BE SENT TO REGISTER-ROLE.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:187

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
// MASTER ADMIN MUST NEVER BE SENT TO REGISTER-ROLE.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:188

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role === "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:189

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
hardRedirect(next || "/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:189

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
hardRedirect(next || "/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:193

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:195

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
`/auth/register-role${next ? `?next=${encodeURIComponent(next)}` : ""}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:195

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
`/auth/register-role${next ? `?next=${encodeURIComponent(next)}` : ""}`
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:210

**Area:** Application Route

**Category:** Human Identity

**Matched:** `profile.role`

**Current system**

```text
hasValue(profile.role_display_label);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:218

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!profile.requested_role && role) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:219

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
patch.requested_role = role;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:236

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role !== "master_admin" && !basicComplete) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:239

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role) qs.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:241

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
hardRedirect(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:241

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
hardRedirect(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:245

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role !== "master_admin" && !onboardingReady) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:246

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (isBusinessRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:249

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role) qs.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:270

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
hardRedirect(`/onboarding/business?${qs.toString()}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:274

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
hardRedirect(next || "/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:274

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
hardRedirect(next || "/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:280

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role) qs.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:282

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
hardRedirect(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:282

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
hardRedirect(`/auth/register-role?${qs.toString()}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:286

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (role !== "master_admin" && isBusinessRole(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:304

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
hardRedirect(next || "/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/post-login/PostLoginPageClient.tsx:304

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
hardRedirect(next || "/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:42

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function goesToBusinessOnboarding(role: PortalRole | "") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:44

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role === "vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:45

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
role === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:46

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "hub_vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:47

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "blogger"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:56

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: PortalRole,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:59

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
if (role === "buyer") return "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:60

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
if (role === "builder") return "Builder / Developer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:61

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (role === "hub_vendor") return "Vendor Hub";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `author`

**Current system**

```text
if (role === "blogger") return "Blogger / Author";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:64

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (role === "vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:88

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const preselectedRole = (sp.get("role") || "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:89

**Area:** Application Route

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
const isMasterAdminRequest = preselectedRole === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:92

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
// Master admin must not repeatedly auto-redirect from register-role.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:92

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
// Master admin must not repeatedly auto-redirect from register-role.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:96

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const [role, setRole] = useState<PortalRole | "">(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:164

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) return "Please choose your role.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:171

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (role === "vendor" && caps.filter((x) => x !== "investor").length === 0)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:180

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (selectedRole === "buyer") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:184

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (selectedRole === "vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:202

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (selectedRole === "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:272

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role === "vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:273

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "hub_vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:274

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
role === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:275

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "blogger";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:277

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const roleDisplayLabel = getRoleDisplayLabel(role as PortalRole, caps);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:286

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
requested_role: role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:287

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:291

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
onboarding_completed: !goesToBusinessOnboarding(role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:308

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role as PortalRole
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:317

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (goesToBusinessOnboarding(role)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:322

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
role === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:324

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: role === "hub_vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:326

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: role === "blogger"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:330

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
role === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:332

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: role === "hub_vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:334

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: role === "blogger"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:357

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:371

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
qs.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:372

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace(`/onboarding/business?${qs.toString()}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:376

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace(next || "/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:400

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Tell us who you are and why you want to use 3bigha. If your role needs
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:546

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
name="role"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:548

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
checked={role === item.value}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/auth/register-role/RegisterRolePageClient.tsx:634

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
{role === "vendor" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/buyer/rfq/[rfq_id]/compare/page.tsx:14

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
redirect(`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/buyer/rfq/[rfq_id]/compare/page.tsx:14

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect(`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/compare-rates/page.tsx:5

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
redirect("/dashboard/buyer/rfqs");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/compare-rates/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/buyer/rfqs");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/components/ai/DealReadyClient.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/DealReadyClient.tsx:32

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m, i) => `${i}:${m.role || ""}:${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/DealScoreClient.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/DealScoreClient.tsx:25

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
m?.role === "system" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/DealScoreClient.tsx:31

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: String(m?.sender_role || m?.role || "user"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/DealScoreClient.tsx:76

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.map((m, i) => `${i}:${m.role || ""}:${m.body || ""}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role: "buyer" | "ai";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:39

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:66

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "ai",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:80

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "ai",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:131

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
key={`${message.role}-${index}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:133

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
justifySelf: message.role === "buyer" ? "end" : "start",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:137

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
background: message.role === "buyer" ? "#2563eb" : "white",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:138

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
color: message.role === "buyer" ? "white" : "#0f172a",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/ai/ProcurementCopilotBox.tsx:139

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
border: message.role === "buyer" ? "0" : "1px solid #bfdbfe",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/chat/ConversationActionMenu.tsx:33

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
toDisplayRole: (role?: string | null) => string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/chat/ConversationMessageList.tsx:120

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
toDisplayRole: (role?: string | null) => string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/chat/ConversationMessageList.tsx:161

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: m.sender_user_id === currentUserId ? "me" : toDisplayRole(m.sender_role),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/chat/ConversationMessageList.tsx:1344

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{String(m.sender_role || "").toLowerCase() === "buyer" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/inbox/InboxAiAction.tsx:157

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: side, body: `${title}. ${subtitle}. ${metaLine ?? ""}` }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/inbox/InboxAiSummary.tsx:126

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
messages: [{ role: side, body: `${title}. ${subtitle}. ${metaLine ?? ""}` }],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/ExpressInterestButton.tsx:212

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/dashboard/investor/deal-rooms/${json.deal_room_id}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/components/investment/ExpressInterestButton.tsx:214

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/dashboard/investor/deal-rooms/${json.deal_room_id}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/components/investment/ExpressInterestButton.tsx:226

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/dashboard/investor/deal-rooms/${existingDealRoomId}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
type ViewerRole = "investor" | "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:209

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:216

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:223

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:763

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:774

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:779

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:1438

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
viewerRole === "builder" ? getInvestorLabel(room) : getBuilderLabel(room);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:1493

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String((msg as any).sender_role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:1494

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role && role !== viewerRole;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:1704

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (senderRole === "builder") return "Builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:1989

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? "Builder Hub" : "Investor Hub"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2049

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? "Builder Deal Room" : "Investment Deal Room"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2071

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? "Investor" : "Builder"}: {counterpartLabel}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2160

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" && ndaRequired ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2215

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2241

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2284

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
label={viewerRole === "builder" ? "Investor" : "Builder"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2333

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? `${viewerRole === "builder" ? "Investor" : "Builder"} typing...`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2335

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? `${viewerRole === "builder" ? "Investor" : "Builder"} online`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2337

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? `${viewerRole === "builder" ? "Investor" : "Builder"} last active ${fmtShortSeen(counterpartLastSeenAt)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2338

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: `${viewerRole === "builder" ? "Investor" : "Builder"} offline`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2388

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String((m as any).sender_role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:2389

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role === viewerRole && !isSystemMessage(m);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:3123

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder" ? "Investor" : "Builder"} User ID
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/components/investment/InvestmentDealRoomClient.tsx:3126

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{viewerRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/BankerLenderOfferForm.tsx:87

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Add ROI, tenure, CIBIL, FOIR and LTV rules for your bank. Admin can
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/BankerLenderOfferForm.tsx:191

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Offer submitted. It can be activated after admin verification.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/page.tsx:38

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/banker/page.tsx:45

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/banker/page.tsx:90

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Your daily workspace for borrower follow-up, document collection, lender offers and loan progress.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/banker/page.tsx:185

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/page.tsx:185

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/banker/page.tsx:310

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Pending Admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/page.tsx:368

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/banker/page.tsx:368

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/admin/dashboard/finance-leads/${lead.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/[id]/page.tsx:3

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/[id]/page.tsx:18

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return <InvestmentDealRoomClient roomId={id} viewerRole="builder" />;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:200

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
params.set("role", "builder");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:218

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const returnTo = encodeURIComponent("/dashboard/builder/deal-rooms");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:218

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const returnTo = encodeURIComponent("/dashboard/builder/deal-rooms");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:362

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Hub
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:383

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:420

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtext="Rooms needing builder attention"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:438

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Rooms waiting for builder NDA action.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:454

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: "No NDA is currently pending in your builder deal rooms."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:516

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
href={`/dashboard/builder/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:516

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/builder/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:781

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
href={`/dashboard/builder/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/builder/deal-rooms/page.tsx:781

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/builder/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:347

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function toDisplayRole(role?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:348

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const value = String(role ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:357

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:363

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(context.module || "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:370

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:375

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:379

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:383

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:443

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Ask vendor to confirm delivery/work timeline and availability."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:446

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Ask vendor to confirm price, availability, timeline and hidden charges.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:450

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Escalation recommended if vendor does not reply soon."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:556

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: String(m.sender_role || "user"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1278

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sender_role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1659

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
secondaryHref="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1714

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🤖 Autonomous Buyer Procurement Agent
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1717

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI tracks supplier reliability, payment/timeline commitment, escalation risk and next action.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1741

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
["Supplier", buyerAgent.supplierReliability, "🏆"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1756

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title="Buyer Work Space"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1760

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Reply Vendor", href: "#message-composer", tone: "primary" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1762

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Compare Vendors", href: "/dashboard/inbox-v2?module=rfq", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1762

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: "Compare Vendors", href: "/dashboard/inbox-v2?module=rfq", tone: "neutral" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1763

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Open Inbox", href: "/dashboard/inbox-v2", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1781

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1782

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
conversationContext?.module ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1813

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: conversationContext?.module || contextType || "inbox",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1900

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
toDisplayRole={(role?: string | null) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1901

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) return counterpartName || "User";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1902

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
return String(role).toLowerCase() === "buyer" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1903

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
String(role).toLowerCase() === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1904

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
? counterpartName || toDisplayRole(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box.tsx:1905

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: toDisplayRole(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:4

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerConversationChatBox from "./buyer-conversation-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:47

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:88

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (!id) return "/dashboard/buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:88

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (!id) return "/dashboard/buyer";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:94

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (ct === "rfq") return `/dashboard/buyer/quote-compare/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:94

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (ct === "rfq") return `/dashboard/buyer/quote-compare/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:96

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return "/dashboard/buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:96

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard/buyer";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:113

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:113

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:114

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
← Back to Buyer Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:114

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
← Back to Buyer Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:116

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:116

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:117

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:119

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:174

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:174

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:175

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
← Back to Buyer Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:175

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
← Back to Buyer Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:177

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:177

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:178

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:180

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:188

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
const role = "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:193

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at,last_seen_message_id")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:223

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:242

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:277

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:277

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:278

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/chat/[conversationId]/page.tsx:280

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx:82

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ✅ Fetch latest quote per vendor using "max version" strategy
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx:83

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Step A: get max version per vendor for this rfq
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/page.tsx:99

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// pick latest per vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:63

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
latestQuotes: QuoteRow[]; // one per vendor (latest version)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:68

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Selected vendor column highlight (optional)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:103

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Lowest grand total vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:118

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Lowest unit price per item (per vendor)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:148

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
No vendor quotes found yet.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/[rfqId]/quotes/quote-compare-client.tsx:159

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Focus vendor:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:17

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`, `vendor`, `buyer`

**Current system**

```text
type SenderRole = "buyer" | "vendor" | "admin" | string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:40

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:153

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
nextUrl: "/dashboard/buyer/enquiries",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:153

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
nextUrl: "/dashboard/buyer/enquiries",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:181

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// ✅ Buyer’s inbox is tied to buyer_user_id now (BEST)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:203

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:281

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sender_role: "buyer" as SenderRole,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:359

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:360

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
← Back to Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:379

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:380

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
← Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:456

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
placeholder="Search by message / vendor id / subject…"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:502

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{focused.vendor_user_id ? <Pill>vendor: {focused.vendor_user_id.slice(0, 8)}…</Pill> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:521

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = (m.sender_role ?? "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:522

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
const isMine = role === "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:541

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<Pill tone={isMine ? "ok" : "neutral"}>{isMine ? "You" : titleCase(role)}</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:608

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:608

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:618

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(focused.id)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:618

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(focused.id)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:651

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{e.vendor_user_id ? <Pill>vendor: {e.vendor_user_id.slice(0, 8)}…</Pill> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:663

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:663

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:675

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:675

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/enquiries/BuyerEnquiriesPageClient.tsx:681

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
Status is controlled by vendor/admin (you can still send follow-ups).
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:3

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import { fetchBuyerListingConversations } from "@/lib/conversations/buyer-listing-inbox";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:4

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerWorkMenu from "@/components/buyer/BuyerWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:144

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<h1 style={{ fontSize: 18, fontWeight: 800, margin: 0 }}>Buyer Inbox</h1>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:151

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800, textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:151

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer" style={{ fontWeight: 800, textDecoration: "none" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:152

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
← Buyer Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:152

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
← Buyer Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:154

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/enquiries" style={{ fontWeight: 800, textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:154

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/enquiries" style={{ fontWeight: 800, textDecoration: "none" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:177

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Open any conversation to continue chat, call the vendor, or review the latest messages.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:251

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const chatUrl = `/dashboard/buyer/chat/${encodeURIComponent(conversationId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:251

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const chatUrl = `/dashboard/buyer/chat/${encodeURIComponent(conversationId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/inbox/page.tsx:309

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor: {c.vendor_name ?? "Vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/dashboard/buyer/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/buyer/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:6

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:20

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerWorkMenu from "@/components/buyer/BuyerWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:41

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = String(r.module || "Procurement").trim();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:78

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:166

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
router.replace("/login?next=/dashboard/buyer");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:166

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/dashboard/buyer");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:175

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,title,module,status,created_at,needed_by")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:177

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/page.tsx:221

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:242

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:251

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
body: `${row.title || ""} ${row.module || ""} ${row.status || ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:266

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
// Dashboard intelligence should never block dashboard loading.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:303

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: rfq.module || "general",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:337

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: rfq.module || "general",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:358

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/page.tsx:384

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Use the RFQ workspace for structured procurement, vendor discovery and readiness checking.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:384

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
: "Use the RFQ workspace for structured procurement, vendor discovery and readiness checking.",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:387

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
cta: "Open RFQ Workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:396

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:396

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:400

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Check vendor conversations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:402

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Continue vendor negotiation from unified chat before closing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:404

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:422

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
eyebrow="Buyer Workspace"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:422

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
eyebrow="Buyer Workspace"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:423

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title="Buyer Dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:423

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
title="Buyer Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:424

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
subtitle="Preparing your procurement workspace."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:434

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
eyebrow="Buyer Workspace"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:434

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
eyebrow="Buyer Workspace"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:435

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title="Buyer Dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:435

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
title="Buyer Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:436

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
subtitle="Unable to load your procurement workspace."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:439

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title="Buyer dashboard could not load"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:439

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
title="Buyer dashboard could not load"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:445

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:456

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
eyebrow="Buyer Operations"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:457

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title="Buyer Work Desk"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:458

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Manage RFQs, compare quotations, continue vendor conversations and complete buying decisions."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:507

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Create RFQs, compare vendor quotes, continue conversations and finalize buying decisions from one simple buyer workspace.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:507

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Create RFQs, compare vendor quotes, continue conversations and finalize buying decisions from one simple buyer workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:519

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
["1", "Create RFQ", "Submit buyer requirement"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:520

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["2", "Compare Quotes", "Review vendor quotations"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:573

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Important Buyer Reminders
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:630

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Procurement Overview
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:641

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Manage requirements, compare quotations, continue vendor conversations and complete procurement decisions from one operational workspace.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:641

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Manage requirements, compare quotations, continue vendor conversations and complete procurement decisions from one operational workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:705

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:705

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:709

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/inbox-v2" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:713

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/inbox" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:713

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/inbox" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:714

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:746

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Review pending work, vendor responses and required follow-ups.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:924

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:958

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Pill>buyer</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:965

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div style={{ fontWeight: 900, fontSize: 16, marginBottom: 6 }}>Buyer Inbox</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:972

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Pill>buyer</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:978

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/inbox" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:978

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/inbox" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:979

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Open Buyer Inbox →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:981

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:981

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/inbox" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:982

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
/dashboard/buyer/inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:982

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
/dashboard/buyer/inbox
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:992

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Open buyer and vendor side inbox access from one common place.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:996

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Pill>buyer</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:997

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Pill>vendor</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1003

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/inbox" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1006

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1007

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
/dashboard/inbox
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1028

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1028

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1031

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/enquiries" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1031

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/enquiries" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1032

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
/dashboard/buyer/enquiries
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1032

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
/dashboard/buyer/enquiries
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1042

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Track vendor responses, compare quotations, and continue RFQ decisions.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1053

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1053

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1056

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1056

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 800, alignSelf: "center" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1057

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
/dashboard/buyer/rfqs
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1057

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
/dashboard/buyer/rfqs
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/page.tsx:1172

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
🧠 Buyer Workflow Assistant — click to open quote guidance, vendor follow-up and procurement intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1223

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Buyer dashboard now connects procurement creation, RFQ comparison, vendor chat, inbox, marketplace discovery and Procurement guidance.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/page.tsx:1223

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
Buyer dashboard now connects procurement creation, RFQ comparison, vendor chat, inbox, marketplace discovery and Procurement guidance.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:39

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:308

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: String(m.sender_role || "user"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:316

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch("/api/ai/vendor-alert", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:321

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:376

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: m.sender_role === "buyer" ? "buyer" : "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:422

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: m.sender_role === "buyer" ? "buyer" : "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:438

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:666

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:692

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
current_page: `/dashboard/buyer/quote-compare/${rfqId}/chat`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:692

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
current_page: `/dashboard/buyer/quote-compare/${rfqId}/chat`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:728

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:752

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:868

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
.channel(`buyer-rfq-chat-${conversationId}-${currentUserId}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1039

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
.channel(`buyer-presence-${conversationId}-${counterpartUserId}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1153

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return String(message.sender_user_id ?? "") === String(currentUserId) ? "You" : "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1547

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sender_role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1800

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: m.sender_role === "buyer" ? "buyer" : "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1814

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch("/api/ai/vendor-alert", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1819

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1829

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
message: "Buyer is ready to close. Vendor should respond quickly.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:1945

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div style={{ fontSize: 12, opacity: 0.7 }}>Vendor</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:2222

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{mine ? "You" : "Vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:2243

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:2532

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor message"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:2771

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor is typing...
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box.tsx:2786

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🤖 AI Buyer Workflow Assistant
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:5

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerRfqChatBox from "@/app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
import BuyerRfqChatBox from "@/app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:41

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return <div style={{ padding: 12 }}>Invalid vendor ID</div>;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:80

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:100

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:100

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:133

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:148

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:166

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}?vendorId=${encodeURIComponent(vendorId)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:166

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}?vendorId=${encodeURIComponent(vendorId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:167

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:167

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:175

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Buyer ↔ Vendor conversation for RFQ #{rfqId.slice(0, 8)}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:214

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<strong>Vendor:</strong> {vendorName}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/chat/page.tsx:217

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
<strong>Role:</strong> Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/MarketplaceAiDashboard.tsx:81

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
🧠 Marketplace AI Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:2

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import { fetchBuyerQuoteCompare } from "@/lib/rfq/buyer-quote-compare/server";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:82

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (isPremium) signals.push({ label: "⭐ Premium", tone: "neutral" });
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:176

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Waiting for vendor responses"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:185

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
? "Continue vendor discussion or expand supplier search."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:192

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "This RFQ has low vendor response or missing quote data, which can block procurement execution."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:308

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:308

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:335

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:335

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:338

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:339

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:369

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = rfq?.module ?? null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:371

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:373

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:375

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:377

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:379

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: titleCase(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:489

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
? "Strong shortlist option with balanced pricing, delivery and reliability."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:498

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `Ask vendor to reduce delivery time closer to ${fastestDays} days.`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:499

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Ask vendor to confirm final price, delivery charges, GST invoice and payment terms.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:503

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Carefully verify vendor details, pricing and delivery commitment before accepting."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:503

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
? "Carefully verify vendor details, pricing and delivery commitment before accepting."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:506

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Lower visible workflow risk based on current vendor signals.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:513

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(v.vendor_id ? `Vendor ${String(v.vendor_id).slice(0, 8)}…` : "Vendor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:575

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Wait for vendor responses or continue discussion with matched vendors."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:610

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "This requirement now has enough vendor activity for a confident decision."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:613

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Wait for more quotes or stronger vendor responses.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:650

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const buyerPrintHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/print`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:650

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const buyerPrintHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/print`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:656

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:708

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:710

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Comparison Workspace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:710

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
title: "Vendor Comparison Workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:711

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
summary: `Compare ${vendorsSorted.length} vendor response${vendorsSorted.length === 1 ? "" : "s"} and continue quotation decision.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:712

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:712

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:721

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "quote",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:723

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail: `Review ${vendorsSorted.length} vendor response${vendorsSorted.length === 1 ? "" : "s"} and continue decision.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:724

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:724

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${rfqId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:729

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
<OperationalEventStream title="Recent quotation activity" limit={5} />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:733

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<h1 style={{ fontSize: 18, fontWeight: 900, margin: 0 }}>Vendor Comparison Workspace</h1>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:733

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<h1 style={{ fontSize: 18, fontWeight: 900, margin: 0 }}>Vendor Comparison Workspace</h1>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:735

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Review vendor quotations, delivery timelines and conversations before making the final decision.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:740

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:740

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:743

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard" style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:744

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:785

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<strong>Buyer:</strong> {rfq?.contact_name ?? "—"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:803

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Vendor Comparison Workspace"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:803

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
title="Vendor Comparison Workspace"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:809

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Negotiate Best Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:810

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:810

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:815

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: { label: "Wait for Quotes", href: "/dashboard/inbox-v2?module=rfq", tone: "warning" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:815

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: { label: "Wait for Quotes", href: "/dashboard/inbox-v2?module=rfq", tone: "warning" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:817

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Open Inbox", href: "/dashboard/inbox-v2?module=rfq", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:817

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: "Open Inbox", href: "/dashboard/inbox-v2?module=rfq", tone: "neutral" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:818

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{ label: "My RFQs", href: "/dashboard/buyer/rfqs", tone: "neutral" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:818

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "My RFQs", href: "/dashboard/buyer/rfqs", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:860

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:860

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:873

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2?module=rfq" style={actionBtnStyle("normal")}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:873

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Link href="/dashboard/inbox-v2?module=rfq" style={actionBtnStyle("normal")}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:892

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
SELECTED VENDOR SUMMARY
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:896

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(selectedVendor.vendor_id ? `Vendor ${String(selectedVendor.vendor_id).slice(0, 8)}…` : "Vendor")}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:915

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<span className="trustChip">✅ Verified Vendor</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:918

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
<span className="trustChip">📍 Local Supplier</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:944

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:944

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}/chat?vendorId=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:949

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
💬 Talk to Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:967

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Comparison
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:971

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Continue vendor discussion carefully before accepting a quotation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:975

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div style={{ marginTop: 10, opacity: 0.75 }}>No vendor quotations received yet. Vendors usually respond after viewing the requirement.</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:981

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<th style={{ textAlign: "left", padding: 10 }}>Vendor</th>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:993

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(v.vendor_id ? `Vendor ${String(v.vendor_id).slice(0, 8)}…` : "Vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1007

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const talkHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1007

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const talkHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1058

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
💬 Talk to Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1067

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
action={`/api/buyer/rfq/${encodeURIComponent(rfqId)}/accept`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1123

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Responses
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1134

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
v.vendor_business_name ?? (v.vendor_id ? `Vendor ${String(v.vendor_id).slice(0, 8)}…` : "Vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1146

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/thread/${encodeURIComponent(v.conversation_id)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1147

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: `/dashboard/thread/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1214

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
💬 Talk to Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1240

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
v.vendor_business_name ?? (v.vendor_id ? `Vendor ${String(v.vendor_id).slice(0, 8)}…` : "Vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1349

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/page.tsx:1376

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Tip: If a vendor shows <strong>outdated</strong>, ask them to submit a revised quote for the latest RFQ revision.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/PricePredictionToggle.tsx:51

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: payload?.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/PricePredictionToggle.tsx:102

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Click to check whether buyer should buy now, wait, or negotiate.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:4

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import PrintQuoteButton from "@/app/dashboard/buyer/quote-compare/[rfqId]/print/print-quote-button";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
import PrintQuoteButton from "@/app/dashboard/buyer/quote-compare/[rfqId]/print/print-quote-button";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:109

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/print/page.tsx:145

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div><strong>Vendor:</strong> {(vendorBiz as any)?.business_name ?? "—"}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/SmartDecisionBox.tsx:75

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Analyze vendor quotes, price, risk, urgency and recommend the best next action.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/SmartDecisionBox.tsx:115

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
⭐ Best Vendor:{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/quote-compare/[rfqId]/SmartDecisionBox.tsx:120

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `Vendor ${String(decision.bestVendor.vendor_id).slice(0, 8)}…`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/dashboard/buyer/rfqs/[id]/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/buyer/rfqs/[id]/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:3

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import { fetchBuyerQuoteCompare } from "@/lib/rfq/buyer-quote-compare/server";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:68

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
/access denied|not your rfq|created_by/i.test(msg);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:81

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Note: Your RFQ create API stores buyer in{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:96

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:96

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:100

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:100

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:113

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const procurementHref = `/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:113

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const procurementHref = `/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:115

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `Buyer RFQ ${rfq.public_id}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:116

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: `Buyer RFQ ${String(rfq?.id ?? rfqId).slice(0, 8)}…`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:122

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
module: "buyer" as const,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:122

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "buyer" as const,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:129

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ✅ Determine lowest TOTAL vendor (overall best price)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:184

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Styles (overall best vendor column = green, best per item cell = amber)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:210

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Compare latest vendor quotes item-by-item."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:221

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:221

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/rfqs" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:225

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:225

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/buyer/enquiries" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:270

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer: <b>{rfq?.contact_name ?? "—"}</b>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:289

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Shows latest quote per vendor (max version).
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:295

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{/* Vendor cards */}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:298

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<EmptyState message="No vendor quotes received yet." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:303

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Quotes (Latest)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:458

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Best overall vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:556

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: `vendor ${String(v.vendor_id).slice(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:701

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/[id]/page.tsx:701

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/rfqs/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:14

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerWorkMenu from "@/components/buyer/BuyerWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:34

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:157

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const who = senderIsSelf ? "You" : "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:191

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "Waiting for vendor responses"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:257

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Waiting for vendor responses";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:264

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Review selected vendor and download quote."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:266

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Continue vendor conversation."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:271

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Continue vendor discussion or wait for more responses.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:275

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfq.id)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:275

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfq.id)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:282

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "This requirement may need faster vendor attention."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:284

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `${unreadChatCount} unread vendor message(s).`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:291

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Continue vendor discussion and finalize delivery timeline and payment terms."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:293

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Use vendor discovery or improve RFQ details to attract faster quotes."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:295

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
? "You may ask one more vendor or continue negotiation with the current supplier."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:296

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Review quotations and continue vendor discussion before final selection.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:357

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
r.module ?? "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:439

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
nextUrl: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:439

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
nextUrl: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:590

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:607

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:612

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (filterModule !== "all") query = query.eq("module", filterModule);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:639

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
r.module ?? "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:740

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:851

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
.channel(`buyer-rfqs-chat-sync-${userId}`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:940

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Reply to vendor chats first."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:942

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Compare multi-vendor RFQs and shortlist best value."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:949

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: r.module || "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:975

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: filterModule !== "all" ? filterModule : "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:999

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1000

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
← Back to Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1013

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="See requirements you submitted and compare vendor quotes."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1034

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Track vendor responses, quotation progress and buyer conversations.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1157

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Dominant Module: {normalizedRfqBehaviorMemory.hotModules[0] || "Learning"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1205

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1206

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
← Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1259

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<div style={{ fontSize: 12, fontWeight: 900, opacity: 0.7, marginBottom: 6 }}>Module</div>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1283

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
placeholder="Search by title / city / pincode / module…"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1355

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const openHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1355

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const openHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1357

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/thread/${encodeURIComponent(r.conversation_id)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1358

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1358

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1359

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const printHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}/print`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1359

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const printHref = `/dashboard/buyer/quote-compare/${encodeURIComponent(r.id)}/print`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1434

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Pill>{titleCase(r.module)}</Pill>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1454

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendorCount} vendor{vendorCount === 1 ? "" : "s"} responded
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1465

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Pill>💬 New vendor activity</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1610

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
SELECTED VENDOR SUMMARY
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1615

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(selected.vendor_id ? `Vendor ${selected.vendor_id.slice(0, 8)}…` : "Vendor")}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1681

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Contact Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/buyer/rfqs/page.tsx:1828

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
This list helps you continue important vendor conversations and quotation decisions.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/construction-projects/[id]/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/construction-projects/[id]/page.tsx:43

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/construction-projects"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/construction-projects/[id]/page.tsx:74

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/construction-projects"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/construction-projects/page.tsx:44

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
AI Construction Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/construction-projects/page.tsx:52

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Save construction plans, budget estimates, BOQ, timeline,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/construction-projects/page.tsx:53

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
procurement schedule and RFQ planning in one workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/construction-projects/page.tsx:61

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Create New Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:10

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { fetchVendorInbox } from "@/lib/rfq/vendor-inbox/server";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:11

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { fetchVendorListingConversations } from "@/lib/conversations/vendor-listing-inbox";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:70

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:98

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:150

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment" | "rfq" | "direct";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:151

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`, `builder`

**Current system**

```text
side: "investor" | "builder" | "vendor" | "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:387

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// 💰 module priority
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:388

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") score += 40;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:389

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "rfq") score += 20;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:399

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "rfq") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:404

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:409

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "direct") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:448

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module === "rfq"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:450

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor reply pending review"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:454

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: item.module === "investment"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:464

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "rfq") procurementScore += 25;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:465

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") procurementScore += 20;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:507

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module === "rfq" && score >= 70
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:521

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module === "rfq"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:523

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Review vendor reply and compare quote."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:527

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: item.module === "investment"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:569

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "rfq") score += 6;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:570

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") score += 4;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:594

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Recover this thread with follow-up or alternate vendor action."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:629

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
? "Reconnect with buyer/vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:681

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment" && item.unreadCount > 0) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:689

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "rfq" && item.unreadCount > 0) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:751

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:760

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const finalModule = overrides.module ?? String(current.module ?? "all").trim();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:766

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (finalModule && finalModule !== "all") params.set("module", finalModule);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:772

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return qs ? `/dashboard/inbox-v2?${qs}` : "/dashboard/inbox-v2";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:981

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{titleCase(item.module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1069

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier {item.supplierSignal}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1116

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1141

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
String(p.full_name ?? p.name ?? "Vendor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1155

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1204

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorName = profileMap.get(vendorId) ?? "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1252

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1269

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1335

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = String(row.buyer_user_id || "") === userId;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1336

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
const side: UnifiedInboxItem["side"] = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1336

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const side: UnifiedInboxItem["side"] = isBuyer ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1339

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer ? row.vendor_user_id : row.buyer_user_id
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1344

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1355

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
counterpart: profileMap.get(counterpartId) || (isBuyer ? "Vendor" : "Buyer"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1355

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
counterpart: profileMap.get(counterpartId) || (isBuyer ? "Vendor" : "Buyer"),
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1359

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${encodeURIComponent(conversationId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1393

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1418

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1499

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const side: UnifiedInboxItem["side"] = isInvestor ? "investor" : "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1527

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1533

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
(isInvestor ? "Builder" : "Investor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1538

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${encodeURIComponent(conversationId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1556

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleFilter = String(params.module ?? "all").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1611

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
fetchVendorInbox({ limit: 50, offset: 0 }),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1620

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rfq",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1621

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1624

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
[row.module, row.locality_name, row.city, row.district]
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1627

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
counterpart: String(row.buyer_name || "Buyer"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1632

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/inbox-v2/thread/${encodeURIComponent(String(row.rfq_id))}?kind=rfq`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1654

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: `direct-vendor-${String(row.id)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1655

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "direct",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1656

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1658

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
subtitle: String(row.last_message_preview || "Direct buyer enquiry"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1659

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
counterpart: String(row.buyer_name || "Buyer"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1664

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${encodeURIComponent(String(row.id))}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1672

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: `direct-buyer-${String(row.id)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1673

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "direct",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1674

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1676

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle: String(row.last_message_preview || "Direct vendor response"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1677

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
counterpart: String(row.vendor_name || "Vendor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1682

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/inbox-v2/thread/${encodeURIComponent(String(row.id))}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1703

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
} else if (item.module === "investment") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1765

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (moduleFilter !== "all" && item.module !== moduleFilter) return false;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1837

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
investment: filteredItems.filter((item) => item.module === "investment"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1838

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
rfq: filteredItems.filter((item) => item.module === "rfq"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1839

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
direct: filteredItems.filter((item) => item.module === "direct"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1892

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor: filteredItems.filter((item) => item.side === "vendor").length,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1893

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
buyer: filteredItems.filter((item) => item.side === "buyer").length,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1895

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder: filteredItems.filter((item) => item.side === "builder").length,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1981

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
? "Reconnect with inactive buyer/vendor conversations."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:1999

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Shortlist strong supplier threads and move them toward final decision."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2001

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
? "Reconnect with older buyer/vendor conversations."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2006

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2011

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: item.module === "rfq"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2013

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: item.module === "investment"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2026

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
type: item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2043

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: moduleFilter !== "all" ? moduleFilter : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2062

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
detail: `${titleCase(latestUnreadItem.module)} • ${latestUnreadItem.counterpart}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2070

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2100

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2111

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
fallbackHref: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2125

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2133

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2218

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "inbox",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2220

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
title: "Unified Dashboard Inbox",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2221

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
summary: "Continue important buyer/vendor conversations and quotation decisions.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2222

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2231

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "inbox",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2232

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
title: "Inbox workspace opened",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2234

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2239

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
<OperationalEventStream title="Recent activity" limit={inboxSuppressLowValueSignals ? 2 : 3} />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2266

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2283

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
{/* Priority strip moved below the real inbox workspace later. */}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2291

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Unified Dashboard Inbox
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2333

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2342

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Opens: {titleCase(latestUnreadItem.module)} •{" "}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2353

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2371

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
primaryHref={latestUnreadItem?.href ?? "/dashboard/inbox-v2"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2372

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
secondaryHref="/dashboard/procurement-os"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2373

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
secondaryLabel="Procurement Workspace"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2391

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: { label: "View All Threads", href: "/dashboard/inbox-v2", primary: true },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2394

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Procurement Live", href: "/dashboard/procurement-live" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2405

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: { label: "View All Threads", href: "/dashboard/inbox-v2", tone: "primary" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2408

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Procurement Live", href: "/dashboard/procurement-live", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2431

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
This section helps you continue important buyer/vendor discussions and quotation decisions.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2468

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={buildInboxHref(params, { module: "rfq" })}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2500

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Follow-up, supplier review and execution support
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2553

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Dominant Module:{" "}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2580

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
One place to continue important buyer, vendor and RFQ conversations.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2589

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-live"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2638

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtext="Investor + builder deal rooms"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2855

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={buildInboxHref(params, { module: "all" })}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2862

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
<span>All Modules</span>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2869

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={buildInboxHref(params, { module: "investment" })}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2883

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={buildInboxHref(params, { module: "rfq" })}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2897

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={buildInboxHref(params, { module: "direct" })}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2927

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={buildInboxHref(params, { side: "vendor" })}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2929

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sideFilter === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2934

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<span>Vendor</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2936

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{sideCounts.vendor}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2941

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={buildInboxHref(params, { side: "buyer" })}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2943

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sideFilter === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2948

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<span>Buyer</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2950

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{sideCounts.buyer}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2969

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
href={buildInboxHref(params, { side: "builder" })}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2971

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
sideFilter === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2976

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<span>Builder</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:2978

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{sideCounts.builder}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3003

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module: {moduleFilter === "all" ? "All" : titleCase(moduleFilter)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3026

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3198

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Filter by module, role-side, title, counterpart, preview,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3198

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Filter by module, role-side, title, counterpart, preview,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3224

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3227

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
name="module"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3231

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
<option value="all">All Modules</option>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3248

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="vendor">Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3249

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<option value="buyer">Buyer</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3251

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<option value="builder">Builder</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3293

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3319

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
description="Investor-builder deal room threads for opportunity discussions, NDA flow, documents, and stage progression."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3347

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
description="Buyer-vendor conversations that may need reply, follow-up, or next-step confirmation."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/page.tsx:3349

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
emptyMessage="No direct buyer-vendor conversations match the current filters."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/inbox-v2/thread/[threadId]/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:3

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:30

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:43

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/inbox-v2/thread/${rawThreadId}${resolvedKind ? `?kind=${resolvedKind}` : ""}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:49

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect(`/vendor/inbox-v2?focus=${encodeURIComponent(rawThreadId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:61

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:66

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = String(row.buyer_user_id || "") === userId;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:67

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const isVendor = String(row.vendor_user_id || "") === userId;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:73

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:76

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
if (isBuyer) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:78

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/investor/deal-rooms/${encodeURIComponent(dealRoomId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:82

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:84

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
`/dashboard/builder/deal-rooms/${encodeURIComponent(dealRoomId)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:84

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/builder/deal-rooms/${encodeURIComponent(dealRoomId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:88

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:97

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
if (isBuyer) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:98

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
redirect(`/dashboard/buyer/chat/${encodeURIComponent(rawThreadId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:98

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect(`/dashboard/buyer/chat/${encodeURIComponent(rawThreadId)}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:101

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:102

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect(`/dashboard/vendor/chat/${encodeURIComponent(rawThreadId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:102

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect(`/dashboard/vendor/chat/${encodeURIComponent(rawThreadId)}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:105

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:109

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect("/vendor/inbox-v2");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox-v2/thread/[threadId]/page.tsx:112

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/inbox-v2");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:6

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerWorkMenu from "@/components/buyer/BuyerWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:19

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:20

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
side: "investor" | "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:132

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"Builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:239

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
async function fetchInvestmentByRole(role: "investor" | "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:241

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
params.set("role", role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:253

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const returnTo = encodeURIComponent("/dashboard/inbox");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:259

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
throw new Error(json?.error || `Failed to load ${role} investment inbox.`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:272

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
fetchInvestmentByRole("builder"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:291

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:300

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/investor/deal-rooms/${encodeURIComponent(String(row.id))}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:305

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
id: `investment-builder-${String(row.id)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:306

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:307

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
side: "builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:315

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
href: `/dashboard/builder/deal-rooms/${encodeURIComponent(String(row.id))}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:315

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/builder/deal-rooms/${encodeURIComponent(String(row.id))}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:334

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (moduleFilter !== "all" && item.module !== moduleFilter) return false;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:367

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const builderSide = investmentItems.filter((x) => x.side === "builder").length;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:441

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
label="Investor + Builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:443

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtext={`${analytics.investorSide} investor side • ${analytics.builderSide} builder side`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:481

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:490

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
<option value="all">All Loaded Modules</option>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:531

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Side: {analytics.builderSide}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:542

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Open any conversation to continue from the correct module workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/inbox/page.tsx:542

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Open any conversation to continue from the correct module workspace.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:584

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module === "investment" ? "Investment" : item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/inbox/page.tsx:588

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{item.side === "investor" ? "Investor Side" : "Builder Side"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/inbox/page.tsx:615

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{item.side === "investor" ? "Builder / promoter" : "Investor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/applications/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/investor/applications/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:50

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/investor/deal-rooms/${dealRoomId}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:51

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: "/dashboard/investor/deal-rooms";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:55

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/investor/deal-rooms/${row.investment_deal_rooms.id}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:58

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard/investor/deal-rooms";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:70

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/login?next=/dashboard/investor/applications");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/applications/page.tsx:129

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{getDealRoomHref(item) !== "/dashboard/investor/deal-rooms" ? (
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/[id]/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/[id]/page.tsx:31

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/[id]/page.tsx:38

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/applications"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/InvestorDealRoomsClient.tsx:57

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
"/api/investment/deal-rooms?role=investor&status=open",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/deal-rooms/InvestorDealRoomsClient.tsx:133

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/investor/deal-rooms/${room.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:204

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
params.set("role", "investor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:222

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const returnTo = encodeURIComponent("/dashboard/investor/deal-rooms");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:371

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Track your builder discussions, monitor deal progress, and reopen
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:372

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
important conversations from one organized workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:386

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:520

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/investor/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:565

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Narrow down deal rooms by keyword, builder, location, or status.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:585

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Search by title, builder, location, status, slug, or room id"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/deal-rooms/page.tsx:758

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/investor/deal-rooms/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:318

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/investor/deal-rooms/${encodeURIComponent(String(room.id))}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:361

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:380

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:460

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
You can start a deal room with the builder by expressing interest.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:471

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
placeholder="Write your interest message to the builder..."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/investor/opportunities/[id]/page.tsx:486

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/page.tsx:180

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
window.location.href = `/dashboard/investor/opportunities/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/page.tsx:242

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/page.tsx:451

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/investor/opportunities/${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/opportunities/page.tsx:465

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/investor/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:86

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/login?next=/dashboard/investor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:95

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/investor/page.tsx:104

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/investor/page.tsx:111

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/investor/page.tsx:122

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/investor/opportunities",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:127

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/investor/applications",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:132

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/investor/deal-rooms",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:140

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
<h1 className="text-2xl font-bold tracking-tight">Investor Dashboard</h1>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:148

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:152

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/applications"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:156

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:219

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/opportunities"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:263

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/applications"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/investor/page.tsx:307

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/investor/deal-rooms/${item.id}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:56

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
const [message, setMessage] = useState("Preparing your marketplace workspace...");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:85

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
setMessage("Unable to load your workspace.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:93

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:113

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:167

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
latestRole: String(profile?.role || profile?.requested_role || "user"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:172

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (target && target !== "/dashboard") {
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:173

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
setMessage(`AI analytics ready. Your default workspace is ${target}.`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:240

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
setFatalError(e?.message || "Something went wrong while loading your dashboard.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:241

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
setMessage("Unable to load your workspace.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:269

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Moderate procurement activity. More RFQs and vendor responses will improve intelligence."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:270

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Early-stage procurement data. Create RFQs and collect vendor/price signals.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:284

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
title="Dashboard could not load"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:309

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Simple view of RFQs, conversations, vendor alerts, price signals and next actions.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:331

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Alerts", stats.vendorAlerts, "🔔"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:358

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Clear unread vendor alerts and follow up active procurement threads."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:380

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier prediction, demand signal, budget risk and conversion insight.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:425

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
<div style={{ color: "#1e3a8a", fontWeight: 800 }}>Best Supplier Prediction</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:427

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{procurementRecommendation.supplierPrediction || "More RFQ/vendor data needed."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:478

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
3Bigha connects RFQs, suppliers, conversations, pricing, closure and attention signals.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/page.tsx:498

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
["Buyer Behavior", procurementMemory.buyerBehavior || "—", "🧑‍💼"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:499

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Reliability", procurementMemory.vendorReliability || "—", "🏆"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:562

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer" className="topBtn topBtnPrimary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:562

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer" className="topBtn topBtnPrimary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:563

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Intelligence →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:565

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/dashboard/vendor" className="topBtn topBtnGhost">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:565

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/vendor" className="topBtn topBtnGhost">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:566

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Intelligence →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:568

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" className="topBtn topBtnGhost">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:571

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" className="topBtn topBtnGhost">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/page.tsx:571

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/buyer/rfqs" className="topBtn topBtnGhost">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/page.tsx:584

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
{loading ? message : `${message} Current detected role: ${stats.latestRole}.`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-actions/page.tsx:194

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-actions/page.tsx:201

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-execution"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-actions/page.tsx:204

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Open Execution Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-actions/page.tsx:208

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-crisis-center"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:113

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Predictive Procurement Analytics Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:118

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier reliability intelligence and pipeline health analytics.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:123

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-control-tower"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:130

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:191

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Predictive shortage, supplier exhaustion and negotiation inflation intelligence.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:227

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
label="Supplier Stress"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:253

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier reliability.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-analytics/page.tsx:276

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} • {item.ageHours}h old
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-anomaly/page.tsx:67

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Detect stalled workflows, abnormal inactivity, supplier silence,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-anomaly/page.tsx:116

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} • {item.ageHours}h old
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:49

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
messages, supplier nudges and workflow recovery tasks.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:162

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous Supplier Collapse Monitoring
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:166

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI is actively generating emergency procurement recovery and supplier rerouting tasks.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:170

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier silence, overload pressure and procurement continuity risks
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:188

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier silence and procurement degradation risk.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:270

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:277

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-followup-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:284

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-inbox-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-autonomous-tasks/page.tsx:291

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-task-execution-log"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-briefing/page.tsx:247

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Tag>{step.module}</Tag>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-briefing/page.tsx:315

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Tag>{attention?.attentionLevel || event.tone || event.priority || "active"}</Tag><Tag>{event.module || "procurement"}</Tag><Tag>attention {attention?.attentionScore ?? 0}</Tag>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-closure-agent/page.tsx:44

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-closure-agent/page.tsx:51

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-negotiation-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-closure-agent/page.tsx:58

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-followup-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-control-tower/page.tsx:141

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Executive Procurement Intelligence Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-control-tower/page.tsx:151

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-control-tower/page.tsx:281

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module} • Last active {item.ageHours}h ago
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-copilot/page.tsx:26

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-control-tower"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-copilot/page.tsx:33

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-analytics"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-copilot/page.tsx:40

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-copilot/ProcurementCopilotClient.tsx:309

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={item.href || "/dashboard/inbox-v2"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-copilot/ProcurementCopilotClient.tsx:316

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module || item.risk || item.projectedStatus || "Procurement intelligence"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-crisis-center/page.tsx:253

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-crisis-center/page.tsx:260

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-war-room"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-crisis-center/page.tsx:267

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-situation-room"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:54

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = Array.isArray(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:64

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Autonomous Procurement Execution Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:130

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:137

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:144

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-followup-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:151

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-situation-room"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:160

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Workflow Execution Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/procurement-execution/page.tsx:164

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.map((step: string, idx: number) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/procurement-followup-agent/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/procurement-followup-agent/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-followup-agent/page.tsx:33

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous workflow follow-up, supplier recovery,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-followup-agent/page.tsx:95

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI is now generating backup supplier follow-up and emergency recovery workflows.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-followup-agent/page.tsx:99

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier silence, overload escalation and procurement continuity
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-health/page.tsx:91

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-health/page.tsx:98

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-war-room"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-health/page.tsx:105

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-crisis-center"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-health/page.tsx:112

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-analytics"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-heatmap/page.tsx:59

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Detect hottest procurement categories, active RFQ zones, supplier
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-heatmap/page.tsx:65

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier-side activity signals.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/procurement-inbox-actions/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:30

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Autonomous Inbox Execution Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:60

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous supplier replacement and backup procurement routing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:100

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Failed Supplier
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:110

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Backup Supplier
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:133

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-followup-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-inbox-actions/page.tsx:140

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-live/page.tsx:115

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-live/page.tsx:230

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
event.module === filter ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-live/page.tsx:329

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
event.module ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-live/page.tsx:915

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-live/page.tsx:922

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-autonomous-tasks"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-live/page.tsx:966

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={event.href || "/dashboard/procurement-live"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/procurement-memory-intelligence/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:32

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier continuity intelligence and
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:47

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:54

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-negotiation-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:61

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
href="/dashboard/procurement-supplier-reliability"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:61

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-supplier-reliability"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-memory-intelligence/page.tsx:64

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Open Supplier Overview
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1364

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-crisis-center" icon="🚨" title="Issue Center" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1365

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`, `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-os" icon="🧭" title="Procurement Workspace" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1366

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-war-room" icon="🏛️" title="Priority Work" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1367

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-situation-room" icon="📡" title="Work Updates" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1368

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-heatmap" icon="🔥" title="Risk Overview" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1369

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-actions" icon="⚡" title="Pending Actions" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1370

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-followup-agent" icon="🤖" title="Follow-up AI" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1371

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-inbox-actions" icon="📥" title="Inbox Help" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1372

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-negotiation-agent" icon="🤝" title="Negotiation" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1373

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
<Shortcut href="/dashboard/procurement-supplier-reliability" icon="🏭" title="Supplier Overview" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1373

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-supplier-reliability" icon="🏭" title="Supplier Overview" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1374

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-memory-intelligence" icon="🧠" title="Recent Workflow" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1375

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-closure-agent" icon="✅" title="Closure Tracking" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1376

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-autonomous-tasks" icon="🛠️" title="Pending Tasks" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1377

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-task-execution-log" icon="📜" title="Activity Log" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1378

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-real-execution" icon="🚀" title="Real Execute" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-mission-control/page.tsx:1379

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Shortcut href="/dashboard/procurement-crisis-center" icon="🛡️" title="Recovery Command" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/procurement-negotiation-agent/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:31

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous supplier negotiation analysis,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:47

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:54

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-inbox-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:61

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-followup-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:74

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI fallback negotiation and backup supplier leverage analysis is active.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-negotiation-agent/page.tsx:78

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Procurement negotiation continuity now includes alternate supplier
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:20

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
const MODULES = [
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-os/page.tsx:21

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🚨", "Work Updates", "/dashboard/procurement-situation-room", "Live procurement operations overview"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:22

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["⚡", "Activity Feed", "/dashboard/procurement-live", "Live workflow and procurement activity"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:23

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🔥", "Heatmap Analysis", "/dashboard/procurement-heatmap", "Zone, category and workflow heatmap"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:24

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
["📈", "Shortage Forecast", "/dashboard/procurement-analytics", "Material shortage and supplier pressure forecast"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:24

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["📈", "Shortage Forecast", "/dashboard/procurement-analytics", "Material shortage and supplier pressure forecast"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:25

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`, `dashboard`, `/dashboard`

**Current system**

```text
["🛰️", "Work Desk", "/dashboard/procurement-mission-control", "Central procurement operations workspace"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:26

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`, `dashboard`, `/dashboard`

**Current system**

```text
["🏛️", "Priority Work", "/dashboard/procurement-war-room", "Critical procurement coordination workspace"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:27

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`, `dashboard`, `/dashboard`

**Current system**

```text
["🛡️", "Issue Center", "/dashboard/procurement-crisis-center", "Issue escalation and recovery workspace"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:28

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
["🏭", "Supplier Reliability", "/dashboard/procurement-supplier-reliability", "Supplier response and reliability overview"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:28

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🏭", "Supplier Reliability", "/dashboard/procurement-supplier-reliability", "Supplier response and reliability overview"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:29

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["📥", "Inbox Actions", "/dashboard/procurement-inbox-actions", "Inbox follow-up and rerouting support"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:30

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🤖", "Follow-up Support", "/dashboard/procurement-followup-agent", "Follow-up and recovery assistance"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:31

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
["🤝", "Negotiation Guidance", "/dashboard/procurement-negotiation-agent", "Negotiation and supplier discussion support"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:31

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🤝", "Negotiation Guidance", "/dashboard/procurement-negotiation-agent", "Negotiation and supplier discussion support"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:32

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🧩", "Pending Actions", "/dashboard/procurement-actions", "Workflow execution and operational actions"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:33

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🛠️", "Pending Tasks", "/dashboard/procurement-autonomous-tasks", "Automatic task and reminder queue"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:34

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🚀", "Real Execute", "/dashboard/procurement-real-execution", "Execution preparation and readiness"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:35

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["📜", "Activity Log", "/dashboard/procurement-task-execution-log", "Execution history log"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:36

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["🧠", "Procurement Guidance", "/dashboard/procurement-copilot", "Get procurement workflow assistance"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:63

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-os/page.tsx:66

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
summary: "Continue supplier coordination, follow-up, risk review and execution planning.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:67

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-os",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:76

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-os/page.tsx:77

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
title: "Procurement workspace opened",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:78

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
detail: "Review supplier coordination, follow-up, risk and execution activity.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:79

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-os",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-os/page.tsx:89

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit={6}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/procurement-os/page.tsx:101

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
One place to manage procurement workflows, supplier coordination, follow-up, recovery, forecasting and execution tasks.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-os/page.tsx:142

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
{MODULES.map(([emoji, title, href, text]) => (
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-real-execution/page.tsx:53

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Crisis escalation, supplier recovery, emergency rerouting and AI operational stabilization
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-real-execution/page.tsx:136

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-autonomous-tasks"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-real-execution/page.tsx:143

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-task-execution-log"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-real-execution/page.tsx:150

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:290

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Badge>{step.module}</Badge>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:312

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Command href="/dashboard/procurement-live" title="Open Live Feed" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:313

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Command href="/dashboard/procurement-timeline" title="Open Timeline" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:314

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Command href="/dashboard/procurement-heatmap" title="Open Risk Overview" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:315

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Command href="/dashboard/procurement-autonomous-tasks" title="Pending Tasks" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:316

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Command href="/dashboard/procurement-mission-control" title="Work Desk" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:361

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={event.href || "/dashboard/procurement-live"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-situation-room/page.tsx:366

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Badge>{event.module || "procurement"}</Badge><Badge>attention {attention?.attentionScore ?? 0}</Badge>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
// app/dashboard/procurement-supplier-reliability/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/procurement-supplier-reliability/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:17

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
fetch("/api/ai/procurement-supplier-reliability")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:21

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
fetch("/api/ai/procurement-supplier-collapse")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:44

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier Reliability Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:48

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI Supplier Reliability Engine
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:52

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous supplier reliability analysis,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:54

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor operational risk monitoring.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:70

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI Supplier Collapse Predictor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:74

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Autonomous supplier silence, overload and collapse-risk forecasting.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:104

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
{item.supplier}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:134

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:141

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-inbox-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-supplier-reliability/page.tsx:148

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-negotiation-agent"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-task-execution-log/page.tsx:88

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-autonomous-tasks"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-task-execution-log/page.tsx:95

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-timeline/page.tsx:13

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-timeline/page.tsx:214

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{step.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:159

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:173

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-crisis-center"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:187

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-anomaly"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:202

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:217

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-live"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/procurement-war-room/page.tsx:232

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-copilot"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:37

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
async function activatePlan(plan: string) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:38

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setBusyPlan(plan);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:47

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push("/login?next=/dashboard/subscription/boost");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:47

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
router.push("/login?next=/dashboard/subscription/boost");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:57

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
body: JSON.stringify({ plan }),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:89

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Higher visibility → More buyer leads → More deals
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:124

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{BOOST_PLANS.map((plan) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:126

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
key={plan.key}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:128

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
border: plan.highlight ? "2px solid #f59e0b" : "1px solid #ddd",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:131

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
background: plan.highlight
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:134

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
boxShadow: plan.highlight
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:140

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.name}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:144

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.desc}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:148

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.price}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:153

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
disabled={busyPlan === plan.key}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:156

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
background: plan.highlight ? "#f59e0b" : "#111827",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:162

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
cursor: busyPlan === plan.key ? "not-allowed" : "pointer",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:163

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
opacity: busyPlan === plan.key ? 0.65 : 1,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:165

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
onClick={() => activatePlan(plan.key)}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/boost/page.tsx:167

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{busyPlan === plan.key ? "Preparing..." : "Activate Boost"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/page.tsx:9

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
<Suspense fallback={<div>Loading subscription…</div>}>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:7

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
import { getGrowthPlanPresentation } from "@/lib/3bos/capability";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:18

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
| "gold_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:19

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
| "platinum_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:27

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
| "gold_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:28

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
| "platinum_vendor";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:30

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function normalizePlanKey(plan: PlanKey): DisplayPlanKey {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:31

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `gold_vendor`

**Current system**

```text
if (plan === "premium_vendor") return "gold_vendor";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:32

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `platinum_vendor`

**Current system**

```text
if (plan === "hub_vendor") return "platinum_vendor";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:33

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
return plan;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:57

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
boost: "+0 AI Boost",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:72

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
boost: "+3 AI Boost",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:87

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
boost: "+5 AI Boost",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:91

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
visibility: "Stronger buyer visibility",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:95

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
trust: "🥈 AI BOOSTED",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:99

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
gold_vendor: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:102

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
boost: "+10 AI Boost",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:106

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
visibility: "Premium ranking advantage",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:110

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai verified`

**Current system**

```text
trust: "⭐ AI VERIFIED",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:112

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
storagePlan: "gold_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:114

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
platinum_vendor: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:117

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
boost: "+20 AI Boost",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:125

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai verified`

**Current system**

```text
trust: "🔥 AI VERIFIED MAX",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:127

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
storagePlan: "platinum_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:131

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function planAiBoostPower(plan: PlanKey) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:132

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p = normalizePlanKey(plan);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:133

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
if (p === "platinum_vendor") return 20;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:134

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
if (p === "gold_vendor") return 10;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:140

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function planAiTrustLevel(plan: PlanKey) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:141

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
return PLAN_META[normalizePlanKey(plan)].trust;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:144

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function planLeadPrediction(plan: PlanKey) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:145

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const meta = PLAN_META[normalizePlanKey(plan)];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:172

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:186

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/subscription?${q.toString()}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:186

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
return `/dashboard/subscription?${q.toString()}`;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:218

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
const goldPrediction = planLeadPrediction("gold_vendor");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:219

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
const platinumPrediction = planLeadPrediction("platinum_vendor");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:224

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
"gold_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:225

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
"platinum_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:277

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub?.subscription?.unsubscribe?.();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:282

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
// 2) Load active plan from business_profiles
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:282

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
// 2) Load active plan from business_profiles
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:290

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:307

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
pk === "gold_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:308

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
pk === "platinum_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:333

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
async function handlePayment(plan: PlanKey) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:348

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:358

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:360

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subscription_plan: plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:367

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
console.warn("subscription request update failed:", requestError.message);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:370

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setActivePlan(plan);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:375

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Your Growth Plan request has been recorded. Online payment is not active yet; the team can activate the requested plan after payment confirmation."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:378

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
console.warn("subscription purchase intent tracking failed:", e?.message || e);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:380

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Online payment is not active yet. Please contact the 3Bigha team for plan activation support."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:387

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
async function activatePlan(plan: PlanKey) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:395

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan !== "free") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:397

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Online payment is not active yet. Please contact the 3Bigha team to activate this Growth Plan."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:404

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:413

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
console.warn("business_profiles subscription update error:", error.message);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:413

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
console.warn("business_profiles subscription update error:", error.message);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:423

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
setErr(e?.message || "Failed to activate plan.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:435

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
<h1 className="h1">Business Growth Plans</h1>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:438

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Recover missed leads by boosting your visibility in AI vendor matching."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:440

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
? "Improve deal closing with AI-powered replies and smarter follow-ups."
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:441

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
: focus === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:442

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Maintain top vendor position with maximum visibility and priority ranking."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:443

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
: "3Bigha is a Business Operating System with an integrated Marketplace. Customers pay businesses directly; Growth Plans support business tools, visibility and operations without commission."}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:451

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="btn btnOutline" href="/dashboard">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:452

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Back to Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:472

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
Review the Growth Plans and choose the visibility support appropriate for your business.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:482

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
) : focus === "premium" ? (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:493

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
You can still review and request a Growth Plan for your business.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:531

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<b>Current Growth Plan:</b>{" "}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:547

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
? "Recover missed leads with stronger AI boost visibility"
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:549

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Improve deal closing with AI-assisted vendor growth"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:550

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
: focus === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:551

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Stay ahead with premium vendor visibility"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:551

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Stay ahead with premium vendor visibility"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:556

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Your vendor dashboard found missed lead opportunities. Review the Growth Plans and choose the visibility support appropriate for your business."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:556

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
? "Your vendor dashboard found missed lead opportunities. Review the Growth Plans and choose the visibility support appropriate for your business."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:556

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
? "Your vendor dashboard found missed lead opportunities. Review the Growth Plans and choose the visibility support appropriate for your business."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:558

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
? "Use AI-powered visibility and smarter follow-ups to convert more conversations into completed deals."
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:559

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
: focus === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:560

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Keep your ranking strong with higher boost power, premium trust signals, and maximum buyer visibility."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:560

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Keep your ranking strong with higher boost power, premium trust signals, and maximum buyer visibility."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:561

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Every Growth Plan supports a different stage of business. Choose according to the tools, team size and operating support you currently need."}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:573

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan level affects included business tools and promotional support. Trust and verification remain based on evidence.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:593

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Compare the commercial offers currently available while the new Growth Plan system is introduced.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:605

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontSize: 12, color: "#92400e", fontWeight: 900 }}>Current Plan</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:615

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
<div style={{ fontSize: 12, color: "#1d4ed8", fontWeight: 900 }}>Gold Upgrade</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:625

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
<div style={{ fontSize: 12, color: "#be123c", fontWeight: 900 }}>Platinum Upgrade</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:641

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<b>Payment update:</b> Online payment is not active yet. Plan requests are recorded safely, and activation is completed after payment confirmation.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:647

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
<div className="comparisonKicker">Business Growth Plans</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:655

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{displayPlans.map((plan) => {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:656

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const meta = PLAN_META[plan];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:657

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const active = activeDisplayPlan === plan && isActive;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:662

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
key={plan}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:664

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div className="comparePlan">{getGrowthPlanPresentation(plan).offerLabel}</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:695

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{displayPlans.map((plan) => {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:696

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const meta = PLAN_META[plan];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:697

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const active = activeDisplayPlan === plan && isActive;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:698

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const isFree = plan === "free";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:702

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
key={plan}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:703

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title={getGrowthPlanPresentation(plan).offerLabel}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:712

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `gold_vendor`

**Current system**

```text
plan === "gold_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:714

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `platinum_vendor`

**Current system**

```text
: plan === "platinum_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:716

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: plan === "silver_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:717

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
? "Best starter upgrade"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:718

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: plan === "basic_vendor"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:723

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
(focus === "boost" && plan === "silver_vendor") ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:724

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `gold_vendor`

**Current system**

```text
(focus === "ai" && plan === "gold_vendor") ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:725

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `premium`, `platinum_vendor`

**Current system**

```text
(focus === "premium" && plan === "platinum_vendor")
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:742

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: `Request ${getGrowthPlanPresentation(plan).offerLabel}`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/subscription/SubscriptionPageClient.tsx:1155

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div className={`badge ${props.active ? "badgeOn" : ""}`}>{props.active ? "ACTIVE" : "PLAN"}</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/thread/[conversationId]/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:5

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerRfqChatBox from "@/app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
import BuyerRfqChatBox from "@/app/dashboard/buyer/quote-compare/[rfqId]/chat/buyer-rfq-chat-box";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:6

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorRfqChatBox from "@/app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:8

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:11

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerConversationChatBox from "@/app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:11

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
import BuyerConversationChatBox from "@/app/dashboard/buyer/chat/[conversationId]/buyer-conversation-chat-box";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:76

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:140

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer: boolean;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:233

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
leverage: args.isBuyer
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:287

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer: boolean;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:295

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: args.conversation.context_type || "thread",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:297

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
side: args.isBuyer ? "buyer" : "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:297

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
side: args.isBuyer ? "buyer" : "vendor",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:306

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: m.sender_role || "user",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:416

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
function buildBackHref(conv: ConversationRow, isBuyer: boolean) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:419

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (!dealRoomId) return "/dashboard/inbox-v2";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:420

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
return isBuyer
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:421

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/investor/deal-rooms/${encodeURIComponent(dealRoomId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:422

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: `/dashboard/builder/deal-rooms/${encodeURIComponent(dealRoomId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:422

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: `/dashboard/builder/deal-rooms/${encodeURIComponent(dealRoomId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:426

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return isBuyer ? "/dashboard/inbox-v2" : "/vendor/inbox-v2";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:426

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return isBuyer ? "/dashboard/inbox-v2" : "/vendor/inbox-v2";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:426

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
return isBuyer ? "/dashboard/inbox-v2" : "/vendor/inbox-v2";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:429

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard/inbox-v2";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:454

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:475

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/login?next=${encodeURIComponent(`/dashboard/thread/${conversationId}`)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:513

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:525

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = String(conv.buyer_user_id || "") === userId;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:526

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const isVendor = String(conv.vendor_user_id || "") === userId;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:528

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`, `isbuyer`

**Current system**

```text
if (!isBuyer && !isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:533

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
Access denied
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:540

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:551

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const counterpartUserId = isBuyer
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:565

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:572

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:596

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
(isBuyer ? "Vendor" : "Buyer");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:596

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
(isBuyer ? "Vendor" : "Buyer");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:599

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
const counterpartRoleLabel = isBuyer ? "Vendor" : "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:599

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const counterpartRoleLabel = isBuyer ? "Vendor" : "Buyer";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:600

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const backHref = buildBackHref(conv, isBuyer);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:612

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:628

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:635

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:671

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:688

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "inbox",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:690

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
title: "Conversation Workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:692

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${conversationId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:701

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "thread",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:704

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/thread/${conversationId}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:709

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
<OperationalEventStream title="Recent conversation activity" limit={5} />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:750

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:750

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:784

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:784

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:786

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: { label: "Back to Inbox", href: "/dashboard/inbox-v2" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:807

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
{isBuyer ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:809

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
⭐ Premium visibility ready
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:847

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
{isBuyer ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:942

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:942

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:950

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:1002

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
🏆 Supplier Signal
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:1066

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Module: {threadBehaviorSignals.module || "Learning"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/thread/[conversationId]/page.tsx:1323

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:21

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:22

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:141

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/billing")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:141

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/billing")}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:180

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:485

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "billing",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:594

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:594

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:595

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:595

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:597

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:597

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:600

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:600

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:607

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="When a vendor creates a bill, stock is reduced automatically and a stock movement log is created."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:628

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/billing"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:628

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/billing"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:635

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:635

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:642

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:642

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:649

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:649

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/billing/page.tsx:1048

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/api/vendor/billing/${encodeURIComponent(bill.bill_no)}/pdf`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:4

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorConversationChatBox from "./vendor-conversation-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:54

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:81

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (!id) return "/vendor/inbox-v2";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:87

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (ct === "rfq") return `/vendor/inbox-v2/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:89

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "/vendor/inbox-v2";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:106

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:107

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Back to Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:109

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:164

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:165

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Back to Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:167

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:177

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at,last_seen_message_id")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:217

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const buyerName = profile?.full_name ?? profile?.name ?? "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:251

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:252

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:254

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/inbox-v2" style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/page.tsx:284

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div><strong>Buyer:</strong> {buyerName}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:336

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function toDisplayRole(role?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:337

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const value = String(role ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:345

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:350

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title: "General buyer intent",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:352

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Continue qualifying buyer quantity, location, timeline and pricing expectations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:352

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Continue qualifying buyer quantity, location, timeline and pricing expectations.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:359

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(context.module || "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:366

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:371

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer likely comparing multiple suppliers for pricing, delivery and GST billing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:371

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Buyer likely comparing multiple suppliers for pricing, delivery and GST billing.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:377

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:381

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer may require ownership verification, pricing negotiation and site visit coordination.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:381

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Buyer may require ownership verification, pricing negotiation and site visit coordination.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:387

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:391

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer may compare labour responsibility, scope coverage, timeline and warranty support.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:397

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:401

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer may evaluate transport cost, rental duration and equipment availability.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:410

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer is continuing an active procurement workflow from marketplace discovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:469

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Send a follow-up and ask buyer to confirm quantity, location and timeline."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:470

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Qualify the buyer by asking quantity, location, timeline and expected budget.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:474

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Escalate this lead with quick reply or premium alert protection."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:476

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Reply soon to avoid losing buyer interest."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:663

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "🔥 Buyer is ready to close"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:665

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "⚡ Buyer is almost ready"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:666

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "⚡ Buyer is waiting",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:669

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
: "Reply now with clear final terms before the buyer moves to another vendor.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1374

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sender_role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1501

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1503

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1504

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1523

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1534

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1584

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch("/api/ai/vendor-alert", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1591

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1603

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1605

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1606

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1786

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1788

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1789

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1796

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/ai/vendor-alert", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1802

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1820

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
const isPremium = Boolean(monetization?.premium);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1831

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title: json?.title || json?.alert?.title || "High-Intent Buyer Detected",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1835

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"AI detected that this buyer may be close to a deal. Upgrade to unlock WhatsApp alerts and priority deal protection.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1835

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
"AI detected that this buyer may be close to a deal. Upgrade to unlock WhatsApp alerts and priority deal protection.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1836

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
cta: "Unlock Premium AI Alerts",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1843

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
title: "Premium WhatsApp Alert Triggered",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1846

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "AI detected a serious buyer, but WhatsApp delivery could not be confirmed. The in-app alert is still active."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1847

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "AI detected a serious buyer and triggered your premium WhatsApp alert.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1847

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
: "AI detected a serious buyer and triggered your premium WhatsApp alert.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1848

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `premium`

**Current system**

```text
cta: "View Premium Plan",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1849

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: true,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1987

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
background: aiUpgradeAlert.premium
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:1994

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
border: aiUpgradeAlert.premium
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2006

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
color: aiUpgradeAlert.premium ? "#065f46" : "#92400e",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2028

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
window.location.href = "/dashboard/subscription?focus=ai";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2028

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
window.location.href = "/dashboard/subscription?focus=ai";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2033

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
background: aiUpgradeAlert.premium ? "#059669" : "#f59e0b",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2115

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
toDisplayRole={(role?: string | null) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2116

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) return counterpartName || "User";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2117

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
return String(role).toLowerCase() === "buyer" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2118

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
String(role).toLowerCase() === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2119

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
? counterpartName || toDisplayRole(role)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2120

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: toDisplayRole(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2214

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Vendor Work Space"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2221

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Open Inbox", href: "/dashboard/inbox-v2", tone: "neutral" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2239

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2240

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
conversationContext?.module ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2271

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: conversationContext?.module || contextType || "inbox",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2302

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🤖 Autonomous Vendor Procurement Agent
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2305

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
AI tracks buyer reliability, lead urgency, escalation risk, closing stage and next best vendor action.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2329

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
["Buyer", vendorAgent.buyerReliability, "🏆"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2445

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{vendorAi.isHotBuyer ? "🔥 Buyer looks ready to close" : "🤖 AI Vendor Workflow Assistant"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2479

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
{!aiUpgradeAlert?.premium ? (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2481

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
🔒 Premium required to get priority advantage on this deal
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2488

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (!aiUpgradeAlert?.premium) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2489

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push("/dashboard/subscription?focus=ai");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2489

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
router.push("/dashboard/subscription?focus=ai");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2503

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
background: aiUpgradeAlert?.premium ? "#fff" : "#f59e0b",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2504

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
color: aiUpgradeAlert?.premium ? "#c2410c" : "#fff",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2509

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
{aiUpgradeAlert?.premium
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2511

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
: "⭐ Unlock Premium"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2662

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push("/dashboard/subscription/boost");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/chat/[conversationId]/vendor-conversation-chat-box.tsx:2662

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
router.push("/dashboard/subscription/boost");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:120

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent(`/dashboard/vendor/dispatch/${dispatchId}`)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:120

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent(`/dashboard/vendor/dispatch/${dispatchId}`)}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:178

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/vendor/dispatch/send-whatsapp-update", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:275

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:275

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:279

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:279

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:283

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:283

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:384

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Info title="Buyer" value={dispatch.buyer_name || "—"} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/[dispatchId]/page.tsx:385

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Info title="Buyer Phone" value={dispatch.buyer_phone || "—"} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:21

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:22

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:106

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/dispatch")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:106

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/dispatch")}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:277

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:277

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:278

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:278

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:281

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:281

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:285

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:285

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:292

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
subtitle="Manage material delivery, vehicle assignment and buyer delivery updates."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:306

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
subtitle="Manage deliveries, buyer updates and vehicle movement."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:313

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:313

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:320

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:320

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:327

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:327

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:334

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:334

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:430

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Buyer name"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:437

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Buyer phone"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/dispatch/page.tsx:566

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Badge>Buyer: {d.buyer_name}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/page.tsx:9

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Suspense fallback={<div>Loading vendor enquiries…</div>}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:16

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
type SenderRole = "buyer" | "vendor" | string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:177

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor/enquiries");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:177

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor/enquiries");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:190

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:227

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:296

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:303

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.replace("/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:303

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:435

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sender_role: "vendor" as SenderRole,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:461

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
buyer_name: "Test Buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:496

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:496

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:497

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Back to Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:497

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Back to Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:513

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<SectionHeader title="Enquiries Inbox" subtitle="View, filter, and reply to buyer enquiries." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:516

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:516

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:517

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:517

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:549

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Creates a test enquiry addressed to your own vendor account (safe for UI testing)."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:602

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Search message / buyer name / phone / email..."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:643

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{focused.buyer_name?.trim() ? focused.buyer_name : "Buyer"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:655

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/dashboard/vendor/chat/${encodeURIComponent(focusedConversationId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:655

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/vendor/chat/${encodeURIComponent(focusedConversationId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:682

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/enquiries" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:682

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/enquiries" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:704

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const isMine = (m.sender_role ?? "").toLowerCase() === "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:723

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
<Pill tone={isMine ? "ok" : "neutral"}>{isMine ? "Vendor" : "Buyer"}</Pill>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:736

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Reply as Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:742

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Type your reply to the buyer…"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:789

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Vendor Notes (private)</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:848

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(focused.id)}`} style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:848

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(focused.id)}`} style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:852

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Inbox uses the same thread.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:888

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{e.buyer_name?.trim() ? e.buyer_name : "Buyer"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:903

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:903

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(e.id)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:911

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/dashboard/vendor/chat/${encodeURIComponent(liveConversationId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:911

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/vendor/chat/${encodeURIComponent(liveConversationId)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:943

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(e.id)}`} style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/enquiries/VendorEnquiriesInboxPageClient.tsx:943

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href={`/dashboard/vendor/enquiries?focus=${encodeURIComponent(e.id)}`} style={{ fontWeight: 900 }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:21

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:22

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:66

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/fleet")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:66

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/fleet")}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:216

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:216

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:217

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:217

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:219

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:219

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:261

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
description="Register new trucks, pickups and delivery vehicles."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:262

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:262

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:269

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:269

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:276

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:276

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:283

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/fleet/page.tsx:283

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inbox/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inbox/page.tsx:4

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect("/vendor/inbox-v2");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory-intelligence/page.tsx:10

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { ErpKpiCard, ErpKpiGrid, ErpPanel } from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory-intelligence/page.tsx:11

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorErpNav } from "@/components/vendor-erp/VendorErpNav";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:5

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:14

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:23

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:87

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/inventory")}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:87

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.push(`/login?next=${encodeURIComponent("/dashboard/vendor/inventory")}`);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:96

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:227

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:227

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:228

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:228

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:260

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:260

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="primary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:268

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:268

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:272

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:272

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/fleet" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:276

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:276

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/dispatch" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:280

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory-intelligence" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:280

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory-intelligence" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:284

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:284

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/inventory" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:288

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:288

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor/billing" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:296

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Materials added from the normal vendor material form now become inventory items. Shop owners can search by SKU, barcode, godown, room, rack or vehicle number."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:318

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:318

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:325

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/billing"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:325

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/billing"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:332

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:332

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/dispatch"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:339

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:339

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/fleet"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:346

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/inventory/page.tsx:346

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/materials/page.tsx:6

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:13

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: string;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:18

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function isPaidActive(plan: string, status: string) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:20

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
String(plan || "").toLowerCase() !== "free";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:120

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: "free",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:140

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: "free",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:148

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:155

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String((bp as any)?.subscription_plan || "free");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:163

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const paid = isPaidActive(plan, status);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:178

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:187

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: e?.message || "Unable to check vendor access.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:189

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: "free",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:204

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
eyebrow="Vendor Product Setup"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:210

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Checking your vendor access"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:211

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
subtitle="Please wait while we confirm your business profile."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:217

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
title="Upgrade and verify to unlock this"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:243

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/subscription"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:243

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
href="/dashboard/subscription"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:244

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Activate Vendor Plan"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:244

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title="Activate Vendor Plan"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:245

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail="Unlock premium vendor tools and visibility."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:245

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
detail="Unlock premium vendor tools and visibility."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:248

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href="/onboarding/business"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:249

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
title="Verify Business Profile"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:253

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:253

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:254

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Back to Vendor Dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:254

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
title="Back to Vendor Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:255

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail="Continue normal vendor operations."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:263

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Add only the variations you actually sell or rent. These will be kept connected to your vendor profile."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:275

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `paid plan`

**Current system**

```text
Paid plan: {access.plan}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:299

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/master-data/materials"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:299

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/master-data/materials"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:317

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/master-data/rentals"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:317

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/master-data/rentals"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:342

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
["Use buyer language", "Write names the way local buyers search."],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/page.tsx:345

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
["Admin can promote later", "Good repeated vendor options may become global marketplace options."],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/rentals/page.tsx:6

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:9

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:14

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:53

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:60

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String((bp as any)?.subscription_plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:68

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const paid = status === "active" && plan !== "free";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:78

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:92

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setMsg("Saved successfully. This variation is now connected to your vendor profile.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:110

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
eyebrow="Vendor Product Setup"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:116

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Add in simple buyer language
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:130

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder="Notes for your team or admin review" className="vendorInput" rows={4} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:169

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/dashboard/vendor/master-data" style={{ fontWeight: 900, color: "#1d4ed8" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/master-data/VendorVariationForm.tsx:169

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/vendor/master-data" style={{ fontWeight: 900, color: "#1d4ed8" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:11

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:41

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3Bigha Vendor Alert",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:45

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Open your vendor dashboard to improve visibility:",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:45

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
"Open your vendor dashboard to improve visibility:",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:46

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"https://www.3bigha.com/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:46

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"https://www.3bigha.com/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:69

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor/notifications");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:69

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor/notifications");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:78

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:110

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Vendor Notifications"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:111

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Important vendor alerts, rank updates and visibility reminders."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:117

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:117

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:118

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Back to Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:118

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
← Back to Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:143

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<EmptyState message="No notifications yet. Rank drops, improvements, and important vendor alerts will appear here." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:199

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription/boost")}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/notifications/page.tsx:199

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription/boost")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:4

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:23

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/components/vendor-erp/VendorErpWidgets";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:24

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorErpNav } from "@/components/vendor-erp/VendorErpNav";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:25

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorOperationStream } from "@/components/vendor-erp/VendorOperationStream";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:37

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete?: boolean;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:95

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function getUpsellMessage(plan: string) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:96

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p = String(plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:101

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: "You are currently on Free plan. Your visibility is limited in AI vendor matching.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:101

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `free plan`

**Current system**

```text
text: "You are currently on Free plan. Your visibility is limited in AI vendor matching.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:102

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
highlight: "Upgrade to get more buyer enquiries.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:102

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
highlight: "Upgrade to get more buyer enquiries.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:109

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
text: "You are getting some visibility, but higher plans rank above you.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:110

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
highlight: "Upgrade to increase your RFQ chances.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:171

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (cap === "property_builder") return "Property Builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:175

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (cap === "blog_author") return "Blog Author";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:180

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function getPlanBoostLabel(plan: string, boostPriority: number) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:181

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p = String(plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:183

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
if (p === "platinum_vendor" || p === "hub_vendor") return "Platinum AI Boost";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:183

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
if (p === "platinum_vendor" || p === "hub_vendor") return "Platinum AI Boost";
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:184

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
if (p === "gold_vendor" || p === "premium_vendor") return "Gold AI Boost";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:184

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
if (p === "gold_vendor" || p === "premium_vendor") return "Gold AI Boost";
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:185

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
if (p === "silver_vendor") return "Silver AI Boost";
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:186

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
if (p === "basic_vendor") return "Basic AI Boost";
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:192

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function getPlanBoostPower(plan: string, boostPriority: number) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:193

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const p = String(plan || "free").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:195

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
if (p === "platinum_vendor" || p === "hub_vendor") return 20 + boostPriority;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:196

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
if (p === "gold_vendor" || p === "premium_vendor") return 10 + boostPriority;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:212

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const [isVendor, setIsVendor] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/page.tsx:223

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor Hub Dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:223

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
? "Vendor Hub Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:226

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Builder Dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:226

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
? "Builder Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:229

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
? "Investor Dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:230

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor Dashboard";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:230

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
: "Vendor Dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:286

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Top Performing Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:288

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Trusted Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:290

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Active Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:291

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "New Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:294

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorPerformanceLevel === "Top Performing Vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:295

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorPerformanceLevel === "Trusted Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:297

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: vendorPerformanceLevel === "Active Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:306

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: vendorPerformanceLevel === "Top Performing Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:316

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "🏆 Maintain Premium Visibility"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:317

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "🚀 Boost to Top Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:344

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
// ⭐ BOOST / SUBSCRIPTION VISIBILITY
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:456

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Reply to every buyer lead quickly to improve Ranking and conversion."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:463

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "🔥 Top Vendor Zone"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:465

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "⚡ Rising Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:472

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "You are close to premium leaderboard strength."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:473

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Upgrade visibility, improve AI pricing, and reply faster to enter top vendor zone.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:473

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`, `upgrade`

**Current system**

```text
: "Upgrade visibility, improve AI pricing, and reply faster to enter top vendor zone.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:504

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "🥇 Top Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:540

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
`📉 Your prices are ${priceIntelligenceStats.averageDeviation}% above market. Adjust pricing to improve visibility.`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:546

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"📊 Increase AI-optimized price updates to enter top vendor ranking."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:558

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`, `upgrade`

**Current system**

```text
"🚀 Upgrade your plan to unlock higher Visibility and better RFQ routing."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:564

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"🔥 Strong performance! Maintain pricing accuracy and response speed to dominate rankings."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:578

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
normalizedVendorPlan === "gold_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:579

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
normalizedVendorPlan === "platinum_vendor" ||
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:609

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:609

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
router.replace("/login?next=/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:622

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:623

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("nature_of_business,business_profile_complete,is_complete,registration_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:634

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (item === "builder") return "property_builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:649

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
access.isAdmin ||
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/page.tsx:650

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
access.isVendor ||
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/page.tsx:654

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
(profileAccess as any)?.is_complete === true ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:659

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
setVendorHasFullAccess(access.isAdmin || access.vendorHasFullAccess || mergedCapabilities.length >= 4);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/page.tsx:681

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
row.business_profile_complete === true || row.is_complete === true;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:699

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:738

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const leaderboardRes = await fetch("/api/vendor/leaderboard", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:758

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:839

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const rfqRes = await fetch("/api/vendor/rfqs?limit=100", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:839

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const rfqRes = await fetch("/api/vendor/rfqs?limit=100", {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:870

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:901

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
side: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:915

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
body: `${row.rfq_no || ""} ${row.module || ""} ${row.message || ""} ${row.status || ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/page.tsx:942

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor dashboard load failed:", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:942

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
console.error("Vendor dashboard load failed:", error);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:943

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setErr(error?.message || "Vendor dashboard failed to load.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:943

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
setErr(error?.message || "Vendor dashboard failed to load.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:951

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch("/api/ai/vendor-coach", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1042

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/vendor/rank-history", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1064

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
// Do not block dashboard if backend rank tracking fails.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1075

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<SectionHeader title={dashboardTitle} subtitle="Preparing your vendor workspace." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1075

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<SectionHeader title={dashboardTitle} subtitle="Preparing your vendor workspace." />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1086

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<SectionHeader title={dashboardTitle} subtitle="Unable to load your vendor workspace." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1086

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<SectionHeader title={dashboardTitle} subtitle="Unable to load your vendor workspace." />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1089

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Vendor dashboard could not load"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1089

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
title="Vendor dashboard could not load"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1095

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1095

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard/vendor" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1096

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
← Back to Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1120

**Area:** Dashboard

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (!isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/dashboard/vendor/page.tsx:1124

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<SectionHeader title={dashboardTitle} subtitle="Vendor access required." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1141

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Your pricing accuracy now affects vendor matching and buyer lead visibility.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1141

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Your pricing accuracy now affects vendor matching and buyer lead visibility.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1177

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/vendor/price-updates/new" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1238

**Area:** Dashboard

**Category:** Visible AI Terminology

**Matched:** `ai boost`

**Current system**

```text
AI Boost: +{getPlanBoostPower(vendorPlan, vendorBoostPriority)}
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1244

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription/boost")}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1244

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription/boost")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1256

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🏆 Enter Top Vendor Zone
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1288

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription?focus=ai")}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1288

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
onClick={() => router.push("/dashboard/subscription?focus=ai")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1305

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1309

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<ActionButton href="/auth/register-role" variant="primary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1309

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
<ActionButton href="/auth/register-role" variant="primary">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:1310

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Start Vendor Registration →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1310

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Start Vendor Registration →
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:1314

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<EmptyState message="You do not currently have vendor access. Start vendor registration to unlock your business dashboard." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1314

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
<EmptyState message="You do not currently have vendor access. Start vendor registration to unlock your business dashboard." />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1314

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<EmptyState message="You do not currently have vendor access. Start vendor registration to unlock your business dashboard." />
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:1329

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-dashboard",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1329

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
id: "vendor-dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1330

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
module: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1330

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "vendor",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/page.tsx:1332

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Work Desk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1333

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
summary: "Continue buyer replies, RFQs, inventory, billing and dispatch.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1334

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1334

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1335

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
primaryActionLabel: "Open Vendor Work Desk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1342

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-dashboard-opened",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1342

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
id: "vendor-dashboard-opened",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1343

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
module: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1343

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "vendor",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/page.tsx:1344

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor work desk opened",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1345

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
detail: "Daily vendor work desk opened for buyer replies, RFQs and operations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1346

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1346

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1378

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Work Desk
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1382

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Start your daily vendor work here
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1391

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
["RFQs / Buyer Leads", "Check new requirements and quote quickly.", "/dashboard/vendor/rfqs"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1391

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["RFQs / Buyer Leads", "Check new requirements and quote quickly.", "/dashboard/vendor/rfqs"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1392

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
["Messages", "Reply to buyer conversations.", "/dashboard/vendor/inbox"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1392

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Messages", "Reply to buyer conversations.", "/dashboard/vendor/inbox"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1393

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Inventory", "Update stock and available items.", "/dashboard/vendor/inventory"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1393

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Inventory", "Update stock and available items.", "/dashboard/vendor/inventory"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1394

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Billing", "Create invoice and bill records.", "/dashboard/vendor/billing"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1394

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Billing", "Create invoice and bill records.", "/dashboard/vendor/billing"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1395

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Dispatch", "Track delivery and dispatch work.", "/dashboard/vendor/dispatch"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1395

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Dispatch", "Track delivery and dispatch work.", "/dashboard/vendor/dispatch"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1396

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Notifications", "Review important vendor alerts.", "/dashboard/vendor/notifications"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1396

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Notifications", "Review important vendor alerts.", "/dashboard/vendor/notifications"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1437

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{leadStats.newLeadCount > 0 ? `${leadStats.newLeadCount} new buyer lead(s) need checking.` : "No urgent buyer follow-up pending."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1458

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Important Vendor Alerts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1461

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Simple reminders for replies, visibility and pending vendor work.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1492

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
["Builder Projects", "/services/turnkey"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1496

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
["Buyer Leads", "/dashboard/vendor/rfqs"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1496

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Buyer Leads", "/dashboard/vendor/rfqs"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1497

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Messages", "/dashboard/vendor/inbox"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1497

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Messages", "/dashboard/vendor/inbox"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1498

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Inventory", "/dashboard/vendor/inventory"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1498

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Inventory", "/dashboard/vendor/inventory"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1499

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Billing", "/dashboard/vendor/billing"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1499

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Billing", "/dashboard/vendor/billing"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1500

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Fleet", "/dashboard/vendor/fleet"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1500

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Fleet", "/dashboard/vendor/fleet"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1501

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Dispatch", "/dashboard/vendor/dispatch"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1501

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Dispatch", "/dashboard/vendor/dispatch"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1542

**Area:** Dashboard

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<div style={{ marginTop: 4, fontSize: 12, fontWeight: 700, color: "#475569" }}>Business profile</div>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/dashboard/vendor/page.tsx:1548

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div style={{ marginTop: 4, fontSize: 12, fontWeight: 700, color: "#475569" }}>New buyer leads</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1558

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/subscription"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/page.tsx:1558

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
href="/dashboard/subscription"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1567

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontSize: 12, fontWeight: 850, color: "#7e22ce" }}>Plan</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1569

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div style={{ marginTop: 4, fontSize: 12, fontWeight: 700, color: "#475569" }}>Manage vendor plan</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/page.tsx:1569

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ marginTop: 4, fontSize: 12, fontWeight: 700, color: "#475569" }}>Manage vendor plan</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/page.tsx:1600

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Daily vendor work simplified for easy business operations.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/dashboard/vendor/rfqs/[id]/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/vendor/rfqs/[id]/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:6

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:76

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const loginHref = `/login?next=${encodeURIComponent(`/dashboard/vendor/rfqs/${rfqId}`)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:76

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const loginHref = `/login?next=${encodeURIComponent(`/dashboard/vendor/rfqs/${rfqId}`)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:101

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setErr("Not logged in. Please login as vendor.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:106

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const resp = await fetch(`/api/vendor/rfqs/${encodeURIComponent(rfqId)}`, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:149

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: `Hi ${buyerName}, I am a vendor on 3Bigha. I can supply materials for your RFQ (${rfqId}). Please share item list/brand/quantity and delivery time so I can send best quotation.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:170

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`Hi ${buyerName}, I am a vendor on 3Bigha regarding your RFQ (${rfqId}).`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:175

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `mailto:${buyerEmail}?subject=${encodeMsg(`3Bigha RFQ (${rfqId}) - Vendor Contact`)}&body=${encodeMsg(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:176

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`Hi ${buyerName},\n\nI am a vendor on 3Bigha. I’m contacting you regarding your RFQ (${rfqId}).\n\nPlease share item list/brand/quantity and delivery time.\n\nThanks,\n`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:203

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const procurementHref = `/dashboard/vendor/rfqs/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:203

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const procurementHref = `/dashboard/vendor/rfqs/${encodeURIComponent(rfqId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:204

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const procurementTitle = `Vendor RFQ ${rfqId.slice(0, 8)}…`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:211

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
module: "vendor" as const,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:211

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "vendor" as const,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:239

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<SectionHeader title="RFQ Details" subtitle="Buyer requirement overview" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:242

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/dashboard/vendor/rfqs" className="topBtn topBtnGhost" style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:242

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/vendor/rfqs" className="topBtn topBtnGhost" style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:270

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<EmptyState message="Preparing buyer requirement…" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:286

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{/* Buyer Contact Details + Rapido-style toggles */}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:289

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div style={{ fontWeight: 800, marginBottom: 8 }}>Buyer Contact Details</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:361

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
(tap to quickly contact buyer)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:388

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
copyToClipboard(`Hi ${buyerName}, I am a vendor on 3Bigha regarding your RFQ (${rfqId}).`)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/[id]/page.tsx:403

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer contact details are not available yet.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:1

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/dashboard/vendor/rfqs/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:1

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
// app/dashboard/vendor/rfqs/page.tsx
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:6

**Area:** Dashboard

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:14

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:19

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:74

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setErr("Not logged in. Please login as vendor.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:84

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", "materials")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:87

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:107

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const loginHref = `/login?next=${encodeURIComponent("/dashboard/vendor/rfqs")}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:107

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const loginHref = `/login?next=${encodeURIComponent("/dashboard/vendor/rfqs")}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:111

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<SectionHeader title="Buyer Requirements" subtitle="Review buyer requirements, respond quickly and continue buyer conversation." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:116

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor/inbox-v2" className="topBtn topBtnGhost" style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:117

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
← Back to Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:125

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActionButton href="/vendor" variant="secondary">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:126

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:126

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:186

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
⚡ Active Buyer
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/rfqs/page.tsx:212

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/inbox-v2/${r.rfq_id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:55

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:55

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:64

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:64

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:91

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:91

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:99

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "RFQs, procurement continuity and vendor sourcing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:100

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:100

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:111

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:111

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:115

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:115

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:131

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:131

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:284

**Area:** Dashboard

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(8),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:824

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ExecutiveFlowMemory pageKey="vendor-workspace" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:824

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<ExecutiveFlowMemory pageKey="vendor-workspace" />
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:831

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor ERP Workspace
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:831

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Vendor ERP Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:835

**Area:** Dashboard

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Unified operational workspace connecting inventory,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:910

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/dashboard/vendor/inventory" className="rounded-full bg-slate-900 px-3 py-2 text-xs font-black text-white">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:910

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/vendor/inventory" className="rounded-full bg-slate-900 px-3 py-2 text-xs font-black text-white">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:913

**Area:** Dashboard

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/dashboard/vendor/billing" className="rounded-full bg-orange-600 px-3 py-2 text-xs font-black text-white">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:913

**Area:** Dashboard

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link href="/dashboard/vendor/billing" className="rounded-full bg-orange-600 px-3 py-2 text-xs font-black text-white">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1062

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{workspaceModules.map((module) => (
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1064

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={module.title}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1065

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={module.href}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1071

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{module.title}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1075

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{module.description}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1085

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{module.metrics.map(([label, value]) => (
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/dashboard/vendor/workspace/page.tsx:1222

**Area:** Dashboard

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{(event.type || event.module || "erp")}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/enquiries/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
redirect("/dashboard/buyer/enquiries");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/enquiries/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
redirect("/dashboard/buyer/enquiries");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/globals.css:697

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
/* FINAL SIDEBAR HEIGHT FIX - show all public menus, no role-based Vendor Hub */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/globals.css:697

**Area:** Application Route

**Category:** Workspace

**Matched:** `sidebar`

**Current system**

```text
/* FINAL SIDEBAR HEIGHT FIX - show all public menus, no role-based Vendor Hub */
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: "buyer" | "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:65

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
role: "buyer" | "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:69

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
const counterpartLabel = role === "buyer" ? "Vendor" : "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:330

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
row.role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:344

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
{pill(row.role === "buyer" ? "Buyer Side" : "Vendor Side", row.role === "buyer" ? "ok" : "neutral")}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/CombinedInboxClient.tsx:370

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
{row.role === "buyer" ? "Vendor" : "Buyer"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
type RoleFilter = "" | "buyer" | "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:34

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
searchParams?: { q?: string; role?: string };
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = normalizeRole(searchParams?.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:48

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
const buyerRows = rows.filter((r) => r.role === "buyer").length;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:49

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
const vendorRows = rows.filter((r) => r.role === "vendor").length;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:80

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<select name="role" defaultValue={role} style={{ padding: 8, minWidth: 180 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/inbox/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `roles`

**Current system**

```text
<option value="">All Roles</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/OpportunitySidebarClient.tsx:152

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builderRole || "builder"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:79

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:192

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
let role: string | null = null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:216

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role = row.role ? String(row.role) : role;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:240

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role = row.role ? String(row.role) : role;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:271

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/investment/opportunities/[slug]/page.tsx:513

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
builderRole={builderSummary?.role || "builder"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/land-area-calculator/page.tsx:527

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permissions`

**Current system**

```text
items.push("Large area detected. Verify land classification, conversion and agricultural permissions.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/layout.tsx:224

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div className="postMenuPanel" style={{ zIndex: 99999 }} role="dialog" aria-label="Post or List">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/layout.tsx:427

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div className="rfqTogglePanel" role="dialog" aria-label="My Dashboard menu">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/layout.tsx:427

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
<div className="rfqTogglePanel" role="dialog" aria-label="My Dashboard menu">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### MEDIUM — app/layout.tsx:465

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div className="rfqTogglePanel" role="dialog" aria-label="Investment menu">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/layout.tsx:519

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div className="rfqTogglePanel" role="dialog" aria-label="Submit Requirement menu">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:371

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const roleFromQuery = (sp.get("role") || "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:489

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:498

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:504

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: roleFromQuery === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:534

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:544

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
(roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:550

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: roleFromQuery === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:925

**Area:** Onboarding

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
setMsg("Please allow location permission in your browser popup...");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1048

**Area:** Onboarding

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
"Location permission is blocked. Tap the lock/site icon beside the browser address bar, open Site settings, allow Location, reload this page, then try again."
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1153

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1162

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1185

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role:
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1186

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1194

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1197

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
roleFromQuery === "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1222

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
roleFromQuery === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1241

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: finalRole === "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/onboarding/business/BusinessOnboardingPageClient.tsx:1782

**Area:** Onboarding

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
Location permission, reload this page, then try again.
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/page.tsx:361

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
router.push(`/search?module=${scope}&q=${encodeURIComponent(clean)}`);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### MEDIUM — app/page.tsx:801

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<a href="/login?next=/auth/register-role">Create Account</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/page.tsx:801

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
<a href="/login?next=/auth/register-role">Create Account</a>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/price-today/PriceTodayClient.tsx:1092

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,is_vendor,approval_status")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/price-today/PriceTodayClient.tsx:1111

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(profile?.role || "");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/price-today/PriceTodayClient.tsx:1117

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`, `vendor`, `builder`

**Current system**

```text
["vendor", "builder", "property_owner", "hub_vendor", "master_admin"].includes(role) ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/price-today/PriceTodayClient.tsx:1118

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
["vendor", "builder", "property_owner", "hub_vendor"].includes(requestedRole);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/price-today/PriceTodayClient.tsx:1122

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/[projectId]/units/add/page.tsx:754

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace(`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/property/builder/projects/[projectId]/units/add/page.tsx:1177

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
router.push(`/property/builder/projects/${encodeURIComponent(projectId)}/units`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/[projectId]/units/page.tsx:180

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace(`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/property/builder/projects/[projectId]/units/page.tsx:333

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
router.push(`/property/builder/projects/${encodeURIComponent(projectId)}/units/add`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/add/page.tsx:356

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace(`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/property/builder/projects/add/page.tsx:495

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
? "Location permission denied. Allow location OR paste a Google Maps link."
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/property/builder/projects/add/page.tsx:753

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
router.push(`/property/builder/projects?created=${encodeURIComponent(projectId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:137

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.replace(`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:141

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// 3) ensure builder profile exists (DB function handles required fields + permissions)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:141

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permissions`

**Current system**

```text
// 3) ensure builder profile exists (DB function handles required fields + permissions)
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:244

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
router.push(`/property/builder/projects/${encodeURIComponent(projectId)}/units`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:252

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
router.push(`/property/builder/projects/${encodeURIComponent(projectId)}/units/add`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/property/builder/projects/BuilderProjectsPageClient.tsx:259

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
router.push(`/admin/property/inventory?project=${encodeURIComponent(projectId)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/rfq/browse/[module]/page.tsx:48

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
"NOC / permission assistance",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/rfq/general/browse/[module]/page.tsx:85

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
"NOC / permission assistance",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/rfq/general/browse/[module]/page.tsx:551

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
If you see “permission denied”, your RLS on <code>material_taxons</code> is blocking public read.
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/search/page.tsx:978

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
setNote("Location permission denied. Please allow location to use Near Me.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — app/services/add/page.tsx:1115

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.push(`/onboarding/business?returnTo=${encodeURIComponent("/services/add")}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/services/turnkey/add/page.tsx:519

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
router.push(`/onboarding/business?returnTo=${encodeURIComponent("/services/turnkey/add")}`);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/signup/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
redirect("/login?next=/auth/register-role");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/signup/page.tsx:6

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
redirect("/login?next=/auth/register-role");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### MEDIUM — app/support/new/page.tsx:76

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,is_vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/support/new/page.tsx:80

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/support/new/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
profile?.role ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/support/new/page.tsx:86

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
setUserRole(role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/support/new/page.tsx:111

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: userRole,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/support/new/page.tsx:277

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
Your role: <b>{userRole}</b> | User ID: <b>{userRole.toUpperCase()}-
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor-inbox/page.tsx:13

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect("/vendor/inbox-v2");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:160

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
<div style={{ marginTop: 6 }}><strong>Role:</strong> Buyer</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:40

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function toDisplayRole(role?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const value = String(role ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:121

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:639

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:702

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:725

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:1223

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(message.sender_role ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:1224

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (!role) return "Other user";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:1226

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
return role.charAt(0).toUpperCase() + role.slice(1);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:1597

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sender_role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:1742

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
)?.sender_role || "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/delivery-update/route.ts:110

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/[rfqId]/delivery-update/route.ts:127

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sender_role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/VendorInboxV2Client.tsx:34

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox-v2/VendorInboxV2Client.tsx:249

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/inbox/page.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
redirect("/vendor/inbox-v2");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/price-updates/new/page.tsx:214

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role,requested_role,is_vendor,approval_status")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/price-updates/new/page.tsx:218

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(profile?.role || "");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/price-updates/new/page.tsx:231

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
].includes(role) ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — app/vendor/price-updates/new/page.tsx:242

**Area:** Application Route

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/ai/DealScoreClient.tsx:6

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/geography/GeoSelector.tsx:236

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="combobox"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/geography/GeoSelector.tsx:269

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="listbox"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/geography/GeoSelector.tsx:285

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="option"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:100

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`, `admin`, `buyer`

**Current system**

```text
// MASTER / ADMIN / BUYER ROLE CHECK
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:103

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:107

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(profile?.role || "").toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:111

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
role === "master_admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:112

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`, `admin`

**Current system**

```text
role === "admin" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:113

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "super_admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:121

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "banker" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:122

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "finance_banker"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:135

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (vendor || role.includes("vendor")) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/layout/TopHeaderClient.tsx:412

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
<div className="postMenuPanel" style={{ zIndex: 99999 }} role="dialog" aria-label="Post or list options">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/mobile/MobilePushRuntime.tsx:72

**Area:** Shared UI

**Category:** Capability and Permission

**Matched:** `permission`

**Current system**

```text
console.log("Push permission denied");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — components/ui/FilterBar.tsx:32

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="group"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — components/ui/Toast.tsx:60

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role="alert"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:27

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: PortalRole | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
let role: PortalRole | null = null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:119

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
"role,is_vendor,onboarding_completed,onboarding_version,portal_use_reason"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:143

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `profile.role`

**Current system**

```text
role = normalizeRole(profile.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:145

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (role === "vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:149

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
if (role === "builder") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:153

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role === "hub_vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:158

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role === "blogger") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:195

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role === "hub_vendor" || portalUseReason === "operate_multiple_businesses"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:205

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
: role === "builder" || portalUseReason === "manage_builder_projects"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:207

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
: role === "blogger" || portalUseReason === "publish_blog_or_news"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:240

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (isVendor || isBuilder || isHubVendor || role === "blogger") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:240

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (isVendor || isBuilder || isHubVendor || role === "blogger") {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — lib/access/resolveAccess.ts:262

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (role === "hub_vendor" && vendorCapabilities.length === 0) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:274

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `builder`

**Current system**

```text
if (role === "builder" && vendorCapabilities.length === 0) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:278

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (role === "vendor" && vendorCapabilities.length === 0) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:282

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
if (!role && isVendor) role = "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:282

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (!role && isVendor) role = "vendor";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — lib/access/resolveAccess.ts:285

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
const isAdmin = role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:285

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const isAdmin = role === "master_admin";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — lib/access/resolveAccess.ts:286

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
const isBlogAdmin = role === "blog_admin" || role === "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:287

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
const isBuyer = role === "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:287

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
const isBuyer = role === "buyer";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### MEDIUM — lib/access/resolveAccess.ts:301

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/access/resolveAccess.ts:338

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
if (access.role === "blogger") return "/blog/my";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/ai/openai-runtime.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "system" as const,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/ai/openai-runtime.ts:50

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: "user" as const,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/chat/chat-client-shared.ts:31

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
export function toDisplayRole(role?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/chat/chat-client-shared.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const value = String(role ?? "").trim();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/buyer-listing-inbox.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/buyer-listing-inbox.ts:53

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(senderRole ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/buyer-listing-inbox.ts:54

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
const who = role === "system" ? "System" : senderIsBuyer ? "You" : "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/buyer-listing-inbox.ts:142

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/chat-client-shared.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/chat-client-shared.ts:394

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/ensureConversation.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/ensureConversation.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
role: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/ensureConversation.ts:176

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
sender_role: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/vendor-listing-inbox.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/vendor-listing-inbox.ts:45

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role = String(senderRole ?? "").trim().toLowerCase();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/vendor-listing-inbox.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
const who = role === "system" ? "System" : senderIsVendor ? "You" : "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/conversations/vendor-listing-inbox.ts:123

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("conversation_id,user_id,role,last_read_at")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/finance/requireVerifiedBanker.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
.select("role")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/finance/requireVerifiedBanker.ts:47

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `master_admin`

**Current system**

```text
if (profile?.role === "master_admin") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/marketplace/rfq-intelligence-engine.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.filter((message) => String(message.sender_role || "").toLowerCase() === "vendor")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/mobile/savePushToken.ts:16

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: string | null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/mobile/savePushToken.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/mobile/savePushToken.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: role || null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
export type CombinedInboxRole = "buyer" | "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:12

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: CombinedInboxRole;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
function buildOpenHref(role: CombinedInboxRole, rfqId: string, conversationId?: string | null) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:106

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
return role === "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role?: "" | CombinedInboxRole;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:140

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
const role: CombinedInboxRole =
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:144

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `buyer`

**Current system**

```text
role === "buyer" ? c.vendor_user_id ?? null : c.buyer_user_id ?? null;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:148

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:153

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`, `vendor`, `buyer`

**Current system**

```text
if (params?.role === "buyer" || params?.role === "vendor") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:154

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
conversations = conversations.filter((c) => c.role === params.role);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:277

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
role: c.role,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:298

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
open_href: buildOpenHref(c.role, String(c.rfq_id ?? c.context_id ?? ""), c.id),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — lib/rfq/combined-inbox/server.ts:310

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
r.role ?? "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — scripts/3bos/test-identity-compatibility.mjs:30

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `role`, `vendor`

**Current system**

```text
source.includes('role === "vendor") {\n addSuggestion') ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — scripts/3bos/test-identity-compatibility.mjs:31

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `role`

**Current system**

```text
source.includes('role === "hub_vendor") {\n addSuggestion')
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### MEDIUM — scripts/3bos/test-identity-compatibility.mjs:34

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `roles`, `vendor`

**Current system**

```text
"Legacy vendor roles must not directly assign a human identity."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/ActiveLink.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:36

**Area:** Application Route

**Category:** Human Identity

**Matched:** `master_admin`, `admin`

**Current system**

```text
if (r === "master_admin") return "Master Admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (r === "blog_admin") return "Blog Admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:40

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
r === "vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
r === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:44

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:51

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:58

**Area:** Application Route

**Category:** Human Identity

**Matched:** `master_admin`, `admin`

**Current system**

```text
if (r === "master_admin") return "/admin/dashboard";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:58

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (r === "master_admin") return "/admin/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:59

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (r === "blog_admin") return "/admin/blog";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
r === "vendor" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:63

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
r === "builder" ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:67

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (reason === "invest_in_opportunities") return "/dashboard/investor";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:68

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "/dashboard/vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:68

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard/vendor";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:72

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (r === "buyer") return "/dashboard/buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:72

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (r === "buyer") return "/dashboard/buyer";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:74

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return "/dashboard/buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:74

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard/buyer";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:79

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
return r === "vendor" || r === "builder" || r === "hub_vendor" || r === "blogger";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:89

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const [dashboardHref, setDashboardHref] = useState<string>("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:95

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:125

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
currentPath.startsWith("/onboarding/business") ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/AuthButtons.tsx:139

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Header must not force business users back to onboarding.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/AuthButtons.tsx:140

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Vendor onboarding is progressive; dashboard pages handle their own gates.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:140

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
// Vendor onboarding is progressive; dashboard pages handle their own gates.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:140

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Vendor onboarding is progressive; dashboard pages handle their own gates.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/AuthButtons.tsx:145

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
qs.set("next", currentPath || "/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:151

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:201

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:206

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:236

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:258

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/_components/AuthButtons.tsx:268

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:295

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const displayDashboardHref = isMasterAdminEmail(email) ? "/admin/dashboard" : dashboardHref;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/AuthButtons.tsx:295

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const displayDashboardHref = isMasterAdminEmail(email) ? "/admin/dashboard" : dashboardHref;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:311

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
<span>My Dashboard</span>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/AuthButtons.tsx:332

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
My Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/BuildConOnboardingNotice.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/BuildConOnboardingNotice.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
program !== "district-founding-vendor" &&
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/BuildConOnboardingNotice.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
program !== "founding-vendor" &&
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/BuildConOnboardingNotice.tsx:24

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
Complete your business onboarding now. One selected business from each
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/BuildConVendorPopup.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/BuildConVendorPopup.tsx:13

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
pathname?.startsWith("/admin") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/BuildConVendorPopup.tsx:14

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
pathname?.startsWith("/dashboard") ||
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/BuildConVendorPopup.tsx:15

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
pathname?.startsWith("/onboarding") ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/BuildConVendorPopup.tsx:37

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Free first year • Founder badge • AI priority • Lifetime preferential pricing.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/_components/BuildConVendorPopup.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="foundingPartnerPopupPrimary" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/BuildConVendorPopup.tsx:41

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link className="foundingPartnerPopupPrimary" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/_components/GlobalAiCopilot.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/GlobalAiCopilot.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/vendor-opportunities",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:68

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail: "Create a requirement and collect vendor quotes.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:117

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
detail: "Open buyer-vendor messages and AI assistance.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:118

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/GlobalAiCopilot.tsx:147

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
aria-label={open ? "Close 3Bigha AI Tools" : "Open 3Bigha AI Tools"}
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:152

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
<small>{open ? "Close Tools" : "AI Tools"}</small>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:162

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
aria-label="Close AI Tools"
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:168

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
<strong>🤖 3Bigha AI Tools</strong>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:169

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
<span>AI-powered tools for construction, property and procurement.</span>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalAiCopilot.tsx:176

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
aria-label="Close AI Tools"
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/_components/GlobalNotificationBell.tsx:53

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/notifications"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/_components/GlobalNotificationBell.tsx:53

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/notifications"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/GlobalUnreadBadge.tsx:99

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/_components/MobileMenuAutoClose.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/_components/PresenceHeartbeat.tsx:207

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
authSub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/about/page.tsx:45

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<li>Rental equipment and local vendor access</li>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/about/page.tsx:48

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<li>Price Today market signals and vendor price updates</li>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/ai-search-guide/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Search Guide for Property, Construction, RFQ and Vendor Marketplace | 3bigha",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/ai-search-guide/page.tsx:8

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"Learn how 3bigha.com helps users search property, construction materials, services, rentals, RFQs, vendors and price intelligence through AI-powered marketplace workflows.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/ai-search-guide/page.tsx:49

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
AI-powered search for property, construction, RFQ, materials, rentals
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/ai-search-guide/page.tsx:54

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
3bigha.com is built as an AI-powered marketplace operating system where
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/ai-search-guide/page.tsx:70

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
3bigha.com is an AI-powered marketplace operating system for property,
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/ai-search-guide/page.tsx:107

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Find Vendors", "/vendor/discovery"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/ai-search-guide/page.tsx:136

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
near me, construction material supplier, JCB rental and RFQ-based
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/ai-search-guide/page.tsx:138

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
search and AI-assisted buyer-vendor workflows.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/awaiting-approval/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/awaiting-approval/page.tsx:47

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/awaiting-approval/page.tsx:49

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
"nature_of_business,is_complete,registration_complete,business_profile_complete"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/awaiting-approval/page.tsx:62

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
businessProfile?.is_complete === true ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/callback/AuthCallbackPageClient.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:34

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
return ["vendor", "builder", "hub_vendor", "blogger"].includes(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:159

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
setMsg("Could not prepare your registration. Redirecting…");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:181

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// 🚀 MASTER ADMIN FULL BYPASS (FINAL FIX)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:248

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
qs.set("returnTo", next || "/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:248

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
qs.set("returnTo", next || "/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:252

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:253

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("nature_of_business,is_complete,registration_complete,business_profile_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:265

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
businessProfile?.is_complete === true ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:290

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:303

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Progressive onboarding: allow vendor dashboard entry after business capability selection.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:303

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
// Progressive onboarding: allow vendor dashboard entry after business capability selection.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:303

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
// Progressive onboarding: allow vendor dashboard entry after business capability selection.
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:303

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Progressive onboarding: allow vendor dashboard entry after business capability selection.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:310

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/subscription?reason=district_free_not_eligible"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:310

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
"/dashboard/subscription?reason=district_free_not_eligible"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:316

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
setMsg("Preparing your dashboard…");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/post-login/PostLoginPageClient.tsx:318

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
let redirectTo = next || "/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/page.tsx:23

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Complete Your Registration
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/page.tsx:25

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<div style={{ opacity: 0.8 }}>Loading registration form…</div>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "@/components/marketplace/vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:9

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:10

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
| "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:67

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (c === "materials") return "Materials Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:68

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (c === "services") return "Service Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:69

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (c === "rentals") return "Rental Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:70

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `seller`

**Current system**

```text
if (c === "property_owner") return "Property Vendor / Seller";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:71

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (c === "property_builder") return "Builder / Developer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:72

**Area:** Application Route

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (c === "blog_author") return "Blogger / Author";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:76

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return caps.includes("investor") ? "Multi-Service Vendor / Investor" : "Multi-Service Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:93

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
// Post-login and dashboard routing will handle the correct destination.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:97

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`, `builder`

**Current system**

```text
["buyer", "vendor", "builder", "hub_vendor", "blogger"].includes(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:116

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Registration Started",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:116

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
label: "Vendor Registration Started",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:128

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Master Admin access is already configured.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:131

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Open the admin dashboard from the button below.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:131

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
Open the admin dashboard from the button below.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:134

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:134

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/admin/dashboard"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Open Master Admin Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:146

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `admin dashboard`

**Current system**

```text
Open Master Admin Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:172

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Please choose at least one vendor capability.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:172

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
return "Please choose at least one vendor capability.";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:271

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
const isVendor =
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:289

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
is_vendor: isVendor,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:301

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
setMsg(profileError.message || "Could not save registration.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:312

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
setMsg(grantsError.message || "Could not save module access.");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:323

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:328

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:348

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:355

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Registration Completed",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:355

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
label: "Vendor Registration Completed",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:358

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `capabilities`

**Current system**

```text
capabilities: caps,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:364

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
setMsg(businessError.message || "Could not save business profile.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:370

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
qs.set("returnTo", next || "/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:396

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Complete Your Registration
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:481

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
value: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:488

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
value: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:490

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
desc: "List materials, receive buyer requirements and send quotations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:495

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
value: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:497

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
desc: "Offer construction, repair, labour, technical or professional services.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:502

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
value: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:509

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
value: "builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:510

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
label: "🏗 Builder / Developer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:511

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `builder`

**Current system**

```text
desc: "List builder projects, manage inventory and receive buyer interest.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:524

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "🏢 Multi-Business Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:620

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
To manage builder projects
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:637

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Choose Your Vendor Type *
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:642

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["materials", "Materials Vendor"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:643

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["services", "Service Vendor"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:644

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["rentals", "Rental Vendor"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:645

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `seller`

**Current system**

```text
["property_owner", "Property Vendor / Seller"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:646

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
["property_builder", "Builder / Developer"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/auth/register-role/RegisterRolePageClient.tsx:647

**Area:** Application Route

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
["blog_author", "Blogger / Author"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/banker/apply/opengraph-image.tsx:15

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontSize: 32, color: "#fef3c7", fontWeight: 700 }}>Check loan eligibility, connect with bankers and plan property finance.</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/banker/apply/page.tsx:134

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
verification, access to banker dashboard may be approved.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/banker/apply/page.tsx:270

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
privately and viewed only through secure signed admin links. AI OCR checking
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/banking-finance-assistance/opengraph-image.tsx:15

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontSize: 32, color: "#fef3c7", fontWeight: 700 }}>Check loan eligibility, connect with bankers and plan property finance.</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/banking-finance-assistance/page.tsx:49

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/banking-finance-assistance/page.tsx:84

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
"3Bigha supports banker onboarding and manual verification so finance leads can be handled through verified banker workflows.",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/banking-finance-assistance/page.tsx:107

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
3Bigha helps property buyers, builders and families plan loans with
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/banking-finance-assistance/page.tsx:141

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Plan home loan EMI, eligibility, CIBIL requirement and repayment capacity before buying property.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/banking-finance-assistance/page.tsx:151

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"Connect finance leads with verified banker workflows, lender offers and admin-approved finance operations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/[slug]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/blog/[slug]/page.tsx:30

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "blog";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/blog/[slug]/page.tsx:148

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "blog",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/blog/[slug]/page.tsx:231

**Area:** Application Route

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
author: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/edit/[id]/page.tsx:7

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/blog/edit/[id]/page.tsx:76

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
const [isAdmin, setIsAdmin] = useState(false);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/edit/[id]/page.tsx:130

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// Load post + admin flag
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/edit/[id]/page.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// Admin check via RPC (safe with RLS)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/edit/[id]/page.tsx:212

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) return;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/edit/[id]/page.tsx:224

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (!isAdmin) return;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/edit/[id]/page.tsx:259

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
<MessageBox title="Unable to open post" description={error ?? "Not found or access denied."} />
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/edit/[id]/page.tsx:276

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<div style={{ fontWeight: 800, fontSize: 16 }}>Your Blog Workspace</div>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/blog/edit/[id]/page.tsx:351

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
{isAdmin ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/edit/[id]/page.tsx:364

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{isAdmin ? "Admin can publish/unpublish." : "Only admins can publish."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/edit/[id]/page.tsx:364

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
{isAdmin ? "Admin can publish/unpublish." : "Only admins can publish."}
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/blog/layout.tsx:7

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"Read 3bigha.com blog articles on real estate, land, construction materials, building services, rentals, investment, market prices and AI-powered marketplace insights.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/blog/my/page.tsx:7

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/blog/my/page.tsx:82

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(80);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/blog/new/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/blog/new/page.tsx:67

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
group: "Land, Registration & Documentation",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/new/page.tsx:69

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
{ name: "Land & Property Registration (State-wise)", slug: "registration-state-wise" },
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/new/page.tsx:70

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
{ name: "Stamp Duty, Registration Fees & Charges", slug: "stamp-duty-fees" },
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/new/page.tsx:267

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
subtitle="Select a category first, then write your post. Draft only (publish by admin)."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/page.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
{ name: "Construction Guides", slug: "construction-guides", keywords: ["construction", "contractor", "site", "supervision", "plan", "estimate"] },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/page.tsx:62

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ name: "Construction Guides", slug: "construction-guides", keywords: ["construction", "contractor", "site", "supervision", "plan", "estimate"] },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/blog/page.tsx:65

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
{ name: "Services & Hiring Tips", slug: "services-hiring", keywords: ["engineer", "architect", "contractor", "hire", "quotation", "scope", "warranty"] },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/blog/page.tsx:70

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
group: "Land, Registration & Documentation",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/page.tsx:73

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
name: "Land & Property Registration (State-wise)",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/page.tsx:74

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
slug: "registration-state-wise",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/page.tsx:75

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`, `register`

**Current system**

```text
keywords: ["registration", "register", "sale deed", "sub registrar", "sro", "state wise", "online registration", "flat registration", "land registration"],
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/page.tsx:77

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
{ name: "Stamp Duty, Registration Fees & Charges", slug: "stamp-duty-fees", keywords: ["stamp duty", "fees", "charges", "registration fee", "circle rate", "guideline value"] },
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/blog/page.tsx:95

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
keywords: ["municipality", "corporation", "panchayat", "building plan", "sanction", "setback", "approval", "noc", "permit"],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/blog/page.tsx:174

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(60);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/blog/page.tsx:186

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(60);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/buyer/rfq/[rfq_id]/compare/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// app/buyer/rfq/[rfq_id]/compare/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/buyer/rfq/[rfq_id]/compare/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/compare-rates/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/ai/MemoryEventTracker.tsx:18

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/MemoryEventTracker.tsx:67

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: props.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/MemoryEventTracker.tsx:94

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
props.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/MemoryLink.tsx:12

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/MemoryLink.tsx:49

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/MemoryLink.tsx:70

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementAssistantLink.tsx:8

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementAssistantLink.tsx:15

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementAssistantLink.tsx:24

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module) params.set("module", module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementAssistantLink.tsx:29

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?${params.toString()}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/ai/ProcurementCopilotBox.tsx:12

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementCopilotBox.tsx:18

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementCopilotBox.tsx:51

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/ai/ProcurementCopilotBox.tsx:105

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Chat with AI to understand your requirement, draft RFQ details, and plan procurement.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationComposer.tsx:159

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
setIsPremium(true); // TEMP: replace with real subscription later
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationComposer.tsx:228

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai assistant`

**Current system**

```text
setAiAssistError(error?.message || "AI assistant is not available right now.");
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/components/chat/ConversationComposer.tsx:249

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🤖 AI Suggestion: Buyer is ready. Push for final confirmation & payment.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationComposer.tsx:323

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Verify vendor trust
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationComposer.tsx:574

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
window.location.href = "/dashboard/subscription/boost";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/chat/ConversationComposer.tsx:574

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
window.location.href = "/dashboard/subscription/boost";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationComposer.tsx:953

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
onClick={() => alert("Upgrade to Gold to unlock Quick Reply Help")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:70

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
audience: "buyer" | "vendor" | "both";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:71

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
priority: "free" | "premium";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:204

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor should reply quickly with price, availability and delivery timeline.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:206

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium vendors can receive stronger priority alerts when buyers show closing intent.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:329

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/ai/vendor-alert", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:334

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
side: "buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:798

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
⏳ Buyer waiting for your reply
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:802

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
AI detected a high-intent buyer message. Fast replies can improve your closing chance.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:1509

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
aiVendorAlert.priority === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:1513

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
aiVendorAlert.priority === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:1524

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
color: aiVendorAlert.priority === "premium" ? "#c2410c" : "#92400e",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:1543

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
{aiVendorAlert.priority === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:1544

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "🔥 Premium Priority Signal"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/chat/ConversationMessageList.tsx:1581

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🧠 Buyer AI: {aiVendorAlert.buyerHint}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/chat/ConversationMessageList.tsx:1590

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
background: aiVendorAlert.priority === "premium" ? "#f97316" : "#f59e0b",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:8

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "material" | "service" | "rental" | "other" | string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:55

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function modulePublicDetailPath(module: string, refId: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:56

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const m = String(module ?? "").trim().toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:66

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function draftKey(module: string, refId: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:67

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `enquiryDraft::${String(module)}::${String(refId)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:71

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:95

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `${String(module)} enquiry`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:96

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [title, module]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:105

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (t && p) return `Hi, I am interested in this ${module} (${t}) at ${p}. Please share details.`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:106

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (t) return `Hi, I am interested in this ${module} (${t}). Please share details.`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:107

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `Hi, I am interested in this ${module}. Please share details.`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:108

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [defaultMessage, module, title, priceText]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:121

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const k = draftKey(String(module), String(refId));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:204

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setErr("Vendor information is missing for this listing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:209

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setErr("This listing is not yet linked to a valid vendor account.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:223

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
sessionStorage.setItem(draftKey(String(module), String(refId)), body);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:226

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const fallbackBack = modulePublicDetailPath(String(module), String(refId));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:253

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/enquiry/SendEnquiryButton.tsx:322

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{String(module).toUpperCase()} • {enquiryName}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiAction.tsx:12

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiAction.tsx:79

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiAction.tsx:136

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiAction.tsx:152

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiAction.tsx:213

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiSummary.tsx:12

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiSummary.tsx:48

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiSummary.tsx:105

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiSummary.tsx:121

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAiSummary.tsx:175

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAutoReminderSuggestion.tsx:7

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment" | "rfq" | "direct";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAutoReminderSuggestion.tsx:59

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAutoReminderSuggestion.tsx:83

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "investment" && hrs >= 24) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxAutoReminderSuggestion.tsx:108

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [mounted, lastActivityAt, module, unreadCount]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxPrioritySummaryStrip.tsx:56

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") score += 20;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxPrioritySummaryStrip.tsx:57

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
else if (item.module === "rfq") score += 12;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxRealtimeWrapper.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/inbox/InboxThreadCard.tsx:15

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment" | "rfq" | "direct";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxThreadCard.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`, `builder`

**Current system**

```text
side: "investor" | "builder" | "vendor" | "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/inbox/InboxThreadCard.tsx:140

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxThreadCard.tsx:194

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{titleCase(item.module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxThreadCard.tsx:250

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxThreadCard.tsx:265

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/InboxThreadCard.tsx:275

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/SectionSummaryChips.tsx:56

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") score += 20;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/SectionSummaryChips.tsx:57

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
else if (item.module === "rfq") score += 12;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/TestGptButton.tsx:15

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtitle: "Builder discussion",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/inbox/TestGptButton.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
counterpart: "Test Builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/inbox/TestGptButton.tsx:19

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "investment",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/ThreadDueReminderState.tsx:216

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/ThreadDueReminderState.tsx:225

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/ThreadDueReminderState.tsx:235

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
data-thread-module={module ?? ""}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/ThreadSectionLiveList.tsx:58

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === "investment") score += 20;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/inbox/ThreadSectionLiveList.tsx:59

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
else if (item.module === "rfq") score += 12;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/investment/ExpressInterestButton.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:83

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!room) return "Builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:93

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"Builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:357

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder: [
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:358

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Response & onboarding
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:392

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder: [
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:640

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"I can share the main project risks and mitigation plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:764

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "/dashboard/builder/deal-rooms"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:764

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? "/dashboard/builder/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:765

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: "/dashboard/investor/deal-rooms";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:768

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/inbox-v2?conversationId=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:771

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
: "/dashboard/inbox-v2";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:2752

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
: "Builder Smart Replies"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/investment/InvestmentDealRoomClient.tsx:2866

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Send to Builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/media/UniversalMediaUploader.tsx:22

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UniversalMediaModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/media/UniversalMediaUploader.tsx:35

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/media/UniversalMediaUploader.tsx:81

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const bucket = MEDIA_BUCKET_BY_MODULE[module];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/media/UniversalMediaUploader.tsx:85

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
folder?.trim() || `${module}/${new Date().getFullYear()}/${Date.now()}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/media/UniversalMediaUploader.tsx:352

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
Short videos should be steady and under the size limit.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:11

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-os",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:14

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
description: "Main procurement workflow workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:17

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-briefing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:23

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-health",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:35

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:41

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-situation-room",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:47

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-timeline",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:59

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
href: "/dashboard/procurement-supplier-reliability",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:59

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-supplier-reliability",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:60

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
label: "Supplier Reliability",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
description: "Supplier trust and response overview",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:65

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-negotiation-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:77

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-actions",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:83

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-inbox-actions",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:89

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-followup-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:101

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-crisis-center",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:104

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
description: "Escalation and recovery workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:107

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-anomaly",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:113

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-autonomous-tasks",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:125

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-analytics",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:131

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-heatmap",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementCommandCenterNav.tsx:137

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-copilot",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/components/procurement/ProcurementHeatmapIntelligence.tsx:153

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Live AI heat detection across RFQ demand, supplier activity,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementHeatmapIntelligence.tsx:181

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title="🏭 Supplier Density"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementLiveTicker.tsx:9

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/components/procurement/ProcurementLiveTicker.tsx:46

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
return "Procurement Workspace is monitoring live RFQs, chats, recommendations, memory events and supplier signals.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/components/procurement/ProcurementLiveTicker.tsx:46

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
return "Procurement Workspace is monitoring live RFQs, chats, recommendations, memory events and supplier signals.";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/construction-cost/[state]/[city]/page.tsx:182

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Post your construction requirement and get contractor responses
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/construction-cost/page.tsx:30

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/construction-projects"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/contact/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Contact 3Bigha for support, business onboarding, vendor registration, RFQ, property, materials, services, rentals and investment-related queries.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/contact/page.tsx:6

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`, `registration`

**Current system**

```text
"Contact 3Bigha for support, business onboarding, vendor registration, RFQ, property, materials, services, rentals and investment-related queries.",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/contact/page.tsx:32

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
For support, business onboarding, vendor registration, property,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/contact/page.tsx:32

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`, `registration`

**Current system**

```text
For support, business onboarding, vendor registration, property,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/cost-calculator/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/delivery-track/[dispatchId]/page.tsx:142

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Please check the tracking link or contact the vendor.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/delivery-track/[dispatchId]/page.tsx:234

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<InfoCard title="Buyer Name" value={dispatch.buyer_name || "—"} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/emi-calculator/page.tsx:764

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Your current EMI plan looks comfortable. Keep emergency savings and avoid unnecessary extra debt.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/emi-calculator/page.tsx:770

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"This plan may create pressure on monthly income. Reduce loan amount, increase down payment, or choose a lower budget.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/emi-calculator/page.tsx:775

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"This plan is possible, but maintain reserve funds and avoid stretching your EMI too high.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/emi-calculator/page.tsx:813

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Buying may be a better long-term decision if your EMI is comfortable and you plan to stay for several years.";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/emi-calculator/page.tsx:1433

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
<option value="doctor_professor">Doctor / professor / professional service up to 65</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/emi-calculator/page.tsx:1688

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
registration and reserve fund may need around{" "}
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/emi-calculator/page.tsx:1825

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<summary>View registration and legal cost</summary>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/emi-calculator/page.tsx:1828

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Estimated registration/stamp duty related cost:
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/emi-calculator/page.tsx:2222

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
processing fees, offers and loan terms directly from their dashboard.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/emi-calculator/page.tsx:2276

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<span>Plan house construction finance and cash flow.</span>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/enquiries/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/founding-vendors/page.tsx:21

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
"Free registration for the first year",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:26

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Lifetime preferential pricing",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/founding-vendors/page.tsx:27

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
"Early access to future AI tools",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/founding-vendors/page.tsx:48

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
"Complete Business Profile",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:74

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="foundingPrimaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/founding-vendors/page.tsx:74

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link className="foundingPrimaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:77

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="foundingSecondaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor&intent=start-listing">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/founding-vendors/page.tsx:77

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link className="foundingSecondaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor&intent=start-listing">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:78

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
🧾 Create Business Profile
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:96

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
3Bigha is building India’s AI-powered marketplace for construction,
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/founding-vendors/page.tsx:103

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`, `pricing`

**Current system**

```text
lifetime preferential pricing compared to standard plans.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/founding-vendors/page.tsx:121

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div className="foundingMiniCard">💰 Lifetime Preferential Pricing</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/founding-vendors/page.tsx:124

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai tool`, `ai tools`

**Current system**

```text
<div className="foundingMiniCard">⚡ Early Access to Future AI Tools</div>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/founding-vendors/page.tsx:164

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<summary>Is registration free?</summary>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:165

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<p>Yes. Applying and completing your business profile is free.</p>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/founding-vendors/page.tsx:192

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="foundingPrimaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/founding-vendors/page.tsx:192

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link className="foundingPrimaryBtn" href="/onboarding/business?source=district-founder&program=district-founding-vendor">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/global.d.ts:1

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
declare module "*.css" {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/globals.css:251

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
/* 3bigha cleaner navigation upgrade */
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:251

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
/* 3bigha cleaner navigation upgrade */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/globals.css:293

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
content: "3Bigha Navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:443

**Area:** Application Route

**Category:** Workspace

**Matched:** `sidebar`

**Current system**

```text
/* FINAL DESKTOP SIDEBAR FIX - desktop rail, no overlap, working hover preview */
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:483

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
content: "3Bigha Navigation" !important;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:574

**Area:** Application Route

**Category:** Workspace

**Matched:** `sidebar`

**Current system**

```text
/* FINAL CORRECTED SIDEBAR — clean desktop rail, no top overlap, all items visible */
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:613

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
content: "3Bigha Navigation" !important;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:990

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
PREMIUM SAAS UX COMPRESSION — 3BIGHA
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/globals.css:1197

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
/* Final readable Vendor Discovery dark intelligence strip */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:1719

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
body:has(a[href="/admin/dashboard/finance-leads"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:1719

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/admin/dashboard/finance-leads"]),
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:1720

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
body:has(a[href="/admin/dashboard/banker-verification"]) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:1720

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/admin/dashboard/banker-verification"]) {
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2373

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Mobile order: Hero → Categories → Featured → Stats → AI/Vendor/Tools.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2832

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
/* Vendor enquiries mobile compression */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2834

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) main,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2834

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) main,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2835

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) .container {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2835

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) .container {
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2840

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) .card,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2840

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) .card,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2841

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) [class*="Card"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2841

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) [class*="Card"] {
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2845

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) input,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2845

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) input,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2846

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) select,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2846

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) select,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2847

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) button {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2847

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
body:has(a[href="/dashboard/vendor/enquiries"]) button {
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/globals.css:2852

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
/* Vendor chat mobile compression - safe CSS only */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2854

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="Agent Score"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2855

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="heat score"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2856

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="AI Reply Suggestions"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2860

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="box-shadow"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2864

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="grid-template-columns: repeat(auto-fit, minmax(145px, 1fr))"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2869

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
/* Vendor chat mobile compression - safe CSS only */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2871

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="Agent Score"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2872

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="heat score"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2873

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="AI Reply Suggestions"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2877

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="box-shadow"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:2881

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
body:has(a[href="/vendor/inbox-v2"]) [style*="grid-template-columns: repeat(auto-fit, minmax(145px, 1fr))"] {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:3011

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
/* Bharat BuildCon 2026 founding vendor popup */
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/globals.css:3121

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
/* BuildCon onboarding notice */
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/globals.css:3675

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
/* Project SAHAJ shared workspace */
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/house-construction-cost/[state]/[city]/page.tsx:101

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Get Contractor Quote
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/house-construction-cost/[state]/[city]/page.tsx:175

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Plan your house with 3bigha
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/house-construction-cost/[state]/[city]/page.tsx:180

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
packages and vendor quotations from one marketplace.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:10

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
if (v === "buyer" || v === "vendor") return v;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:57

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Buyer + Vendor RFQ chat conversations in one place, with unread counts and latest message preview.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:66

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<StatCard label="Buyer Side" value={buyerRows} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:67

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<StatCard label="Vendor Side" value={vendorRows} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:82

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<option value="buyer">Buyer Side</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/inbox/page.tsx:83

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="vendor">Vendor Side</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/opportunities/[slug]/OpportunitySidebarClient.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Credibility
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/opportunities/[slug]/OpportunitySidebarClient.tsx:149

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builderName || "Builder"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/opportunities/[slug]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/investment/opportunities/[slug]/page.tsx:191

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
let displayName = "Builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/opportunities/[slug]/page.tsx:512

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builderName={builderSummary?.displayName || "Builder"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/opportunities/page.tsx:31

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/investment/opportunities/page.tsx:207

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
params.set("limit", "12");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/investment/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
import BuyerWorkMenu from "@/components/buyer/BuyerWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/page.tsx:66

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!t) return "Builder project open for investment.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/page.tsx:133

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Explore only those builder projects and properties that are open for investment.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/investment/page.tsx:164

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Please check again later for new builder projects open for investment.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/land-area-calculator/page.tsx:1074

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
property registration, boundary disputes and legal agreements should be verified
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/land-area-calculator/page.tsx:1348

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Use this calculated area for construction estimate, material planning, vendor RFQ,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
import MasterAdminDebugTools from "@/components/admin/MasterAdminDebugTools";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha.com | AI-Powered Property, Construction, RFQ, Materials & Vendor Marketplace India",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:37

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"3bigha.com | AI-Powered Property, Construction, RFQ, Materials & Vendor Marketplace India",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/layout.tsx:40

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha.com is India's AI-powered real estate, construction, RFQ, materials, rentals and vendor marketplace platform. Search property, compare vendors, submit procurement RFQs, discover local services and track AI-powered price intelligenc…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:40

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"3bigha.com is India's AI-powered real estate, construction, RFQ, materials, rentals and vendor marketplace platform. Search property, compare vendors, submit procurement RFQs, discover local services and track AI-powered price intelligenc…
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/layout.tsx:56

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor marketplace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:148

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alt="3bigha.com AI-powered property construction materials and vendor marketplace logo"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:148

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
alt="3bigha.com AI-powered property construction materials and vendor marketplace logo"
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/layout.tsx:188

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alt="3bigha.com AI-powered property construction materials and vendor marketplace logo"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:188

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
alt="3bigha.com AI-powered property construction materials and vendor marketplace logo"
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/layout.tsx:245

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link className="postCardBtn" href="/property/builder/projects">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:246

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Projects →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:275

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Legal, professional &amp; technical services listing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:327

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
New vendor? Start with{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:371

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/buyer">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:371

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/buyer">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:372

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
My Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:375

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:375

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:388

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:388

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActiveLink className="topMobileLink" href="/dashboard/vendor">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:389

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Hub
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:394

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:396

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Investment Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:401

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-os"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:403

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Procurement Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:424

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
🧑‍💼 My Dashboard <span className="rfqToggleCaret">▾</span>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:428

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
<div className="rfqToggleTitle">My Dashboard</div>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:432

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
track vendor responses, compare quotes, and continue
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:441

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:441

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:442

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
📄 My RFQs (View vendor responses + Compare quotes) →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:445

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/inbox-v2">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:449

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/buyer">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:449

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/buyer">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:450

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🧾 Buyer Dashboard →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:450

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `buyer dashboard`

**Current system**

```text
🧾 Buyer Dashboard →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:453

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:453

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="rfqToggleSecondary" href="/dashboard/vendor">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:454

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🏪 Vendor Hub Dashboard →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:454

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
🏪 Vendor Hub Dashboard →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:462

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
🧰 Workspace <span className="rfqToggleCaret">▾</span>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:466

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<div className="rfqToggleTitle">Workspace</div>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:470

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
place. Open investor or builder deal rooms and continue
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:478

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/investor/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:485

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
href="/dashboard/builder/deal-rooms"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:485

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/builder/deal-rooms"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:487

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
🏗️ Builder Deal Rooms →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:492

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-health"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:494

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
⚙️ Procurement Workspace →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/layout.tsx:554

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
3bigha.com is an AI-powered property marketplace, construction marketplace,
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/layout.tsx:556

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor discovery platform serving India with regional language support,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:557

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI procurement workflows, vendor comparison and local marketplace search.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/layout.tsx:592

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor-opportunities">🚀 Vendor Opportunities</Link>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/location/[slug]/page.tsx:61

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`Explore local demand and vendor opportunity for ${readableTitle(row)} on 3Bigha.`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:92

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/location/[slug]/page.tsx:99

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const title = `Construction, Property & Vendor Marketplace in ${location} | 3Bigha`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:100

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const description = `Explore property, building materials, services, rentals and local vendor opportunities in ${location} on 3Bigha.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:119

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/location/[slug]/page.tsx:134

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
getOpportunityGroup("property-seller", params.slug),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:135

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
getOpportunityGroup("building-material-supplier", params.slug),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:136

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
getOpportunityGroup("service-provider", params.slug),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/location/[slug]/page.tsx:137

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
getOpportunityGroup("rental-provider", params.slug),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/login/LoginClient.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/login/LoginClient.tsx:334

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: "google",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/login/LoginClient.tsx:356

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// ✅ Key fix stays: use SAME client as admin (cookie/session compatible)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/logout/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/manifest.ts:8

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"AI-powered real estate, construction, materials, RFQ and procurement platform for India.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/market-rfq/[slug]/page.tsx:69

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<p className="mt-2 text-lg font-black text-slate-950">{vm.module}</p>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/market-rfq/[slug]/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
service providers and property businesses understand where buyer-side demand
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:87

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
improve marketplace visibility and discover similar buyer requirements through
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:94

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Are you a vendor for this requirement?
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:98

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Join 3Bigha to discover buyer demand for materials, services, rentals,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:104

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:107

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/[slug]/page.tsx:125

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
This page shows only marketplace demand information. Buyer identity,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market-rfq/page.tsx:36

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/market/[category]/[location]/page.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/market/[category]/[location]/page.tsx:51

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
if (/building material supplier/i.test(label)) return "Building Material Suppliers";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:52

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
if (/property seller/i.test(label)) return "Property Sellers";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:53

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (/service provider/i.test(label)) return "Service Providers";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:54

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (/rental provider/i.test(label)) return "Rental Providers";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const description = `Find marketplace demand, vendor opportunities and local business signals for ${category} in ${location} on 3Bigha.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:118

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/market/[category]/[location]/page.tsx:126

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/market/[category]/[location]/page.tsx:140

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: `Marketplace demand and vendor opportunity page for ${category} in ${location}.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:170

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Explore public marketplace demand, vendor opportunities and local
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:226

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
It connects local marketplace demand with vendor onboarding,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:226

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
It connects local marketplace demand with vendor onboarding,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/market/[category]/[location]/page.tsx:227

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
procurement discovery and buyer requirement signals.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:232

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
registering on 3Bigha and keeping their business profile updated.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/market/[category]/[location]/page.tsx:238

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Related vendor demand opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:269

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Join 3Bigha to discover buyer demand, vendor opportunities and
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:275

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:278

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:288

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/market/[category]/[location]/page.tsx:291

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:142

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"material supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:243

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:298

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:330

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:352

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:365

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:443

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:756

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:826

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:869

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai score`

**Current system**

```text
AI Score {item.score}
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/materials/[id]/page.tsx:932

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor account not linked to this listing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:937

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="material"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/[id]/page.tsx:946

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:947

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title || "building material supplier"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/[id]/page.tsx:958

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/materials/add/page.tsx:22

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "@/components/marketplace/vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:198

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `contractor`

**Current system**

```text
const [aiBuyerType, setAiBuyerType] = useState<"home_owner" | "contractor" | "mason" | "vendor">("home_owner");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:211

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:239

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:241

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 6,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/materials/add/page.tsx:410

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: `vendor-${x.id}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:430

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const isVendorProductGroup = productGroupId.startsWith("vendor-");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:705

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
aiBuyerType === "contractor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:706

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `contractor`

**Current system**

```text
? "contractor / bulk buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:709

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: aiBuyerType === "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:710

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "reseller / vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:711

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "home owner / small buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:727

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Target buyer: ${buyerTypeLabel}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:732

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Mention practical site-use points that a buyer actually checks before purchase.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:733

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
- Do not claim BIS/ISI/lab-tested/warranty/brand guarantee unless entered by seller.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:752

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
- Appliances/fittings: installation, utility, compatibility, buyer checking points.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:810

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:812

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
tone: "professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:921

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer Trust Description",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1030

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1044

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const geoRes = await fetch("/api/admin/geography/resolve", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1083

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: productGroupId.replace("vendor-", ""),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1125

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:1141

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:1181

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor memory save failed", memoryErr);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1321

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{String(pg.id).startsWith("vendor-") ? " • My Added Option" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1474

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Smart suggestions based on your frequently reused inventory, pricing and delivery workflows.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/materials/add/page.tsx:1520

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<option value="home_owner">Target: Home Owner / Small Buyer</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1521

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `contractor`

**Current system**

```text
<option value="contractor">Target: Contractor / Bulk Buyer</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1523

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="vendor">Target: Reseller / Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1543

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{aiLoading ? `Generating ${aiLoading}...` : "✨ Generate Complete Buyer-Ready Description"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1551

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer Trust Description",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1576

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Level 10 AI uses Product Group, local name, attributes and buyer type to write material-specific content.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/add/page.tsx:1647

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/add/page.tsx:1711

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
No attributes mapped to this product group yet (admin → Mapping page → map attributes).
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/layout.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"cement supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/layout.tsx:14

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"sand supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/layout.tsx:15

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"bricks supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/my/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/materials/my/page.tsx:136

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/materials/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/materials/page.tsx:111

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Find nearby vendors", "/vendor/discovery"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/materials/page.tsx:363

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/materials/page.tsx:498

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<PublicVendorOpportunityBanner module="materials" />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/page.tsx:700

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="material"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/rfq/new/page.tsx:7

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/materials/rfq/new/page.tsx:75

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/rfq/new/page.tsx:77

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 8,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/materials/rfq/new/page.tsx:205

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/materials/rfq/new/page.tsx:250

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
subtitle="Upload your contractor list or type items — vendors will send competitive quotations."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/need/[slug]/page.tsx:36

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"building-material-supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
"property-seller",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:38

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"service-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:39

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"rental-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:63

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"building-material-supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:64

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
"property-seller",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:65

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"service-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:66

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"rental-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:137

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
parsed.prefix === "service-provider"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:138

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
? ["electrician", "plumber", "civil-contractor", "raj-mistri"]
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:139

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: parsed.prefix === "building-material-supplier"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:141

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
: parsed.prefix === "rental-provider"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:143

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
: ["residential-plot", "commercial-land", "house", "ready-to-register-land"];
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/need/[slug]/page.tsx:167

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Demand Opportunity | 3Bigha",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:181

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Demand Opportunity Not Found | 3Bigha",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:189

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: `${clean(data.seo_title)} | 3Bigha Vendor Demand`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:193

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: `${clean(data.seo_title)} | 3Bigha Vendor Demand`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:201

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: `${clean(data.seo_title)} | 3Bigha Vendor Demand`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:233

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
answer: `You can use 3Bigha to discover local demand, marketplace options and vendor opportunities for ${requirement} in ${location}.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:236

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
question: `Can ${requirement} providers register on 3Bigha?`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/need/[slug]/page.tsx:237

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
answer: `Yes. Local businesses and vendors can register on 3Bigha to improve visibility and receive relevant buyer enquiries.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:237

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
answer: `Yes. Local businesses and vendors can register on 3Bigha to improve visibility and receive relevant buyer enquiries.`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/need/[slug]/page.tsx:240

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
question: `Does 3Bigha support RFQ and buyer enquiries?`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:241

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
answer: `Yes. 3Bigha supports marketplace discovery, RFQ workflows and buyer-vendor connection features for construction and property needs.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:245

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
answer: `Yes. This page shows public, privacy-safe vendor demand intelligence without exposing private buyer details, chats or RFQ information.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:257

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
category: "Vendor Demand Opportunity",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:258

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:296

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Demand Opportunity
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:311

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Requirement
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:351

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha marketplace intelligence has detected a local vendor demand
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:358

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
Businesses operating near {location} can register on 3Bigha, improve
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/need/[slug]/page.tsx:359

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
visibility, receive relevant enquiries and participate in future buyer
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:370

**Area:** Application Route

**Category:** Onboarding

**Matched:** `register`, `business profile`

**Current system**

```text
Register your business profile and explore demand opportunities across
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/need/[slug]/page.tsx:376

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:379

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/[slug]/page.tsx:452

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
This page shows only public vendor demand intelligence. Buyer identity,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Demand Opportunities | 3Bigha",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:9

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
"Browse public vendor demand opportunities where 3Bigha marketplace intelligence is identifying supplier, service, rental and property business gaps.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:36

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/need/page.tsx:45

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Demand SEO
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:49

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Demand Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:53

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Browse public vendor demand opportunities where 3Bigha marketplace
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:54

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
intelligence is identifying supplier, service, rental and property
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:83

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor opportunities are being prepared
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:86

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha is preparing public vendor demand pages from marketplace
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:90

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/need/page.tsx:93

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:5

**Area:** Onboarding

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:9

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { validateGstin } from "@/lib/vendor-verification/gstin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:85

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: boolean;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:86

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score: number;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:87

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: string[];
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:124

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: boolean;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:125

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score: number;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:126

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: string[] | null;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:132

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `seller`, `supplier`

**Current system**

```text
{ key: "materials", label: "Materials (Seller/Supplier)" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:133

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
{ key: "services", label: "Services (Professional/Skilled)" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:135

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
{ key: "blog", label: "Blog / Writer / Author" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:173

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
missing.push(isPureBlogOnly ? "Author Display Name" : "Business Name");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:180

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
missing.push("Author Display Name (Blog)");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:212

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
| "author"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:225

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
author: [],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:239

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (s.includes("author")) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:240

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
push("author", m);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:285

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (!opts.hasBlog) buckets.author = [];
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:340

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (one === "materials") return "Materials Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:341

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (one === "services") return "Service Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:342

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (one === "rentals") return "Rental Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:343

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`, `seller`

**Current system**

```text
if (one === "property") return "Property Vendor / Seller";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:344

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (one === "blog") return "Blogger / Author";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:353

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Vendor Hub";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:357

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`, `seller`

**Current system**

```text
return "Property Vendor / Seller";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:360

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Multi-Service Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:368

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const rawReturnTo = sp.get("returnTo") || "/dashboard/vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:368

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const rawReturnTo = sp.get("returnTo") || "/dashboard/vendor";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:370

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
rawReturnTo === "/dashboard" ? "/dashboard/vendor" : rawReturnTo;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:370

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
rawReturnTo === "/dashboard" ? "/dashboard/vendor" : rawReturnTo;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:425

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: !!data.is_complete,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:426

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score:
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:427

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
typeof data.completion_score === "number" ? data.completion_score : 0,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:428

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: safeArr(data.missing_fields),
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:454

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:466

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:474

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:499

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:505

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:509

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:545

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:551

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:564

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
bucket: String(x?.bucket || "vendor-media"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:582

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: safeArr(data.missing_fields),
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:594

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
setMsg(e?.message || "Failed to load business profile.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:633

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/geography");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:675

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/ai/vendor-document-verify", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:731

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
return s.includes("business name") || s.includes("author display name");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:751

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
m.toLowerCase().includes("author display name")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:773

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
{ key: "author", title: "Step 6 — Author Identity", subtitle: "Blog profile", show: hasBlog, targetId: "sec-author" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:795

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
`/onboarding/business?returnTo=${encodeURIComponent(returnTo)}`
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:805

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: isComplete,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:806

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score: score,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:807

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: missing,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:822

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const geoRes = await fetch("/api/admin/geography/resolve", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:849

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Geography resolver is best-effort and must not block business onboarding.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:861

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:895

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: isComplete,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:896

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score: score,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:897

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: missing,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:935

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
const res = await fetch("/api/onboarding/verify-location", {
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:957

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const geoRes = await fetch("/api/admin/geography/resolve", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1000

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1018

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: completion.isComplete,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1019

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `completion_score`

**Current system**

```text
completion_score: completion.score,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1020

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `missing_fields`

**Current system**

```text
missing_fields: completion.missing,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1105

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
? "Saved ✅ Registration already complete. You can go to the dashboard."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1105

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
? "Saved ✅ Registration already complete. You can go to the dashboard."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1106

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
: "Saved ✅ Profile is complete. Now click 'Activate My Dashboard' to finish setup."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1109

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
setMsg("Saved ✅ Profile is complete. Now click 'Activate My Dashboard' to finish setup.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1129

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
setMsg("Please complete the highlighted required fields before finishing registration.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1147

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
setMsg("Profile complete, but registration is still not marked complete. Try again.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1154

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Builder / Developer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1156

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor Hub"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1158

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
? "Blogger / Author"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1187

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1192

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1220

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// Rebuild grants from final onboarding selection
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1223

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1228

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1287

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
setMsg("✅ Registration Complete. Redirecting...");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1296

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
Loading business profile...
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1303

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<h1 style={{ fontSize: 26, fontWeight: 800 }}>Complete Your Business Profile</h1>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1305

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Complete this profile to activate your business-side access on 3bigha. After all required details are filled, click <b>Activate My Dashboard</b> to enable your dashboard actions, listing submission, and publishing access.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1320

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
Registration:{" "}
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1383

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
✅ Registration Complete
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1399

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Open Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1419

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
{saving ? "Activating..." : "🚀 Activate My Dashboard"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1424

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Complete the highlighted fields, then activate your dashboard.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1454

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<Field label="Business Name (or use Author Display Name if only blog)" required missing={missingBusinessOrAuthor}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1559

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
module="vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1559

**Area:** Onboarding

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="vendor"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1591

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
This is not official government verification; admin/manual review may still be required.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1735

**Area:** Onboarding

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
? "Device live location verification is mandatory. Manual address or copied district will not activate dashboard access."
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1796

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Service Radius
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1800

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
This helps 3Bigha route RFQs to vendors who can actually serve the buyer location.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1957

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
<Field label="RERA Registration No (optional)">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1987

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<section id="sec-author" style={{ padding: 12, border: "1px solid #ddd", borderRadius: 8 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1988

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<h3 style={{ marginTop: 0 }}>Author / Writer Identity (Blog)</h3>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1991

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<Field label="Author Display Name" required missing={missingAuthorName}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/BusinessOnboardingPageClient.tsx:1999

**Area:** Onboarding

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<Field label="Author Category (optional)">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/onboarding/business/page.tsx:10

**Area:** Onboarding

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Suspense fallback={<div>Loading business onboarding…</div>}>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/opengraph-image.tsx:59

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Buy property, source building materials, hire services, rent equipment and grow as a vendor.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/opengraph-image.tsx:80

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<span>Vendor Opportunities</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:18

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:43

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "Property" | "Material" | "Service" | "Rental";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ title: "Vendor Opportunities", icon: "🚀", text: "Discover where buyers need suppliers, contractors and equipment owners", count: "Join • Grow • Expand", href: "/vendor-opportunities" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:107

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ id: "plot", module: "Property", title: "2 Katha Residential Plot", subtitle: "Cooch Behar, WB", meta: "2 Katha · North Facing", price: "₹12.5 Lakh", href: "/property", badge: "Plot", image: null },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:108

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ id: "cement", module: "Material", title: "UltraTech Cement 53 Grade", subtitle: "Cooch Behar, WB", meta: "Bulk Available", price: "₹425 / Bag", href: "/materials", badge: "Material", image: null },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:109

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ id: "mason", module: "Service", title: "Mason for House Construction", subtitle: "Cooch Behar, WB", meta: "Experienced · Verified", price: "₹700 / Day", href: "/services", badge: "Service", image: null },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:110

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ id: "jcb", module: "Rental", title: "JCB 3DX Rental", subtitle: "Cooch Behar, WB", meta: "With Operator", price: "₹1,800 / Hour", href: "/rentals", badge: "Rental", image: null },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:111

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ id: "house", module: "Property", title: "3 BHK Independent House", subtitle: "Cooch Behar, WB", meta: "3 BHK · 1200 sqft", price: "₹35 Lakh", href: "/property", badge: "Property", image: null },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:114

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleIcon(module: MarketplaceItem["module"]) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:115

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "Property") return "🏡";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:116

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "Material") return "🏗️";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:117

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "Service") return "👷";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:158

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title: "Plan your construction",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:159

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
subtitle: "Plan house construction cost, materials and contractor requirements.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:159

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle: "Plan house construction cost, materials and contractor requirements.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:187

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const hasLiveProperty = featuredItems.some((item) => item.module === "Property");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:220

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
["🏗️", "Build", "Plan construction, estimate cost, find contractors, materials and equipment.", "/construction-cost"],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:225

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["📋", "Manage", "Track RFQs, vendors, bills, dispatch and project work.", "/dashboard"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:226

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `provider`, `contractor`, `supplier`

**Current system**

```text
["📈", "Grow", "Join as vendor, supplier, contractor or service provider.", "/vendor-opportunities"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:266

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(2);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:273

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(2);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:280

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(2);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:293

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "Property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:307

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "Material",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:320

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "Service",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:321

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
title: s.custom_service || "Professional service",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:322

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
subtitle: s.provider_name || clipText(s.service_description) || "Service provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:332

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "Rental",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:386

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">📈 Grow</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:393

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai assistant`

**Current system**

```text
<button type="button" className={activeTab === "ai" ? "active" : ""} onClick={() => setActiveTab("ai")}>🤖 AI Assistant</button>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/page.tsx:460

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<a key={`${item.module}:${item.id}`} href={item.href}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:461

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<b>✨ {item.module}</b>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:472

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<a href={item.href} className="listingCard" key={`${item.module}-${item.id}`}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:474

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.image ? <img src={item.image} alt={item.title} /> : <b>{moduleIcon(item.module)}</b>}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/page.tsx:503

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
<p>RFQ, vendor response and supplier activity</p>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:513

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["14 min", "Avg. Vendor Response", "⏱️"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:515

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
["129", "Buyer-Vendor Chats", "🤝"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:544

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/dashboard/vendor/inventory">Open Vendor OS →</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:544

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/dashboard/vendor/inventory">Open Vendor OS →</a>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:553

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["🧠", "418", "AI decisions", "Procurement, pricing and vendor routing signals"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:553

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
["🧠", "418", "AI decisions", "Procurement, pricing and vendor routing signals"],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:569

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<h2>Vendor Growth Opportunities</h2>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:580

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<span>🚀 Vendor Growth Opportunities</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:584

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">View All Opportunities →</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:588

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:592

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:596

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:621

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
to understand demand, pricing and operational risks.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:626

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/dashboard/vendor/inventory">📦 Inventory</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:626

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/dashboard/vendor/inventory">📦 Inventory</a>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:627

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/dashboard/vendor/billing">🧾 Billing</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:627

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/dashboard/vendor/billing">🧾 Billing</a>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:628

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/dashboard/vendor/fleet">🚚 Fleet</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:628

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/dashboard/vendor/fleet">🚚 Fleet</a>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:629

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/dashboard/vendor/dispatch">📍 Dispatch</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:629

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<a href="/dashboard/vendor/dispatch">📍 Dispatch</a>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:687

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<p>Tools, RFQ and pricing actions in one compact workspace</p>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/page.tsx:687

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<p>Tools, RFQ and pricing actions in one compact workspace</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/page.tsx:777

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<p>Verified listings, vendor discovery, RFQ support and local marketplace intelligence in one place.</p>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:783

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Secure Process", "Enquiry, RFQ and vendor workflow support"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/page.tsx:816

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<a href="/vendor-opportunities">Vendor Opportunities</a>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/layout.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Track today's market prices on 3bigha.com for building materials, property, construction services, rentals, vendor rates and AI-powered price prediction insights.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/layout.tsx:7

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"Track today's market prices on 3bigha.com for building materials, property, construction services, rentals, vendor rates and AI-powered price prediction insights.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:135

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sourceType: "Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:153

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
brand: "Local Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:161

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sourceType: "Local Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:458

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} else if (source.includes("vendor")) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:460

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label = "Vendor submitted";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:466

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// Vendor count weight
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:548

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(row.subscriptionPlan || "free");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:560

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (active && (plan === "hub_vendor" || plan === "premium_vendor")) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:568

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (active && plan === "basic_vendor") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:584

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = String(row.subscriptionPlan || "free");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:598

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "hub_vendor") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:601

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🔥 Hub Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:606

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "premium_vendor") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:609

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:609

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:614

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:617

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Basic Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:684

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return `${item} is AI-assisted draft intelligence and should be treated as indicative until more vendor sources are added.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:707

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// vendor strength
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:763

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail: "More verified vendor prices are needed before making a strong buying decision.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:770

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
detail: "Check brand/source, grade and vendor confirmation before final decision.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:783

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.category.toLowerCase(),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/price-today/PriceTodayClient.tsx:804

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
window.location.href = `/dashboard/thread/${data.conversationId}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/price-today/PriceTodayClient.tsx:846

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
? "Service demand or pricing pressure is rising. Confirm provider availability early."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:846

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
? "Service demand or pricing pressure is rising. Confirm provider availability early."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:858

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Market is softening. This may be a negotiation opportunity with owner or builder."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:928

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
recommendation: "Search listings or create an AI RFQ to get live vendor quotes.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:949

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Talk to owner/builder early before price rises further."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:950

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Buy/rent/hire soon or lock vendor quote early."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:953

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Proceed after comparing vendor availability, trust and final terms.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:958

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Create a procurement RFQ and compare at least 2–3 supplier quotes."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:963

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
: "Compare location, legal clarity, price trend and seller urgency.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1097

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1152

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:1160

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
supabase.from("property_types").select("name,slug").limit(100),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:1162

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
supabase.from("property_subtypes").select("name,slug").limit(200),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:1167

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
"id,category,item,brand,grade,price_min,price_max,unit,location,trend,offer,offer_start,offer_end,source_type,created_at,verified,user_id,ai_suggested_price,ai_price_deviation_percent,business_profiles!material_price_updates_created_by_fke…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1171

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:1268

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
sourceType: String(row.source_type || "Vendor").trim(),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1275

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
// 🔥 SUBSCRIPTION VISIBILITY
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:1277

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.subscription_plan ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1278

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.[0]?.subscription_plan ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1281

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.subscription_status ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1282

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.[0]?.subscription_status ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1285

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.subscription_expires_at ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1286

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
row.business_profiles?.[0]?.subscription_expires_at ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1822

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor launch discount available
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1831

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor/price-updates/new"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1835

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
Vendor / Builder / Owner
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:1847

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
Vendor / Builder / Owner only
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2087

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI reads price movement, confidence, vendor count and category signals to guide procurement timing and budget risk.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2132

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/rfq?module=${encodeURIComponent(category.toLowerCase())}&q=${encodeURIComponent(searchText)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/price-today/PriceTodayClient.tsx:2146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href="/dashboard/buyer/rfqs"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2146

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/buyer/rfqs"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/price-today/PriceTodayClient.tsx:2165

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
This compares available {category === "Properties" ? "property" : category === "Services" ? "service" : category === "Rentals" ? "rental" : "material"} price signals using rate range, vendor count,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2218

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{row.vendorCount} vendor{row.vendorCount > 1 ? "s" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2416

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `builder`

**Current system**

```text
vendor, distributor, builder or owner.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2447

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🤖 AI Decision: This option has the best balance of price, confidence and vendor availability.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2465

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "⭐ Premium vendors dominate here"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:2516

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
📈 High buyer activity detected in this category
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2585

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🏆 Top Vendor Zone
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2627

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
window.location.href = `/dashboard/thread/${json.conversationId}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/price-today/PriceTodayClient.tsx:2629

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alert(json?.error || "No verified vendor found for this price lead yet.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2640

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
❌ You are NOT in top vendor matches. Buyers are choosing AI-optimized and premium vendors first.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/price-today/PriceTodayClient.tsx:2640

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
❌ You are NOT in top vendor matches. Buyers are choosing AI-optimized and premium vendors first.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:2648

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
onClick={() => (window.location.href = "/dashboard/subscription?focus=boost")}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/price-today/PriceTodayClient.tsx:2648

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
onClick={() => (window.location.href = "/dashboard/subscription?focus=boost")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/price-today/PriceTodayClient.tsx:2663

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{row.vendorCount} vendor{row.vendorCount > 1 ? "s" : ""} contributing
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/InvestmentApplyButton.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/[id]/InvestmentApplyButton.tsx:80

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/investor/deal-rooms/${encodeURIComponent(dealRoomId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/[id]/page.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/[id]/page.tsx:32

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
import { buildPropertyCrossModuleSuggestions } from "@/lib/marketplace-orchestration/property-cross-module";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:164

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `seller`, `builder`

**Current system**

```text
`Explore this property listing${location ? ` in ${location}` : ""} on 3bigha.com. Compare, enquire and connect with the seller, owner, builder or vendor.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:303

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// In that case, do one safe read from the base table only for vendor linkage fields.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:363

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
// If the current row itself already has a direct plan, prefer it
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:401

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:417

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
// Otherwise fall back to project-level default plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:468

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:494

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:527

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:530

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:649

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:801

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:844

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai score`

**Current system**

```text
AI Score {item.score}
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/property/[id]/page.tsx:887

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:942

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:1220

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:1426

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div style={{ fontSize: 11, opacity: 0.65 }}>Buyer Momentum</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:1430

**Area:** Application Route

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
<div style={{ fontSize: 11, opacity: 0.65 }}>Seller Pressure</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:1462

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
AI Liquidity & Buyer Demand Signals
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:1474

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div style={{ fontSize: 11, opacity: 0.65 }}>Buyer Demand</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:1721

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{safeText(resolvedInvestmentPlan?.title) || "Investment Plan Available"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:1730

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
? "Unit-specific plan"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:1731

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Project default plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:1884

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "This property is showing a project-level investment plan. Invest Now will appear after a property-level investment opportunity is created for this listing."}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:1894

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit={5}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/[id]/page.tsx:1905

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor account not linked to this listing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:1910

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/page.tsx:1928

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/[id]/page.tsx:2013

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/[id]/PropertyDiscoveryMemoryTracker.tsx:28

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:12

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/add/AddPropertyPageClient.tsx:98

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
| "Independent / Builder Floor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:124

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"Independent / Builder Floor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:1342

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// ✅ Pricing restore
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:1442

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
// Profile completion banner only (NO auto redirect)
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:1589

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// BUILDER-LIKE SECTIONS (5-7) for Individual Listings (House(s))
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:1828

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:1846

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:1996

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
// 1.5) Profile completion banner load (no redirect)
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2003

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:2004

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("is_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2011

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
setProfileComplete(!!bp?.is_complete);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2017

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("is_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2022

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
setProfileComplete(!!comp?.is_complete);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2265

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:2267

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 8,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:2462

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
pricing: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:2530

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
setSaveMsg("❌ Please complete your Business Profile before saving or submitting.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2531

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
window.location.href = `/onboarding/business?returnTo=${encodeURIComponent("/property/add")}`;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:2578

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:2580

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
tone: "professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:2615

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `quota`

**Current system**

```text
setSaveMsg("⚠️ AI quota exceeded. Please try again later or contact support.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:2706

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `seller`

**Current system**

```text
? "Draft clear direct EMI terms for seller-to-buyer understanding. Keep it practical and safe."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:2715

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:2717

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
tone: "professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:2759

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `quota`

**Current system**

```text
setSaveMsg("⚠️ AI quota exceeded. Please try again later.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3134

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
pricing_json: payload.pricing,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3254

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Please select a builder project.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3257

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Please select a builder unit.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3260

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Builder listing requires a valid price.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3302

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
pricing_json: payload.pricing,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3311

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:3349

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/subscription?source=property&listingId=${encodeURIComponent(newId)}&return=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/add/AddPropertyPageClient.tsx:3349

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
`/dashboard/subscription?source=property&listingId=${encodeURIComponent(newId)}&return=${encodeURIComponent(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3377

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Please select a builder project.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3380

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Please select a builder unit.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3383

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Builder listing requires a valid price.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3423

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
pricing_json: payload.pricing,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3429

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/subscription?source=property&listingId=${encodeURIComponent(id)}&return=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/add/AddPropertyPageClient.tsx:3429

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
`/dashboard/subscription?source=property&listingId=${encodeURIComponent(id)}&return=${encodeURIComponent(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3508

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
setSaveMsg("Checking business profile...");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:3572

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!selectedBuilderProjectId) throw new Error("Please select a builder project.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3573

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!selectedBuilderUnitId) throw new Error("Please select a builder unit.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3575

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Builder listing requires a valid price.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3578

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setSaveMsg("Syncing builder inventory...");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3630

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!selectedBuilderProjectId) throw new Error("Please select a builder project.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3631

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!selectedBuilderUnitId) throw new Error("Please select a builder unit.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3633

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Builder listing requires a valid price.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3636

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setSaveMsg("Syncing builder inventory before submit...");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3731

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setBuilderProjectsError(e?.message || "Failed to load builder projects.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3805

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setBuilderUnitsError(e?.message || "Failed to load builder units.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3820

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:3912

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
throw new Error("Please select a valid builder unit.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:3923

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:4114

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 6 }}>Complete your Business Profile to save drafts or submit</div>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:4116

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
You can fill the form now, but saving/submitting will redirect you to profile completion.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:4119

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<ActionButton href={`/onboarding/business?returnTo=${encodeURIComponent("/property/add")}`} variant="primary">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:4201

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Builder Project Listing
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4208

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Select Builder Project"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4210

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
hint="Only your own builder projects are shown."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4214

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ color: "#6b7280", fontSize: 13, marginTop: 8 }}>Loading builder projects…</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4222

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{ value: "", label: "Select builder project..." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4369

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Smart suggestions based on your frequently reused property location and pricing workflows.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:4668

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 10 }}>5) Fill Details (Builder-style)</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4705

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Example: Start 1 + Qty 4 → 401, 402, 403, 404 (builder inventory can use this later)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4799

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
This helps future inventory pricing / linking (like builder flow).
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:4799

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
This helps future inventory pricing / linking (like builder flow).
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:4971

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
behavior as builder (Supabase Storage), we’ll add it next.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5269

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Builder Inventory Link</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5272

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Select Builder Unit"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5278

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ color: "#6b7280", fontSize: 13, marginTop: 8 }}>Loading builder units…</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5282

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<div style={{ color: "#6b7280", fontSize: 13, marginTop: 8 }}>Select a builder project first.</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5288

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{ value: "", label: "Select builder unit..." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5454

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
label="Registration after down payment?"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/add/AddPropertyPageClient.tsx:5465

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `seller`

**Current system**

```text
placeholder="e.g., Seller / Buyer / Bank / Advocate"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5684

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/add/AddPropertyPageClient.tsx:5774

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
No media added yet. Add photos or videos to improve buyer trust.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/add/AddPropertyPageClient.tsx:5948

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
Save draft (Supabase) → Subscription
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/add/AddPropertyPageClient.tsx:5998

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
? "Step 1 needs: Intent + Property Type + Subcategory + Builder Project."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:223

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const [builder, setBuilder] = useState<BuilderProfileRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:235

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
// Wizard fields
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:385

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `seller`

**Current system**

```text
const [originalDeedHolder, setOriginalDeedHolder] = useState<"buyer" | "seller" | "bank" | "other">("bank");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:730

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError("Invalid Project ID in URL. Please open this page from Builder Projects list.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:744

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
const bpRes = await supabase.from("business_profiles").select("user_id,is_complete").eq("user_id", uid).maybeSingle();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:744

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
const bpRes = await supabase.from("business_profiles").select("user_id,is_complete").eq("user_id", uid).maybeSingle();
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:751

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
const isComplete = !!(bpRes.data as any)?.is_complete;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:753

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const returnTo = `/property/builder/projects/${encodeURIComponent(projectId)}/units/add`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:761

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError(`Could not ensure builder profile — ${friendlyDbError(ensureRes.error)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:938

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const BUCKET = "builder-unit-media";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1010

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
flashError("Unit created, but media upload is taking too long. Please add media later from inventory/admin.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1092

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_profile_id: builder?.id || null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1149

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// ✅ Optional: save price into pricing table (best effort)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1159

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// ignore silently (pricing table might be different)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1200

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href={`/property/builder/projects/${encodeURIComponent(projectId)}/units`}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1203

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1258

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builder ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1260

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>Builder: {builder.brand_name || builder.legal_name || builder.id.slice(0, 8)}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1261

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builder.status ? <Badge>status: {builder.status}</Badge> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1268

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{projectInvestmentPlanId ? <Badge>Investment Plan Attached</Badge> : <Badge>No Investment Plan</Badge>}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1288

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 4 }}>Investment Plan Notice</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1290

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This project already has an investment plan attached. New units created under this project will now
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1291

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
inherit that investment plan by default. Unit-level override can be added later without breaking this flow.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:1764

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
This is what helps you price the exact unit later in <b>Builder Inventory</b>.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:2077

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
Keep this OFF unless you want room-by-room measurements & photos (best for premium listings).
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:2098

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Note: Media uploads to Supabase Storage bucket: <b>builder-unit-media</b>. For media upload, create <b>1 unit at a time</b>.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:2164

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
<div style={{ fontSize: 12, opacity: 0.7, marginTop: 6 }}>If empty, we auto-generate from your wizard inputs.</div>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:2237

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
After creation: go to <b>Units list</b> → then <b>Builder Inventory</b> for pricing, availability, and listing linking.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/add/page.tsx:2237

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
After creation: go to <b>Units list</b> → then <b>Builder Inventory</b> for pricing, availability, and listing linking.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// app/property/builder/projects/[projectId]/units/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:114

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const [builder, setBuilder] = useState<BuilderProfileRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:136

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError("Invalid Project ID in URL. Please open this page from Builder Projects list.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:162

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
// 2) must have completed BUSINESS profile
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:164

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:165

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("user_id,is_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:177

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
const isComplete = !!(bpRes.data as any)?.is_complete;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:179

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const returnTo = `/property/builder/projects/${encodeURIComponent(projectId)}/units`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:184

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// 3) ensure builder profile exists
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:189

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError(`Could not ensure builder profile — ${friendlyDbError(ensureRes.error)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:194

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// 4) fetch builder profile
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:213

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError("Builder profile could not be loaded (unexpected). Please refresh once.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:267

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
// 6) fetch units from base table + active plans
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:302

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
setGlobalError(`Could not load investment plans — ${friendlyDbError(planRes.error)}`);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:328

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
`/admin/property/inventory?projectId=${encodeURIComponent(projectId)}&source=builder_projects`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:351

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Unit investment plan update"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:363

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Unit investment plan reread"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:386

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
? "Unit investment plan override saved."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:387

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
: "Unit reverted to inherited project plan."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:390

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const msg = `Could not update unit plan — ${friendlyDbError(e)}`;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:403

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Builder • Project Units"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:412

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Inventory
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:415

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:419

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:420

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<ActionButton variant="secondary">Builder Home</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:480

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link href="/onboarding/business?returnTo=%2Fproperty%2Fbuilder%2Fprojects">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:481

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<ActionButton variant="secondary">Business Profile</ActionButton>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:489

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builder ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:491

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>Builder: {builder.brand_name || builder.legal_name}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:492

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>status: {builder.status}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:499

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{projectInvestmentPlanId ? <Badge>Default Investment Plan Attached</Badge> : <Badge>No Investment Plan</Badge>}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:504

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
<Badge>Plans loaded: {investmentPlans.length}</Badge>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:518

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Inventory
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:520

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:541

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 4 }}>Investment Plan Notice</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:543

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This project has a default investment plan. Units under this project inherit that plan by default.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:544

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
If a unit has its own investment plan assigned later, that unit-level override should take priority.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:555

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<th style={{ padding: "10px 8px", borderBottom: "1px solid #eee" }}>Investment Plan</th>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:576

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{investmentPlans.find((p) => p.id === u.investment_plan_master_id)?.title || "Unit Override Plan"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:600

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<span style={{ opacity: 0.7 }}>No plan</span>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:615

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<option value="">Use inherited project plan</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:644

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
flashSuccess("Open Admin Inventory to edit unit pricing / availability / listing link.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:644

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
flashSuccess("Open Admin Inventory to edit unit pricing / availability / listing link.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:647

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Edit in Admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/[projectId]/units/page.tsx:663

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
Next step: Add more units via <b>Add Units / Property</b> (plot/flat/house wizard).
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/add/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// app/property/builder/projects/add/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:9

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/builder/projects/add/page.tsx:156

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const [builder, setBuilder] = useState<BuilderProfileRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:268

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/builder/projects/add/page.tsx:270

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 8,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:337

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:338

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("user_id,is_complete,business_name")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/add/page.tsx:352

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
const isComplete = !!bp?.is_complete;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/add/page.tsx:355

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const returnTo = "/property/builder/projects/add";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:365

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError(`Could not ensure builder profile — ${friendlyDbError(ensureRes.error)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:408

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
? `${prev}\nCould not load investment plans — ${friendlyDbError(planResult.reason)}`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:409

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
: `Could not load investment plans — ${friendlyDbError(planResult.reason)}`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:610

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (!builder?.id) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:611

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const msg = "No builder profile found.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:649

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
builder_profile_id: builder.id,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:719

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/builder/projects/add/page.tsx:750

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
console.error("Builder project memory save failed", memoryErr);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:775

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const canSubmit = !!builder?.id && !!name.trim() && !saving;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:789

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Builder • Add Project"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:790

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtitle="Create a builder project. Next, we will add inventory via the wizard (plots/towers) and generate listings per unit."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:790

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
subtitle="Create a builder project. Next, we will add inventory via the wizard (plots/towers) and generate listings per unit."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/add/page.tsx:793

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:794

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<ActionButton variant="secondary">Builder Home</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:853

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>Builder: {builder?.brand_name ?? builder?.legal_name ?? builder?.id?.slice(0, 8)}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:854

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builder?.status ? <Badge>status: {builder.status}</Badge> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:855

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
<Badge>Plans loaded: {investmentPlans.length}</Badge>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:915

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Quickly reuse your previously used builder project setup.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:981

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 10 }}>Investment plan (optional now, project-level)</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:986

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Select approved investment plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:995

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<option value="">No investment plan attached</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:1004

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
This attaches one approved admin-created participation plan to the whole project.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:1004

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
This attaches one approved admin-created participation plan to the whole project.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/add/page.tsx:1132

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="project"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/builder/projects/add/page.tsx:1329

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/add/page.tsx:1337

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
Next step: Projects list → Units → Inventory wizard (plots/towers).
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// app/property/builder/projects/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:85

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const [builder, setBuilder] = useState<BuilderProfileRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:119

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
// 2) must have completed BUSINESS profile
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:121

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:122

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("user_id,is_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:134

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
const isComplete = !!(bpRes.data as any)?.is_complete;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:136

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
const returnTo = "/property/builder/projects";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:146

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError(`Could not ensure builder profile — ${friendlyDbError(ensureRes.error)}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:151

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// 4) fetch builder profile
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:170

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
setGlobalError("Builder profile could not be loaded (unexpected). Please refresh once.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:221

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
setGlobalError(`Could not load investment plans — ${friendlyDbError(planRes.error)}`);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:337

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
throw new Error("Project could not be reloaded after plan update.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:342

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
flashSuccess(planId ? "Investment plan attached." : "Investment plan removed.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:344

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const msg = `Could not update project plan — ${friendlyDbError(e)}`;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:355

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title="Builder • Projects"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:356

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
subtitle="Manage your builder projects. Next step: open Units for a project → then Inventory wizard."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:356

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
subtitle="Manage your builder projects. Next step: open Units for a project → then Inventory wizard."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:359

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects/add">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:362

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:363

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<ActionButton variant="secondary">Builder Home</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:427

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
<Link href="/onboarding/business?returnTo=%2Fproperty%2Fbuilder%2Fprojects">
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:428

**Area:** Application Route

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
<ActionButton variant="secondary">Business Profile</ActionButton>
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:436

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{builder ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:438

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>Builder: {builder.brand_name || builder.legal_name}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:439

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Badge>status: {builder.status}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:443

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
<Badge>Plans loaded: {investmentPlans.length}</Badge>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:451

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Create your first builder project. Then you can generate units and connect inventory/listings.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:455

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder/projects/add">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:458

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link href="/property/builder">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:459

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<ActionButton variant="secondary">Builder Home</ActionButton>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:474

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<th style={{ padding: "10px 8px", borderBottom: "1px solid #eee" }}>Investment Plan</th>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:514

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<span style={{ opacity: 0.7 }}>No plan</span>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:529

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<option value="">Select investment plan</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/builder/projects/BuilderProjectsPageClient.tsx:559

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Inventory
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/edit/[id]/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/inventory/[slug]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/inventory/[slug]/page.tsx:181

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Tip: Unit availability updates live from the builder inventory.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/inventory/PublicInventoryPageClient.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/layout.tsx:19

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder projects",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/my/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/my/page.tsx:139

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/my/page.tsx:155

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/my/page.tsx:336

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `canedit`

**Current system**

```text
const canEdit =
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/property/my/page.tsx:374

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `canedit`

**Current system**

```text
{canEdit ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/property/page.tsx:32

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
| "Independent / Builder Floor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:45

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"Independent / Builder Floor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:54

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
// ✅ We will NOT increase "limit".
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/property/page.tsx:88

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title: "Builder Projects",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:96

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
["Browse Builder Projects", "/property/projects"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:100

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["Submit Requirement", "/rfq?module=property"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/page.tsx:729

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Showing only published public listings. Sell / Rent / Lease / PG will be chosen only when a vendor lists a property."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:796

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<div className="propertyMobileDeferred"><PublicVendorOpportunityBanner module="property" /></div>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/page.tsx:1151

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/page.tsx:1204

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<summary>🚀 Vendor Opportunities</summary>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/page.tsx:1206

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<PublicVendorOpportunityBanner module="property" />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/property/projects/[slug]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/property/projects/page.tsx:120

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<SectionHeader title="Projects" subtitle="Public builder projects" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/property/projects/page.tsx:135

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<EmptyState message="No public active builder projects are available right now." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/refund-cancellation-policy/page.tsx:6

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
"Refund and Cancellation Policy of 3Bigha covering subscription, future payments and platform usage terms.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/refund-cancellation-policy/page.tsx:39

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Once payment features (such as subscriptions, vendor boosts or other
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/refund-cancellation-policy/page.tsx:39

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscriptions`

**Current system**

```text
Once payment features (such as subscriptions, vendor boosts or other
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/[id]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/[id]/page.tsx:255

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:278

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:296

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:306

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:478

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:517

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai score`

**Current system**

```text
AI Score {item.score}
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/rentals/[id]/page.tsx:566

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:765

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit={5}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/[id]/page.tsx:776

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor account not linked to this rental.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/[id]/page.tsx:781

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rental"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/[id]/page.tsx:790

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/[id]/page.tsx:791

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
row.title || "rental vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/[id]/page.tsx:802

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/add/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/add/page.tsx:24

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "@/components/marketplace/vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:116

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
// Profile completion banner (no auto redirect)
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:211

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub?.subscription?.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:215

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
// ---------- Profile completion banner (no redirect) ----------
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:223

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:224

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("is_complete")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:236

**Area:** Application Route

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
setProfileComplete(!!bp?.is_complete);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:244

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// ---------- Load vendor-private rental options ----------
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:255

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/add/page.tsx:364

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: `vendor-${x.id}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:383

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: `vendor-${x.id}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:406

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: `vendor-${x.id}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:589

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const geoRes = await fetch("/api/admin/geography/resolve", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:612

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
equipment_id: equipmentId.startsWith("vendor-") ? null : equipmentId || null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:703

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/add/page.tsx:720

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/add/page.tsx:785

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
// FINAL STAGE: subscription page
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:788

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
`/dashboard/subscription?source=rentals&listingId=${encodeURIComponent(res.id)}&return=${encodeURIComponent(
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/add/page.tsx:788

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
`/dashboard/subscription?source=rentals&listingId=${encodeURIComponent(res.id)}&return=${encodeURIComponent(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:841

**Area:** Application Route

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
<div className="bannerTitle">Complete your Business / Author Profile to save drafts</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:843

**Area:** Application Route

**Category:** Onboarding

**Matched:** `profile completion`

**Current system**

```text
You can fill the form now, but “Save Draft” will redirect you to profile completion.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:848

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href={`/onboarding/business?returnTo=${encodeURIComponent("/rentals/add")}`}
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rentals/add/page.tsx:955

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Smart suggestions based on your frequently reused rental pricing, location and equipment workflows.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:979

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{String(c.id).startsWith("vendor-") ? " • My Added Option" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:1012

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{String(s.id).startsWith("vendor-") ? " • My Added Option" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:1048

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{String(eq.id).startsWith("vendor-") ? " • My Added Option" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/add/page.tsx:1113

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<Section title="3) Pricing" open={openPricing} setOpen={setOpenPricing}>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:1116

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<label className="label">Pricing unit</label>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/add/page.tsx:1208

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/add/page.tsx:1241

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
<span className="tiny">After save, you’ll be sent to the subscription final stage.</span>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/catalog/[id]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/catalog/[id]/page.tsx:226

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor: Add Listing
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/catalog/RentalsCatalogPageClient.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/catalog/RentalsCatalogPageClient.tsx:216

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor: Add Rental Listing
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/catalog/RentalsCatalogPageClient.tsx:297

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<span className="muted">No provider details shown</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/my/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/rentals/my/page.tsx (VENDOR - AUTH REQUIRED, but redirects public to /rentals)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/my/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rentals/my/page.tsx:200

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/my/page.tsx:257

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/my/page.tsx:290

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/my/page.tsx:323

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/my/page.tsx:375

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub?.subscription?.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/my/page.tsx:414

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(400),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/page.tsx:114

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["Submit Rental Requirement", "/rfq?module=rentals"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/page.tsx:412

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(400);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rentals/page.tsx:581

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<PublicVendorOpportunityBanner module="rentals" />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rentals/page.tsx:775

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
No rentals found. If you are a vendor, create one from{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/page.tsx:865

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Rate", hasRate ? "Available" : "Ask vendor"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/page.tsx:914

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Hot rental lead: rate and location are available. Buyer can quickly confirm availability, operator, deposit and delivery."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rentals/page.tsx:921

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="rental"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:1

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// app/rfq/general/browse/[module]/page.tsx
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/browse/[module]/page.tsx:18

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Civil contractor / building contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/browse/[module]/page.tsx:28

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
title: "Professional Services",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/browse/[module]/page.tsx:33

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"3D elevation / 3D plan",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rfq/browse/[module]/page.tsx:43

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
"Sale deed / registration assistance",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rfq/browse/[module]/page.tsx:134

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const params = useParams<{ module: string }>();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:137

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const raw = String(params?.module ?? "services").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:138

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = (["materials", "services", "rentals", "properties"].includes(raw) ? raw : "services") as RfqModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:148

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const groups = useMemo(() => DATA[module] || [], [module]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:181

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:199

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const payload = { mode: "other", text: v, module };
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:239

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<h1 style={{ fontSize: 18, margin: 0 }}>Browse for RFQ • {moduleLabel(module)}</h1>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:251

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<input className="searchInput" value={q} onChange={(e) => setQ(e.target.value)} placeholder={`Search inside ${moduleLabel(module)}…`} />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/browse/[module]/page.tsx:354

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
placeholder={`Write your ${moduleLabel(module)} requirement in your words…`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:1

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// app/rfq/general/browse/[module]/page.tsx
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/general/browse/[module]/page.tsx:42

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
/** ✅ Curated lists for non-material modules (keep as you already use) */
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:50

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Civil contractor / building contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/general/browse/[module]/page.tsx:60

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
key: "professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/general/browse/[module]/page.tsx:61

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
title: "Professional Services",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/general/browse/[module]/page.tsx:66

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"3D elevation / 3D plan",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/rfq/general/browse/[module]/page.tsx:80

**Area:** Application Route

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
"Sale deed / registration assistance",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/rfq/general/browse/[module]/page.tsx:117

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
items: ["Independent House", "Builder Floor", "Villa", "Farm House", "Bungalow", "Office Space", "Shop / Showroom"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/general/browse/[module]/page.tsx:150

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleParam = String((params as any)?.module || "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:151

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = (["materials", "services", "rentals", "properties"].includes(moduleParam)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:190

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:199

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const url = `${returnTo}?module=${encodeURIComponent(module)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:223

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module !== "materials") return;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:268

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module, supabase]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:309

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module !== "materials") return [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:327

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module, taxons, q, maps]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:330

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module !== "materials" || !openTypeId) return [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:347

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module, openTypeId, q, maps]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:356

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module !== "materials" || !selectedCategoryId) return [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:385

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module, selectedCategoryId, q, maps]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:389

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return SERVICES_GROUPS;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:390

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return RENTALS_GROUPS;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:391

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "properties") return PROPERTIES_GROUPS;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:393

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:396

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:406

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module, groups, q]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:408

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
// ✅ Helpful label prefixes for non-material modules (so RFQ shows readable text)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:411

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `${moduleLabel(module)} > ${norm(item)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:449

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const label = `${moduleLabel(module)} > Other: ${v}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:458

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Browse for RFQ • {moduleLabel(module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:474

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
placeholder={`Search inside ${moduleLabel(module)}...`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:479

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{module === "materials" ? (
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:534

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{module === "materials" ? (
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/browse/[module]/page.tsx:841

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
placeholder={`Write your ${moduleLabel(module)} requirement in your words...`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/general/new/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/general/page.tsx:10

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
We will connect this to the new <b>rfqs</b> table with <b>module</b> selection and smarter forms.
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/new/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/new/page.tsx:54

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
// (Your unified page already supports module selection)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/new/page.tsx:55

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = "materials" as const;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/new/page.tsx:171

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module, // ✅ REQUIRED for rfqs_v2_module_check
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/new/page.tsx:233

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
PROFESSIONAL RFQ WORKSPACE
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/new/page.tsx:233

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
PROFESSIONAL RFQ WORKSPACE
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/new/page.tsx:239

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Use this workspace when you want technical details, item rows, uploads,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/new/page.tsx:240

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI writing help and vendor-ready procurement information.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/new/page.tsx:488

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Professional tools stay here, separate from the form.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/new/page.tsx:517

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<summary style={{ cursor: "pointer", fontWeight: 900 }}>Vendor matching</summary>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:19

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const initialModule = (params.get("module") as ModuleChoice) || null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/SahajRfqClient.tsx:22

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const [module, setModule] = useState<ModuleChoice | null>(initialModule);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/SahajRfqClient.tsx:60

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module) sp.set("module", module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/SahajRfqClient.tsx:68

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/SahajRfqClient.tsx:72

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
subtitle="Choose what you need. 3Bigha will keep the professional RFQ engine behind the scenes."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:79

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `seller`

**Current system**

```text
["properties", "🏡", "I need property help", "Land, house, flat, project, seller or buyer"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:99

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
title={`How will you tell us about ${moduleLabels[module]}?`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/rfq/SahajRfqClient.tsx:105

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
["photo", "📷", "Upload handwritten note / photo", "Notebook page, contractor list or product photo."],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:140

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Choose the easy path or open the full professional RFQ workspace.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:140

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Choose the easy path or open the full professional RFQ workspace.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/SahajRfqClient.tsx:151

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Recommended for most users. Answer simple questions. 3Bigha prepares the professional RFQ automatically.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:161

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
<h2 className="text-lg font-black text-blue-900">Professional RFQ Workspace</h2>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:161

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<h2 className="text-lg font-black text-blue-900">Professional RFQ Workspace</h2>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/SahajRfqClient.tsx:165

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
<b className="mt-3 block text-blue-700">Open Professional Workspace →</b>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:165

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
<b className="mt-3 block text-blue-700">Open Professional Workspace →</b>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/SahajRfqClient.tsx:173

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Answer a few simple questions. 3Bigha will prepare the professional RFQ automatically.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:215

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
You can submit even with a handwritten note or file. AI will prepare the professional version.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:261

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
3Bigha has prepared the professional RFQ in the background.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:276

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Review Professional RFQ
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/SahajRfqClient.tsx:293

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
{preparing ? "Preparing professional RFQ..." : "Submit Requirement"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/start/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/success/page.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="topBtn topBtnGhost" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/success/page.tsx:16

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topBtn topBtnGhost" href="/dashboard/buyer/rfqs">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/success/page.tsx:20

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="topBtn topBtnGhost" href="/dashboard/buyer/inbox">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/rfq/success/page.tsx:20

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topBtn topBtnGhost" href="/dashboard/buyer/inbox">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/rfq/success/page.tsx:21

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Open Buyer Inbox →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/robots.ts:15

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"/vendor-opportunities/",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/robots.ts:29

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"/admin/",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/robots.ts:30

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/robots.ts:38

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
"/subscription/",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/[slug]/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/search/page.tsx:7

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/search/page.tsx:37

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorLiquidityInsight } from "@/lib/search/vendor-liquidity-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:51

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorIntelligenceInsight } from "@/lib/search/vendor-intelligence-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:56

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorNegotiationInsight } from "@/lib/search/vendor-negotiation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:97

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: ModFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:109

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:130

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SearchModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:165

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ModFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:194

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
x.module === item.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:259

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (m === "services") return "Provider match";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:265

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (r.module === "materials") return `/rfq?query=${encodeURIComponent(q || r.title)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:266

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (r.module === "services") return `/vendor/discovery?q=${encodeURIComponent(q || r.title)}&module=services`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:266

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (r.module === "services") return `/vendor/discovery?q=${encodeURIComponent(q || r.title)}&module=services`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:267

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (r.module === "rentals") return `/rentals?search=${encodeURIComponent(q || r.title)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:268

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (r.module === "property") return `/search?module=property&q=${encodeURIComponent(q || r.title)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:272

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function fallbackRecommendations(q: string, module: ModFilter): AiRecommendation[] {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:274

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleLabelText = module === "all" ? "marketplace" : module;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:279

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: "Convert this search into a requirement and get vendor responses.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:287

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:287

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:288

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
badge: "Vendor discovery",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:310

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ModFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:336

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=branded ${encoded}&module=materials`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:361

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `contractor`

**Current system**

```text
href: `/vendor/discovery?q=contractor&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:361

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=contractor&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:367

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
if (/mason|contractor|electrician|plumber|rajmistri/.test(q)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:376

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:376

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:381

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=materials for ${encoded}&module=materials`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:411

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:463

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
/mason|rajmistri|contractor|electrician|plumber|architect|service|মিস্ত্রি|রাজমিস্ত্রি|ठेकेदार/.test(q),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:488

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (moduleFilter !== "all" && row.module === moduleFilter) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:490

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
reasons.push("module match");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:502

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (intent.wantsProperty && row.module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:506

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (intent.wantsMaterials && row.module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:510

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (intent.wantsServices && row.module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:514

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (intent.wantsRentals && row.module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:524

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (intent.wantsInvestment && row.module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:533

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (item.module === row.module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:649

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:661

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
if (opts.limit) q.limit(opts.limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:771

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const modFromUrl = (safeText(sp.get("module")) as ModFilter) || "all";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:851

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:855

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:858

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:859

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:876

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ModFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:888

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (next.module !== "all") qs.set("module", next.module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:913

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modInput,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:966

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modInput,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1038

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const nextModule = data.module || "all";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1056

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: nextModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1119

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(40);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:1132

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1154

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:1168

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1211

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:1237

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1261

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:1270

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1288

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(30);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:1298

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "blog",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1316

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
filtered = filtered.filter((r) => r.module !== "property" || safeText(r._intent).toLowerCase() === intent);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1319

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
filtered = filtered.filter((r) => r.module !== "property" || (r._price != null && r._price >= minN));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1322

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
filtered = filtered.filter((r) => r.module !== "property" || (r._price != null && r._price <= maxN));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1380

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1435

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return (order[a.module] ?? 99) - (order[b.module] ?? 99);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1445

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1508

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: vendors.length > 0 ? `${vendors.length} vendor signals found` : "Vendor network ready",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1512

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(q)}${modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1512

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(q)}${modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1519

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=${encodeURIComponent(q)}${modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1573

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module:
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1589

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1598

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1608

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1619

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1637

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1665

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: action.href.includes("/vendor/discovery")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1666

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1683

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1703

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor liquidity",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1709

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor quality",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1732

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1746

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1747

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1760

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1761

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1770

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<SectionHeader eyebrow="AI Search" title="Marketplace Search" subtitle="Start with what you need. 3Bigha will guide discovery, comparison, RFQ and vendor workflow." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1817

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={modInput}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1825

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (suggestion.module && suggestion.module !== "all") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1826

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
setModInput(suggestion.module as ModFilter);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1831

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: (suggestion.module as ModFilter) || modInput,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1883

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:1885

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
)}${modInput !== "all" ? `&module=${encodeURIComponent(modInput)}` : ""}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:1898

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="Open workflow assistant with vendor recommendations"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2076

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Card key={`quick:${r.module}:${r.id}`}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2082

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<span style={{ fontSize: 13 }}>{moduleEmoji(r.module)}</span>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2084

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleLabel(r.module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2115

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{r.module === "materials" ? "RFQ" : r.module === "services" ? "Vendors" : r.module === "rentals" ? "Rental" : "Next"}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2161

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""}`} className="topBtn" style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2161

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Link href={`/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""}`} className="topBtn" style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2198

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Use RFQ, vendor discovery and price tools when you want to continue from this search.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2233

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Vendor Discovery", active: true },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2282

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Execution Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/search/page.tsx:2348

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Continue this search through RFQ, vendor discovery or price check.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2392

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2477

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(qFromUrl)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2478

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
modFromUrl !== "all" ? `&module=${encodeURIComponent(modFromUrl)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2529

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
subtitle="Optional next-step prompts for procurement, vendors, RFQ and pricing"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:2561

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Continue with practical next actions for RFQ, vendors, pricing and execution.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/search/page.tsx:2673

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Readiness, complexity, vendor count and RFQ probability"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2690

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="🎯 Vendor Liquidity"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2699

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title="🏅 Vendor Insights"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2700

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle="Vendor quality, fit, locality and procurement suitability"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2752

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleEmoji((lastAiIntent?.module === "all" ? "property" : lastAiIntent?.module) as SearchModule)}{" "}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2753

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{lastAiIntent?.module === "all" ? "All Marketplace" : moduleLabel(lastAiIntent?.module as SearchModule)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2779

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/vendor/discovery?q=${encodeURIComponent(qFromUrl)}`} className="topBtn topBtnGhost" style={{ textDecoration: "none", background: "rgba(255,255,255,0.12)", color: "#ffffff" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2807

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{recommendationSummary || "3Bigha is connecting search intent with RFQ, vendor and price workflows."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:2938

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
localSearchIntent.module ? `&module=${localSearchIntent.module}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:2967

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
localSearchIntent.module ? `&module=${localSearchIntent.module}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3016

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["🧱 500 bags cement", "/search?q=500 bags cement&module=materials"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3017

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["🏠 2 katha land", "/search?q=2 katha land&module=property"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3018

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["👷 Need rajmistri", "/search?q=rajmistri for house&module=services"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3019

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["🚜 JCB rental", "/search?q=jcb rental&module=rentals"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3090

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={`${item.query}-${item.module}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3092

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3093

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `&module=${encodeURIComponent(item.module)}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3130

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={`${item.module}:${item.id}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3219

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(qFromUrl)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:3295

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!acc[row.module]) acc[row.module] = [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3296

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
acc[row.module].push(row);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3347

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: r.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3355

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<Card key={`${r.module}:${r.id}`}>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3361

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<span style={{ fontSize: 20 }}>{moduleEmoji(r.module)}</span>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3372

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleLabel(r.module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3384

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleTrustLabel(r.module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3441

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3464

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: modFromUrl,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3473

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
background: r.module === "materials" ? "#7c3aed" : "#0b57d0",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3479

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{r.module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3481

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: r.module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3483

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: r.module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3531

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
• Module intelligence:{" "}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3532

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<b>{moduleLabel(r.module)}</b>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3537

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor discovery and execution intent.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/search/page.tsx:3554

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={`${r.module}:${r.id}:${card.label}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/search/page.tsx:3600

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={modFromUrl}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap-categories.xml/route.ts:21

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const urls = seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap-categories.xml/route.ts:23

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
getSeoCategories(module).map((category) => ({
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap-categories.xml/route.ts:24

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
loc: `${siteConfig.url}/seo/${module}/${geo.stateSlug}/${geo.districtSlug}/${geo.citySlug}/category/${category.slug}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap.xml/route.ts:19

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
for (const module of seoModules) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap.xml/route.ts:23

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
urls.push(buildUrl(`/seo/${module}/${state}`));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap.xml/route.ts:35

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
buildUrl(`/seo/${module}/${state}/${district}`)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap.xml/route.ts:47

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`/seo/${module}/${state}/${district}/${city.citySlug}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo-sitemap.xml/route.ts:62

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`/seo/${module}/${state}/${district}/${city.citySlug}/${locality}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:10

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
import { getModuleKeywordGroups } from "@/lib/seo/module-keywords";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:46

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:47

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:48

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:49

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:53

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function modulePath(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:54

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "/property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:55

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "/materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:56

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "/services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:61

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:64

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:76

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:87

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:88

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? params.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:104

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = `${moduleTitle(module)} in ${locality}, ${geo.city} | 3Bigha`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:107

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:110

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const path = `/seo/${module}/${params.state}/${params.district}/${params.city}/${params.locality}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:129

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:130

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? params.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:145

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = moduleTitle(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:151

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const canonicalUrl = `${siteConfig.url}/seo/${module}/${params.state}/${params.district}/${params.city}/${params.locality}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:154

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:160

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywordGroups = getSeoKeywordGroups(module, locality);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:161

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const regionalKeywordGroups = getRegionalKeywordGroups(module, locality);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:162

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleKeywordGroups = getModuleKeywordGroups(module, locality);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:165

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:174

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
currentModule: module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:182

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:188

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:200

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:204

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}/${params.district}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:208

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}/${params.district}/${params.city}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:232

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
seoRouteParams.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:364

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={modulePath(module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:390

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:428

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Local buyer demand",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:553

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?module=${module}&q=${encodeURIComponent(locality)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:559

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?module=${module}&q=${encodeURIComponent(locality)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:564

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: modulePath(module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:688

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Explore hyperlocal marketplace searches, vendor discovery, RFQ intent
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:702

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...keywordGroups.vendor.slice(0, 3),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:705

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
...regionalKeywordGroups.supplier.slice(0, 6),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:715

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/search?module=${module}&q=${encodeURIComponent(item.keyword)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:748

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/rfq?module=${module}&q=${encodeURIComponent(item.keyword)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:814

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: item.intent === "module"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:864

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}/${params.district}/${params.city}/${item}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:908

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}/${params.district}/${params.city}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:922

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}/${params.district}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/[locality]/page.tsx:936

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:15

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
import { getModuleKeywordGroups } from "@/lib/seo/module-keywords";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:32

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:43

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:47

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
getSeoCategories(module).map((category) => ({
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:48

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:59

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!isSeoModule(params.module)) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:63

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module: SeoModule = params.module;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:71

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const category = getSeoCategory(module, params.category);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:77

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const canonicalUrl = `${siteConfig.url}/seo/${module}/${params.state}/${params.district}/${params.city}/category/${params.category}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:80

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:89

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:96

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywordGroups = getModuleKeywordGroups(module, geo.city);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:100

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:106

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:111

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:118

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
currentModule: module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:125

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:130

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const relatedCategories = getSeoCategories(module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:135

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:144

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ name: module, url: `${siteConfig.url}/seo/${module}/${params.state}` },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:147

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}/${params.district}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:151

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}/${params.district}/${params.city}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:163

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
seoRouteParams.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:263

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/category/[category]/page.tsx:496

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}/${params.district}/${params.city}/category/${item.slug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:17

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
import { getModuleKeywordGroups } from "@/lib/seo/module-keywords";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:37

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:51

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:52

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:53

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:54

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:58

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function modulePath(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:59

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "/property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:60

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "/materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:61

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "/services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:66

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:71

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:72

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return `Find land, plots, flats, houses, commercial property, builder projects and real estate opportunities in ${city}, ${district}, ${state}. 3Bigha helps local buyers, sellers, builders and agents connect through an AI-assisted regional…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:75

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:79

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:86

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function seoBullets(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:87

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:90

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"Builder and project property support",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:91

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`, `seller`

**Current system**

```text
"Buyer-seller enquiry workflow",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:96

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:99

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Local vendor discovery",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:105

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:123

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:130

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = `${moduleTitle(module)} in ${city}, ${district}, ${state} | 3Bigha`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:131

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = moduleDescription(module, city, district, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:132

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const path = `/seo/${module}/${params.state}/${params.district}/${params.city}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:156

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:163

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = moduleTitle(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:164

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = moduleDescription(module, city, district, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:165

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const canonicalUrl = `${siteConfig.url}/seo/${module}/${params.state}/${params.district}/${params.city}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:167

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const bullets = seoBullets(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:168

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const regionalContent = getRegionalSeoContent(module, city, district, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:169

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const discoveryItems = getRegionalDiscoveryItems(module, city, district);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:170

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const marketData = getRegionalMarketData(module, city, district);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:171

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const liveMarketplace = await getLiveMarketplaceData(module, city, district);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:172

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywordGroups = getSeoKeywordGroups(module, city);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:173

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const regionalKeywordGroups = getRegionalKeywordGroups(module, city);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:174

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleKeywordGroups = getModuleKeywordGroups(module, city);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:179

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:188

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:199

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:203

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}/${params.district}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:218

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
seoRouteParams.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:325

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={modulePath(module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:464

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${item.stateSlug}/${item.districtSlug}/${item.citySlug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:500

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={item.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:511

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleTitle(item.module)} in {city}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:548

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${params.state}/${params.district}/${params.city}/${locality}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:632

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?module=${module}&q=${encodeURIComponent(city)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:637

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?module=${module}&q=${encodeURIComponent(city)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:642

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: modulePath(module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:807

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={getRegionalSearchUrl(item, module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:821

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={getRegionalRfqUrl(item, module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:835

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:873

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Explore long-tail local marketplace searches, vendor discovery,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:887

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...keywordGroups.vendor.slice(0, 3),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:890

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
...regionalKeywordGroups.supplier.slice(0, 6),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:900

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/search?module=${module}&q=${encodeURIComponent(item.keyword)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/[city]/page.tsx:933

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/rfq?module=${module}&q=${encodeURIComponent(item.keyword)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:21

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:34

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:35

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:36

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:37

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:41

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function modulePath(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:42

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "/property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:43

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "/materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:44

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "/services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:48

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function popularSearches(module: SeoModule, location: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:49

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:59

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:69

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:89

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:93

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:94

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return `Explore land, plots, flats, houses, commercial property and builder projects across ${district}, ${state}. 3Bigha helps buyers, sellers, builders and local agents connect through an AI-powered regional property marketplace.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:94

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
return `Explore land, plots, flats, houses, commercial property and builder projects across ${district}, ${state}. 3Bigha helps buyers, sellers, builders and local agents connect through an AI-powered regional property marketplace.`;
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:97

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:101

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:111

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:119

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = `${moduleTitle(module)} in ${district}, ${state} | 3Bigha`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:120

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = districtDescription(module, district, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:121

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const path = `/seo/${module}/${params.state}/${params.district}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:145

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:154

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = moduleTitle(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:155

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = districtDescription(module, district, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:161

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const relatedModules = seoModules.filter((item) => item !== module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:162

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const relatedCategories = getSeoCategories(module).slice(0, 12);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:170

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const searches = popularSearches(module, district);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:174

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: "Marketplace Module", value: title },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:180

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: `Search ${title} in ${district}`, href: `/search?module=${module}&q=${encodeURIComponent(district)}` },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:181

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: `Post ${title} Requirement`, href: `/rfq?module=${module}&q=${encodeURIComponent(district)}` },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:182

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: `Browse Vendors in ${district}`, href: `/vendor/discovery?q=${encodeURIComponent(title)}&district=${encodeURIComponent(district)}` },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:193

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const canonicalUrl = `${siteConfig.url}/seo/${module}/${params.state}/${params.district}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:196

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:206

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `${siteConfig.url}/seo/${module}/${params.state}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:230

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
seoRouteParams.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:365

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={modulePath(module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:391

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:578

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${city.stateSlug}/${city.districtSlug}/${city.citySlug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:656

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha connects local discovery, RFQ submission, vendor responses,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/[district]/page.tsx:695

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${categoryBaseGeo.stateSlug}/${categoryBaseGeo.districtSlug}/${categoryBaseGeo.citySlug}/category/${item.slug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/seo/[module]/[state]/page.tsx:19

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:31

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:32

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:33

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:34

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:38

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function modulePath(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:39

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "/property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:40

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "/materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:41

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "/services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:45

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function popularSearches(module: SeoModule, location: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:46

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:56

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:66

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:85

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function stateDescription(module: SeoModule, state: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:86

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:87

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
return `Explore land, plots, flats, houses, commercial property and builder projects across ${state}. 3Bigha helps buyers, sellers, builders and agents connect through an AI-powered regional property marketplace.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/page.tsx:87

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
return `Explore land, plots, flats, houses, commercial property and builder projects across ${state}. 3Bigha helps buyers, sellers, builders and agents connect through an AI-powered regional property marketplace.`;
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/seo/[module]/[state]/page.tsx:90

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:94

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:104

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:109

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = `${moduleTitle(module)} in ${state} | 3Bigha`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:110

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = stateDescription(module, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:111

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const path = `/seo/${module}/${params.state}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:135

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = isSeoModule(params.module) ? params.module : "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:140

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = moduleTitle(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:141

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const description = stateDescription(module, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:162

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const relatedModules = seoModules.filter((item) => item !== module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:163

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const relatedCategories = getSeoCategories(module).slice(0, 12);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:167

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const searches = popularSearches(module, state);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:169

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const canonicalUrl = `${siteConfig.url}/seo/${module}/${params.state}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:187

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/page.tsx:199

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
seoRouteParams.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:306

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={modulePath(module)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:333

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/page.tsx:386

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${item.stateSlug}/${item.districtSlug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/seo/[module]/[state]/page.tsx:464

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha connects state-wide discovery, RFQ submission, vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/seo/[module]/[state]/page.tsx:503

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/seo/${module}/${categoryBaseGeo.stateSlug}/${categoryBaseGeo.districtSlug}/${categoryBaseGeo.citySlug}/category/${item.slug}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/[id]/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/[id]/page.tsx:148

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:226

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/[id]/page.tsx:229

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
type: row.custom_service || row.provider_kind || "Service Provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:248

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:250

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
name: clean(row.provider_name) || "3Bigha Service Provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:278

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?module=services&q=${encodeURIComponent(name)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/[id]/page.tsx:282

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(name)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:374

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`${name} service available through 3Bigha. Connect with local providers, compare options and submit your requirement for faster vendor response.`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:386

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
["Provider", clean(row.provider_name) || "3Bigha Provider"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:419

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/rfq?module=services&q=${encodeURIComponent(name)}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/[id]/page.tsx:433

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(name)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/[id]/page.tsx:456

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit={5}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/add/page.tsx:22

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "@/components/marketplace/vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:86

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// new: pricing & terms (stored into service_description; also influences min/max_price)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:419

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
throw new Error("Failed to save provider service after removing missing schema columns.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:467

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
const [provider, setProvider] = useState<ProviderRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:518

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/add/page.tsx:520

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 8,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:619

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `Write a unique, service-specific scope of work for "${serviceName}". Include exact work items, inclusions, exclusions, tools/material responsibility, work process, quality checks, and buyer instructions. Do not write a generic descripti…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:622

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
: "Write a short refund and cancellation policy for a local service provider. Keep it fair, practical, and legally safe. Do not promise refunds unless conditions are stated.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:630

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "service",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/add/page.tsx:632

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
tone: "professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:648

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
`Do not write a generic service provider description.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:720

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
// ---------- description builder (stores all extra fields without schema changes) ----------
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:749

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// pricing summary (existing)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:753

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
parts.push(`Pricing (primary): ${rateLabel(d.rate_type)} ₹${d.rate_min ?? "…"}–₹${d.rate_max ?? "…"} ${unit}`);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:755

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
parts.push(`Pricing (primary): ${rateLabel(d.rate_type)} ₹${d.rate_value ?? "…"} ${unit}`);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:759

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// new pricing terms
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:821

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
// ---------- provider: guarantee provider row exists ----------
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:869

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:984

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
Open Turnkey Wizard →
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/add/page.tsx:1113

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
const providerId = provider?.id ?? (await ensureProviderId());
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1143

**Area:** Application Route

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1157

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const geoRes = await fetch("/api/admin/geography/resolve", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1226

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/add/page.tsx:1255

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
setErr(error.message || "Failed to save provider service.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1321

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (!provider?.id) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1325

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<SectionHeader title="Add / List Your Services" subtitle="Please complete your provider profile first." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1329

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 6 }}>Provider profile required</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1331

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
To list services, you must have a Service Provider profile linked to your account.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1340

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href={`/onboarding/business?returnTo=${encodeURIComponent("/services/add")}`}
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/add/page.tsx:1343

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Complete Provider Profile →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1371

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
subtitle="Wizard: select services → fill details → review & save. Public visibility depends on provider status = published."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1371

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
subtitle="Wizard: select services → fill details → review & save. Public visibility depends on provider status = published."
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/add/page.tsx:1376

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{/* Provider + Stepper */}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1381

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<div style={{ fontWeight: 900, fontSize: 16 }}>Your Provider Account</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1383

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Provider status: <b>{provider.status ?? "—"}</b>{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1384

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.status?.toLowerCase() !== "published" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1417

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`, `professional`

**Current system**

```text
Tip: For professional services, a provider photo helps customers choose faster. Update your provider profile if needed.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1434

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<EmptyState message="No provider services yet. Add your first listings using the wizard below." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:1434

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
<EmptyState message="No provider services yet. Add your first listings using the wizard below." />
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/add/page.tsx:1890

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Selection is already done in Step 1. Here you only fill work details, pricing, terms and payments.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:2153

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
{/* Pricing & terms */}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:2155

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Service Pricing & Work Terms</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:2476

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/add/page.tsx:2491

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Primary Pricing Model (for sorting / filters)</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/add/page.tsx:2602

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Choose admin templates and set your own price per sqft. Scope/spec is template-driven.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:2643

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin guidance: {guidance}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/add/page.tsx:2778

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Primary pricing: <b>{rateLabel(d.rate_type)}</b> {pricePreview}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/layout.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"building contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/layout.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"civil contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/layout.tsx:18

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"labour contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/my/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/my/page.tsx:65

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `canpublish`

**Current system**

```text
function canPublish(status: RecordStatus) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/services/my/page.tsx:239

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/my/page.tsx:299

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/my/page.tsx:729

**Area:** Application Route

**Category:** Capability and Permission

**Matched:** `canpublish`

**Current system**

```text
{canPublish(r.record_status) ? (
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — app/services/page.tsx:22

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
type ServiceGroup = "Professional / Skilled" | "Legal";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:62

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
{ key: "Professional / Skilled", label: "Professional / Skilled" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:108

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
group: "Professional / Skilled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:115

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
group: "Professional / Skilled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:122

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
group: "Professional / Skilled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:129

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
group: "Professional / Skilled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:151

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
["Submit Service Requirement", "/rfq?module=services"],
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/page.tsx:277

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
const [group, setGroup] = useState<ServiceGroup>("Professional / Skilled");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:388

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
if (!r.segment && !r.segment_slug) return group === "Professional / Skilled";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:434

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
const title = group === "Legal" ? "Legal Services" : "Professional / Skilled Services";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:456

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<PublicVendorOpportunityBanner module="services" />
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/page.tsx:670

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<Badge>{r.provider_kind ?? "Service Provider"}</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:688

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
["Provider", r.provider_name ? "Listed" : "Pending"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:690

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
["Pricing", priceText ? "Available" : "Ask provider"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:690

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
["Pricing", priceText ? "Available" : "Ask provider"],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/page.tsx:719

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{r.provider_name ? <span>Provider: {r.provider_name}</span> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/page.tsx:726

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="service"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/services/providers/[slug]/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/providers/[slug]/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
setLoadErr("Missing provider slug in URL.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:128

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
setLoadErr(error.message || "Failed to load provider profile.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:142

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
const provider = useMemo(() => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:173

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
title={provider ? provider.name : "Provider"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:174

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
subtitle={provider ? "Provider profile and all active services posted by this provider." : "Loading provider…"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:202

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<EmptyState message="Loading provider profile…" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:204

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<EmptyState message={`Provider load failed: ${loadErr}`} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:205

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
) : !provider ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:206

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<EmptyState message="Provider not found (or no active services posted yet)." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:213

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<h2 style={{ margin: 0, lineHeight: 1.2 }}>{provider.name}</h2>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:215

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.kind ? <div>Kind: {provider.kind}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:216

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.location ? <div>Location: {provider.location}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:217

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.pincode ? <div>Pincode: {provider.pincode}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:218

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.phone ? <div>Phone: {provider.phone}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:219

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.email ? <div>Email: {provider.email}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:220

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.status ? <div>Status: {provider.status}</div> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:225

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<Badge>{provider.services_count} services</Badge>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:230

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.segments.slice(0, 6).map((s) => (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:233

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.categories.slice(0, 6).map((c) => (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:241

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<SectionHeader title="Services from this provider" subtitle="Open any service to see details, pricing, and description." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/[slug]/page.tsx:241

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<SectionHeader title="Services from this provider" subtitle="Open any service to see details, pricing, and description." />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/providers/[slug]/page.tsx:302

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{rows.length === 0 ? <EmptyState message="No active services found for this provider." /> : null}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:16

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
type ServiceGroup = "Professional / Skilled" | "Legal";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:78

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
{ key: "Professional / Skilled", label: "Professional / Skilled" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:99

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
const [group, setGroup] = useState<ServiceGroup>("Professional / Skilled");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:246

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
// Provider belongs to a group if ANY of its services rows had that segment name.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:248

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
if (!p.segments.length) return group === "Professional / Skilled";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:314

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
ariaLabel="Provider groups"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/providers/page.tsx:350

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<EmptyState message={`Provider load failed: ${loadErr}`} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/[packageID]/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/turnkey/[packageID]/page.tsx:25

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// vendor package fields
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/[packageID]/page.tsx:123

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
let q = (supabase as any).from("v_turnkey_packages_public").select("*").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/[packageID]/page.tsx:168

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
subtitle="Full scope, material specification, pricing and terms for this turnkey package."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/[packageID]/page.tsx:209

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Provider: <b>{row.provider_name ?? "—"}</b>{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/[packageID]/page.tsx:304

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Contact / View provider →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/turnkey/add/page.tsx:63

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// pricing
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:189

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (subgrade.includes("premium") || grade.includes("PREMIUM") || String(t?.template_code || "").endsWith("3")) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:190

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
return "premium";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:204

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:208

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "ISI/local non-branded TMT Fe500 subject to buyer approval and structural requirement.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:211

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:212

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Premium OPC 53 / PPC cement from reputed brands as per work stage requirement."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:218

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:219

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Premium first-class brick / AAC block / fly-ash block as mutually finalized."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:252

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:253

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Premium vitrified tiles / equivalent flooring range as finalized."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:263

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:274

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:275

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Branded CPVC/UPVC plumbing lines with premium sanitary fittings as finalized."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:285

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:286

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Premium flush doors / laminated doors / aluminium or UPVC windows as per package."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:296

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:297

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? "Premium interior/exterior paint system with primer and putty as applicable."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:307

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Land cost, mutation, conversion, plan sanction fees, architect/engineer fees and government charges.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:308

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Deep piling, soil testing, boundary wall, gate, extra height, premium elevation, modular kitchen and furniture.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:356

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
const [provider, setProvider] = useState<ProviderRow | null>(null);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:517

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
const providerId = provider?.id ?? (await ensureProviderId());
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:608

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (!provider?.id) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:612

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<SectionHeader title="Turnkey House Construction" subtitle="Please complete your provider profile first." />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:616

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 6 }}>Provider profile required</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:618

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
To list turnkey packages, you must have a Service Provider profile linked to your account.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:627

**Area:** Application Route

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href={`/onboarding/business?returnTo=${encodeURIComponent("/services/turnkey/add")}`}
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/turnkey/add/page.tsx:630

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Complete Provider Profile →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:653

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<SectionHeader title="Turnkey House Construction" subtitle="Wizard: select packages → fill pricing & terms → review & publish." />
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:653

**Area:** Application Route

**Category:** Onboarding

**Matched:** `wizard`

**Current system**

```text
<SectionHeader title="Turnkey House Construction" subtitle="Wizard: select packages → fill pricing & terms → review & publish." />
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — app/services/turnkey/add/page.tsx:661

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<div style={{ fontWeight: 900, fontSize: 16 }}>Your Provider Account</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:663

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Provider status: <b>{provider.status ?? "—"}</b>{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:664

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
{provider.status?.toLowerCase() !== "published" ? (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:694

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
Enable the packages you want to offer. Then go to Step 2 to set pricing and terms.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:727

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
<div style={{ color: "#5b6472", fontSize: 12, fontWeight: 700 }}>Admin guidance: {guidance}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:901

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, fontSize: 16 }}>Step 2: Pricing & Terms</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:920

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Helps contractor understand approximate project value before entering per sq.ft rate.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:965

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
<option value="premium">Premium</option>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/add/page.tsx:1112

**Area:** Application Route

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Contractor should check material grade, TMT, cement, brick/block, electrical,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/add/page.tsx:1185

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 8 }}>Pricing</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/page.tsx:5

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/turnkey/page.tsx:224

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 6 }}>Quick navigation</div>
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/services/turnkey/page.tsx:252

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Provider: <b>{p?.display_name ?? "—"}</b> • {formatLocation(p)}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/page.tsx:293

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
View provider →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/page.tsx:322

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Provider: <b>{openRow.service_providers?.display_name ?? "—"}</b> • {formatLocation(openRow.service_providers)}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/services/turnkey/page.tsx:332

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<div style={{ fontWeight: 900, marginBottom: 6 }}>Pricing</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/services/turnkey/page.tsx:410

**Area:** Application Route

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
Contact / View Provider →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/settings/page.tsx:6

**Area:** Application Route

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
"Manage your 3bigha experience mode, visibility, notifications and workspace preferences.",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/settings/page.tsx:31

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Notifications", "RFQ alerts, vendor replies, project updates and price alerts."],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/signup/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/sitemap.ts:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorOpportunityUrls } from "@/lib/seo/vendor-opportunity-seo";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/sitemap.ts:9

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorDemandSitemapEntries, getMarketDemandSitemapEntries } from "@/lib/seo/vendor-demand-seo";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/sitemap.ts:53

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:216

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:223

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:228

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:233

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:239

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/sitemap.ts:244

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/support/my/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/support/my/page.tsx:73

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (s === "in_review") return "Under admin review";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/my/page.tsx:189

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
subtitle="Track your written complaints, ticket status and admin replies."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/my/page.tsx:247

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Raise a written ticket whenever you face a listing, login, buyer,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/my/page.tsx:248

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor, RFQ, chat or technical issue.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/my/page.tsx:336

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin Reply
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/my/page.tsx:363

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
Admin reply is pending. Please wait and track this ticket number.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/new/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/support/new/page.tsx:29

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ value: "vendor", label: "Vendor Issue" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/new/page.tsx:30

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{ value: "buyer", label: "Buyer Issue" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/new/page.tsx:83

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(profile?.is_vendor ? "vendor" : "user")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/new/page.tsx:243

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<ActionButton href="/dashboard" variant="secondary">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/support/new/page.tsx:244

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Back to Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/support/new/page.tsx:350

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Example: login failed, listing not showing, buyer issue, wrong price showing..."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/new/page.tsx:402

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="support"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/support/ticket/[ticketId]/page.tsx:4

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useParams, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/support/ticket/[ticketId]/page.tsx:82

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (s === "in_review") return "Under admin review";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/ticket/[ticketId]/page.tsx:481

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
{m.is_admin_message ? "Admin Support" : "You"} ·{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/support/ticket/[ticketId]/page.tsx:542

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module="support"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/terms-and-conditions/page.tsx:50

**Area:** Application Route

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
<li>Respect other users and maintain professional conduct</li>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/terms-and-conditions/page.tsx:53

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<h2 style={{ marginTop: 16 }}>Vendor and listing responsibility</h2>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/terms-and-conditions/page.tsx:56

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
the accuracy of their listings, pricing, availability and
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/test/[id]/page.tsx:1

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { notFound } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/ui-kit.css:1012

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
/* Premium hero logo + mobile header cleanup */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/ui-kit.css:1067

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
/* Desktop hero logo premium alignment */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/ui-kit.css:1302

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
/* Premium desktop hero balance */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/ui-kit.css:1342

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
/* ===== PREMIUM HERO POLISH ===== */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/ui-kit.css:1398

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
/* Compact right hero panel after premium polish */
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor-inbox/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/vendor-inbox/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-inbox/page.tsx:3

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/vendor-opportunities/[state]/[district]/[place]/page.tsx:3

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorOpportunitySeoPage } from "@/lib/seo/vendor-opportunity-page";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/[place]/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const description = "See active vendor demand for suppliers, services, rentals and property businesses on 3Bigha.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/[place]/page.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alternates: { canonical: `${siteConfig.url}/vendor-opportunities/${params.state}/${params.district}/${params.place}` },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/[place]/page.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
openGraph: { title, description, url: `${siteConfig.url}/vendor-opportunities/${params.state}/${params.district}/${params.place}`, type: "website" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/page.tsx:3

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorOpportunitySeoPage } from "@/lib/seo/vendor-opportunity-page";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const title = `Vendor opportunities in ${params.district.replace(/-/g, " ")} | 3Bigha`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const description = "Find active local vendor requirements and business gaps on 3Bigha.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/page.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alternates: { canonical: `${siteConfig.url}/vendor-opportunities/${params.state}/${params.district}` },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/[district]/page.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
openGraph: { title, description, url: `${siteConfig.url}/vendor-opportunities/${params.state}/${params.district}`, type: "website" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/page.tsx:3

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { VendorOpportunitySeoPage } from "@/lib/seo/vendor-opportunity-page";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const title = `Vendor opportunities in ${params.state.replace(/-/g, " ")} | 3Bigha`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const description = "Find active vendor requirements for property, materials, services and rentals on 3Bigha.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/page.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
alternates: { canonical: `${siteConfig.url}/vendor-opportunities/${params.state}` },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/[state]/page.tsx:12

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
openGraph: { title, description, url: `${siteConfig.url}/vendor-opportunities/${params.state}`, type: "website" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { PublicOpportunityModule } from "@/components/marketplace/public-vendor-opportunity-utils";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:14

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await supabase.from(table).select("id,name").limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor-opportunities/page.tsx:30

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Become a Vendor on 3Bigha | Vendor Opportunities",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:32

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Discover public vendor opportunities on 3Bigha for suppliers, contractors, service providers, rental owners and property sellers.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:40

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,module,category,promotion_score,promotion_type,shortage_score,geo_state_id,geo_district_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor-opportunities/page.tsx:42

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.in("module", ["property", "materials", "services", "rentals"])
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor-opportunities/page.tsx:44

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(24),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor-opportunities/page.tsx:71

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Public Vendor Opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:79

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
These opportunities are generated from marketplace demand areas. Internal scores and admin intelligence remain private.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor-opportunities/page.tsx:101

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module={row.module as PublicOpportunityModule}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor-opportunities/page.tsx:133

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
No public vendor opportunities are available right now. Please check again soon.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorTrustSignals } from "@/lib/seo/vendor-trust-signals";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:5

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildRelatedVendorEntities } from "@/lib/seo/vendor-related-entities";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorAuthorityDataBySlug } from "@/lib/seo/vendor-authority-data";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:7

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorReputationData } from "@/lib/seo/vendor-reputation-data";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:8

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { calculateVendorLeaderboardScore } from "@/lib/seo/vendor-leaderboard-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:9

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { calculateVendorRecommendationScore } from "@/lib/seo/vendor-recommendation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:10

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorRecommendationClusters } from "@/lib/seo/vendor-recommendation-graph";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:11

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorRecommendationCandidates } from "@/lib/seo/vendor-recommendation-data";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:13

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorInternalLinks } from "@/lib/seo/vendor-internal-links";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:18

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/lib/seo/vendor-authority-graph";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:20

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorAuthorityJsonLd } from "@/lib/seo/vendor-authority-jsonld";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:21

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorTrustReputation } from "@/lib/vendors/vendor-trust-reputation";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:41

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: `${vendorName} | Vendor Authority | 3Bigha`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:42

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
description: `${vendorName} marketplace authority profile, services, materials, locations, trust signals and supplier expertise on 3Bigha.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:100

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"construction materials supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:101

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"best building materials vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:116

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
category: summary.categories[0] || "Marketplace Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:138

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
category: summary.categories[0] || "Marketplace Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:211

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
url: `https://www.3bigha.com/vendor/${params.slug}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:249

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id="vendor-authority-jsonld"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:268

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:290

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Authority Score
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:300

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI Vendor Trust
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:567

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Recommended Vendor Clusters
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:571

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
AI-generated supplier clusters based on locality, category, material, service and trust intelligence.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:586

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{cluster.vendors.map((vendor) => (
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:588

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
key={vendor.vendorId}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:589

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/${vendor.slug}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:593

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendor.businessName}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:597

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendor.locality || vendor.city || vendor.district}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:601

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Recommendation: {vendor.recommendationScore}/100
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:605

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendor.recommendationReason}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/[slug]/page.tsx:640

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Related Vendor Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:10

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorTrustReputation } from "@/lib/vendors/vendor-trust-reputation";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:20

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor/discovery/page.tsx:69

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: searchParams.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor/discovery/page.tsx:90

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI Vendor Discovery
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:143

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI Vendor Discovery Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:153

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai verified`

**Current system**

```text
<div className="mt-2 text-lg font-black text-white">AI Verified Matching</div>
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/vendor/discovery/page.tsx:158

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div className="mt-2 text-lg font-black text-white">Fast Vendor Priority</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:207

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{rankedVendors.map((vendor, index) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:208

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const score = vendor.recommendationScore;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:211

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
city: vendor.city,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:212

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
locality: vendor.locality,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:213

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
district: vendor.district,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:221

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
key={vendor.vendorId}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:227

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{index === 0 ? "🏆 Top AI Recommended" : "🤖 AI Ranked Vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:231

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendor.businessName}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:241

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{[vendor.locality, vendor.city, vendor.district]
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:262

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{vendor.recommendationReason}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:278

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/${vendor.slug}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:281

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View Vendor →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:285

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/rfq?query=${encodeURIComponent(query)}&vendor=${encodeURIComponent(vendor.vendorId)}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/discovery/page.tsx:288

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Start RFQ with this vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/vendor/inbox-v2/[rfqId]/chat/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:5

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorRfqChatBox from "@/app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:66

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/vendor/inbox-v2/${encodeURIComponent(rfqId)}`} style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:101

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:124

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Buyer ↔ Vendor conversation for RFQ #{rfqId.slice(0, 8)}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:129

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/vendor/inbox-v2/${encodeURIComponent(rfqId)}`} style={{ fontWeight: 800 }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/page.tsx:159

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div><strong>Buyer:</strong> {buyerName}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/chat/vendor-rfq-chat-box.tsx:666

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
current_page: `/vendor/inbox-v2/${rfqId}/chat`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/delivery-update-form.tsx:25

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch(`/api/vendor/rfq/${encodeURIComponent(rfqId)}/delivery-update`, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/delivery-update-form.tsx:115

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Write a short update for buyer..."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/delivery-update/route.ts:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/api/vendor/rfq/[rfqId]/delivery-update/route.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/delivery-update/route.ts:53

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// verify vendor has accepted target for this RFQ
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/delivery-update/route.ts:68

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ error: "Only the accepted vendor can update delivery schedule." },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:271

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:290

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
🎉 Your quotation has been <strong>ACCEPTED</strong> by the buyer.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:292

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer accepted your quotation. You can continue delivery coordination below.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:311

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
You can still continue buyer discussion and review quotation history below.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:335

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<strong>Buyer:</strong> {buyerName ?? "—"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:345

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<span className="trustChip">🟢 Buyer active recently</span>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:371

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{isAccepted ? "Next Steps" : "Buyer Communication"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:376

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/inbox-v2/${encodeURIComponent(rfqId)}/chat`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:379

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
💬 {isAccepted ? "Talk to Buyer" : "Talk to Buyer"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:384

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/inbox-v2/${encodeURIComponent(rfqId)}/print`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:403

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
📞 Call Buyer
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/page.tsx:411

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Tip: Continue buyer discussion to finalize rates, delivery schedule and requirement details."}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/print/page.tsx:1

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// app/vendor/inbox-v2/[rfqId]/print/page.tsx
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/print/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import PrintQuoteButton from "@/app/vendor/inbox-v2/[rfqId]/print/print-quote-button";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/print/page.tsx:124

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div><strong>Buyer:</strong> {(header as any).buyer_name ?? "—"}</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:72

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
// Pricing inputs (existing + extra)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:184

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:273

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:290

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setMsg({ kind: "err", text: "You are not logged in. Please login as vendor and try again." });
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:523

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
If buyer forgot to add something, you can add it here before submitting your quote.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/[rfqId]/quote-form.tsx:528

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
{/* Items pricing table */}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/page.tsx:3

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { fetchVendorInbox, fetchVendorInboxStats } from "@/lib/rfq/vendor-inbox/server";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:4

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { fetchVendorListingConversations } from "../../../lib/conversations/vendor-listing-inbox";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:6

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import VendorWorkMenu from "@/components/vendor-erp/VendorWorkMenu";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:147

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: pageSize,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/inbox-v2/page.tsx:199

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<h1 style={{ fontSize: 18, fontWeight: 800, margin: 0 }}>Vendor Inbox</h1>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:201

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
Reply to buyer messages, check RFQ conversations and continue vendor follow-up.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:223

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Direct buyer enquiries from Property, Materials, Services and Rentals.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:276

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const chatUrl = `/dashboard/vendor/chat/${encodeURIComponent(conversationId)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:276

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const chatUrl = `/dashboard/vendor/chat/${encodeURIComponent(conversationId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/vendor/inbox-v2/page.tsx:381

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer: {c.buyer_name ?? "Buyer"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/page.tsx:488

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
placeholder="Search RFQ / Buyer / Locality"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:5

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorInboxRow } from "@/lib/rfq/vendor-inbox/server";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:54

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
await fetch("/api/vendor/rfq/mark-viewed", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:64

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
await fetch("/api/vendor/rfq/mark-viewed-bulk", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:188

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<th style={{ padding: 12 }}>Module</th>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:189

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<th style={{ padding: 12 }}>Buyer</th>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:215

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const href = `/vendor/inbox-v2/${r.rfq_id}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/vendor-inbox-table.tsx:256

**Area:** Application Route

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
<td style={{ padding: 12 }}>{chip(r.module ?? "—")}</td>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:121

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:345

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
await fetch("/api/vendor/rfq/mark-viewed-bulk", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:372

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.channel("vendor-inbox-realtime")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:453

**Area:** Application Route

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<th style={{ padding: 8 }}>Buyer</th>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:472

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const chatHref = `/vendor/inbox-v2/${row.rfq_id}/chat`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:499

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href={`/vendor/inbox-v2/${row.rfq_id}`} style={{ textDecoration: "none" }}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox-v2/VendorInboxV2Client.tsx:630

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/inbox-v2/${row.rfq_id}`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/inbox/page.tsx:2

**Area:** Application Route

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/vendor/page.tsx:8

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha Vendor Marketplace
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/page.tsx:12

**Area:** Application Route

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
Discover trusted vendors, suppliers and service providers using AI-powered marketplace intelligence.
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — app/vendor/page.tsx:17

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor/discovery"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/page.tsx:20

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Explore AI Vendor Discovery
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/page.tsx:24

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/page.tsx:24

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/vendor/page.tsx:27

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Dashboard
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/page.tsx:27

**Area:** Application Route

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
Vendor Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — app/vendor/price-updates/new/page.tsx:9

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
type BoostPlanKey = "basic" | "premium" | "super";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:81

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Basic Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:86

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:87

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Premium Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:87

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
label: "Premium Vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:93

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Hub Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:108

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:112

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
Properties: ["Residential", "Commercial", "Road Side", "Premium Location"],
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:128

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
source_type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:167

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
const [boostPlan, setBoostPlan] = useState<BoostPlanKey>("premium");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:194

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:225

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:226

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:230

**Area:** Application Route

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
"master_admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:233

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:234

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:360

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
source_type: price.source_type || "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:452

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/vendor/price-updates", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:569

**Area:** Application Route

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
placeholder="Brand / Source, e.g. UltraTech, Local Supplier"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:748

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="vendor">Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:749

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<option value="builder">Builder</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:751

**Area:** Application Route

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<option value="property_builder">Property Builder</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:752

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<option value="hub_vendor">Hub Vendor</option>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:769

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
Request Subscription Priority
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:773

**Area:** Application Route

**Category:** Human Identity

**Matched:** `admin`, `buyer`

**Current system**

```text
is a subscription/admin-approved visibility benefit. Buyer
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:773

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
is a subscription/admin-approved visibility benefit. Buyer
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:775

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
buyer and vendor.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:783

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = boostPlans[key];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:797

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.label}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:800

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
₹{plan.price} · {plan.duration}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:803

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Priority {plan.priority}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:876

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — app/vendor/price-updates/new/page.tsx:876

**Area:** Application Route

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
⭐ Premium Vendor
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — app/vendor/price-updates/new/page.tsx:898

**Area:** Application Route

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Source: {price.source_type || "vendor"} • Added:{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/admin/geography/GeographyOperationsPanel.tsx:61

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/geography");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/admin/geography/GeographyOperationsPanel.tsx:103

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const res = await fetch("/api/admin/geography/manage", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/admin/MasterAdminDebugTools.tsx:42

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:21

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = buildAiExecutionPlan(input);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:23

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (!plan.show || !plan.actions.length) return null;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:35

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.title}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:40

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.subtitle}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:46

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<span className={`rounded-full border px-3 py-1 text-xs font-black ${urgencyClass(plan.urgency)}`}>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:47

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.urgency.toUpperCase()}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:51

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Score {plan.score}/100
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:60

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Stage: {plan.stage}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:63

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.signals.map((signal) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-execution/AIExecutionDrawer.tsx:74

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.actions.map((action) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/ai-operational/GlobalAiOperationalStatus.tsx:77

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-war-room"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:21

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor response delayed",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:23

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"One RFQ has not received a vendor reply for 18 hours.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:29

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:29

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:42

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-war-room",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:49

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Delivery delay probability increasing for one supplier.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:53

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
actionLabel: "Review Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ai-operational/OperationalRecoveryFeed.tsx:55

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/auth/AuthButtons.tsx:5

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/auth/AuthButtons.tsx:48

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/auth/AuthSwitch.tsx:5

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/auth/AuthSwitch.tsx:29

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/auth/FinalSubmitGateButton.tsx:3

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:8

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
label: "Buyer Work Desk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:9

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:9

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:10

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
help: "Main buyer control room",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:14

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:14

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:15

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
help: "Compare vendor quotations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:18

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
label: "Buyer Inbox",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:19

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/inbox",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:19

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/inbox",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:20

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
help: "Direct vendor conversations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:24

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/buyer/BuyerWorkMenu.tsx:25

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
help: "All buyer/vendor messages",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/buyer/BuyerWorkMenu.tsx:61

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer Master Menu
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/chat/ConversationContextBanner.tsx:53

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Source: {context.source} • {context.module || "marketplace"} • saved {ageLabel(context.timestamp)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ConversationContextBanner.tsx:72

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={`/vendor/discovery?q=${encodeURIComponent(context.query)}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/chat/ConversationContextBanner.tsx:73

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
context.module && context.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ConversationContextBanner.tsx:74

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `&module=${encodeURIComponent(context.module)}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ProcurementActionSuggestions.tsx:8

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function buildSuggestions(query: string, module: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ProcurementActionSuggestions.tsx:32

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ProcurementActionSuggestions.tsx:36

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
description: "Bulk procurement may reduce material pricing.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/chat/ProcurementActionSuggestions.tsx:41

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Convert this discussion into structured vendor comparison.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/chat/ProcurementActionSuggestions.tsx:46

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ProcurementActionSuggestions.tsx:53

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/chat/ProcurementActionSuggestions.tsx:70

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
context.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/construction-cost/BoqPreviewTable.tsx:68

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div>Vendor</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:58

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Convert this construction plan into RFQs
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:62

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
AI can split your construction plan into materials, services and rental
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:63

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
RFQ packages for vendor discovery.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:80

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{draft.module} • {draft.priority} priority
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:94

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href={`/rfq?module=${encodeURIComponent(
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/construction-cost/ConstructionAutoRfqPanel.tsx:95

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
draft.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/construction-cost/ConstructionCostCalculator.tsx:35

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
const GRADES: ConstructionGrade[] = ["economy", "standard", "premium"];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionCostCalculator.tsx:455

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
Adds extra time for premium finishing, fittings and interior coordination.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionCostCalculator.tsx:689

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
BOQ, RFQ or contractor negotiation.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionMilestoneTracker.tsx:41

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = useMemo(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionMilestoneTracker.tsx:66

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
useState<ConstructionMilestone[]>(plan.milestones);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionMilestoneTracker.tsx:209

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<b>Vendor:</b> {milestone.vendorCategory}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionMilestoneTracker.tsx:225

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.assumptions.slice(0, 4).map((item) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:6

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: ConstructionRecoveryPlan | null;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:17

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
export default function ConstructionRecoveryPanel({ plan, loading = false }: Props) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:27

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (!plan) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:52

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
severityClass[plan.overallSeverity] || severityClass.low
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:55

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.overallSeverity} risk
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:60

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<div className="text-sm font-black text-slate-900">Buyer warning</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:61

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<p className="mt-1 text-sm leading-6 text-slate-700">{plan.buyerWarning}</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:67

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.delayedMilestones.length}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:73

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.blockedMilestones.length}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:79

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.actions.filter((action) => action.automationReady).length}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:90

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.actions.map((action, index) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:111

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<p className="mt-1 text-sm leading-6 text-blue-900">{plan.procurementEscalation}</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:115

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<div className="text-sm font-black text-violet-950">Alternate vendor action</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionRecoveryPanel.tsx:117

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.alternateVendorSuggestion}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionRiskRadar.tsx:65

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
<RiskBar label="Contractor performance risk" value={forecast.contractorPerformanceRisk} />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:6

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: AutonomousConstructionSupervisorPlan | null;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:10

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
export default function ConstructionSupervisorConsole({ plan, loading = false }: Props) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:20

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (!plan) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:24

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<p className="mt-2 text-sm text-slate-500">No supervisor plan available.</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:42

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.mode.replaceAll("_", " ")}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:48

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div className="text-2xl font-black">{plan.alerts.length}</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:52

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<div className="text-2xl font-black">{plan.commands.length}</div>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:57

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.siteIntelligence.siteSupervisionNeed}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:63

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.futureAutonomousExecutionReady ? "Ready" : "Review"}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:71

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<p className="mt-1 text-sm leading-6 text-blue-900">{plan.ownerMessageDraft}</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:75

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
<div className="text-sm font-black text-violet-950">Contractor message draft</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:76

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
<p className="mt-1 text-sm leading-6 text-violet-900">{plan.contractorMessageDraft}</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:82

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.commands.map((command, index) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ConstructionSupervisorConsole.tsx:102

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{plan.siteIntelligence.dailyChecklist.map((item) => (
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/DrawingProcurementPackagePanel.tsx:56

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Drawing-based vendor package planning
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/DrawingProcurementPackagePanel.tsx:60

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Converts drawing intelligence into material groups, vendor categories
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/DrawingUploadPanel.tsx:171

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Floor Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/DrawingUploadPanel.tsx:175

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Room layout, apartment, residential plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/DrawingUploadPanel.tsx:231

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
PDF • Floor Plan • Image • DWG • DXF
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/DrawingUploadPanel.tsx:364

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
? "Floor Plan"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ProcurementExecutionTimeline.tsx:214

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
RFQ, vendor matching, material arrival and risk guidance
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ProcurementExecutionTimeline.tsx:250

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Arrival plan:
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ProcurementExecutionTimeline.tsx:282

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Next actions for site manager, contractor and procurement team
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ProcurementPhaseScheduler.tsx:83

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Automatically plan when to trigger materials, services, rentals and inspection RFQs.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/ProcurementPhaseScheduler.tsx:167

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<b className="text-slate-800">Vendor:</b> {trigger.vendorCategory}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/ProcurementStageCard.tsx:83

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Medium priority. Keep vendor and material confirmation ready."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction-cost/SaveConstructionProjectButton.tsx:102

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Save AI Construction Plan
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/SaveConstructionProjectButton.tsx:106

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Save this project to your dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/construction-cost/SaveConstructionProjectButton.tsx:110

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
Store this construction estimate, BOQ, material plan, timeline and
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction-cost/SaveConstructionProjectButton.tsx:111

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
procurement-ready project profile in your 3bigha dashboard.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/construction-cost/SavedConstructionProjectCard.tsx:71

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={`/dashboard/construction-projects/${encodeURIComponent(project.id)}`}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/construction-cost/TimelineEstimatePreview.tsx:124

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<b className="text-slate-800">Vendor:</b> {phase.vendorCategory}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:5

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
type ConstructionQuality = "economy" | "standard" | "premium";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:41

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (quality === "premium") return 1.28;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:85

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
quality === "economy" ? 1450 : quality === "premium" ? 2400 : 1850;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:179

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
href={`/services?${params.toString()}&q=construction contractor`}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:182

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
Find Contractor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/construction/ConstructionIntelligencePanel.tsx:187

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
This is practical planning guidance, not a final BOQ. Final estimate should be checked by an engineer, architect or experienced contractor.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/experience/ExperienceModeProvider.tsx:81

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<ExperienceModeContext.Provider value={value}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/experience/ExperienceModeProvider.tsx:83

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
</ExperienceModeContext.Provider>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/flow/ExecutiveFlowMemory.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/flow/ExecutiveFlowMemory.tsx:16

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
const storageKey = `3bigha_executive_flow:${pageKey || pathname || "dashboard"}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/geography/GeoSelector.tsx:65

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/GeoSelector.tsx:86

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: params.limit || 50,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/GeoSelector.tsx:95

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const merged = { limit: 50, offset: 0, ...params };
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/GeoSelector.tsx:110

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
url.searchParams.set("limit", String(merged.limit));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/GeoSelector.tsx:182

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: 50,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/NearbyMarketplace.tsx:10

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/NearbyMarketplace.tsx:77

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 5,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/NearbyMarketplace.tsx:96

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: String(limit),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/NearbyMarketplace.tsx:117

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}, [latitude, longitude, radiusKm, limit]);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/geography/NearbyMarketplace.tsx:142

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Section title="🏢 Nearby Vendors" items={data?.vendors} hrefBase="/vendor" />
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/language/AutoTranslatePage.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/language/LanguageSwitcher.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/language/TranslatedText.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/DesktopMegaNavClient.tsx:7

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { MENUS } from "@/lib/navigation/main-menu";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/DesktopMegaNavClient.tsx:23

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
aria-label="Marketplace mega navigation"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:311

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="btnGhost" href="/dashboard">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:312

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:341

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="subLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/GlobalHeaderClient.tsx:341

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="subLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:344

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="subLink" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/GlobalHeaderClient.tsx:344

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="subLink" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/GlobalHeaderClient.tsx:345

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:29

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Vendor Opportunities",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:31

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/vendor-opportunities",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:67

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/MarketplaceSidebar.tsx:68

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
preview: "Continue buyer and vendor conversations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:73

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:73

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/MarketplaceSidebar.tsx:74

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
preview: "Track requirements and vendor quotations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/MarketplaceSidebar.tsx:77

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
title: "My Dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/MarketplaceSidebar.tsx:79

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/MobileMegaNavClient.tsx:6

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { MENUS } from "@/lib/navigation/main-menu";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:6

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:33

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
name: "property" | "builder" | "materials" | "services" | "rentals" | "blog";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:52

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (name === "builder")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:157

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topBtn topBtnGhost" href="/dashboard">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:158

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:172

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href="/dashboard/buyer/enquiries"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:172

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/buyer/enquiries"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:181

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor/inbox-v2"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:184

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:206

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topMobileLink" href="/dashboard">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:207

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:210

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="topMobileLink" href="/vendor/inbox-v2">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:211

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:229

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:229

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/SiteHeader.tsx:234

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="topSubLink" href="/vendor/inbox-v2">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/SiteHeader.tsx:235

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:7

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:58

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/search?q=${enc}&module=${encodeURIComponent(scope)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/layout/TopHeaderClient.tsx:77

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const [dashboardHref, setDashboardHref] = useState("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:95

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:109

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
// MASTER ADMIN
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:115

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
setDashboardHref("/admin/dashboard");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:115

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/admin/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:124

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard/banker");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:128

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// VENDOR / HUB VENDOR
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:129

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const { data: vendor } = await supabase
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:130

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:136

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
setDashboardHref("/dashboard/vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:136

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard/vendor");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:140

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
// DEFAULT BUYER / USER
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:141

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:143

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
setDashboardHref("/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:186

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
sub.subscription.unsubscribe();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/layout/TopHeaderClient.tsx:241

**Area:** Shared UI

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
adminMode: !!json.isAdmin,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — components/layout/TopHeaderClient.tsx:244

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
// Silent: header alerts should never break navigation.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:354

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
<nav className="topNav" aria-label="Main navigation">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:416

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Post property (individual or builder project), list materials, services, rentals — or publish blog/news.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:428

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
Post an individual property, or list a builder project with multiple units/options.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:434

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
<Link className="postCardBtn" href="/property/builder/projects/add">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:435

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
List Builder Project →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:454

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="postCardBtn" href="/dashboard/vendor">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:454

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="postCardBtn" href="/dashboard/vendor">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:455

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Tools →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:468

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
List professional, legal, technical, and skilled services with pricing and coverage area.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:468

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
List professional, legal, technical, and skilled services with pricing and coverage area.
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/layout/TopHeaderClient.tsx:527

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
<div className="postCardDesc">Track buyer enquiries and vendor inbox conversations.</div>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:529

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="postCardBtn postCardBtnPrimary" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:529

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="postCardBtn postCardBtnPrimary" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:532

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="postCardBtn" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:532

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="postCardBtn" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:533

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:543

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="postMenuInlineLink" href="/dashboard">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:544

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Go to Dashboard →
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:724

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:728

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={supportAlerts.adminMode ? "/admin/dashboard/support" : "/support/my"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:728

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={supportAlerts.adminMode ? "/admin/dashboard/support" : "/support/my"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:829

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
Dashboard
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:861

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:861

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/buyer/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:864

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:864

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
<Link className="topSubLink" href="/dashboard/vendor/enquiries">
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/layout/TopHeaderClient.tsx:865

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Inbox
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:869

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
href={supportAlerts.adminMode ? "/admin/dashboard/support" : "/support/my"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/layout/TopHeaderClient.tsx:869

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={supportAlerts.adminMode ? "/admin/dashboard/support" : "/support/my"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:3

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function moduleLabel(module: PublicOpportunityModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:4

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property sellers / builders";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:5

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Materials suppliers";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:6

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Service providers";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:10

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function moduleCta(module: PublicOpportunityModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:11

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Become Property Partner";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:12

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
if (module === "materials") return "Become Supplier";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:12

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Become Supplier";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:13

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (module === "services") return "Join as Service Provider";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:13

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Join as Service Provider";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:25

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function opportunityIcon(module: PublicOpportunityModule, category?: string | null) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:26

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const text = `${module} ${category || ""}`.toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:31

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (text.includes("plot") || module === "property") return "🏡";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:32

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "🧱";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:33

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "🛠️";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:34

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return "🚜";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:38

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function vendorTypeText(module: PublicOpportunityModule, category?: string | null) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:41

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
if (module === "materials") return cleanCategory ? `${cleanCategory} Supplier` : "Materials Supplier";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:41

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return cleanCategory ? `${cleanCategory} Supplier` : "Materials Supplier";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:42

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (module === "services") return cleanCategory ? `${cleanCategory} Provider` : "Service Provider";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:42

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return cleanCategory ? `${cleanCategory} Provider` : "Service Provider";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:43

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (module === "rentals") return cleanCategory ? `${cleanCategory} Rental Provider` : "Equipment Rental Provider";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:43

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return cleanCategory ? `${cleanCategory} Rental Provider` : "Equipment Rental Provider";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/public-vendor-opportunity-utils.ts:44

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
return cleanCategory ? `${cleanCategory} Seller` : "Property Seller";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:11

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "./public-vendor-opportunity-utils";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:12

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "./vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:16

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: PublicOpportunityModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:26

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:28

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: PublicOpportunityModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:33

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:35

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:37

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:39

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:46

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch(`/api/public/vendor-opportunities?module=${module}`, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:46

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
fetch(`/api/public/vendor-opportunities?module=${module}`, {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:60

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [module]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:68

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:74

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
label: `Need ${vendorTypeText(module, row.category)} in ${row.location}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:84

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [rows, module]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:107

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
🚀 Vendor Opportunity
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:111

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleLabel(module)} required in active demand areas
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:115

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
Buyer demand is active in these locations. Verified vendors joining now may receive better visibility and local enquiries.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:129

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
Need {vendorTypeText(module, row.category)} in {row.location}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:137

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{opportunityLevel(row.priority)} · Need {row.vendorsNeeded} vendor{row.vendorsNeeded > 1 ? "s" : ""}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:145

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href="/onboarding/business"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:149

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:154

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
label: moduleCta(module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:163

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{moduleCta(module)} →
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:167

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/vendor-opportunities"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityBanner.tsx:171

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:10

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "./public-vendor-opportunity-utils";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:11

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "./vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:16

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: PublicOpportunityModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:30

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:40

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const typeText = vendorTypeText(module, category);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:42

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const icon = opportunityIcon(module, category);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:57

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:72

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
}, [finalOpportunityId, module, category, typeText, location, district, state, vendorsNeeded, priority]);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:133

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<strong style={{ color: "#0f172a" }}>Vendor Type:</strong> {typeText}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:146

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href="/onboarding/business"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:151

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:157

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Become a Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityCard.tsx:179

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Become a Vendor →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:5

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { trackVendorConversionClient } from "./vendor-conversion-client";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:15

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Become a Vendor on 3Bigha",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:39

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
AI powered 3Bigha vendor growth
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:39

**Area:** Shared UI

**Category:** Visible AI Terminology

**Matched:** `ai powered`

**Current system**

```text
AI powered 3Bigha vendor growth
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:50

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Become a Vendor on 3Bigha
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:70

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
href="/onboarding/business"
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:78

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Become a Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/PublicVendorOpportunityHero.tsx:91

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Become a Vendor
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/vendor-conversion-client.ts:10

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/marketplace/vendor-conversion-client.ts:36

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
navigator.sendBeacon("/api/marketplace/vendor-conversion", blob);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/marketplace/vendor-conversion-client.ts:40

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
fetch("/api/marketplace/vendor-conversion", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/mobile/MobileAppSearchBar.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname, useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobileForegroundNotifications.tsx:5

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobileOperationalDock.tsx:3

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobileOperationalDock.tsx:47

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Chat", href: "/dashboard/inbox-v2", count: chatCount },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobileOperationalDock.tsx:48

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
{ label: "RFQ", href: "/dashboard/buyer/rfqs", count: rfqCount },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/mobile/MobileOperationalDock.tsx:48

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "RFQ", href: "/dashboard/buyer/rfqs", count: rfqCount },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobileOperationalDock.tsx:49

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Ops", href: "/dashboard/procurement-os", count: procurementCount },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mobile/MobilePushRuntime.tsx:46

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
await fetch("/api/mobile/push/register", {
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/mobile/MobilePushRuntime.tsx:76

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
await PushNotifications.register();
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/mobile/MobilePushRuntime.tsx:78

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
PushNotifications.addListener("registration", async (token: Token) => {
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/mobile/MobilePushRuntime.tsx:84

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
console.error("Push registration error", error);
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/mos/DemandAroundMeCard.tsx:39

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// MOS card must never break vendor dashboard.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/mos/DemandAroundMeCard.tsx:39

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `vendor dashboard`

**Current system**

```text
// MOS card must never break vendor dashboard.
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/mos/MarketplaceOpportunityFeed.tsx:31

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const res = await fetch("/api/vendor/notifications?limit=5&type=marketplace_opportunity", {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/mos/MarketplaceOpportunityFeed.tsx:31

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const res = await fetch("/api/vendor/notifications?limit=5&type=marketplace_opportunity", {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/mos/MarketplaceOpportunityFeed.tsx:101

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href={r.data?.action_href || "/dashboard/vendor"}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/mos/MarketplaceOpportunityFeed.tsx:101

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href={r.data?.action_href || "/dashboard/vendor"}
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/operational-events/OperationalEventStream.tsx:35

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 5,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/operational-events/OperationalEventStream.tsx:38

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/operational-events/OperationalEventStream.tsx:44

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
setEvents(getOperationalEvents().slice(0, limit));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/operational-events/OperationalEventStream.tsx:56

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
}, [limit]);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/operational/UniversalDashboardShell.tsx:24

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit={5}
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/procurement/NegotiationInsightCard.tsx:50

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
Supplier Risk
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/procurement/ProcurementCommandPalette.tsx:117

**Area:** Shared UI

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Enterprise Procurement Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:128

**Area:** Shared UI

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Live Procurement Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:158

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-mission-control"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:169

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-anomaly"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:181

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-actions"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:193

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-copilot"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:205

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/procurement-live"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementCommandPalette.tsx:270

**Area:** Shared UI

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
Live Enterprise Procurement Workspace
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/procurement/ProcurementFollowupAgentCard.tsx:8

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementFollowupAgentCard.tsx:29

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{workflow.module}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementJourneyBar.tsx:40

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Pick up RFQ, vendor discovery, price checking or negotiation from this search.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/procurement/ProcurementMemoryTimeline.tsx:53

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Continue previous search, RFQ, vendor discovery or negotiation workflows.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/procurement/ProcurementMemoryTimeline.tsx:61

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module && item.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementMemoryTimeline.tsx:62

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `&module=${encodeURIComponent(item.module)}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementMemoryTimeline.tsx:68

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
key={`${item.query}-${item.module}-${item.timestamp}-${index}`}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementMemoryTimeline.tsx:85

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{item.module || "marketplace"} • {item.source || "workflow"} • {timeAgo(item.timestamp)}
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/procurement/ProcurementReEngagement.tsx:179

**Area:** Shared UI

**Category:** Visible AI Terminology

**Matched:** `ai score`

**Current system**

```text
Recovery AI score:
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — components/procurement/SupplierReliabilityCard.tsx:7

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/procurement/SupplierReliabilityCard.tsx:24

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
{item.supplier}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:17

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor-selection": "Vendor Selection",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:27

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor-selection",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:59

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return `/dashboard/buyer/rfqs`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:59

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/buyer/rfqs`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/project/ProjectWorkflowHub.tsx:62

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (workflow.currentStage === "vendor-selection") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:63

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/search?module=materials&q=construction materials`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/project/ProjectWorkflowHub.tsx:67

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/procurement-os`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/project/ProjectWorkflowHub.tsx:70

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/construction-projects`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/project/ProjectWorkflowHub.tsx:78

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (workflow.currentStage === "vendor-selection") return "Find vendors";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/project/ProjectWorkflowHub.tsx:80

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
return "Open project dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/pwa/RegisterServiceWorker.tsx:12

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
navigator.serviceWorker.register("/sw.js").catch(() => {});
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/sahaj/ProfessionalReview.tsx:7

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Professional review optional
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/sahaj/ProfessionalReview.tsx:14

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
<p>Estimate pricing and timeline</p>
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/sahaj/SahajIntentChooser.tsx:28

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
Choose the easiest option. 3Bigha will prepare the professional format.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/ProcurementDecisionPanel.tsx:43

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
RFQ success probability is estimated at {insight.rfqSuccessProbability}%. Recommended vendor count:{" "}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/ProcurementRecommendationSidebar.tsx:12

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/ProcurementRecommendationSidebar.tsx:15

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/ProcurementRecommendationSidebar.tsx:19

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/ProcurementRecommendationSidebar.tsx:73

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha detected related procurement, construction and vendor workflows for this search.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/UnifiedSearchAutocomplete.tsx:12

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/UnifiedSearchAutocomplete.tsx:17

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/UnifiedSearchAutocomplete.tsx:23

**Area:** Shared UI

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — components/search/VendorIntelligencePanel.tsx:4

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorIntelligenceInsight } from "@/lib/search/vendor-intelligence-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorIntelligencePanel.tsx:37

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Unified Vendor Intelligence Layer
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorIntelligencePanel.tsx:61

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{insight.reliabilityLabel} vendor score
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorIntelligencePanel.tsx:74

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Best Vendor", insight.bestVendorType],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorLiquidityPanel.tsx:4

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorLiquidityInsight } from "@/lib/search/vendor-liquidity-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorLiquidityPanel.tsx:44

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Liquidity Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorNegotiationPanel.tsx:4

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorNegotiationInsight } from "@/lib/search/vendor-negotiation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorNegotiationPanel.tsx:37

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Recommendation & Negotiation Intelligence
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/search/VendorNegotiationPanel.tsx:76

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Type", insight.recommendedVendorType],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ui/Toast.tsx:41

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
<ToastContext.Provider value={value}>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/ui/Toast.tsx:102

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
</ToastContext.Provider>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:10

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Dashboard", href: "/dashboard/vendor", icon: "🏠" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:10

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Dashboard", href: "/dashboard/vendor", icon: "🏠" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:11

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Inventory", href: "/dashboard/vendor/inventory", icon: "📦" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:11

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Inventory", href: "/dashboard/vendor/inventory", icon: "📦" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:12

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Billing", href: "/dashboard/vendor/billing", icon: "🧾" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:12

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Billing", href: "/dashboard/vendor/billing", icon: "🧾" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:13

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Fleet", href: "/dashboard/vendor/fleet", icon: "🚚" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:13

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Fleet", href: "/dashboard/vendor/fleet", icon: "🚚" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:14

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Dispatch", href: "/dashboard/vendor/dispatch", icon: "📍" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:14

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Dispatch", href: "/dashboard/vendor/dispatch", icon: "📍" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:20

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ label: "Buyer RFQs", href: "/dashboard/vendor/rfqs", icon: "📨" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:20

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Buyer RFQs", href: "/dashboard/vendor/rfqs", icon: "📨" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:21

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ label: "Buyer Enquiries", href: "/dashboard/vendor/enquiries", icon: "💬" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:21

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Buyer Enquiries", href: "/dashboard/vendor/enquiries", icon: "💬" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:22

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Inbox", href: "/dashboard/inbox-v2", icon: "📥" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:24

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
{ label: "Builder Projects", href: "/property/builder/projects", icon: "🏢" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:33

**Area:** Shared UI

**Category:** Onboarding

**Matched:** `onboarding`, `business profile`

**Current system**

```text
{ label: "Business Profile", href: "/onboarding/business", icon: "🏢" },
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — components/vendor-erp/VendorErpNav.tsx:34

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Subscription", href: "/dashboard/subscription", icon: "💳" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:34

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
{ label: "Subscription", href: "/dashboard/subscription", icon: "💳" },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/vendor-erp/VendorErpNav.tsx:35

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Boost Visibility", href: "/dashboard/subscription/boost", icon: "🚀" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:35

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
{ label: "Boost Visibility", href: "/dashboard/subscription/boost", icon: "🚀" },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/vendor-erp/VendorErpNav.tsx:41

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Supervisor", href: "/dashboard/vendor/inventory-intelligence", icon: "📌" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:41

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Supervisor", href: "/dashboard/vendor/inventory-intelligence", icon: "📌" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:42

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Notifications", href: "/dashboard/vendor/notifications", icon: "🔔" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:42

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Notifications", href: "/dashboard/vendor/notifications", icon: "🔔" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:73

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
3Bigha Vendor ERP
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:81

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:81

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/vendor/inventory-intelligence"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorErpNav.tsx:130

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(item.href !== "/dashboard/vendor" && pathname.startsWith(item.href));
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorErpNav.tsx:130

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
(item.href !== "/dashboard/vendor" && pathname.startsWith(item.href));
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:50

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `${missedFollowups} buyer follow-up pending`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:51

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Buyer follow-ups are clear",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:54

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Reply quickly before buyers move to another vendor."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:55

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "No urgent buyer reply pressure detected.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:56

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:56

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:66

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? "Open buyer leads and respond from one place."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:67

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "New buyer RFQs will appear here.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:68

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:68

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:81

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "AI will highlight strong buyer intent here.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:82

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:90

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `${unreadAlerts} unread vendor alert`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:95

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "No unread vendor alert at this moment.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:96

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/notifications",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:96

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/notifications",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:108

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
? "Improve replies, pricing and boost strength to appear higher."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/vendor-erp/VendorOperationStream.tsx:110

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorOperationStream.tsx:110

**Area:** Shared UI

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — components/vendor-erp/VendorOperationStream.tsx:139

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Today’s Vendor Operation Stream
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorOperationStream.tsx:155

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href="/dashboard/inbox-v2"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:4

**Area:** Shared UI

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { usePathname } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:7

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Work Desk", href: "/dashboard/vendor", help: "Main vendor control room" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:7

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Work Desk", href: "/dashboard/vendor", help: "Main vendor control room" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:8

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ label: "RFQs / Leads", href: "/dashboard/vendor/rfqs", help: "Buyer requirements" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:8

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "RFQs / Leads", href: "/dashboard/vendor/rfqs", help: "Buyer requirements" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:9

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
{ label: "Messages", href: "/dashboard/vendor/inbox", help: "Buyer conversations" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:9

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Messages", href: "/dashboard/vendor/inbox", help: "Buyer conversations" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:10

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Inventory", href: "/dashboard/vendor/inventory", help: "Stock items" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:10

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Inventory", href: "/dashboard/vendor/inventory", help: "Stock items" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:11

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Billing", href: "/dashboard/vendor/billing", help: "Invoice / challan" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:11

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Billing", href: "/dashboard/vendor/billing", help: "Invoice / challan" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:12

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Fleet", href: "/dashboard/vendor/fleet", help: "Vehicles" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:12

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Fleet", href: "/dashboard/vendor/fleet", help: "Vehicles" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:13

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Dispatch", href: "/dashboard/vendor/dispatch", help: "Delivery work" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:13

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Dispatch", href: "/dashboard/vendor/dispatch", help: "Delivery work" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:14

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Alerts", href: "/dashboard/vendor/notifications", help: "Important updates" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:14

**Area:** Shared UI

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
{ label: "Alerts", href: "/dashboard/vendor/notifications", help: "Important updates" },
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:31

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor Master Menu
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — components/vendor-erp/VendorWorkMenu.tsx:35

**Area:** Shared UI

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Control daily vendor work from one menu. Use this same menu on every vendor page.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:8

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
| "builder"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:12

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
| "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:28

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
isAdmin: boolean;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:30

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer: boolean;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:31

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
isVendor: boolean;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (v === "buyer") return "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (v === "vendor") return "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:45

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (v === "builder") return "builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
if (v === "master_admin") return "master_admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:72

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (v === "builder") return "property_builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:73

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
if (v === "author") return "blog_author";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:109

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
let isVendor = false;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:128

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
"business_profiles lookup"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:146

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
isVendor = true;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:155

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
isVendor = true;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:159

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
isVendor = false;
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:167

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `business_profiles`

**Current system**

```text
// Intentionally not inferring vendor access from business_profiles alone.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:171

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
// AUTO-HEAL: if onboarding is complete but grants are missing, rebuild them.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/access/resolveAccess.ts:302

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
isAdmin,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:304

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
isBuyer,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:305

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
isVendor,
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:314

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (access.isAdmin) return "/admin/dashboard";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:314

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (access.isAdmin) return "/admin/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/access/resolveAccess.ts:314

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isadmin`

**Current system**

```text
if (access.isAdmin) return "/admin/dashboard";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:315

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (access.isBlogAdmin) return "/admin/blog";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:317

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (access.isBuilder) return "/dashboard/builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:317

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (access.isBuilder) return "/dashboard/builder";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/access/resolveAccess.ts:319

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (access.isHubVendor) return "/vendor/hub";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:321

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isvendor`

**Current system**

```text
if (access.isVendor) {
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:326

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
if (c === "property_builder") return "/property/builder/projects";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:331

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (c === "investor") return "/dashboard/investor";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/access/resolveAccess.ts:334

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "/vendor/inbox-v2";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:337

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (access.isBuyer) return "/dashboard/buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/access/resolveAccess.ts:337

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (access.isBuyer) return "/dashboard/buyer";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/access/resolveAccess.ts:337

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `isbuyer`

**Current system**

```text
if (access.isBuyer) return "/dashboard/buyer";
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/access/resolveAccess.ts:340

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return "/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/activity/projectActivityMemory.ts:4

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:16

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function detectIntent(query: string, module: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials" ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "property" ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "services" ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
/mason|rajmistri|contractor|plumber|electrician|architect|labour|মিস্ত্রি|রাজমিস্ত্রি/.test(q),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "rentals" ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = cleanText(input.module || "all").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const intent = detectIntent(query, module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:119

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Convert this bulk material requirement into vendor quotations."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:121

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?query=${encoded}&module=materials`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:130

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=materials`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:130

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=materials`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:147

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
description: "Plan delivery, unloading and transport before final quote.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai-execution/execution-engine.ts:173

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}%20builder%20contractor&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:173

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}%20builder%20contractor&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:175

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:190

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
description: "Plan title, mutation and document verification.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai-execution/execution-engine.ts:191

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=${encoded}%20legal%20verification&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:207

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?query=${encoded}&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:216

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:216

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-engine.ts:218

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:243

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-engine.ts:257

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2?sort=unread",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/ai-execution/execution-engine.ts:266

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/ai-execution/execution-engine.ts:275

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/ai-execution/execution-engine.ts:288

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `/search?q=${encoded}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-timeline.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-timeline.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(input.module || "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-timeline.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
detail: module ? `${module} intent captured` : "Marketplace intent captured",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-timeline.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-match",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-timeline.ts:57

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Match",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-timeline.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
detail: hasVendors ? `${vendorCount} vendor signal(s)` : "Supplier response pending",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-timeline.ts:67

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
status: hasVendors || module === "materials" ? "done" : "active",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai-execution/execution-types.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-types.ts:15

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai-execution/execution-types.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/behavior-memory.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: "property" | "materials" | "services" | "rentals" | "rfq" | string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/behavior-memory.ts:50

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
function countTop(values: string[], limit = 5) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/behavior-memory.ts:59

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.slice(0, limit)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/behavior-memory.ts:90

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const hotModules = countTop(clean.map((e) => safe(e.module)));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/behavior-memory.ts:107

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
lastModule: last?.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/behavior-memory.ts:120

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/behavior-memory.ts:128

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: memory.hotModules[0] || fallback?.module || "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/build-ai-assistant.ts:18

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Compare vendors and pricing carefully",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/build-ai-assistant.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Review quotation risk and verify supplier reliability"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai/build-ai-assistant.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Prioritize vendor response and procurement execution"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai/build-ai-context.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input?.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:16

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: MarketplaceModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:42

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:113

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ctx.module || rfq.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:133

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ctx.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:156

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ctx.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:179

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ctx.module || "marketplace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:277

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
ctx.module ||
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:301

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: String(ctx.module || "marketplace"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/marketplace-orchestrator.ts:323

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: String(ctx.module || "marketplace"),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/memory-events.ts:22

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/memory-events.ts:78

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: clean(input.module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/memory-events.ts:155

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(300);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/memory-events.ts:168

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 5
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/memory-events.ts:180

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.slice(0, limit)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/memory-events.ts:188

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
rows.map((x) => x.module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-live-feed.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-live-feed.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = safe(row.module || "procurement");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-live-feed.ts:41

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: moduleName,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-live-feed.ts:58

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const rfq = events.filter((e) => e.module === "rfq").length;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-timeline-replay.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-timeline-replay.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (e.includes("vendor") || e.includes("match")) return "matched";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ai/procurement-timeline-replay.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = safe(row.module || "procurement");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/procurement-timeline-replay.ts:59

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: moduleName,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:22

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:50

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function hrefFor(module: string, id: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:59

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:63

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:67

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/${module}/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:205

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: hrefFor(input.module, safe(row.id)),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/recommendation-engine.ts:207

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/user-intelligence.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ai/user-intelligence.ts:34

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
function topValues(values: string[], limit = 5) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/user-intelligence.ts:43

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.slice(0, limit)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/ai/user-intelligence.ts:62

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const preferredModules = topValues(cleanSignals.map((s) => safe(s.module)));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/callbackClient/CallbackClient.tsx:4

**Area:** Business Logic

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { useRouter, useSearchParams } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/construction-cost/auto-rfq-builder.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: AutoConstructionRfqModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/auto-rfq-builder.ts:120

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/auto-rfq-builder.ts:132

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/auto-rfq-builder.ts:147

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Check material/vendor dependency and prepare procurement recovery or RFQ action.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Prepare alternate vendor backup",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Create alternate vendor shortlist for material/labour/equipment continuity.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
title: "Ask contractor for recovery commitment",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Ask contractor/supervisor to provide revised timeline, manpower plan, and daily progress commitment.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/autonomous-supervisor-engine.ts:98

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Ask contractor/supervisor to provide revised timeline, manpower plan, and daily progress commitment.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/boq-generator.ts:26

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (value === "economy" || value === "standard" || value === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/boq-generator.ts:118

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Rates are intentionally not fixed here because local market rates vary by location, vendor and brand.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:18

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: 1.12,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/boq-room-rules.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Earthwork Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "RCC Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:48

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Masonry Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Plaster Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Tile Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Painting Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:88

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Electrical Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Plumbing Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorCategory: "Door & Window Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/boq-room-rules.ts:118

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "General Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
: "Ask contractor/supervisor for recovery schedule.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:75

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Escalate blocker, check material/labour/vendor dependency, and prepare backup action.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:83

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
if (/cement|steel|tmt|sand|brick|material|supplier|vendor/.test(noteText)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Material/vendor dependency detected from milestone notes.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:91

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Prepare backup RFQ and alternate vendor shortlist for critical material supply.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:97

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
if (/labour|mason|worker|contractor|supervisor|manpower/.test(noteText)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:103

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
message: "Labour/contractor dependency detected from milestone notes.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-alert-engine.ts:105

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Ask contractor to confirm manpower strength and daily execution commitment.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-command-center.ts:20

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
modules: {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/construction-command-center.ts:64

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
? "Active recovery required. Escalate site, contractor, and procurement actions."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-command-center.ts:83

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Prepare backup material RFQ and vendor escalation."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-command-center.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
verification.photoVerificationRecommended ? "Ask contractor for milestone photos." : "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/construction-command-center.ts:91

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
modules: {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/cost-ai-prompts.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
3. Material upgrade suggestions
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:1

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
export type ConstructionGrade = "economy" | "standard" | "premium";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:45

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:46

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade: "premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
label: "Premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:49

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Higher quality construction with branded materials, better fittings and premium finishing.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-config.ts:82

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-engine.ts:150

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
baseEstimate.grade === "premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/cost-utils.ts:70

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
contractorMargin: "Contractor Margin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/cost-utils.ts:77

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (grade === "economy" || grade === "standard" || grade === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/dpr-generator.ts:134

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor quotations should include GST, transportation and unloading charges.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/drawing-visual-signals.ts:25

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
note: "Floor plans normally contain bedrooms, toilets, kitchens, living rooms and circulation spaces.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/grade-multipliers.ts:35

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/grade-multipliers.ts:36

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
grade: "premium",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/material-consumption-engine.ts:53

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
recommendedAction: `Verify ${material} stock, vendor delivery, and backup procurement readiness.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/material-estimator.ts:24

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (value === "economy" || value === "standard" || value === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/material-estimator.ts:75

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Final quantity depends on architectural plan, structural design, soil condition, wall thickness and site wastage.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/material-rules.ts:15

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: 1.08,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:84

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Actual progress should be updated by project owner, contractor or site supervisor.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/milestone-engine.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Delayed or blocked milestones should trigger procurement review and vendor follow-up.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/milestone-engine.ts:86

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"This milestone plan is prepared for future AI recovery and execution supervision.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:104

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
export function buildMilestoneSummary(plan: ConstructionMilestonePlan): string {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:106

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"AI Construction Milestone Plan:",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:107

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
`Project start: ${plan.projectStartDate}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:108

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
`Estimated completion: ${plan.estimatedCompletionDate}`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:109

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
`Estimated duration: ${plan.totalEstimatedDays} days`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/milestone-engine.ts:112

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
...plan.milestones.map(
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/procurement-execution-engine.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
structure: "Cement / TMT / Sand / Aggregate Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:18

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
masonry: "Brick / Block / Masonry Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:19

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
plumbing: "Plumbing / Sanitary Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`, `supplier`

**Current system**

```text
electrical: "Electrical Contractor / Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:21

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
finishing: "Tiles / Paint / Finishing Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
VENDOR_CATEGORY_BY_STAGE[stage.key] || "Construction Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Delay may affect finishing speed, but can be controlled with early vendor confirmation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:57

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Ready for RFQ and vendor matching"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:116

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Procurement sequence is stable. Keep vendor confirmation and delivery dates updated."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:119

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Immediately lock critical materials, vendor delivery dates and backup suppliers.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:140

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: `Confirm vendor for ${item.vendorCategory}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:143

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
action: "Call vendor, confirm stock, rate validity, delivery date and unloading support.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:150

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: `Prepare backup supplier for ${item.vendorCategory}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-execution-engine.ts:153

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
action: "Keep second vendor ready before work starts to avoid labour idle time.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Cement / RMC / Aggregate Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:39

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "TMT Steel / Reinforcement Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:48

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Brick / Block Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:57

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorCategory: "Plumbing / Sanitary Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`, `supplier`

**Current system**

```text
vendorCategory: "Electrical Contractor / Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-package-engine.ts:75

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorCategory: "Tiles / Paint / Finishing Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-phase-engine.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Trigger dates indicate when RFQ/vendor coordination should ideally begin.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-phase-engine.ts:111

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Actual procurement dates may change due to vendor availability, material stock, transport, payment timing and weather.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-phase-engine.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Note: Please coordinate vendor quotations, delivery timeline and site readiness according to the phase schedule.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:9

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Confirm engineer, site supervisor, layout marking and project execution plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
title: "Book excavation contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Earthwork Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:27

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
rfqReadyName: "Foundation excavation contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Building Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "RCC Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:77

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Brick / Block Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Masonry Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:92

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
rfqReadyName: "Masonry labour contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:103

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Electrical / Plumbing Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:116

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Building Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:129

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Tile Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:139

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Finalize measurements, material type and fabrication vendor before installation phase.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:142

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorCategory: "Door / Window Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:155

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Paint Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/procurement-trigger-rules.ts:168

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorCategory: "Finishing Material Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/project-health-engine.ts:93

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `contractor`

**Current system**

```text
grade === "risk" || grade === "critical" ? "Prepare alternate vendor and contractor backup" : "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/pwd-price-sync.ts:50

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
source: "Brick supplier intelligence",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/pwd-price-sync.ts:62

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
source: "Sanitary supplier intelligence",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Notify the buyer/project owner that one or more construction milestones are delayed and require immediate supervision.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:50

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Identify materials, labour, equipment, or vendor commitments causing blockage and escalate them through RFQ/vendor communication.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
title: "Suggest alternate vendor action",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:60

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Prepare alternate vendor/vendor-shortlist action for delayed materials, labour, or machinery supply.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Ask site supervisor/contractor to submit daily progress status until the delayed or blocked milestone is recovered.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? `Escalate procurement/vendor dependency for: ${blockedMilestones
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:117

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Prepare alternate vendor shortlist and backup RFQ for delayed material/labour/equipment supply."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-engine.ts:118

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Alternate vendor action is not urgent, but backup vendor readiness may be maintained.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/recovery-types.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`, `contractor`

**Current system**

```text
recommendedOwner: "buyer" | "vendor" | "contractor" | "supervisor" | "system";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/rfq-package-generator.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
} from "./auto-rfq-builder";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/rfq-package-generator.ts:9

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: AutoConstructionRfqPackage["module"];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/rfq-package-generator.ts:23

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan: AutoConstructionRfqPlan;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/rfq-package-generator.ts:30

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = buildAutoConstructionRfqPlan(input);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/rfq-package-generator.ts:32

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const drafts: ConstructionRfqDraftPayload[] = plan.packages.map((pkg) => ({
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/rfq-package-generator.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: pkg.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/construction-cost/rfq-package-generator.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
plan,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/risk-forecast-engine.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
.match(/material|cement|steel|tmt|sand|brick|supplier|vendor/),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/risk-forecast-engine.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
.match(/labour|mason|contractor|worker|supervisor/),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/risk-forecast-engine.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Material/vendor dependency may affect execution continuity.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/risk-forecast-engine.ts:97

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
message: "Labour, contractor, or site supervision may need escalation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/risk-forecast-engine.ts:115

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
labourRiskProbability >= 35 ? "Ask contractor/supervisor for daily manpower commitment." : "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/risk-forecast-engine.ts:117

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
contractorPerformanceRisk >= 35 ? "Track contractor performance and prepare alternate execution support." : "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/seo-content.ts:60

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
const description = `Check latest house construction cost in ${cityName}, ${stateName}. Get AI-powered budget estimate, rate per sq.ft, material cost, labour cost and turnkey construction insights on 3bigha.`;
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/construction-cost/seo-content.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"3bigha uses construction cost intelligence to support buyer budget prediction, turnkey quotation comparison, RFQ estimation, future BOQ planning and procurement workflows.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/site-intelligence-engine.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/site-intelligence-engine.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/site-intelligence-engine.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/site-intelligence-engine.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Collect contractor/supervisor update.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/site-intelligence-engine.ts:97

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
materialCoordinationNeed !== "low" ? "Check material availability and vendor delivery status." : "",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-estimator.ts:25

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
if (value === "economy" || value === "standard" || value === "premium") {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/timeline-estimator.ts:124

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"Actual duration depends on labour availability, weather, material delivery, cash flow, site access and contractor coordination.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-estimator.ts:126

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
"Premium finishing and interior work may increase project duration.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/timeline-estimator.ts:150

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Note: Vendor should confirm actual work schedule, manpower deployment, material dependency and milestone-wise completion plan.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-estimator.ts:150

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"Note: Vendor should confirm actual work schedule, manpower deployment, material dependency and milestone-wise completion plan.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/timeline-rules.ts:24

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
premium: 1.12,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/construction-cost/timeline-rules.ts:48

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Foundation Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:60

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "RCC Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:72

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Masonry Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:84

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Electrical / Plumbing Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Plaster Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Tile Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:120

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorCategory: "Door & Window Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Painting Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:144

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
vendorCategory: "Finishing Contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/construction-cost/timeline-rules.ts:146

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
note: "Depends on availability of fittings, fixtures and final vendor coordination.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/buyer-listing-inbox.ts:96

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/conversations/buyer-listing-inbox.ts:112

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/buyer-listing-inbox.ts:128

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
String(p.full_name ?? p.name ?? "Vendor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/buyer-listing-inbox.ts:196

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/ensureConversation.ts:116

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const existing = await query.limit(1).maybeSingle();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/conversations/ensureConversation.ts:132

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
chatUrl: `/dashboard/thread/${encodeURIComponent(existingId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/conversations/ensureConversation.ts:188

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
chatUrl: `/dashboard/thread/${encodeURIComponent(conversationId)}`,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/conversations/vendor-listing-inbox.ts:88

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/conversations/vendor-listing-inbox.ts:109

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
String(p.full_name ?? p.name ?? "Buyer"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/vendor-listing-inbox.ts:177

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? String(profileMap.get(String(r.buyer_user_id)) ?? "").trim() || "Buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/conversations/vendor-listing-inbox.ts:178

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/discovery/build-discovery-sections.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/discovery/build-discovery-sections.ts:55

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Track regional material pricing and marketplace movement.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/discovery/build-discovery-sections.ts:64

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Receive supplier quotations with workflow-driven procurement support.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ensureBusinessProfileComplete.ts:12

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
* Gate vendor-only actions (submit/publish).
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ensureBusinessProfileComplete.ts:14

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
* Falls back to business_profiles.is_complete if the view is missing.
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ensureBusinessProfileComplete.ts:14

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
* Falls back to business_profiles.is_complete if the view is missing.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:20

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
const safeReturnTo = safePath(returnTo, "/dashboard");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/ensureBusinessProfileComplete.ts:38

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("is_complete,business_profile_complete,percent,completion_percent")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:44

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
comp.is_complete === true ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:50

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
return { ok: false, redirectTo: `/onboarding/business?next=${encodeURIComponent(safeReturnTo)}` };
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ensureBusinessProfileComplete.ts:59

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
.select("is_complete,nature_of_business")
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:68

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
bp?.is_complete === true ||
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/ensureBusinessProfileComplete.ts:74

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
redirectTo: `/onboarding/business?next=${encodeURIComponent(safeReturnTo)}`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/experience/experience-mode.ts:31

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
badge: "Full Workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/experience/experience-mode.ts:32

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
description: "Full operational and AI-powered workspace visibility.",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/experience/experience-mode.ts:32

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
description: "Full operational and AI-powered workspace visibility.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/finance/financeLeadScoring.ts:48

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
return [...common, "Approved building plan", "Construction estimate", "Land ownership document"];
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/finance/requireVerifiedBanker.ts:1

**Area:** Business Logic

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
import { redirect } from "next/navigation";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/finance/requireVerifiedBanker.ts:53

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/finance/requireVerifiedBanker.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
id: "master-admin-preview",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:60

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
full_name: "Master Admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
bank_name: "Admin Finance Desk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
ifsc_code: "ADMIN",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:65

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `master_admin`

**Current system**

```text
employee_id: "MASTER_ADMIN",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
ai_verification_status: "admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/finance/requireVerifiedBanker.ts:74

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
manual_verification_notes: "Master admin access",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geo/india-geo.ts:79

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:81

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:92

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`/seo/${item.module}/${item.state}/${item.district}/${item.city}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:100

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 5
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geo/india-geo.ts:115

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
return [...sameDistrict, ...sameState].slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geo/india-geo.ts:125

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.filter((module) => module !== currentModule)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:126

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.map((module) => ({
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:128

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
url: `/seo/${module}/${stateSlug}/${districtSlug}/${citySlug}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:142

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:144

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
(item) => `/seo/${module}/${item.state}/${item.district}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:152

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:153

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
states.map((state) => `/seo/${module}/${state}`)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:179

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoModules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geo/india-geo.ts:183

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`/seo/${module}/${geo.stateSlug}/${geo.districtSlug}/${geo.citySlug}/${locality}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/repository.ts:43

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:52

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(Math.max(Number(options.limit || 50), 1), 500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:60

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:76

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
rows: rows.slice(0, limit),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:77

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
hasMore: rows.length > limit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:78

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
nextOffset: offset + Math.min(rows.length, limit),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:82

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
export function getStates(options?: { q?: string; limit?: number; offset?: number }) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:88

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
options?: { q?: string; limit?: number; offset?: number }
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:95

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
options?: { q?: string; limit?: number; offset?: number }
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:102

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
options?: { q?: string; limit?: number; offset?: number }
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/repository.ts:120

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
options?: { q?: string; limit?: number; offset?: number }
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/resolveGeography.ts:60

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/resolveLocation.ts:155

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await query.limit(1).maybeSingle();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/rfqMatching.ts:34

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete: boolean | null;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/geography/rfqMatching.ts:43

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/rfqMatching.ts:51

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 25,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/rfqMatching.ts:57

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
getCoordinates: (vendor) => resolveVendorCoordinates(vendor).coordinates,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/rfqMatching.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.filter((vendor) => matchesBusinessCategory(vendor, category))
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/rfqMatching.ts:63

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
return sortNearbyVendors(nearby).slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/searchLocation.ts:13

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:83

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
export async function getSeoGeoCities(limit = 5000): Promise<SeoGeoCity[]> {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:100

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
modules: readonly SeoModule[]
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:131

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`, `modules`

**Current system**

```text
return modules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:135

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:144

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
modules: readonly SeoModule[]
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:148

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`, `modules`

**Current system**

```text
return modules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:152

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
getSeoCategories(module).map((category) => ({
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:153

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:164

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
export async function getSeoStatePathsFromDb(modules: readonly SeoModule[]) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:170

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:178

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`, `modules`

**Current system**

```text
return modules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:180

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:186

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
export async function getSeoDistrictPathsFromDb(modules: readonly SeoModule[]) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:192

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/seoAdapter.ts:207

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`, `modules`

**Current system**

```text
return modules.flatMap((module) =>
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:209

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:216

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
export async function getAllSeoUrlsFromDb(modules: readonly SeoModule[]) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:218

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
getSeoStatePathsFromDb(modules),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:219

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
getSeoDistrictPathsFromDb(modules),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:220

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
getSeoRegionalPathsFromDb(modules),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:221

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
getSeoCategoryPathsFromDb(modules),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:225

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
...states.map((item) => `/seo/${item.module}/${item.state}`),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:226

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
...districts.map((item) => `/seo/${item.module}/${item.state}/${item.district}`),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:227

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
...cities.map((item) => `/seo/${item.module}/${item.state}/${item.district}/${item.city}`),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/seoAdapter.ts:228

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
...categories.map((item) => `/seo/${item.module}/${item.state}/${item.district}/${item.city}/category/${item.category}`),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/geography/useGeoOptions.ts:25

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/useGeoOptions.ts:46

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: params.limit || 200,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/useGeoOptions.ts:98

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
if (params.limit) url.searchParams.set("limit", String(params.limit));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/vendorCoordinates.ts:23

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor: VendorCoordinateInput
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorCoordinates.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
latitude: toNumber(vendor.verified_lat) ?? Number.NaN,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorCoordinates.ts:27

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
longitude: toNumber(vendor.verified_lng) ?? Number.NaN,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:20

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
is_complete?: boolean | null;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/geography/vendorMatching.ts:34

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor: VendorMatchingInput,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.business_type,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(Array.isArray(vendor.nature_of_business) ? vendor.nature_of_business : []),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:52

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export function vendorCoversDistance(vendor: NearbyVendorLike): boolean {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:53

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (vendor.nationwide_service || vendor.statewide_service) return true;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const serviceRadius = toNumber(vendor.service_radius_km);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const deliveryRadius = toNumber(vendor.delivery_radius_km);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return vendor.distanceKm <= effectiveRadius;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:64

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export function isEligibleVendorProfile(vendor: VendorMatchingInput): boolean {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:65

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (vendor.is_complete === false && vendor.registration_complete === false) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:65

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `is_complete`

**Current system**

```text
if (vendor.is_complete === false && vendor.registration_complete === false) {
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/geography/vendorMatching.ts:72

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export function vendorPriorityScore(vendor: NearbyVendorLike): number {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:73

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const boostPriority = toNumber(vendor.boost_priority);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:76

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.subscription_status === "active"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:77

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? vendor.subscription_plan === "premium"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:77

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? vendor.subscription_plan === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/geography/vendorMatching.ts:83

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
distanceKm: vendor.distanceKm,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reputationScore: toNumber(vendor.reputation_score),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
authorityScore: toNumber(vendor.authority_score),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:88

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
conversionRate: toNumber(vendor.conversion_rate),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
responseRate: toNumber(vendor.response_rate),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
activityScore: toNumber(vendor.activity_score),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:91

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
demandScore: toNumber(vendor.demand_score),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/geography/vendorMatching.ts:92

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
liquidityScore: toNumber(vendor.liquidity_score),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/i18n/dictionaries/bn.ts:11

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
register: "রেজিস্টার",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/i18n/dictionaries/bn.ts:12

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
dashboard: "ড্যাশবোর্ড",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/i18n/dictionaries/en.ts:11

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
register: "Register",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/i18n/dictionaries/en.ts:12

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
dashboard: "Dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/i18n/dictionaries/en.ts:20

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"India's AI-Powered Property, Construction & Procurement Marketplace",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/i18n/dictionaries/en.ts:23

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"Buy, sell, rent, compare vendors, manage RFQs, track prices, and grow your business using AI-powered marketplace intelligence.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/i18n/dictionaries/en.ts:35

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"AI-powered procurement and marketplace operating system for property, materials, rentals, services, RFQ management, vendor intelligence, and price prediction.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/i18n/dictionaries/en.ts:35

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"AI-powered procurement and marketplace operating system for property, materials, rentals, services, RFQ management, vendor intelligence, and price prediction.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/intelligence/operationalHealthEngine.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Review vendor response and procurement execution.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace-orchestration/property-cross-module.ts:67

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title: "Plan house construction",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace-orchestration/property-cross-module.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
subtitle: `Turn this land into a budget, BOQ and contractor plan for ${location}.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace-orchestration/property-cross-module.ts:68

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
subtitle: `Turn this land into a budget, BOQ and contractor plan for ${location}.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace-orchestration/property-cross-module.ts:84

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `upgrade`

**Current system**

```text
badge: "Upgrade",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:19

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:29

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:38

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
row.module || "unknown",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:54

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Recruit immediately. This opportunity has high shortage pressure and proven vendor activation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:90

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,module,category,opportunity_score,shortage_score,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:92

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:96

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"event_type,module,category,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:99

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:145

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/autonomous-vendor-recruitment-intelligence.ts:164

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/executive/adapters/mos.ts:18

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit: context.config.signalLimit,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/executive/adapters/mos.ts:29

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/executive/decision-engine.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (sources.has("vendor") && sources.has("mos")) return "notify_vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/executive/types.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/expansion-recommendation-engine.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/expansion-recommendation-engine.ts:9

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/expansion-recommendation-engine.ts:20

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/expansion-recommendation-engine.ts:29

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/expansion-recommendation-engine.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/index.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export * from "./vendor-core";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/index.ts:8

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export * from "./vendor-recruitment-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/opportunity-engine.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Prioritize vendor onboarding and expand matching radius for new RFQs.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/opportunity-engine.ts:66

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
return "Prioritize vendor onboarding and expand matching radius for new RFQs.";
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/intelligence/opportunity-engine.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Avoid aggressive vendor recruitment; focus on conversion, quality, and differentiation.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorRecruitmentTarget } from "@/lib/marketplace/intelligence/vendor-recruitment-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:28

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
const MODULES = [
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:29

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "property", rfqModule: "properties", listingTable: "property_listings" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "materials", rfqModule: "materials", listingTable: "material_listings" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:31

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "services", rfqModule: "services", listingTable: "service_listings" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:32

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "rentals", rfqModule: "rentals", listingTable: "rental_listings" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:81

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:86

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:100

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:106

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
for (const row of (data as (GeoKey & { module?: string })[]).filter((item) => item.geo_state_id)) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:131

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:134

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:163

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:167

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id,module")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:168

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:169

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:225

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:249

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:260

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:270

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:282

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:293

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:307

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:326

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:340

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:349

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:362

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:381

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
demandRes.error ? `demand:${input.module}:${demandRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:382

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
supplyRes.error ? `supply:${input.module}:${supplyRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:383

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
gapRes.error ? `gap:${input.module}:${gapRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:384

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
opportunityRes.error ? `opportunity:${input.module}:${opportunityRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:385

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
recruitmentRes.error ? `recruitment:${input.module}:${recruitmentRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:386

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
growthRes.error ? `growth:${input.module}:${growthRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:387

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
expansionRes.error ? `expansion:${input.module}:${expansionRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:388

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendorExpansionRes.error ? `vendorExpansion:${input.module}:${vendorExpansionRes.error.message}` : "",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:407

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
const vendorCount = await countRows(supabase, "business_profiles");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:416

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
for (const item of MODULES) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:421

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:427

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendors: item.module === "property" ? 0 : vendorCount,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:454

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: item.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/services/marketplace-intelligence-aggregator.ts:460

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendors: item.module === "property" ? 0 : vendorGeo?.vendors || 0,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/vendor-core.ts:13

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = clean(row.subscription_plan).toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/vendor-core.ts:23

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "hub_vendor" || plan === "platinum") return 20;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/vendor-core.ts:24

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "premium_vendor" || plan === "gold") return 10;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/vendor-core.ts:25

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
if (plan === "basic_vendor" || plan === "silver") return 5;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/intelligence/vendor-recruitment-engine.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/vendor-recruitment-engine.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/intelligence/vendor-recruitment-engine.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials" | "services" | "rentals" | "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:9

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
const MODULES: ModuleConfig[] = [
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "materials", listingTable: "material_listings", vendorColumn: "vendor_user_id" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "services", listingTable: "provider_services", vendorColumn: "user_id" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "rentals", listingTable: "rental_listings", vendorColumn: "vendor_user_id" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:13

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ module: "property", listingTable: "property_listings", vendorColumn: "user_id" },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (zone === "dead_zone") return "Demand exists but vendor response capacity is weak. Recruit vendors immediately.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/liquidity-engine.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (level === "thin") return "Liquidity is thin. Increase vendor onboarding and listing creation.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/liquidity-engine.ts:66

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
if (level === "thin") return "Liquidity is thin. Increase vendor onboarding and listing creation.";
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/liquidity-engine.ts:67

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (level === "active") return "Marketplace is active. Continue recruitment and buyer acquisition.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/liquidity-engine.ts:72

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from(table).select(select).limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/liquidity-engine.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
for (const config of MODULES) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:92

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,module,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:97

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"event_type,module,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:135

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (String(rfq.module || "") !== config.module) continue;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:144

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (String(event.module || "") !== config.module) continue;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/liquidity-engine.ts:173

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: config.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/live-vendor-intelligence.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.map((vendor) => vendor.user_id)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:88

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/live-vendor-intelligence.ts:160

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.flatMap((vendor) => [
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:161

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.business_type,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:162

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(Array.isArray(vendor.nature_of_business)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:163

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? vendor.nature_of_business
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:176

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/live-vendor-intelligence.ts:189

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
for (const vendor of vendors) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:191

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.business_type,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:192

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(Array.isArray(vendor.nature_of_business)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:193

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? vendor.nature_of_business
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:203

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (match) mergePatch(patches, vendor.user_id, match);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:228

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return vendors.map((vendor) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:229

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const patch = patches.get(String(vendor.user_id || "")) || {};
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:232

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...vendor,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:233

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reputation_score: patch.reputation_score ?? vendor.reputation_score ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:234

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
authority_score: patch.authority_score ?? vendor.authority_score ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:235

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
conversion_rate: patch.conversion_rate ?? vendor.conversion_rate ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:236

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
response_rate: patch.response_rate ?? vendor.response_rate ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:237

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
activity_score: patch.activity_score ?? vendor.activity_score ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:238

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
demand_score: patch.demand_score ?? vendor.demand_score ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/live-vendor-intelligence.ts:239

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
liquidity_score: patch.liquidity_score ?? vendor.liquidity_score ?? 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:19

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:42

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
row.module || "unknown",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:82

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Increase recruitment. Expansion potential exists and vendor supply should be strengthened before heavier promotion.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:99

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Watch this expansion area. Continue monitoring demand, supply and vendor conversion evidence.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:110

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module,category,growth_score,shortage_score,expansion_score,recommendation,reason,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:112

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:116

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module,category,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id,optimization_score,shortage_score,decision,action_priority"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:118

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/marketplace-expansion-automation.ts:148

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-intelligence-ranking.ts:111

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (signals.responseScore >= 5) reasons.push("Responsive vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-intelligence-ranking.ts:112

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (signals.verificationScore > 0) reasons.push("Verified vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-intelligence-ranking.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (signals.boostScore > 0) reasons.push("Boosted vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-promotion-engine.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-promotion-engine.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Promote this opportunity in public marketplace areas to increase vendor acquisition.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/marketplace-promotion-engine.ts:46

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"module,category,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id,expansion_score,optimization_score,shortage_score,action_type"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/marketplace-promotion-engine.ts:49

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(100);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/marketplace-promotion-engine.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/demand-around-me.ts:56

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const fastestGrowingCategory = topByCount(rfqs, "module");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/index.ts:9

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
export * from "./vendor-action-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-engine.ts:3

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
type: "rfq" | "listing" | "coverage" | "boost" | "pricing" | "profile";
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:39

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
actionHref: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-engine.ts:39

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
actionHref: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/opportunity-engine.ts:48

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
description: `Demand activity is strongest in ${input.fastestGrowingCategory}. Update inventory, pricing, and availability.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:52

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
actionHref: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-engine.ts:52

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
actionHref: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/opportunity-engine.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Demand exists in your geography while vendor competition is still low.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-engine.ts:65

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
actionHref: "/onboarding/business",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/mos/opportunity-engine.ts:72

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
type: "pricing",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:73

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
title: "Review pricing for current demand",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:74

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
description: `Visible RFQ value is approximately ₹${Math.round(input.estimatedMarketValue).toLocaleString("en-IN")}. Keep pricing competitive.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
actionHref: "/vendor/price-updates/new",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-engine.ts:91

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
actionHref: "/dashboard/subscription/boost",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/opportunity-engine.ts:91

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
actionHref: "/dashboard/subscription/boost",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:99

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
description: "Keep your profile, categories, inventory, and pricing ready so the system can match you when demand rises.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-engine.ts:103

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
actionHref: "/onboarding/business",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/mos/opportunity-events.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { buildVendorAction } from "./vendor-action-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-events.ts:26

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-events.ts:61

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
dashboard: true,
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/opportunity-generator.ts:5

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-generator.ts:7

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(25, Math.max(1, Number(params.limit || 10)));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-generator.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,opportunity_score,priority,recommendation,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/opportunity-generator.ts:13

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-generator.ts:29

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-generator.ts:32

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/opportunity-generator.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
for (const vendor of vendors || []) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-generator.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (!vendor?.user_id) continue;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-generator.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorUserId = String(vendor.user_id);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/opportunity-generator.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
title: `${zone.category || zone.module} opportunity detected`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/opportunity-generator.ts:57

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`High marketplace opportunity detected for ${zone.category || zone.module}.`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/repositories/business.ts:3

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/repositories/rfqs.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,requester_user_id,module,status,title,city,district,locality,pincode,created_at,meta,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/mos/repositories/rfqs.ts:20

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/repositories/supply.ts:3

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:22

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
action_href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:22

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
action_href: "/dashboard/vendor/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
reason: "Active buyer demand is visible in your operating area.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:33

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
action_href: "/onboarding/business",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Low vendor competition is detected in this market.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
action_href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:44

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
action_href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:55

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
action_href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:55

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
action_href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/mos/vendor-action-engine.ts:65

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
action_href: "/onboarding/business",
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:13

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function keyOf(row: Partial<GeoKey> & { module?: string | null }) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
row.module || "unknown",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const [module, geo_state_id, geo_district_id, geo_subdivision_id, geo_block_id, geo_place_id] =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:41

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:71

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:87

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,status,created_at,geo_state_id,geo_district_id,geo_subdivision_id,geo_block_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:88

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!rfq.module) continue;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:142

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: rfq.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:148

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
failure_reason: "No vendor response detected yet.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/rfq-intelligence-engine.ts:163

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: parsed.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/self-optimization-engine.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/self-optimization-engine.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "High shortage and proven vendor activation. Increase homepage, banner and navigation visibility.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/self-optimization-engine.ts:59

**Area:** Business Logic

**Category:** Workspace

**Matched:** `navigation`

**Current system**

```text
reason: "High shortage and proven vendor activation. Increase homepage, banner and navigation visibility.",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/marketplace/services/mos.ts:5

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/services/mos.ts:7

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = Math.min(25, Math.max(1, Number(params.limit || 10)));
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/services/mos.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("id,module,category,opportunity_score,priority,recommendation,geo_state_id,geo_district_id,geo_place_id")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/services/mos.ts:13

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/services/mos.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
title: `${zone.category || zone.module || "Marketplace"} opportunity zone`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/services/mos.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
category: zone.category || zone.module || null,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/services/mos.ts:28

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: zone.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/vendor-conversion-analytics.ts:14

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/vendor-conversion-analytics.ts:41

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: clean(payload.module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/marketplace/vendor-conversion-analytics.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("vendor conversion analytics insert failed:", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-conversion-analytics.ts:64

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("vendor conversion analytics failed:", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
function categoriesFor(vendor: VendorRow) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.business_type,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(Array.isArray(vendor.nature_of_business)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:60

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? vendor.nature_of_business
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:100

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(10000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:146

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:183

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:192

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
for (const vendor of vendors) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:193

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const match = categoriesFor(vendor)
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:199

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
mergePatch(map, vendor.user_id, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:202

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
map.get(vendor.user_id)?.demand_score || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:211

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
for (const vendor of vendors) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:212

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const boost = Number(vendor.boost_priority || 0);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:213

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const subscription = vendor.subscription_status === "active" ? 10 : 0;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:213

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
const subscription = vendor.subscription_status === "active" ? 10 : 0;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:214

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const premium = vendor.subscription_plan === "premium" ? 10 : 0;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:214

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
const premium = vendor.subscription_plan === "premium" ? 10 : 0;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:216

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
mergePatch(map, vendor.user_id, {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:218

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
map.get(vendor.user_id)?.reputation_score || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:219

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
50 + subscription,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:222

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
map.get(vendor.user_id)?.authority_score || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:223

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
40 + premium + boost,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:226

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
map.get(vendor.user_id)?.activity_score || 0,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:240

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.map((vendor) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:241

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const patch = map.get(vendor.user_id) || emptyPatch();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:243

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
boostScore: Number(vendor.boost_priority || 0) * 100,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:245

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.subscription_status === "active"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:246

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? vendor.subscription_plan === "premium"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:246

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
? vendor.subscription_plan === "premium"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:260

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
user_id: vendor.user_id,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:289

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:294

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:315

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
for (const vendor of rows) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:316

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const patch = map.get(vendor.user_id) || emptyPatch();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:319

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-intelligence-refresh.ts:324

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
.eq("user_id", vendor.user_id);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/marketplace/vendor-performance-analytics.ts:32

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/media/media-config.ts:8

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/media/media-config.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor: "vendor-media",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/media/media-utils.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
message: "Image resolution looks low. A clearer photo may improve buyer trust.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousMarketOperatingSystem.ts:128

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Monitor delayed negotiations and supplier recovery pipelines.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousProcurementCopilot.ts:84

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor follow-up recommended",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousProcurementCopilot.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor activity appears stalled. Send a follow-up message.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousProcurementCopilot.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer re-engagement suggested",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousProcurementCopilot.ts:104

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer interaction has slowed down. Reconnect to continue the workflow.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousSupplyChainIntelligence.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
return "Check delivery routes, supplier availability, and backup logistics.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/autonomousSupplyChainIntelligence.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
return "Watch supplier and logistics delays.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/firebaseAdmin.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
import admin from "firebase-admin";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/firebaseAdmin.ts:4

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (admin.apps.length) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/firebaseAdmin.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return admin.app();
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/firebaseAdmin.ts:16

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
return admin.initializeApp({
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/firebaseAdmin.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
credential: admin.credential.cert(serviceAccount),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/getUserPushTokens.ts:3

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/getUserPushTokens.ts:18

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const { data, error } = await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/operationalBattlefield.ts:79

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
action: "Review delayed actions, failed workflows, and supplier stress.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/operationalPriority.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/mobile/operationalPriority.ts:78

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/mobile/operationalPriority.ts:98

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module || "operations",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/mobile/operationalPriority.ts:101

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/mobile/predictiveEconomicIntelligence.ts:74

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Secure critical materials, confirm supplier availability, and review price exposure.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/predictiveEconomicIntelligence.ts:82

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier, shortage, or cost-pressure signals are rising.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/predictiveEconomicIntelligence.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Prioritize urgent procurement and monitor supplier stability.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/predictiveEconomicIntelligence.ts:107

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Monitor demand, supplier response, and cost movement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/procurementWarRoom.ts:81

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Prioritize delayed negotiations and supplier response recovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/savePushToken.ts:3

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
const admin = createClient(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/savePushToken.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
await admin
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/selfHealingProcurement.ts:91

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Review supplier alternatives and reroute stalled procurements.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/selfHealingProcurement.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Watch stalled RFQs and vendor response delays.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/sendOperationalPush.ts:45

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/thread/${input.conversationId}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/mobile/sendOperationalPush.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return `/dashboard/buyer/quote-compare/${input.rfqId}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/sendOperationalPush.ts:49

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/buyer/quote-compare/${input.rfqId}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/mobile/sendOperationalPush.ts:52

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return input.fallback || "/dashboard";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/mobile/strategicMarketStabilization.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Reduce bottlenecks, stabilize supplier coordination, and prioritize critical recovery chains.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/strategicMarketStabilization.ts:122

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Monitor congestion, supplier stress, and delayed procurement fulfillment.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mobile/strategicMarketStabilization.ts:134

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Continue adaptive monitoring and supplier coordination.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:21

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Authorized Dealer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:21

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `authorized`

**Current system**

```text
supplier: "Authorized Dealer",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/mock/materials.ts:31

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Primary Distributor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Local Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Kiln Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/mock/materials.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: "Paint Dealer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:22

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Continue My Work", "/dashboard"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["🚀 Vendor Opportunities", "/vendor-opportunities"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:70

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:76

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
["My RFQs", "/dashboard/buyer/rfqs"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:76

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["My RFQs", "/dashboard/buyer/rfqs"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:77

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Inbox", "/dashboard/inbox-v2"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:78

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Purchase & Procurement", "/dashboard/procurement-os"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:84

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Construction Projects", "/dashboard/construction-projects"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:85

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Site Work", "/dashboard/site-execution"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:86

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Activity Feed", "/dashboard/activity-feed"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:87

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Project Health", "/dashboard/procurement-health"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:94

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:94

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:99

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Dashboard", "/dashboard/vendor"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:99

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`, `vendor dashboard`

**Current system**

```text
["Vendor Dashboard", "/dashboard/vendor"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/navigation/main-menu.ts:100

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Network", "/vendor/discovery"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
["Vendor Master Data", "/dashboard/vendor/master-data"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/navigation/main-menu.ts:101

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
["Vendor Master Data", "/dashboard/vendor/master-data"],
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:27

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: "buyer-start-rfq",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:29

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Post one requirement and 3Bigha can begin vendor matching, quote comparison and follow-up intelligence.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:39

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: "buyer-urgent-rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Some requirements are close to their needed date. Review quotes or continue vendor conversations today.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:42

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: "buyer-active-followup",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:54

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: "buyer-reuse-memory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:74

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
id: "buyer-marketplace-discovery",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:100

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-new-leads",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
title: `${input.newLeadCount} new buyer lead${input.newLeadCount > 1 ? "s" : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:102

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
message: "Reply quickly to improve conversion probability and vendor ranking signals.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:103

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:103

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:112

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-missed-followups",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:115

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:124

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-ready-deals",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:127

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:136

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-reply-rate",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:138

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
message: `Your current reply rate is ${input.replyRate}%. Faster replies can improve buyer trust and lead visibility.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:139

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:139

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/enquiries",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:148

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-rank-risk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:151

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/notifications/smart-reengagement.ts:151

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
href: "/dashboard/subscription/boost",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/notifications/smart-reengagement.ts:160

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-price-correction",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:161

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
title: "Pricing correction recommended",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/notifications/smart-reengagement.ts:162

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
message: `${input.priceNeedsCorrection} price update${input.priceNeedsCorrection > 1 ? "s" : ""} may be hurting buyer conversion.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/notifications/smart-reengagement.ts:163

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/vendor/price-updates/new",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/operational-events/storage.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.filter((event) => event?.id && event?.title && event?.module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/operational-events/types.ts:13

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/operational-events/types.ts:14

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/operational-events/types.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: OperationalEventModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/personalized-discovery/discovery-memory.ts:3

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/personalized-discovery/discovery-memory.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
(x) => !(x.module === item.module && x.id === item.id)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:22

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:34

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:40

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
id: "supplier-response-risk",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Supplier response may slow",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
message: "One or more supplier chains may need a gentle follow-up.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:46

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-health",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/predictive-operational-intelligence/build-predictive-summary.ts:58

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-execution",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/conversation-context.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/conversation-context.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
source: "search" | "rfq" | "vendor" | "price" | "comparison";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/conversation-context.ts:35

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: context.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/continuity-unification.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/continuity-unification.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/continuity-unification.ts:16

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: ContinuityModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/continuity-unification.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function normalizeContinuityModule(module?: string): ContinuityModule {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/continuity-unification.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const value = String(module || "operations").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/continuity-unification.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (value.includes("vendor")) return "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/continuity-unification.ts:31

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
if (value.includes("buyer")) return "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/continuity-unification.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
id: input.id || `${input.module || "operations"}-${Date.now()}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/continuity-unification.ts:65

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: normalizeContinuityModule(input.module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/decay-signals.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reasons.push("Vendor response is delayed.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/decay-signals.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reasons.push("Vendor engagement is becoming inactive.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/decay-signals.ts:103

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Monitor workflow activity and vendor responsiveness.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/interruption-priority-engine.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/memory/procurement-memory-profile.ts:11

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
entityType: "vendor" | "buyer" | "rfq" | "workflow";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/momentum-signals.ts:62

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| Vendor responsiveness
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/momentum-signals.ts:69

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reasons.push("Vendor responses actively continuing.");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/operational-event-compression.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/operational-event-compression.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/operational-event-compression.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = event.module || "operations";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/operational-event-compression.ts:41

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const key = `${module}:${category}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/operational-event-compression.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor-workspace"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:5

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
| "vendor-workspace"
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:15

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: OrchestrationModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "procurement-live",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:28

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:35

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
module: "vendor-workspace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:35

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
module: "vendor-workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:35

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "vendor-workspace",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Workspace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:36

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
label: "Vendor Workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/workspace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:37

**Area:** Business Logic

**Category:** Workspace

**Matched:** `workspace`, `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/workspace",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "inbox-v2",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:46

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:53

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "workflow-continuity",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:61

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "operational-events",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:69

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "recovery",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "memory",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:85

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "executive",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:98

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function findOrchestrationModule(module: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/orchestration-registry.ts:99

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return orchestrationRegistry.find((entry) => entry.module === module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/outcome-learning.ts:99

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Supplier continuity reliability is weakening.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/outcome-learning.ts:112

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Introduce alternate supplier continuity planning.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/cross-module-sequencing-grid.ts:20

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"Cross-module sequencing protection increased because continuity alignment weakened.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/cross-module-sequencing-grid.ts:29

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"Cross-module sequencing remains guided for calmer workflow handling.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/cross-module-sequencing-grid.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"Cross-module sequencing remains stable.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/index.ts:3

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export * from "./cross-module-sequencing-grid";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/mission-recovery-coordinator.ts:20

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
"Recovery pacing is synchronized across procurement modules.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/procurement-mission-intelligence-grid.ts:79

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
? "Mission grid synchronized recovery pacing across procurement modules."
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/procurement-mission-intelligence-grid.ts:83

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
? "Mission-wide sequencing guidance was synchronized across modules."
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/unified-mission-state-engine.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
"Procurement modules are coordinating recovery pacing together.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/procurement-mission-intelligence-grid/unified-mission-state-engine.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"Cross-module continuity protection increased to preserve workflow stability.",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/situational-collaboration-intelligence/procurement-situational-awareness.ts:11

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
"Keep buyer, vendor and recovery follow-ups in one clear operational context.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/unified-cognition.ts:125

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
recommendedFocus = "Prioritize escalation prevention and supplier continuity.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/unified-cognition.ts:139

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplierCollapse >= 70 ? "Supplier collapse pressure elevated." : null,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/intelligence/unified-operational-timeline.ts:13

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/unified-operational-timeline.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
id: String(event.id || `${event.module || "operations"}-${index}`),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/unified-operational-timeline.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: event.module || "operations",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/intelligence/unified-operational-timeline.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const key = `${event.module}:${event.continuityStage || event.category}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/journey-actions.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function buildSearchJourneyActions(query: string, module: string): ProcurementJourneyAction[] {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/journey-actions.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const mod = module && module !== "all" ? `&module=${encodeURIComponent(module)}` : "";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/journey-actions.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
id: `rfq:${q}:${module}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/journey-actions.ts:50

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Convert this search into a vendor-ready requirement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/journey-actions.ts:56

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
id: `vendors:${q}:${module}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/journey-actions.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Continue vendor discovery for this procurement need.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/journey-actions.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encoded}${mod}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/journey-actions.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
id: `price:${q}:${module}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:3

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:42

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
item.module === memory.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:74

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = String(item.module || "").toLowerCase();
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (source.includes("vendor")) score += 18;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/persistent-memory.ts:81

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") score += 10;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") score += 8;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:83

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") score += 6;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/persistent-memory.ts:84

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") score += 6;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/procurementWorkflowMemory.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor?: string;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerAction: "Start vendor discovery",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"RFQ created successfully and ready for vendor engagement.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Discovery",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:75

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor matching and procurement discovery ongoing.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:80

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Engagement",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerAction: "Wait for vendor responses",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:100

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor quotations are now available for review.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:107

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
description: "Buyer reviewing quotations.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:123

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
vendorAction: "Respond to buyer negotiation",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:130

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Shortlisting",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:135

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerAction: "Finalize vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:137

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"Buyer is finalizing preferred procurement partner.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:142

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor Selected",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:144

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Vendor selected successfully.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-state.ts:150

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Vendor selected and procurement nearing confirmation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-timeline.ts:12

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
module?: "rfq" | "quote" | "procurement" | "inbox" | "vendor" | "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/procurement/workflow-timeline.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: "rfq" | "quote" | "procurement" | "inbox" | "vendor" | "buyer";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/workflow-timeline.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: ProcurementTimelineInput["module"]
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/workflow-timeline.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "quote") return "rfq";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/workflow-timeline.ts:28

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return module || "procurement";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/workflow-timeline.ts:49

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module || "procurement",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/procurement/workflow-timeline.ts:65

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: continuityModuleFromTimelineModule(input.module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/project/projectWorkflowMemory.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor-selection"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/property-investment/investment-score.ts:634

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
: "Balanced Buyer Fit";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/buyer-quote-compare/server.ts:60

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id, public_id, module, status, meta, created_at, updated_at, revision_no, requester_user_id, created_by, contact_name, city, district, locality, pincode"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/rfq/buyer-quote-compare/server.ts:69

**Area:** Business Logic

**Category:** Capability and Permission

**Matched:** `access denied`

**Current system**

```text
if (ownerId !== userId) return { error: "Access denied (not your RFQ)." };
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — lib/rfq/buyer-quote-compare/server.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/combined-inbox/server.ts:103

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
return `/dashboard/thread/${encodeURIComponent(conversationId)}`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/rfq/combined-inbox/server.ts:107

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/combined-inbox/server.ts:107

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
? `/dashboard/buyer/quote-compare/${encodeURIComponent(rfqId)}`
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/rfq/combined-inbox/server.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: `/vendor/inbox-v2/${encodeURIComponent(rfqId)}/chat`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/combined-inbox/server.ts:141

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
String(c.buyer_user_id ?? "") === String(userId) ? "buyer" : "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/vendor-inbox/server.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// lib/rfq/vendor-inbox/server.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/rfq/vendor-inbox/server.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/rfq/vendor-inbox/server.ts:67

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/rfq/vendor-inbox/server.ts:73

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const limit = params.limit ?? 25;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/rfq/vendor-inbox/server.ts:80

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.range(offset, offset + limit - 1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/rfq/vendor-inbox/server.ts:112

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
`module.ilike.%${s}%`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/sahaj/types.ts:16

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/sahaj/types.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/sahaj/types.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SahajModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-content.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
`Users searching for "${intent.query}" often want fast local discovery, supplier comparison, vendor contact, price/rate information or RFQ-based marketplace support.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/ai-search-intent.ts:10

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/ai-search-intent.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/ai-search-intent.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
let module: SeoModule | null = null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:85

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:87

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:89

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:91

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module = "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:99

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
normalizedQuery.includes("supplier") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/ai-search-intent.ts:104

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
intent = "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/ai-search-intent.ts:111

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
else if (module) intent = module;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/ai-search-intent.ts:128

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:35

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsProcurement || brain.isBulk || module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:50

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?query=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsVendor || brain.wantsProcurement || module === "services" || module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-action-copilot.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:84

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property" || brain.wantsInvestment) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:86

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
title: "Plan after-purchase workflow",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/search/procurement-action-copilot.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
description: "Estimate construction cost, contractor needs and material procurement before decision.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-action-copilot.ts:94

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:104

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:107

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Add rental duration, location and machine type to improve vendor response.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-action-copilot.ts:117

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-action-copilot.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3Bigha is converting search intelligence into practical procurement steps, RFQ improvements and vendor actions.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-decision-engine.ts:38

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:46

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:47

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:66

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:70

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:80

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "property" || query.toLowerCase().includes("construction")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (brain.wantsVendor) signals.push("Vendor sourcing");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-decision-engine.ts:104

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:105

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
? "Plan next workflow"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/search/procurement-decision-engine.ts:110

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `/rfq?query=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:111

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-decision-engine.ts:113

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
secondaryHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-decision-engine.ts:133

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
secondaryHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-decision-engine.ts:134

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
signals: signals.length ? signals.slice(0, 6) : ["Marketplace intent", "Workflow continuation", "Vendor discovery"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:38

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:45

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:50

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=TMT%20rod&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:52

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:58

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=sand%20aggregate&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:60

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:66

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=rajmistri&module=services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:76

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:87

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=cement&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:89

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:95

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=construction%20rental&module=rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:97

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:105

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:111

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:118

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:122

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
title: "Find architect and contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:124

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=architect%20contractor&module=services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:126

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:131

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
description: "Plan future construction material sourcing.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/search/procurement-recommendation-engine.ts:132

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=building%20materials&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:134

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:140

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:143

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
title: "Construction contractor workflow",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:145

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
href: "/search?q=contractor&module=services",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:145

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=contractor&module=services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:147

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:153

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=cement&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:155

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:161

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsInvestment || module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:167

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/procurement-recommendation-engine.ts:174

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Continue through vendor discovery and procurement workflows.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:175

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/procurement-recommendation-engine.ts:177

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
intent: "search" | "rfq" | "module" | "category" | "price";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:18

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:19

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:20

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module ? `&module=${module}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module ? `&module=${module}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:57

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
label: `${moduleTitle(module)} marketplace in ${area}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:58

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search/${queryToSlug(`${moduleTitle(module)} in ${area}`)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-internal-graph.ts:59

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
intent: "module",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-keyword-clusters.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
import { getModuleKeywordGroups } from "@/lib/seo/module-keywords";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-keyword-clusters.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-keyword-clusters.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-keyword-clusters.ts:15

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (!module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-keyword-clusters.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`${query} supplier`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-keyword-clusters.ts:29

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const groups = getModuleKeywordGroups(module, safeArea);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:44

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:45

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (brain.wantsVendor) chips.push("Vendor sourcing");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-to-rfq-engine.ts:77

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "This search has buying or hiring intent. Convert it into a requirement and collect vendor responses.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-to-rfq-engine.ts:84

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
recommendedModule !== "all" ? `&module=${encodeURIComponent(recommendedModule)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-to-rfq-engine.ts:87

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
recommendedModule !== "all" ? `&module=${encodeURIComponent(recommendedModule)}` : ""
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-to-rfq-engine.ts:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
chips: chips.length ? chips.slice(0, 5) : ["RFQ ready", "Vendor workflow", "Marketplace action"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:33

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
: "This material search is ready for supplier quotation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
label: "Supplier Match",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=materials`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=materials`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:57

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
label: "Contractor Workflow Ready",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:60

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: "Compare service providers and move this into vendor discovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:61

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:85

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: `/search?module=property&q=${clean}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:99

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:111

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: "Use vendor discovery to find faster local rental response.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:112

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=rentals`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/search-workflow-cards.ts:112

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=rentals`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:119

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.module === "blog") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/search-workflow-cards.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
text: "Continue through search, RFQ or vendor discovery.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:14

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
workflow: "search" | "rfq" | "price" | "vendor" | "investment" | "procurement" | "rental";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"mason", "rajmistri", "mistri", "contractor", "plumber", "electrician",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `supplier`

**Current system**

```text
const wantsVendor = hasAny(q, ["vendor", "supplier", "dealer", "shop", "dokan", "সাপ্লায়ার", "দোকান", "डीलर"]);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:115

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (wantsVendor) reasons.push("vendor sourcing signal");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:123

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
else if (wantsVendor) workflow = "vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:160

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:173

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.primaryModule !== "all" && input.module === brain.primaryModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:175

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
reasons.push("workflow module match");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:178

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (input.moduleFilter !== "all" && input.module === input.moduleFilter) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:180

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
reasons.push("selected module");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:187

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsProcurement && input.module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:192

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsVendor && (input.module === "materials" || input.module === "services")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:194

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reasons.push("vendor sourcing fit");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:202

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.wantsInvestment && input.module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:207

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (brain.isUrgent && (input.module === "materials" || input.module === "services" || input.module === "rentals")) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:220

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:225

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:226

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `&module=${encodeURIComponent(module)}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:228

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `&module=${encodeURIComponent(brain.primaryModule)}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-marketplace-brain.ts:238

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Convert this search into a structured requirement and collect vendor responses.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:252

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${moduleParam}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-marketplace-brain.ts:253

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
badge: "Vendor routing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-search-autocomplete.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:28

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:32

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = input.module || "all";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:43

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=cement%20price%20today&module=materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:46

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:59

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=rajmistri%20near%20me&module=services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:62

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:67

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: "/search?q=land%20in%20Cooch%20Behar&module=property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:70

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:79

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:90

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?q=${clean}&module=${encodeURIComponent(brain.primaryModule)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:92

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
badge: "Best module",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:93

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: brain.primaryModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:105

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: brain.primaryModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${brain.primaryModule !== "all" ? `&module=${brain.primaryModule}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-search-autocomplete.ts:110

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}${brain.primaryModule !== "all" ? `&module=${brain.primaryModule}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:113

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: brain.primaryModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:125

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:133

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=services`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/unified-search-autocomplete.ts:133

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/vendor/discovery?q=${clean}&module=services`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:136

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:148

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:156

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/unified-search-autocomplete.ts:168

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:45

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:56

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:71

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:73

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:75

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:90

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:92

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Bulk material supplier"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:94

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
? "Fast-response contractor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:97

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
: "Verified service provider"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:98

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:100

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:102

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Marketplace vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Good fit for vendor discovery and RFQ"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:111

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "More details needed for better vendor matching";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:116

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Add locality for stronger vendor matching";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:122

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (brain.wantsVendor) badges.push("Vendor sourcing");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:124

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module !== "all") badges.push(`${module} specialist`);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
show: Boolean(query && (module !== "property" || brain.wantsVendor || brain.wantsInvestment || input.resultCount > 0)),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:130

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Excellent vendor intelligence match"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Strong vendor intelligence match"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:134

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor intelligence available"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:135

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor discovery can be improved",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:137

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3Bigha is estimating vendor quality, RFQ responsiveness, locality strength and procurement suitability for this search.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:144

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
badges: badges.length ? badges.slice(0, 6) : ["Vendor discovery", "RFQ routing", "Marketplace intelligence"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:145

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-intelligence-engine.ts:145

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-intelligence-engine.ts:146

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
rfqHref: `/rfq?query=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:16

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:46

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:47

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:62

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:64

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:66

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Needs wider vendor discovery";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-liquidity-engine.ts:103

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
show: Boolean(query && (module !== "property" || brain.wantsInvestment || brain.wantsVendor || input.resultCount > 0)),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:106

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor liquidity looks strong"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-liquidity-engine.ts:108

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Vendor liquidity available"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-liquidity-engine.ts:109

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor discovery recommended",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-liquidity-engine.ts:111

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:113

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:115

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:124

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:126

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-liquidity-engine.ts:126

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-liquidity-engine.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
rfqHref: `/rfq?query=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: UnifiedMarketplaceModuleFilter;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:49

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module =
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:50

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module !== "all"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? input.module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:67

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:69

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:71

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:73

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:94

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
? "Bulk supplier with price negotiation"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:98

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:100

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
? "Fast-response contractor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:101

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
: "Verified service provider"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:102

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:104

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Local marketplace vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "Start vendor discovery, then send RFQ to shortlisted vendors."
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:113

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Improve search details before starting vendor negotiation.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:116

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
brain.wantsPrice || module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:119

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
? "Ask for slab pricing and delivery terms."
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/search/vendor-negotiation-engine.ts:120

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Compare at least 3 vendor responses.";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:128

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (brain.wantsVendor) chips.push("Vendor sourcing");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:131

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
show: Boolean(query && (module !== "property" || brain.wantsVendor || brain.wantsInvestment || input.resultCount > 0)),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:139

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
: "Vendor negotiation needs stronger signals",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:141

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3Bigha is estimating RFQ acceptance, negotiation strength, vendor risk and best transaction strategy.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:149

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
chips: chips.length ? chips.slice(0, 6) : ["Vendor negotiation", "RFQ acceptance", "Procurement confidence"],
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:150

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
rfqHref: `/rfq?query=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/search/vendor-negotiation-engine.ts:151

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/search/vendor-negotiation-engine.ts:151

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
vendorHref: `/vendor/discovery?q=${clean}${module !== "all" ? `&module=${encodeURIComponent(module)}` : ""}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-content-generator.ts:53

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
text.includes("seller") ||
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
text.includes("builder")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:79

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
`A visible 3Bigha business profile helps local material vendors receive enquiries from buyers searching for reliable supply options near ${loc}.`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/ai-content-generator.ts:89

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
`Buyers generally look for consistent quality, transport support, clear pricing and availability during foundation and wall construction stages.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-content-generator.ts:100

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`Local buyers often need dependable delivery, proper quantity handling and vendor availability during urgent site work.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:107

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
heading: `Building material supplier opportunity in ${loc}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:109

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
`${req} suppliers in ${loc} support residential construction, renovation work, contractor procurement and local property development activity.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
`Buyers usually compare location, access roads, documentation, pricing, nearby development and seller credibility before making enquiries.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:132

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
`Buyers usually compare location, access roads, documentation, pricing, nearby development and seller credibility before making enquiries.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-content-generator.ts:164

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
`${req} in ${loc} are important for house building, repair work, plastering, concreting, foundation work and small contractor-led projects.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:175

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
`Customers usually prefer professionals who are reachable, locally available and able to explain scope, pricing and timelines clearly.`,
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-content-generator.ts:176

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
`Service providers near ${loc} can use 3Bigha to improve search visibility and receive relevant buyer enquiries from the local marketplace.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:186

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
if (text.includes("building material") || text.includes("supplier")) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:191

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`Local contractors and property owners can use this marketplace view to understand demand, compare supplier availability and move toward faster procurement decisions.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-content-generator.ts:220

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `business profile`

**Current system**

```text
`Service providers can improve visibility by joining 3Bigha and keeping their business profile updated for local search and marketplace enquiries.`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/ai-market-content.ts:3

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "building materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "construction services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:17

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:23

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const title = moduleTitle(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
`${area} is becoming an important local discovery area for land, plots, flats, houses, commercial property and builder projects. Buyers often search for jomi, bastu jomi, residential plots, road-side land, shop space and investment propert…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-market-content.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`3Bigha helps users find building material suppliers, dealers, shops, wholesalers and RFQ-ready vendors around ${location}. The platform connects material demand with local suppliers so users can compare availability, price intent and vend…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-market-content.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-market-content.ts:54

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
`For home construction, renovation, plumbing, electrical work, painting, tiles fitting or contractor services in ${area}, 3Bigha builds a practical local discovery layer for both customers and service providers.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-search-content.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-search-content.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = safe(input.module || "property");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/ai-search-content.ts:69

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
This ${category.toLowerCase()} ${listingType.toLowerCase()} helps buyers, investors, vendors and businesses discover verified opportunities, compare pricing and connect directly with property owners, builders, suppliers and service provide…
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-search-content.ts:81

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plans`, `pricing`

**Current system**

```text
? `Current pricing around ${priceText} may attract both end-users and long-term investors depending on infrastructure growth, accessibility and future development plans.`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-search-content.ts:82

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
: `Pricing trends, infrastructure growth and locality demand may influence future appreciation potential.`
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-search-content.ts:90

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
- pricing trends
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/ai-search-content.ts:93

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
- vendor credibility
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-search-content.ts:97

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
3bigha.com helps users discover verified opportunities with AI-powered search, smart recommendations and locality-aware discovery.
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/ai-search-content.ts:114

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `seller`

**Current system**

```text
question: `Can buyers contact the seller or vendor directly?`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/ai-search-content.ts:120

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
question: `Does 3bigha support AI-powered property discovery?`,
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/ai-search-content.ts:122

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"Yes. 3bigha uses AI-powered discovery, recommendations, procurement intelligence and semantic search infrastructure.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/category-content.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:7

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:19

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-content.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`3Bigha connects this category with local marketplace discovery, RFQ workflows, vendor visibility, search intent and regional SEO signals so that users can find relevant options faster.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/category-slugs.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
{ slug: "contractor", label: "Contractor", aliases: ["building contractor", "civil contractor"] },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/category-slugs.ts:55

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
{ slug: "architect", label: "Architect", aliases: ["house plan", "building planner"] },
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/category-slugs.ts:72

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getSeoCategories(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-slugs.ts:73

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return seoCategoryMap[module] || [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-slugs.ts:76

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getSeoCategory(module: SeoModule, slug: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/category-slugs.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return getSeoCategories(module).find((item) => item.slug === slug);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/graph/internal-link-graph.ts:5

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/graph/internal-link-graph.ts:7

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const { state, district, city, module } = params;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:6

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
intent: "geo" | "module" | "rfq" | "search";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:9

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleTitle(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:31

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
const modules: SeoModule[] = ["property", "materials", "services", "rentals"];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `modules`

**Current system**

```text
return modules
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:34

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.filter((module) => module !== currentModule)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:35

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.map((module) => ({
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
label: `${moduleTitle(module)} in ${areaLabel}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:38

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
? `/seo/${module}/${state}/${district}/${city}/${locality}`
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: `/seo/${module}/${state}/${district}/${city}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:40

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
intent: "module" as const,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:45

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:53

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
label: `Search ${moduleTitle(module)} in ${areaLabel}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?module=${module}&q=${encodeURIComponent(areaLabel)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/internal-links.ts:59

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?module=${module}&q=${encodeURIComponent(areaLabel)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/isIndexable.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"/admin",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/isIndexable.ts:3

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/seo/isIndexable.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"/buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/isIndexable.ts:10

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
"/subscription",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/isIndexable.ts:16

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"/vendor/inbox",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/isIndexable.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"/vendor/inbox-v2",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-db-market-data.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function tableForModule(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "property_listings";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:26

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "material_listings";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "service_listings";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:32

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:52

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const table = tableForModule(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-db-market-data.ts:66

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(12);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/live-market-signals.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:52

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:77

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
label: "Contractor enquiries",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-market-signals.ts:127

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:130

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:133

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:148

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:156

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: "Supplier competition increasing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-market-signals.ts:163

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-market-signals.ts:166

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
title: "Contractor activity trending",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-marketplace.ts:39

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:50

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
{ label: "Marketplace module", value: module },
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:57

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
subtitle: `Explore local ${module} opportunities in ${district}.`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:58

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/${module === "property" ? "property" : module}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:63

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/rfq?module=${module}&q=${encodeURIComponent(area)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:66

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
title: `Search local ${module}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:68

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: `/search?module=${module}&q=${encodeURIComponent(area)}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:97

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:105

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return fallbackData(module, city, district, locality);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:109

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:124

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(6),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/live-marketplace.ts:148

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:163

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(6),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/live-marketplace.ts:177

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
{ label: "Vendor RFQ", value: "Active" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-marketplace.ts:187

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/live-marketplace.ts:202

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(6),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/live-marketplace.ts:219

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
title: item.title || item.service_type || `Service provider in ${locality || city}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/live-marketplace.ts:240

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(6),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/live-marketplace.ts:263

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return fallbackData(module, city, district, locality);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/marketplace-discovery-data.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorRecommendationCandidates } from "@/lib/seo/vendor-recommendation-data";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorRecommendationInput } from "@/lib/seo/vendor-recommendation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorId: "cement-supplier-cooch-behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
businessName: "Cement Supplier Cooch Behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:8

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
slug: "cement-supplier-cooch-behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
vendorId: "steel-supplier-cooch-behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
businessName: "Steel Supplier Cooch Behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
slug: "steel-supplier-cooch-behar",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:70

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
merged.forEach((vendor) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-data.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
unique.set(vendor.vendorId, vendor);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/lib/seo/vendor-recommendation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:33

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendors = input.vendors.map((vendor) => ({
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:34

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...vendor,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerCity: input.city || vendor.city,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerDistrict: input.district || vendor.district,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
buyerLocality: input.locality || vendor.locality,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:50

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (recommendedVendors.some((vendor) => vendor.isVerified)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
discoverySignals.push("verified vendor trust");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:53

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (recommendedVendors.some((vendor) => (vendor.reputationScore || 0) > 70)) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return vendors.filter((vendor) => {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:80

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.businessName,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:81

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.category,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:82

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.city,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:83

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.district,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:84

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.locality,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(vendor.services || []),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/marketplace-discovery-engine.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
...(vendor.materials || []),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:4

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"cement supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"steel supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:65

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"sand supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"balu supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:91

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"stone supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:92

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"pathor supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:93

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"gitti supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:94

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"chips supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:115

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"brick supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:116

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"it supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:131

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"concrete supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:132

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"rmc supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:160

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"door supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:178

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"window supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:199

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"grill supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:200

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"gate supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:308

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"plumbing supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/material-keywords.ts:483

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
`${word} supplier in ${cleanArea}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/module-keywords.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getModuleKeywords(module: SeoModule, area: string): string[] {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return getPropertyKeywords(area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:13

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return getMaterialKeywords(area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:14

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return getServiceKeywords(area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:15

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return getRentalKeywords(area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getModuleKeywordGroups(module: SeoModule, area: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/module-keywords.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywords = getModuleKeywords(module, area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/procurement-knowledge-graph.ts:48

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const category = input.category || input.module || "Marketplace";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:65

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorDiscoveryId = `vendor-discovery:${slugify(title)}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/procurement-knowledge-graph.ts:72

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
input.module === "materials"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:74

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: input.module === "services"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:76

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: input.module === "rentals"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:78

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
: input.module === "property"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/procurement-knowledge-graph.ts:105

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/procurement-knowledge-graph.ts:106

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
url: `/vendor/discovery?q=${encodeURIComponent(title)}&city=${encodeURIComponent(
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/procurement-knowledge-graph.ts:125

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: `AI vendor discovery can recommend suppliers or service providers for ${title}.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/procurement-knowledge-graph.ts:149

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: `Vendor recommendations are generated from category, location and marketplace trust signals.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/property-keywords.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"ready flat", "under construction flat", "builder flat", "resale flat",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/property-keywords.ts:14

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"property broker", "promoter", "builder project", "new project",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-content.ts:16

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-content.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-content.ts:23

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
intro: `3Bigha helps buyers, sellers, builders and local property investors discover real estate opportunities in ${city}, ${district}, ${state}. Users can explore land, flats, houses, commercial properties and builder projects through an …
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-content.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title: `Builder and construction ecosystem`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-content.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-content.ts:83

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
answer: `Yes. Material vendors can post products and respond to buyer requirements.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-content.ts:89

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-content.ts:91

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
intro: `3Bigha connects users with construction and technical service providers in ${city}, ${district}, ${state}, including contractors, labour teams and professional service experts.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-content.ts:117

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
answer: `Yes. Local professionals and businesses can register and list their services.`,
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/regional-discovery.ts:4

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:8

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:14

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
`Builder projects near ${city}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-discovery.ts:19

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:30

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getRegionalSearchUrl(query: string, module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:52

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/search?module=${module}&q=${encodeURIComponent(query)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getRegionalRfqUrl(query: string, module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-discovery.ts:56

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/rfq?module=${module}&q=${encodeURIComponent(query)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
| "supplier"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:40

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder project",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:81

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"cement supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:232

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:233

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"building contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:234

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"civil contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:235

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"house construction contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:240

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"labour contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:246

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"house plan",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/regional-keywords.ts:262

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"flooring contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:276

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"waterproofing contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:282

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"boundary wall contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:283

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"renovation contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:284

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"repair contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:340

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function baseWords(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:341

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return propertyWords;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:342

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return materialWords;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:343

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return serviceWords;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:348

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:352

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const words = baseWords(module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:359

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
{ keyword: `${word} supplier in ${cleanArea}`, intent: "supplier" },
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-keywords.ts:365

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:373

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:381

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:393

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getRegionalKeywordGroups(module: SeoModule, area: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:394

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywords = getRegionalKeywords(module, area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-keywords.ts:400

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
supplier: keywords.filter((item) => item.intent === "supplier"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:23

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-market-data.ts:27

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-market-data.ts:39

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
label: "Builder projects",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
subtitle: `Buyer demand growing around ${district}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
title: `Builder projects near ${city}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:65

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-market-data.ts:81

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Vendor responses",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
subtitle: `Multiple local vendor options`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/regional-market-data.ts:103

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/regional-market-data.ts:164

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
subtitle: `Local contractor equipment demand`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/related-content.ts:2

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals" | string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-content.ts:25

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const moduleName = clean(input.module || "property");
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-content.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: `/vendor/discovery?q=${encodeURIComponent(title)}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/related-content.ts:67

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: `Create a new requirement and receive matched vendor responses.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/related-listings.ts:7

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property" | "materials" | "services" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function hrefFor(module: string, id: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:34

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return `/property/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:35

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return `/materials/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:36

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return `/services/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:37

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") return `/rentals/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:38

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
return `/${module}/${encodeURIComponent(id)}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:107

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
href: hrefFor(input.module, safe(row.id)),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/related-listings.ts:110

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: input.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:13

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string | null;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:88

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
return `A public marketplace demand signal is active for ${item} in ${location}. Local vendors, suppliers, service providers and marketplace businesses can explore similar buyer requirements through 3Bigha.`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/rfq-public-seo.ts:115

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.select("module, title, city, district, locality, needed_by, budget_min, budget_max, currency, description, notes")
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:129

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const module = titleCase(clean(rfq?.module || "Marketplace"));
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:136

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:186

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
category: vm.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/rfq-public-seo.ts:191

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/rfq-public-seo.ts:207

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/schema.ts:94

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"AI-powered property, construction, RFQ, materials, rentals and vendor marketplace platform in India.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/schema.ts:94

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"AI-powered property, construction, RFQ, materials, rentals and vendor marketplace platform in India.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/schema.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor marketplace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/schema.ts:139

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"3bigha.com is an AI-powered marketplace platform for property, construction materials, RFQ procurement, rentals, vendors and local services in India.",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/schema.ts:151

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Users can submit procurement and construction RFQ requirements and receive vendor quotations through AI-assisted marketplace workflows.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
intent: "search" | "rfq" | "price" | "vendor" | "buyer";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:15

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder project",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"cement supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:21

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"steel supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:22

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"sand supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:23

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"brick supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"aggregate supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"labour contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:52

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/seo-keywords.ts:55

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const base = moduleKeywords[module] || [];
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/seo-keywords.ts:73

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
intent: "vendor" as const,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
export function getSeoKeywordGroups(module: SeoModule, area: string) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/seo-keywords.ts:83

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const keywords = getSeoKeywords(module, area);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/seo-keywords.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor: keywords.filter((item) => item.intent === "vendor"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/seo-keywords.ts:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
buyer: keywords.filter((item) => item.intent === "buyer"),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor", "building contractor", "civil contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:3

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"house construction contractor", "turnkey contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"labour", "labour contractor", "construction labour",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:6

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
"architect", "engineer", "civil engineer", "house plan",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/service-keywords.ts:16

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"waterproofing contractor", "roof repair", "roof casting",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:18

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"boundary wall contractor", "renovation contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"vastu consultant", "piling contractor", "soil testing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/service-keywords.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
`${word} contractor in ${cleanArea}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/site.ts:4

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha.com | AI-Powered Property, Construction, RFQ, Materials & Vendor Marketplace India",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/site.ts:4

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"3bigha.com | AI-Powered Property, Construction, RFQ, Materials & Vendor Marketplace India",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/site.ts:6

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"3bigha.com is India's AI-powered real estate, construction, RFQ, materials, rentals and vendor marketplace platform. Search property, compare vendors, submit procurement RFQs, discover local services and track AI-powered price intelligenc…
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/site.ts:6

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
"3bigha.com is India's AI-powered real estate, construction, RFQ, materials, rentals and vendor marketplace platform. Search property, compare vendors, submit procurement RFQs, discover local services and track AI-powered price intelligenc…
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/site.ts:74

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor marketplace",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/site.ts:98

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"vendor comparison",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/structured-data.ts:21

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
function moduleName(module: SeoModule) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:22

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "property") return "Property";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:23

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") return "Building Materials";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:24

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") return "Construction Services";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:42

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:46

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:51

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
const name = `${moduleName(module)} in ${area}`;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:58

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
description: `Explore ${moduleName(module).toLowerCase()} opportunities, vendors, listings and RFQ workflows in ${area} through 3Bigha.`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:66

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
name: moduleName(module),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:82

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:86

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:95

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
name: `3Bigha ${moduleName(module)} Marketplace - ${area}`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:98

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
description: `3Bigha helps users discover ${moduleName(module).toLowerCase()}, vendors, listings, RFQs and marketplace opportunities in ${area}.`,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:113

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:117

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:123

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "materials") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:141

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "services") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:153

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (module === "rentals") {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:170

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
description: `Find land, plots, flats, houses, commercial property and builder projects in ${area}.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/structured-data.ts:209

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:215

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: SeoModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:226

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
buildRegionalSeoSchema({ module, geo, url }),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:227

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
buildMarketplaceSchema({ module, geo, url }),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/structured-data.ts:228

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
buildModuleSchema({ module, geo, url }),
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-authority-data.ts:62

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-data.ts:110

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
businessName: matched.business_name || "3Bigha Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:67

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendorNodeId = `vendor:${input.vendorId}`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
type: "vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:151

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendor = graph.nodes.find((node) => node.type === "vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:174

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendorName: vendor?.label || "Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-graph.ts:181

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
summary: `${vendor?.label || "This vendor"} has marketplace authority across ${[
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-authority-jsonld.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import { getVendorAuthoritySummary } from "@/lib/seo/vendor-authority-graph";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-demand-seo.ts:84

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"building-material-supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-demand-seo.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
"property-seller",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-demand-seo.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"service-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-demand-seo.ts:87

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
"rental-provider",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-internal-links.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor category authority connection",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-internal-links.ts:54

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
title: `${service} supplier`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-internal-links.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor service semantic search connection",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-internal-links.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor material semantic search connection",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-leaderboard-engine.ts:24

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
function planScore(plan?: string | null) {
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-leaderboard-engine.ts:25

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const value = String(plan || "").toLowerCase();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-leaderboard-engine.ts:44

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
const plan = planScore(input.subscriptionPlan);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-leaderboard-engine.ts:52

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`, `plan`

**Current system**

```text
if (plan > 0) rankSignals.push("Subscription strength");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-leaderboard-engine.ts:58

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
reputation + authority + conversion + rfq + plan + verified + boost
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-opportunity-page.tsx:7

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/lib/seo/vendor-opportunity-seo";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const canonical = `${siteConfig.url}/vendor-opportunities/${state}${
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:48

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
name: `Vendor opportunities in ${location}`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:49

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: `Find active vendor requirements in ${location} for property, materials, services and rentals on 3Bigha.`,
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
provider: {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:81

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Become vendor with AI powered 3Bigha
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:81

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai powered`

**Current system**

```text
Become vendor with AI powered 3Bigha
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:90

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor opportunities in {location}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:128

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Register as vendor →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:128

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
Register as vendor →
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/vendor-opportunity-page.tsx:142

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Vendor demand is opening in {location}
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:147

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
Local vendors can register early and appear when demand opens.
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/vendor-opportunity-page.tsx:158

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
Register as vendor →
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:158

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
Register as vendor →
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/vendor-opportunity-page.tsx:165

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
<Link href="/vendor-opportunities">
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-page.tsx:166

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
View all vendor opportunities
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:11

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:31

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data } = await supabase.from(table).select("id,name,slug").limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-opportunity-seo.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 10000
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-opportunity-seo.ts:53

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,module,category,recommended_vendor_count,priority,opportunity_title,opportunity_description,geo_state_id,geo_district_id,geo_place_id"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:57

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(limit),
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-opportunity-seo.ts:73

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: row.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:96

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
urls.add(`/vendor-opportunities/${row.stateSlug}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:99

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
urls.add(`/vendor-opportunities/${row.stateSlug}/${row.districtSlug}`);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:104

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
`/vendor-opportunities/${row.stateSlug}/${row.districtSlug}/${row.placeSlug}`
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:115

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (row.module === "materials") return "Construction Material";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:116

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (row.module === "services") return "Service Provider";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:116

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (row.module === "services") return "Service Provider";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:117

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `provider`

**Current system**

```text
if (row.module === "rentals") return "Rental Provider";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:117

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (row.module === "rentals") return "Rental Provider";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:118

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `seller`

**Current system**

```text
if (row.module === "property") return "Property Seller";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:118

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (row.module === "property") return "Property Seller";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/seo/vendor-opportunity-seo.ts:120

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
return "Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-opportunity-seo.ts:135

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `register`

**Current system**

```text
}. Register your business on 3Bigha to receive local enquiries.`;
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/seo/vendor-recommendation-data.ts:2

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
import type { VendorRecommendationInput } from "@/lib/seo/vendor-recommendation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-data.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
category: "Marketplace Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-data.ts:85

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
.from("business_profiles")
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-data.ts:123

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-recommendation-data.ts:146

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
businessName: row.business_name || "3Bigha Vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:69

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
const vendor = normalize(vendorValue);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:70

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
const buyer = normalize(buyerValue);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:72

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
if (!vendor || !buyer) return 0;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:73

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
if (vendor === buyer) return exactScore;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:74

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`, `buyer`

**Current system**

```text
if (vendor.includes(buyer) || buyer.includes(vendor)) return Math.round(exactScore * 0.7);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-engine.ts:148

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
matchSignals.push("Verified vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:5

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
} from "@/lib/seo/vendor-recommendation-engine";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor) => vendor.vendorId !== base.vendorId
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.locality &&
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.locality.toLowerCase() === base.locality.toLowerCase()
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:34

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:35

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.city &&
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.city.toLowerCase() === base.city.toLowerCase()
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:44

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.category &&
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
vendor.category.toLowerCase() === base.category.toLowerCase()
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:51

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
candidates.filter((vendor) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:52

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor.materials || []).some((material) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:61

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
candidates.filter((vendor) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-recommendation-graph.ts:62

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
(vendor.services || []).some((service) =>
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-trust-signals.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Verified vendor profile",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-trust-signals.ts:52

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
reason: "Active supplier in RFQ ecosystem",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-trust-signals.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
label: "Established Supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/seo/vendor-trust-signals.ts:68

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `subscription`

**Current system**

```text
reason: "Active marketplace subscription",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/seo/vendor-trust-signals.ts:76

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
reason: "Vendor visibility boost enabled",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/supabasePublicBrowser.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`, `vendor`

**Current system**

```text
// IMPORTANT: isolate PUBLIC client from vendor/admin auth storage
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/materials-taxonomy.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/taxonomy/property-taxonomy.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "property",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/taxonomy/property-taxonomy.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
property("Builder Projects", "Projects", ["apartment project"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/rentals-taxonomy.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "rentals",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/taxonomy/services-taxonomy.ts:12

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "services",
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/taxonomy/services-taxonomy.ts:19

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Engineering", "Professional / Skilled", ["civil engineer", "structural engineer"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:20

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Architecture", "Professional / Skilled", ["architect", "building design"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:21

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Design", "Professional / Skilled", ["planning", "drawing"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:22

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Project Management", "Professional / Skilled", ["project", "supervision"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:23

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Estimation", "Professional / Skilled", ["estimate", "boq"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:24

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Testing", "Professional / Skilled", ["material testing", "soil testing"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Surveying", "Professional / Skilled", ["land survey", "site survey"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:26

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("MEP", "Professional / Skilled", ["mechanical electrical plumbing"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:27

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`, `contractor`

**Current system**

```text
service("Contracting", "Professional / Skilled", ["contractor", "turnkey"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:28

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Masonry", "Professional / Skilled", ["mason", "rajmistri"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:29

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Carpentry", "Professional / Skilled", ["wood work"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:30

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Electrical", "Professional / Skilled", ["electrician"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:31

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Plumbing", "Professional / Skilled", ["plumber"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:32

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Painting", "Professional / Skilled", ["painter"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:33

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Flooring", "Professional / Skilled", ["tiles", "floor work"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:34

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Fabrication", "Professional / Skilled", ["grill", "steel fabrication"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:35

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Roofing", "Professional / Skilled", ["roof work"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:36

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Operators", "Professional / Skilled", ["machine operator"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Manpower", "Professional / Skilled", ["labour", "worker"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:38

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Maintenance", "Professional / Skilled", ["repair"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:39

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Interior", "Professional / Skilled", ["interior work"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:40

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Security", "Professional / Skilled", ["security systems"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:41

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Safety", "Professional / Skilled", ["site safety"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:42

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Renewable", "Professional / Skilled", ["solar"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/services-taxonomy.ts:43

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
service("Water", "Professional / Skilled", ["water supply", "boring"]),
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/taxonomy/types.ts:8

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: MarketplaceModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/ux/operational-language.ts:30

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
subtleAiLabel: "AI-powered",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/ux/operational-language.ts:60

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
subtleAiLabel: "AI-powered",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/ux/operational-language.ts:63

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
label: "Step-by-Step RFQ Builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ux/operational-language.ts:80

**Area:** Business Logic

**Category:** Visible AI Terminology

**Matched:** `ai-powered`

**Current system**

```text
subtleAiLabel: "AI-powered",
```

**3BOS replacement**

Keep intelligence internal and present only the prepared business assistance, explanation and user-controlled action.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Replace visible AI terminology with the human outcome while keeping internal intelligence unchanged.

### LOW — lib/ux/operational-language.ts:93

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
label: "Supplier Reliability",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/ux/operational-language.ts:94

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
helper: "Understand supplier strength, response quality, and risk.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendor-verification/gstin.ts:56

**Area:** Business Logic

**Category:** Onboarding

**Matched:** `registration`

**Current system**

```text
errors.push("13th character must be a valid registration/entity code.");
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:1

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
// lib/vendor/loadVendorTaxonomyExtensions.ts
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:8

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:31

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: "materials" | "rentals";
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:40

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:49

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
.eq("module", params.module)
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/loadVendorTaxonomyExtensions.ts:55

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor taxonomy extension load failed", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendor-trust-reputation.ts:45

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
badges.push("Verified Vendor");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendor-trust-reputation.ts:95

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
badges.push("Buyer Preferred");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendor-trust-reputation.ts:110

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `premium`

**Current system**

```text
badges.push("Premium Visibility");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/vendors/vendor-trust-reputation.ts:126

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
? "AI Trusted Vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendorListingMemory.ts:18

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
| "pricing"
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/vendors/vendorListingMemory.ts:33

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: VendorMemoryModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:45

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: VendorMemoryModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:47

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit?: number;
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/vendors/vendorListingMemory.ts:54

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
"id,user_id,module,memory_type,title,payload,usage_count,last_used_at,created_at,updated_at"
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:58

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(params.limit ?? 10);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/vendors/vendorListingMemory.ts:60

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
if (params.module) {
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:61

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
query = query.eq("module", params.module);
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:71

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor listing memory load failed", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendorListingMemory.ts:80

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: VendorMemoryModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:95

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: params.module,
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/vendors/vendorListingMemory.ts:106

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
console.error("Vendor listing memory save failed", error);
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/vendors/vendorSmartSuggestions.ts:112

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
limit = 4
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/vendors/vendorSmartSuggestions.ts:132

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.slice(0, limit);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/workflow-continuity/types.ts:8

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
| "vendor"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow-continuity/types.ts:9

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
| "buyer"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow-continuity/types.ts:31

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module: WorkflowModule;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/workflow-guidance/build-guidance.ts:17

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow-guidance/build-guidance.ts:23

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
id: "supplier",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow-guidance/build-guidance.ts:25

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
message: "Supplier response timing is slowing in some workflows.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow-guidance/build-guidance.ts:27

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-health",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow-guidance/build-guidance.ts:37

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow-guidance/build-guidance.ts:37

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow-guidance/build-guidance.ts:47

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-execution",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/build-workflow-recommendations.ts:10

**Area:** Business Logic

**Category:** Module-Oriented Architecture

**Matched:** `module`

**Current system**

```text
module?: string;
```

**3BOS replacement**

Treat existing modules as internal capability providers and present them to users as their work and available actions.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Retain internal module structure but change visible navigation and labels only after runtime integration.

### LOW — lib/workflow/build-workflow-recommendations.ts:45

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"Compare pricing, vendor reliability and execution readiness.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:45

**Area:** Business Logic

**Category:** Business Growth Plan

**Matched:** `pricing`

**Current system**

```text
"Compare pricing, vendor reliability and execution readiness.",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — lib/workflow/build-workflow-recommendations.ts:46

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:46

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/buyer/rfqs",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/build-workflow-recommendations.ts:56

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Check market movement before supplier negotiation.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:67

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Verify supplier reliability and avoid operational delays.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:68

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-os",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/build-workflow-recommendations.ts:78

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Accelerate procurement coordination and supplier response.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:79

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-live",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/build-workflow-recommendations.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
id: "vendor-followup",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:89

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `supplier`

**Current system**

```text
"Maintain supplier communication and negotiation continuity.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/build-workflow-recommendations.ts:90

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-followup-agent",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:17

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
| "admin"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:34

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/inbox-v2",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:58

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
label: "Compare vendor quotes",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:59

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
description: "Check price, delivery, trust, risk, and then shortlist the best vendor.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:66

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
label: "Check pending buyer work",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:68

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:68

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:69

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
stage: "Vendor operations",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:77

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/procurement-os",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:86

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:86

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/inventory",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:95

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:95

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/billing",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — lib/workflow/next-action-engine.ts:103

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
description: "Check pending delivery, assign vehicle, and send update to buyer.",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:104

**Area:** Business Logic

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — lib/workflow/next-action-engine.ts:104

**Area:** Business Logic

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
href: "/dashboard/vendor/dispatch",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-capability-foundation.mjs:4

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
"lib/3bos/capability/types.ts",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:8

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
"lib/3bos/capability/registry.ts",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:12

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
"lib/3bos/capability/eligibility.ts",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:22

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plans`

**Current system**

```text
console.error("Missing Growth Plans:", missingPlans);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-capability-foundation.mjs:30

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `gold_vendor`

**Current system**

```text
"gold_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-capability-foundation.mjs:31

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `platinum_vendor`

**Current system**

```text
"platinum_vendor",
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-capability-foundation.mjs:41

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
console.error("Missing legacy plan compatibility:", missingLegacyPlans);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-capability-foundation.mjs:58

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
(key) => !registry.includes(` ${key}: capability({`)
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:62

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
console.error("Missing capability definitions:", missingCapabilities);
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:72

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
console.error("Capability resolver is missing.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:76

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
console.log("Capability and Growth Plan source checks passed.");
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-capability-foundation.mjs:76

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
console.log("Capability and Growth Plan source checks passed.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-growth-plan-presentation.mjs:4

**Area:** Operational Script

**Category:** Capability and Permission

**Matched:** `capability`

**Current system**

```text
"lib/3bos/capability/presentation.ts",
```

**3BOS replacement**

Resolve available actions through the 3BOS capability engine while preserving legacy authorization checks as fallbacks.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add capability resolution before gradually replacing duplicated checks. Keep the existing check as fallback.

### LOW — scripts/3bos/test-growth-plan-presentation.mjs:22

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
console.error("Missing Growth Plan presentation labels:", missing);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-growth-plan-presentation.mjs:27

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
console.error("Legacy Growth Plan resolver is not used.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-growth-plan-presentation.mjs:31

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `plan`

**Current system**

```text
console.log("Growth Plan presentation source checks passed.");
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/3bos/test-identity-compatibility.mjs:14

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-identity-compatibility.mjs:15

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
"author",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-identity-compatibility.mjs:17

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
"professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-identity-compatibility.mjs:18

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:4

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
"lib/3bos/workspace/types.ts",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:9

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
"lib/3bos/workspace/registry.ts",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:16

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"builder",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:18

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `contractor`

**Current system**

```text
"contractor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:21

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `professional`

**Current system**

```text
"professional",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:30

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `author`

**Current system**

```text
"author",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:35

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
(key) => !registry.includes(` ${key}: workspace({`)
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:39

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
console.error("Missing workspace definitions:", missing);
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:44

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `buyer`

**Current system**

```text
"/dashboard/buyer",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:44

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/buyer",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:46

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
"/property/builder/projects",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:47

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/construction-projects",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:48

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
"/dashboard/vendor",
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/3bos/test-workspace-registry.mjs:48

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/vendor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:52

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/banker",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:53

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
"/dashboard/investor",
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:70

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
console.error("Workspace lifecycle status is missing.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/3bos/test-workspace-registry.mjs:74

**Area:** Operational Script

**Category:** Workspace

**Matched:** `workspace`

**Current system**

```text
console.log("Workspace registry source checks passed.");
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/geography/audit-current-coordinate-coverage.mjs:14

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
let q = supabase.from(table).select("id,latitude,longitude").order("id", { ascending: true }).limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-geo-place-lgd-link.mjs:25

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(PAGE_SIZE);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-geo-place-metadata-links.mjs:19

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from(table).select("*").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-geo-place-metadata-links.mjs:43

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-geo-place-metadata-links.mjs:59

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(50);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-geo-place-metadata-links.mjs:84

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(20);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-property-public-columns.mjs:10

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from(table).select("*").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-rental-columns.mjs:13

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-service-columns.mjs:10

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from(table).select("*").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/audit-service-nearby-coordinate-source.mjs:9

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
for (const table of ["v_service_listings", "service_listings", "service_providers", "business_profiles"]) {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/geography/audit-service-nearby-coordinate-source.mjs:10

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
const { data, error } = await supabase.from(table).select("*").limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/backfill-marketplace-geography.js:31

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/backfill-marketplace-geography.js:118

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
if (table === "business_profiles") {
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/geography/backfill-marketplace-geography.js:139

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(500);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/backfill-marketplace-geography.js:195

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `business_profiles`

**Current system**

```text
await backfillTable("business_profiles", "user_id");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/geography/coordinates/match-coordinate-source-to-lgd.mjs:67

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/coordinates/match-coordinate-source-to-lgd.mjs:80

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/export-admin-coordinate-fallbacks.mjs:19

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
let q = supabase.from(table).select(select).order("id", { ascending: true }).limit(5000);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/export-geo-place-coordinate-candidates.mjs:43

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(PAGE_SIZE);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/g6/build-lgd-wb-settlements.mjs:137

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/g6/build-lgd-wb-settlements.mjs:144

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `builder`

**Current system**

```text
console.log("G6-G2 WB settlement builder");
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/geography/geocode-district-centroids.mjs:31

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
"https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=in&q=" +
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/geocode-district-centroids.mjs:36

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
"User-Agent": "3bigha-national-coordinate-activation/1.0 admin@3bigha.com"
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/geography/import-geography-csv.js:51

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
let q = supabase.from(table).select("*").eq(column, value).limit(1).maybeSingle();
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/import-lgd-urban-places.mjs:55

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1)
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/geography/inspect-lgd-schema.mjs:24

**Area:** Operational Script

**Category:** Business Growth Plan

**Matched:** `limit`

**Current system**

```text
.limit(1);
```

**3BOS replacement**

Present Business Growth Plans through the growth-plan adapter while retaining legacy plan keys, tables and APIs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Use the Growth Plan presentation/runtime adapter. Preserve legacy plan keys and persistence.

### LOW — scripts/sahaj-generate-platform-map.mjs:9

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (route.includes("/admin")) return "Administration";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/sahaj-generate-platform-map.mjs:10

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (route.includes("/dashboard")) return "Dashboard / Manage";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/sahaj-generate-platform-map.mjs:16

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (route.includes("/vendor")) return "Business / Vendor";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/sahaj-generate-platform-map.mjs:27

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `admin`

**Current system**

```text
if (route.includes("/dashboard") || route.includes("/admin")) return "Manage";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/sahaj-generate-platform-map.mjs:27

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`, `/dashboard`

**Current system**

```text
if (route.includes("/dashboard") || route.includes("/admin")) return "Manage";
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

### LOW — scripts/sahaj-generate-platform-map.mjs:30

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
if (route.includes("/vendor") || route.includes("/onboarding")) return "Grow";
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/sahaj-generate-platform-map.mjs:30

**Area:** Operational Script

**Category:** Onboarding

**Matched:** `onboarding`

**Current system**

```text
if (route.includes("/vendor") || route.includes("/onboarding")) return "Grow";
```

**3BOS replacement**

Derive onboarding steps from identity, workspace and capability requirements while preserving current forms and database fields.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Add derived onboarding requirements around the current workflow. Do not replace existing profile completion logic.

### LOW — scripts/sahaj-generate-platform-map.mjs:62

**Area:** Operational Script

**Category:** Human Identity

**Matched:** `vendor`

**Current system**

```text
out += `1. Shared Foundation\n2. Procurement / RFQ\n3. Search & Discovery\n4. Property\n5. Marketplace Materials & Services\n6. Business / Vendor\n7. Dashboard / Manage\n8. Administration\n`;
```

**3BOS replacement**

Resolve Human Identity through the 3BOS identity registry while retaining existing role and profile fields as compatibility inputs.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Wrap the current role/profile source with the Human Identity resolver. Keep legacy values unchanged.

### LOW — scripts/sahaj-generate-platform-map.mjs:62

**Area:** Operational Script

**Category:** Workspace

**Matched:** `dashboard`

**Current system**

```text
out += `1. Shared Foundation\n2. Procurement / RFQ\n3. Search & Discovery\n4. Property\n5. Marketplace Materials & Services\n6. Business / Vendor\n7. Dashboard / Manage\n8. Administration\n`;
```

**3BOS replacement**

Derive workspaces from resolved Human Identity and expose human-readable work destinations without changing existing routes.

**Backward compatibility**

Keep the existing field, route, API and permission behavior operational during migration.

**Migration strategy**

Map the current route or dashboard to a registered workspace. Preserve the URL and page component.

